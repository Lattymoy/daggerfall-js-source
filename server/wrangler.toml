# ONLINE1 (2026-09-12): THE RELAY. A Cloudflare Worker whose one Durable
# Object class, Room, holds the WebSockets of every player in one room
# and fans each pose out to the peers in range. It stored nothing past
# the connection until WORLD1 (2026-09-12): a world room keeps its
# host's snapshot of the place in the object's storage; the save stays
# on the player's machine.
#   npx wrangler deploy          (CLOUDFLARE_API_TOKEN in the environment)
# ...or, since SRV-N/CI, the 'Deploy relay' workflow-dispatch job, which
# reads the token from the repo's secrets so nobody has to hold it, and
# then POLLS /health until it names the RELAY_VERSION the run built -
# a wrangler exit code of 0 over an unchanged Worker is a green deploy
# that did not happen. It is manual on purpose: a deploy restarts every
# Durable Object and drops every connected player at once.
name = "daggerfall-online"
main = "src/index.js"
compatibility_date = "2026-09-01"
account_id = "a27adccb38c8e8b82fa32e5736274a94"
workers_dev = true

# AUDIT ONLINE A17: an uncaught error in the object is logged, not lost
[observability]
enabled = true

[[durable_objects.bindings]]
name = "ROOMS"
class_name = "Room"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]
