{
  "name": "project-dagger",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
