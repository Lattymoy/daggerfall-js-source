{
  "name": "project-dagger",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm test && npm run build",
    "shot": "node tools/screenshot.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0",
    "pngjs": "^7.0.0",
    "vite": "^5.4.0"
  }
}
