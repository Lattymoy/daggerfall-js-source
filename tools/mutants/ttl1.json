[
  { "name": "cleared TTL is 2 days, not 3", "file": "src/world/spawnedDungeons.js",
    "old": "export const CLEARED_TTL_DAYS = 2;", "new": "export const CLEARED_TTL_DAYS = 3;",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "general TTL is 7 days, not 8", "file": "src/world/spawnedDungeons.js",
    "old": "export const GENERAL_TTL_DAYS = 7;", "new": "export const GENERAL_TTL_DAYS = 8;",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "a day is the clock's own, not a hand-written 1440", "file": "src/world/spawnedDungeons.js",
    "old": "export const GENERAL_TTL_MINUTES = GENERAL_TTL_DAYS * MINUTES_PER_DAY;",
    "new": "export const GENERAL_TTL_MINUTES = GENERAL_TTL_DAYS * 1441;",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "the short clock alone would let a spawn cleared on day six live to day eight", "file": "src/world/spawnedDungeons.js",
    "old": "  if (Number.isFinite(rec.seen) && now - rec.seen >= GENERAL_TTL_MINUTES) return true;",
    "new": "  if (false && Number.isFinite(rec.seen) && now - rec.seen >= GENERAL_TTL_MINUTES) return true;",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "the long clock alone would never notice a clearing", "file": "src/world/spawnedDungeons.js",
    "old": "  if (Number.isFinite(rec.cleared) && now - rec.cleared >= CLEARED_TTL_MINUTES) return true;",
    "new": "  if (false && Number.isFinite(rec.cleared) && now - rec.cleared >= CLEARED_TTL_MINUTES) return true;",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "expiry at the boundary, not one minute past it", "file": "src/world/spawnedDungeons.js",
    "old": "now - rec.seen >= GENERAL_TTL_MINUTES) return true;", "new": "now - rec.seen > GENERAL_TTL_MINUTES) return true;",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "an older save winds the clock back: a negative elapsed is never expiry", "file": "src/world/spawnedDungeons.js",
    "old": "      if (!r) { r = { seen: now }; rows.set(key, r); }",
    "new": "      if (true) { r = { seen: now }; rows.set(key, r); }",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "a second clearing must not buy another two days", "file": "src/world/spawnedDungeons.js",
    "old": "      if (r && !Number.isFinite(r.cleared)) r.cleared = now;",
    "new": "      if (r) r.cleared = now;",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "forgotten is gone, not kept", "file": "src/world/spawnedDungeons.js",
    "old": "    forget(key) { return rows.delete(key); },", "new": "    forget(key) { return rows.has(key); },",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "the cleared time is only written when there is one", "file": "src/world/spawnedDungeons.js",
    "old": "...(Number.isFinite(r.cleared) ? [r.cleared] : [])", "new": "r.cleared",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "a junk row must not load", "file": "src/world/spawnedDungeons.js",
    "old": "        if (typeof k === 'string' && Number.isFinite(seen))", "new": "        if (true)",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "the creator's rule: never while the player is in it", "file": "src/scenes/world.js",
    "old": "if (_spawnLedger.expired(key, _spawnClock()) && !_insideSpawn(key)) {", "new": "if (_spawnLedger.expired(key, _spawnClock()) && true) {",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "an expired spawn leaves the location index too", "file": "src/scenes/world.js",
    "old": "        locationIndex.delete(key);\n        return null;", "new": "        return null;",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "clearing a REAL dungeon must never invent a ledger row", "file": "src/scenes/world.js",
    "old": "    if (!locationIndex.get(key)?.spawned) return;", "new": "    if (false) return;",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "the ledger rides the world host's save", "file": "src/scenes/world.js",
    "old": "session: npcSession }, spawnLedger: _spawnLedger }),", "new": "session: npcSession } }),",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "the ledger rides the dungeon host's save too", "file": "src/scenes/dungeonContext.js",
    "old": "talk: opts.talkSave, spawnLedger: opts.spawnLedger?.() ?? null }),", "new": "talk: opts.talkSave }),",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "the ledger block survives snapshotPlayer's name list", "file": "src/systems/save.js",
    "old": "escortingFaces, quickslots, spawns, smallerDungeonsState, modData, terrainScale: STREAMING_TERRAIN_SCALE };", "new": "escortingFaces, quickslots, smallerDungeonsState, modData, terrainScale: STREAMING_TERRAIN_SCALE };",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "QS1: the quickslot diamond survives it as well", "file": "src/systems/save.js",
    "old": "escortingFaces, quickslots, spawns, smallerDungeonsState, modData, terrainScale: STREAMING_TERRAIN_SCALE };", "new": "escortingFaces, spawns, smallerDungeonsState, modData, terrainScale: STREAMING_TERRAIN_SCALE };",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "the clear notice sits AHEAD of the automap scan's early return", "file": "src/scenes/dungeonContext.js",
    "old": "      if (!_clearedSent && opts.onDungeonCleared) {", "new": "      if (false && !_clearedSent && opts.onDungeonCleared) {",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "fully cleared means the loot too, not just the foes", "file": "src/scenes/dungeonContext.js",
    "old": "foes.every((f) => f.dead) && lootPiles.every((p) => p.items.length === 0)", "new": "foes.every((f) => f.dead)",
    "tests": ["test/spawneddungeons.test.js"] },
  { "name": "the mode machine forwards both clocks", "file": "src/scenes/worldModes.js",
    "old": "          onDungeonCleared: () => host.onDungeonCleared?.(),\n", "new": "",
    "tests": ["test/spawneddungeons.test.js"] }
]
