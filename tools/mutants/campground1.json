[
  { "name": "probe-back-to-buckets-only", "file": "src/scenes/camps.js",
    "old": "      probe: col?.surfaceHit ? (o, d, m) => col.surfaceHit(o, d, m).dist\n        : (col?.raycast ? (o, d, m) => col.raycast(o, d, m) : null),",
    "new": "      probe: col?.raycast ? (o, d, m) => col.raycast(o, d, m) : null,",
    "tests": ["test/campground.test.js"] },
  { "name": "probe-loses-its-fallback", "file": "src/scenes/camps.js",
    "old": "        : (col?.raycast ? (o, d, m) => col.raycast(o, d, m) : null),",
    "new": "        : null,",
    "tests": ["test/campground.test.js"] },
  { "name": "ground-accepted-past-the-probe", "file": "src/systems/survival/camp.js",
    "old": "  const ground = Number.isFinite(d) && d <= GROUND_PROBE ? top - d : null;",
    "new": "  const ground = Number.isFinite(d) ? top - d : null;",
    "tests": ["test/campground.test.js"] },
  { "name": "no-ground-stops-refusing", "file": "src/systems/survival/camp.js",
    "old": "  if (!Number.isFinite(place.ground)) return { ok: false, text: CAMP_TEXT.noGround };",
    "new": "",
    "tests": ["test/campground.test.js"] },
  { "name": "notice-ring-stacks-repeats-again", "file": "src/ui/hudText.js",
    "old": "    const back = this.lines[this.lines.length - 1];\n    if (back && back.text === text) {",
    "new": "    const back = null;\n    if (back && back.text === text) {",
    "tests": ["test/campground.test.js"] },
  { "name": "notice-ring-collapses-any-repeat-anywhere", "file": "src/ui/hudText.js",
    "old": "    const back = this.lines[this.lines.length - 1];\n    if (back && back.text === text) {",
    "new": "    const back = this.lines.find((l) => l.text === text);\n    if (back && back.text === text) {",
    "tests": ["test/campground.test.js"] },
  { "name": "notice-collapse-swallows-the-row", "file": "src/ui/hudText.js",
    "old": "      if (this.timer >= 0) this.timer = Math.max(this.timer, delayInSeconds);\n      else this.nextPopDelay = Math.max(this.nextPopDelay, delayInSeconds);\n      return;\n    }\n    if (this.lines.length === 0) this.timer = delayInSeconds;",
    "new": "      this.lines.pop();\n      return;\n    }\n    if (this.lines.length === 0) this.timer = delayInSeconds;",
    "tests": ["test/campground.test.js"] }
]
