[
  {
    "name": "placement-nearest-first",
    "file": "src/render/shadowPass.js",
    "old": "      if (d <= eps2) { exact = s; break; }\n",
    "new": "",
    "tests": [
      "test/audit_reach.test.js"
    ],
    "equivalent": true,
    "why": "the claim-once rule alone keeps a double door still (each draw's own placement is the nearest unclaimed one); the exact match is the early out, kept for the mover's case where a nearer placement of another instance could be claimed first in a later draw order"
  },
  {
    "name": "placement-claimed-twice",
    "file": "src/render/shadowPass.js",
    "old": "      if (d < bestD && s.seen !== this.frameNo) { bestD = d; best = s; }",
    "new": "      if (d < bestD) { bestD = d; best = s; }",
    "tests": [
      "test/audit_reach.test.js"
    ]
  },
  {
    "name": "placement-no-eviction",
    "file": "src/render/shadowPass.js",
    "old": "        if (!oldest || this.frameNo - oldest.seen < SHADOW_DYNAMIC_HOLD) return true;   // every placement live: dynamic, never wrong",
    "new": "        return true;",
    "tests": [
      "test/audit_reach.test.js"
    ]
  },
  {
    "name": "placement-live-evicted",
    "file": "src/render/shadowPass.js",
    "old": "        if (!oldest || this.frameNo - oldest.seen < SHADOW_DYNAMIC_HOLD) return true;   // every placement live: dynamic, never wrong",
    "new": "        if (!oldest) return true;",
    "tests": [
      "test/audit_reach.test.js"
    ]
  },
  {
    "name": "flat-record-forgotten",
    "file": "src/render/shadowPass.js",
    "old": " && b._shFrame === fr && b._shRec === rec && b._shFlip === flip)) b._shMovedAt = this.frameNo;",
    "new": " && b._shFrame === fr && b._shFlip === flip)) b._shMovedAt = this.frameNo;",
    "tests": [
      "test/audit_reach.test.js"
    ]
  },
  {
    "name": "flat-flip-forgotten",
    "file": "src/render/shadowPass.js",
    "old": " && b._shFrame === fr && b._shRec === rec && b._shFlip === flip)) b._shMovedAt = this.frameNo;",
    "new": " && b._shFrame === fr && b._shRec === rec)) b._shMovedAt = this.frameNo;",
    "tests": [
      "test/audit_reach.test.js"
    ]
  },
  {
    "name": "records-left-behind-at-the-crossing",
    "file": "src/render/shadowPass.js",
    "old": "    for (let i = 0; i < this.count; i++) {\n      const r = this.records[i];\n      if (r.kind === REC_BB) continue;\n      r.matrix[12] += offset[0]; r.matrix[13] += offset[1]; r.matrix[14] += offset[2];",
    "new": "    for (let i = 0; i < 0; i++) {\n      const r = this.records[i];\n      if (r.kind === REC_BB) continue;\n      r.matrix[12] += offset[0]; r.matrix[13] += offset[1]; r.matrix[14] += offset[2];",
    "tests": [
      "test/audit_reach.test.js"
    ]
  },
  {
    "name": "record-spheres-left-behind",
    "file": "src/render/shadowPass.js",
    "old": "      r.sphere[0] += offset[0]; r.sphere[1] += offset[1]; r.sphere[2] += offset[2];\n",
    "new": "",
    "tests": [
      "test/audit_reach.test.js"
    ]
  }
]
