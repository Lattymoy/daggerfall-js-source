[
  {
    "name": "MAIL-guest-sends",
    "file": "server-account/src/letters.js",
    "old": "  if (accountKind(sender) !== 'linked') return { error: 'mail-needs-account' };\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "sendLetter trusts its caller's wall - one refactor from a guest writing letters."
  },
  {
    "name": "MAIL-muted-heard",
    "file": "server-account/src/letters.js",
    "old": "  if (isMuted(sender, nowS)) return { error: 'muted' };\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A moderator's mute stops a player's chat and not their letters."
  },
  {
    "name": "MAIL-words-unchecked",
    "file": "server-account/src/letters.js",
    "old": "  if ('error' in words) return words;\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A letter the law refuses is written anyway (or crashes the route)."
  },
  {
    "name": "MAIL-shape-after-rate",
    "file": "server-account/src/letters.js",
    "old": "  if (!HANDLE_RE.test(handle)) return { error: 'no-reader' };\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A shape no handle has spends the sender's rate on a lookup that cannot find anyone."
  },
  {
    "name": "MAIL-sender-rate-dropped",
    "file": "server-account/src/letters.js",
    "old": "  if (await overRate({ db, nowS }, `mail:${sender.id}`, LETTERS_SENT_MAX, LETTERS_SENT_WINDOW_S)) return { error: 'mail-rate' };\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "One registered player writes to the whole server in an hour."
  },
  {
    "name": "MAIL-pair-rate-dropped",
    "file": "server-account/src/letters.js",
    "old": "  if (await overRate({ db, nowS }, `mail:${sender.id}>${reader.id}`, LETTERS_PAIR_MAX, LETTERS_SENT_WINDOW_S)) return { error: 'mail-rate' };\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "One player fills another's box."
  },
  {
    "name": "MAIL-to-self-kept",
    "file": "server-account/src/letters.js",
    "old": "  if (reader.id === sender.id) return { error: 'to-self' };\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A letter to oneself takes a place in one's own box."
  },
  {
    "name": "MAIL-bound-off-by-one",
    "file": "server-account/src/letters.js",
    "old": "reader.id, LETTERS_INBOX_MAX).run();",
    "new": "reader.id, LETTERS_INBOX_MAX + 1).run();",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A box holds one past its bound."
  },
  {
    "name": "MAIL-full-box-lands-anyway",
    "file": "server-account/src/letters.js",
    "old": "  if (!r?.meta?.changes) return { error: 'inbox-full' };\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A letter the full box refused is reported sent."
  },
  {
    "name": "MAIL-inbox-oldest-first",
    "file": "server-account/src/letters.js",
    "old": "ORDER BY sent_at DESC, id DESC LIMIT ?",
    "new": "ORDER BY sent_at ASC, id DESC LIMIT ?",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "The newest letter is at the bottom of fifty."
  },
  {
    "name": "MAIL-inbox-anybodys",
    "file": "server-account/src/letters.js",
    "old": "    WHERE to_id = ? ORDER BY",
    "new": "    WHERE to_id != ? ORDER BY",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A reader's box lists everybody else's letters."
  },
  {
    "name": "MAIL-unread-is-all",
    "file": "server-account/src/letters.js",
    "old": "unread: letters.filter((l) => !l.read).length,",
    "new": "unread: letters.length,",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "The badge counts letters already read."
  },
  {
    "name": "MAIL-badge-never-read",
    "file": "server-account/src/letters.js",
    "old": "id: l.id, from: l.from_name, ...badgeOf(rows.get(l.from_id), env, nowS),",
    "new": "id: l.id, from: l.from_name, ...badgeOf(null, env, nowS),",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A moderator's letter wears no shield."
  },
  {
    "name": "MAIL-read-anybodys",
    "file": "server-account/src/letters.js",
    "old": "FROM letters WHERE id = ? AND to_id = ?')\n    .bind(id, reader.id).first();",
    "new": "FROM letters WHERE id = ?')\n    .bind(id).first();",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "Anyone who learns an id reads the letter."
  },
  {
    "name": "MAIL-read-never-marked",
    "file": "server-account/src/letters.js",
    "old": "  if (l.read_at == null) await db.prepare('UPDATE letters SET read_at = ? WHERE id = ? AND to_id = ? AND read_at IS NULL').bind(nowS, id, reader.id).run();\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A letter opened stays unread for ever."
  },
  {
    "name": "MAIL-read-time-rewritten",
    "file": "server-account/src/letters.js",
    "old": "sentAt: l.sent_at, readAt: l.read_at ?? nowS,",
    "new": "sentAt: l.sent_at, readAt: nowS,",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A second opening moves the first."
  },
  {
    "name": "MAIL-delete-anybodys",
    "file": "server-account/src/letters.js",
    "old": "DELETE FROM letters WHERE id = ? AND to_id = ?').bind(id, reader.id)",
    "new": "DELETE FROM letters WHERE id = ?').bind(id)",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A sender takes a letter back out of the reader's box."
  },
  {
    "name": "MAIL-wall-dropped",
    "file": "server-account/src/index.js",
    "old": "        if (accountKind(who.player) !== 'linked') return no('mail-needs-account', 403, origin);\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A guest's device reads and writes letters."
  },
  {
    "name": "MAIL-box-any-method",
    "file": "server-account/src/index.js",
    "old": "          if (request.method !== 'GET') return no('method', 405, origin);\n          return json(await inboxOf(",
    "new": "          return json(await inboxOf(",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "The box answers a POST (a body that says nothing, read by nothing)."
  },
  {
    "name": "MAIL-full-is-a-400",
    "file": "server-account/src/index.js",
    "old": "r.error === 'inbox-full' ? 409 : 400;",
    "new": "r.error === 'inbox-full' ? 400 : 400;",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A full box is answered as a malformed request."
  },
  {
    "name": "MAIL-muted-is-a-400",
    "file": "server-account/src/index.js",
    "old": "const status = r.error === 'muted' ? 403 :",
    "new": "const status = r.error === 'muted' ? 400 :",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A mute is answered as a malformed request."
  },
  {
    "name": "MAIL-route-open",
    "file": "server-account/src/service.js",
    "old": "export const OPEN_ROUTES = new Set(['/v1/health', '/v1/auth/guest', '/v1/auth/login', '/v1/auth/recover']);",
    "new": "export const OPEN_ROUTES = new Set(['/v1/health', '/v1/auth/guest', '/v1/auth/login', '/v1/auth/recover', '/v1/mail/inbox']);",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A stranger reaches the box without a credential."
  },
  {
    "name": "MAIL-no-cascade",
    "file": "server-account/migrations/0007_letters.sql",
    "old": "  FOREIGN KEY (to_id) REFERENCES players(id) ON DELETE CASCADE\n",
    "new": "  FOREIGN KEY (to_id) REFERENCES players(id)\n",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A reader who is gone leaves their box behind (or cannot go)."
  },
  {
    "name": "LAW-lines-collapsed",
    "file": "src/net/letterLaw.js",
    "old": "  return foldBlankLines(String(text ?? '').split(LINE_BREAK).map(wordsLine)).join('\\n');",
    "new": "  return foldBlankLines(String(text ?? '').split(LINE_BREAK).map(wordsLine)).join(' ');",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A contract laid out in lines arrives as one run-on line."
  },
  {
    "name": "LAW-blank-runs-kept",
    "file": "src/net/wire.js",
    "old": "    if (!line && (!out.length || out[out.length - 1] === '')) continue;\n",
    "new": "    if (!line && !out.length) continue;\n",
    "tests": [
      "test/mail1.test.js",
      "test/journal1.test.js"
    ],
    "note": "Forty blank lines between two words - in a letter, and on a page (JOURNAL1 moved the law into wire.js, where both read it)."
  },
  {
    "name": "LAW-trailing-blank-kept",
    "file": "src/net/wire.js",
    "old": "  while (out.length && out[out.length - 1] === '') out.pop();\n",
    "new": "",
    "tests": [
      "test/mail1.test.js",
      "test/journal1.test.js"
    ],
    "note": "A letter, or a page, that ends in a blank line (the law's home is wire.js since JOURNAL1)."
  },
  {
    "name": "LAW-own-characters",
    "file": "src/net/wire.js",
    "old": "export const wordsLine = (line) => visibleText(String(line ?? '').replace(/\\t/g, ' ')).replace(/\\s+/g, ' ').trim();",
    "new": "export const wordsLine = (line) => String(line ?? '').replace(/\\t/g, ' ').replace(/\\s+/g, ' ').trim();",
    "tests": [
      "test/mail1.test.js",
      "test/journal1.test.js"
    ],
    "note": "A letter - or a page - carries a bidi override the chat would never pass: two laws for one keyboard."
  },
  {
    "name": "LAW-subject-one-long",
    "file": "src/net/letterLaw.js",
    "old": "  if (s.length > LETTER_SUBJECT_MAX) return { error: 'subject-long' };",
    "new": "  if (s.length > LETTER_SUBJECT_MAX + 1) return { error: 'subject-long' };",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A subject one past its bound."
  },
  {
    "name": "LAW-body-cut-quiet",
    "file": "src/net/letterLaw.js",
    "old": "  if (b.length > LETTER_BODY_MAX) return { error: 'body-long' };",
    "new": "  if (false) return { error: 'body-long' };",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A letter of any length, past the form's cap and the service's size."
  },
  {
    "name": "LAW-lines-one-short",
    "file": "src/net/letterLaw.js",
    "old": "  if (b.split('\\n').length > LETTER_LINES_MAX) return { error: 'body-lines' };",
    "new": "  if (b.split('\\n').length >= LETTER_LINES_MAX) return { error: 'body-lines' };",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A letter of exactly the bound's lines refused."
  },
  {
    "name": "LAW-subject-keeps-lines",
    "file": "src/net/letterLaw.js",
    "old": "  return wordsLine(String(text ?? '').split(LINE_BREAK).join(' '));",
    "new": "  return wordsLine(String(text ?? ''));",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A subject's line break becomes a character the list draws as nothing."
  },
  {
    "name": "BOX-auth-kept",
    "file": "src/net/mail.js",
    "old": "    if (error === 'auth') { forgetSession(io.storage); ",
    "new": "    if (error === 'auth') { ",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A secret the service stopped honouring is kept and every look fails the same way."
  },
  {
    "name": "BOX-every-look-announces",
    "file": "src/net/mail.js",
    "old": "    else if (first && this.unread) this.onLetter?.({ kind: 'waiting', count: this.unread });",
    "new": "    else if (this.unread) this.onLetter?.({ kind: 'waiting', count: this.unread });",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "The chat says 'you have letters' every three minutes."
  },
  {
    "name": "BOX-old-letters-news",
    "file": "src/net/mail.js",
    "old": "    const fresh = first ? [] : letters.filter((l) => !l.read && !this._known.has(l.id));",
    "new": "    const fresh = first ? [] : letters.filter((l) => !l.read);",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "Every unread letter is announced as new at every look."
  },
  {
    "name": "BOX-open-refetches",
    "file": "src/net/mail.js",
    "old": "    if (kept) return { ok: true, letter: kept };\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A letter opened twice is fetched twice."
  },
  {
    "name": "BOX-open-badge-stays",
    "file": "src/net/mail.js",
    "old": "    if (head && !head.read) { head.read = true; this.unread = Math.max(0, this.unread - 1); }\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "The badge still counts a letter just read."
  },
  {
    "name": "BOX-local-check-dropped",
    "file": "src/net/mail.js",
    "old": "    if ('error' in words) return { ok: false, error: words.error };\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A letter the law refuses goes to the service to be refused there."
  },
  {
    "name": "BOX-stamp-after-session",
    "file": "src/net/mail.js",
    "old": "    // when the Letters tab opens.\n    this.at = this.now();\n",
    "new": "    // when the Letters tab opens.\n",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A signed-out box reads the store every frame."
  },
  {
    "name": "BOX-gone-letter-kept",
    "file": "src/net/mail.js",
    "old": "    if (r.ok || r.error === 'no-letter') { this._drop(id); return { ok: true }; }",
    "new": "    if (r.ok) { this._drop(id); return { ok: true }; }",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A letter thrown away on another device stands in this box for ever."
  },
  {
    "name": "BOX-id-unchecked",
    "file": "src/net/mail.js",
    "old": "  if (!l || typeof l !== 'object' || typeof l.id !== 'string' || !LETTER_ID_RE.test(l.id)) return null;",
    "new": "  if (!l || typeof l !== 'object' || typeof l.id !== 'string') return null;",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "An id no service minted is kept and handed back."
  },
  {
    "name": "BOX-re-stacks",
    "file": "src/net/mail.js",
    "old": "replace(/^(re:\\s*)+/i, '')",
    "new": "replace(/^re:\\s*/i, '')",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "Re: Re: Re:"
  },
  {
    "name": "TAB-always-drawn",
    "file": "src/ui/socialPanel.js",
    "old": "...(mail ? [['letters', 'Letters']] : [])",
    "new": "...[['letters', 'Letters']]",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A Letters tab over no letterbox."
  },
  {
    "name": "TAB-badge-not-the-box",
    "file": "src/ui/socialPanel.js",
    "old": "id === 'letters' ? (mail?.unread ?? 0) :",
    "new": "id === 'letters' ? 0 :",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "Unread letters and no number on the tab."
  },
  {
    "name": "TAB-form-rebuilt",
    "file": "src/ui/socialPanel.js",
    "old": "tab !== 'letters' ? social.version !== painted : letters.mode !== 'write' && (mail?.version ?? 0) !== paintedMail;",
    "new": "tab !== 'letters' ? social.version !== painted : (mail?.version ?? 0) !== paintedMail;",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A poll landing rebuilds the form under the player's caret."
  },
  {
    "name": "TAB-to-not-kept",
    "file": "src/ui/socialPanel.js",
    "old": "    to.addEventListener('input', () => { d.to = to.value; });\n",
    "new": "",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "The name typed is not the name sent."
  },
  {
    "name": "TAB-delete-one-press",
    "file": "src/ui/socialPanel.js",
    "old": "      : btn('Delete', { run: () => { letters.del = l.id; letters.delAt = social.now(); ui++; if (open) repaint(); } }));",
    "new": "      : btn('Delete', { run: () => { mail.remove(l.id); } }));",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "One stray press throws a letter away."
  },
  {
    "name": "TAB-guest-friend-written",
    "file": "src/ui/socialPanel.js",
    "old": "        const named = handleShapeOk(r.name);",
    "new": "        const named = true;",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A guest's generated name offered as an address the service will refuse."
  },
  {
    "name": "TAB-refusal-clears-draft",
    "file": "src/ui/socialPanel.js",
    "old": "        } else letters.word = letterWordText(r.error);",
    "new": "        } else { letters.word = letterWordText(r.error); letters.draft = { to: '', subject: '', body: '' }; }",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A refused letter is lost with the refusal."
  },
  {
    "name": "TAB-reply-to-self",
    "file": "src/ui/socialPanel.js",
    "old": "run: () => writeTo(l.from, replySubject(l.subject))",
    "new": "run: () => writeTo('', replySubject(l.subject))",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "Reply opens a letter to nobody."
  },
  {
    "name": "HOST-never-polled",
    "file": "src/scenes/world.js",
    "old": "    mail?.poll();   // MAIL1:",
    "new": "    // mail?.poll();   // MAIL1:",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "Letters are found only when the tab is opened."
  },
  {
    "name": "HOST-notice-dropped",
    "file": "src/scenes/world.js",
    "old": "      onLetter: (event) => { chatLog.push(tab.id, { text: mailNoticeText(event), system: true }); },",
    "new": "      onLetter: null,",
    "tests": [
      "test/mail1.test.js"
    ],
    "note": "A letter arrives and nothing says so."
  }
]
