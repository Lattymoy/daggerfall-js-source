Tree verified clean. Final verdicts.

```json
[
  {
    "id": "PN-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived at HEAD: src/render/waterSurface.js:52/54/60/62/69/70 declare WATER_LIFT 0.08, WATER_OPACITY 0.82, WATER_F0 0.02, SHORE_SOFTNESS 0.16 and the two DEFAULT_SKY_* triples, and waterUniforms (:203-210) forwards each; the only assertions that touch them are test/water.test.js:93 and :98, both comparing waterUniforms()'s output to the very export it read. I applied all six mutants AT ONCE (0.8 / 0.12 / 0.5 / 0.9 / [1,0,0] / [0,1,0]) - water.test.js 10 pass / 0 fail - then ran them together with PN-2's and PN-3's six in ONE `npm test`: 7047 tests, 0 fail. Nothing in the tree outside test/water.test.js names these constants, and :101 (`assert.equal(WATER_SCROLL_TILES_PER_SEC, 0.05, ...)`) is the only literal anchor in the file, so the file already contains the shape the other six need. This is also against the port's OWN recorded law, not only Home.md's: Port-Ledger.md section A's windmill row states skin constants are 'pinned for STRUCTURE ... not asserted back at themselves'.",
    "corrections": "It is SEVEN constants, not six. WATER_TINT (waterSurface.js:58, [0.62, 0.78, 0.86]) is forwarded by waterUniforms as `u.tint` (:208) and has NO assertion anywhere in the tree - not even a tautology (`grep -n tint test/water.test.js` returns nothing). Second: WATER_F0 is NOT a free presentation tunable and the finder's bare-literal fix understates it - Water-Arc.md:92 records 'Schlick's Fresnel (F0 0.02), capped at 0.72', and 0.02 is derivable from n=1.33 (((1.33-1)/(1.33+1))**2 = 0.020059), so its pin should carry the derivation as its reason. Severity 'high' is inflated: no player-visible defect ships today; this is a pin-quality defect in a just-landed module.",
    "fixShape": "test/water.test.js, inside the 'the uniforms' test (lines 93 and 98), keeping the existing identity lines beneath as the wiring check that waterUniforms actually forwards them: add `assert.equal(WATER_LIFT, 0.08, 'Water-Arc.md:44-45 - above the ground the water is built from, below a step at the shore'); assert.equal(WATER_OPACITY, 0.82, 'Water-Arc.md:101'); assert.ok(Math.abs(WATER_F0 - ((1.33 - 1) / (1.33 + 1)) ** 2) < 5e-4, 'Schlick F0 for n = 1.33 - Water-Arc.md:92'); assert.equal(SHORE_SOFTNESS, 0.16); assert.deepEqual([...DEFAULT_SKY_ZENITH], [0.17, 0.35, 0.72]); assert.deepEqual([...DEFAULT_SKY_HORIZON], [0.66, 0.78, 0.92]);` plus the seventh the finder missed: `assert.deepEqual([...WATER_TINT], [0.62, 0.78, 0.86]); assert.deepEqual(u.tint, WATER_TINT);` - u.tint is currently forwarded and never asserted at all. Also add WATER_LIFT's consequence pin, since the visible defect is water floating over a shore: `assert.ok(WATER_LIFT > 0 && WATER_LIFT < 0.2, 'above the ground, below a step - a tile is 6.4')`. No src/ change.",
    "severity": "medium",
    "laneKey": "test/water.test.js"
  },
  {
    "id": "PN-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived: test/volumetricClouds.test.js:59-64 is the whole of what holds QUALITY and SWEEP_FRAMES - two orderings, one modulo, and WORLD_PER_DRIFT (the only literal); :22-28 holds VC_PROFILE by `p.base > 0 && p.top > p.base`, a 0..1 loop over three of six fields, and four comparative claims. The other two QUALITY touches are equally relational: :113-117 (texelsPerPixel = q.shadow/16 whole) and :133 (shadowSteps in 8..24). I re-ran all three named mutants - QUALITY.default halved to {640,128,40,5,256,10}, SWEEP_FRAMES 8 to 4, VC_PROFILE.sunny density 0.60 to 0.95 and shear 0.35 to 0.95 - against volumetricClouds + clockArc + ds2_cloudsUnderMod (21 pass / 0 fail) and then in the combined full-suite run (7047 tests, 0 fail). The test's own title claims 'the numbers the rest of the outdoors already keeps' and the only number it keeps is the drift scale.",
    "corrections": "'against the literals the arc doc settles' is only half true and the fix brief must not assume otherwise. Volumetric-Clouds-Arc.md:57 (the VC5 row) settles the TIER - 'the default tier is a judgment (1024x256, 56 steps)' - but the VC3 row (:55) describes VC_PROFILE only qualitatively ('sunny: scattered towers; overcast: a low grey deck; fog: a low uniform lid'); the numeric table is nowhere but the module. Two gaps the finder underplays: `shear` is excluded from the 0..1 loop at :25, so it is unheld in ALL SEVEN rows, not just sunny; and QUALITY's `light` field is unheld in all three tiers. Finally, the same VC5 row states the tier choice is deliberately left to Mac's measurement, so a bare deepEqual must not replace the structural invariants - a retune that updates the literal and breaks the whole-texel or divisibility law must still redden. Severity 'high' is inflated for a presentation tier table the arc page explicitly leaves open.",
    "fixShape": "test/volumetricClouds.test.js. At :62, widen the existing loop rather than add a line: `for (const k of ['density', 'dark', 'flat', 'shear']) assert.ok(p[k] >= 0 && p[k] <= 1, ...)` (:25) - the cheapest half of this finding. At :60-62 add, keeping :60/:61/:62 and :113-117 and :133 in place beneath: `assert.deepEqual(QUALITY.default, { width: 1024, height: 256, steps: 56, light: 5, shadow: 512, shadowSteps: 12 }, 'Volumetric-Clouds-Arc.md:57 - the judged default tier'); assert.deepEqual(QUALITY.lo, { width: 512, height: 128, steps: 32, light: 4, shadow: 256, shadowSteps: 8 }); assert.deepEqual(QUALITY.hi, { width: 2048, height: 512, steps: 80, light: 6, shadow: 1024, shadowSteps: 16 }); assert.equal(SWEEP_FRAMES, 8, 'a full re-march every eight frames');`. At :27-28 add one deepEqual per row for at least sunny/overcast/fog/thunder against src/render/volumetricClouds.js:112-118, with the arc's VC3 sentence as the reason ('sunny: scattered towers' etc.), so the table is a deliberate edit and not a silent drift. No src/ change.",
    "severity": "medium",
    "laneKey": "test/volumetricClouds.test.js"
  },
  {
    "id": "PN-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived: src/systems/combatVisuals.js:53-62 declares BLEND_ALPHA 0.22, BLEND_SHIMMER 0.08, BLEND_HZ 1.3, SHADE_ALPHA 0.55, SHADE_DARK 0.12, REVEAL_SECONDS 0.35, REVEAL_ALPHA 0.8; every assertion in test/combatVisuals.test.js that says how visible a concealed foe is (:43, :45, :48, :51, :59, :60, :62, :63, :64, :127, :139) takes the constant from the module under test, and the shimmer band at :48 is `BLEND_ALPHA +/- BLEND_SHIMMER` about itself. I ran BLEND_ALPHA 0.22 to 0.95, SHADE_ALPHA 0.55 to 0.02 and REVEAL_SECONDS 0.35 to 9 together: combatVisuals.test.js 7 pass / 0 fail, and 0 fail in the combined 7047-test run. The constants are load-bearing and doc-settled - Enhanced-Combat-Visuals.md:50-52 tabulates all six by name and value - so the fix is mechanical.",
    "corrections": "The finder's account of SHADE_DARK is wrong in a way the fix lane needs. SHADE_DARK is not 'the fix shape for one of its six constants' - test/combatVisuals.test.js:166 builds a RegExp out of SHADE_DARK and matches it against src/render/renderer.js:379, which hardcodes `if (uConceal.x == 2.0) lit *= 0.12;`. The mutant dies only because the shader restates the number as a second literal - a ONE DFU MEMBER, ONE EXPORT duplication, and a second defect hiding inside this one. Severity 'high' is inflated: this is test quality, not a shipping defect.",
    "fixShape": "test/combatVisuals.test.js, beside the existing identity lines at :43-52 and :59-62 (keep them as the wiring check): `assert.equal(BLEND_ALPHA, 0.22, 'Enhanced-Combat-Visuals.md:50'); assert.equal(BLEND_SHIMMER, 0.08); assert.equal(BLEND_HZ, 1.3); assert.equal(SHADE_ALPHA, 0.55, 'Enhanced-Combat-Visuals.md:51'); assert.equal(SHADE_DARK, 0.12); assert.equal(REVEAL_ALPHA, 0.8); assert.equal(REVEAL_SECONDS, 0.35, 'Enhanced-Combat-Visuals.md:52 - a third of a second, not a tell that outlives the swing');` plus the two consequence pins `assert.ok(BLEND_ALPHA + BLEND_SHIMMER < 0.5, 'a concealed foe is never more than half visible')` and `assert.ok(REVEAL_SECONDS < 1, 'shorter than a swing')`. SEPARATELY, and this is the part the finder missed: src/render/renderer.js:379 hardcodes 0.12 where combatVisuals.js:59 exports SHADE_DARK - interpolate the constant into the billboard FS the way waterSurfaceFs already interpolates CLOUD_SHADOW_GLSL, and :166 then pins one number instead of certifying two agree.",
    "severity": "medium",
    "laneKey": "test/combatVisuals.test.js"
  },
  {
    "id": "PN-4",
    "upheld": false,
    "confidence": "high",
    "reasoning": "The C# is as claimed - StreamingWorld.cs:1531 `float extraDistance = RMBLayout.RMBSide * 0.1f`, applied at :1541-1562 - and test/audit64_travel.test.js:133 is a genuine tautology. But the brief's bar has a second prong and this fails it. I mutated src/world/locationEntrance.js:45 to `RMB_SIDE * 0.5`: audit64_travel.test.js passed 7/7, and test/prisonrelease.test.js:54 (`assert.equal(EXTRA_DISTANCE, RMB_SIDE * 0.1)`) killed it, 15 pass / 1 fail. `git blame` puts that line at b8a13b73, which `git merge-base --is-ancestor b8a13b73 0fe2c05b` confirms is PRE-DELTA - so the number cannot move without a full-suite failure, which is what the bar asks. The finder's own evidence says exactly this.",
    "corrections": "The finder ran this and recorded the kill, then filed it anyway; under the stated bar ('AND is there no other test in the FULL suite that catches it') that is a refutation, not a finding. The residual is a readability nit - the message at :133 says 'a tenth of a block' while the expression says 'whatever the module exports' - worth a one-line cleanup in a tidy pass, not an audit finding. Note the same tautological shape is in test/testroom.test.js:256-257 and test/tr4_ship.test.js:145, so a lane that 'fixes' only audit64_travel.test.js leaves two more; all three are backstopped by the same pre-delta line.",
    "fixShape": "Optional cleanup only, no lane needed: test/audit64_travel.test.js:131-134, replace the imported term with the stated number so message and expression agree - `const tenth = RMB_SIDE * 0.1; assert.deepEqual(at.pos, [origin[0] - tenth, origin[1], origin[2] + half], 'StreamingWorld.cs:1531/:1560-1563 - a tenth of a block OUTSIDE the rectangle');` and drop EXTRA_DISTANCE from the import list at :28. Do NOT add `assert.equal(EXTRA_DISTANCE, RMB_SIDE * 0.1)` - that is prisonrelease.test.js:54 verbatim and would be a second home for the same pin.",
    "severity": "low",
    "laneKey": "test/audit64_travel.test.js"
  },
  {
    "id": "PN-5",
    "upheld": false,
    "confidence": "high",
    "reasoning": "The C# checks out - DaggerfallTradeWindow.cs:912-913 `int weightAndNumItems = (int)basketItems.GetWeight() + basketItems.Count` feeding CalculateShopliftingChance, and FormulaHelper.cs:276-278 `chance = 100 - Pickpocket; chance += shopQuality + weightAndNumItems; Clamp(5, 95)` - and test/audit63_windows.test.js:485 and :376/:400/:463 do compute the expected value with the function under test. But the second prong of the bar fails: I flipped the sign at src/combat/formulas.js:833 to `- weightAndNumItems`, audit63_windows.test.js passed 19/19, and test/theft.test.js:43 (`assert.equal(calculateShopliftingChance(40, 10, 6), 76)`) killed it, 9 pass / 1 fail. That line blames to b8a13b73, PRE-DELTA. The formula itself is also not in the delta (PT1); the delta's new law here - shopliftingLoad's `(int)` truncation - is pinned by a literal at :480.",
    "corrections": "Same structural error as PN-4: the finder ran the kill, recorded it, and filed the finding anyway against its own bar. Also, calling this a defect in F48's claim overstates - F48's subject is WHICH arithmetic shopliftAttempt takes (DoSteal's, not AttemptPrivatePropertyTheft's), and the distinguishing evidence for that is the weightAndNumItems term and the absent empty-basket guard (:489-491), both of which ARE pinned by literals in the delta's own file.",
    "fixShape": "Optional cleanup only: test/audit63_windows.test.js:485, spell the reference arithmetic once so the window test stops re-running the formula - `assert.equal(out.chance, Math.max(5, Math.min(95, (100 - 40) + 12 + out.weightAndNumItems)), 'DaggerfallTradeWindow.cs:912-913 / FormulaHelper.cs:276-278');`. Leave :376/:400/:463 as they are: those three compute a ROLL THRESHOLD to stub Math.random against, not an expected value, so replacing them with literals would couple three window tests to a formula they are not about. Do NOT add a `calculateShopliftingChance` literal - test/theft.test.js:43 already owns it.",
    "severity": "low",
    "laneKey": "test/audit63_windows.test.js"
  },
  {
    "id": "PN-6",
    "upheld": false,
    "confidence": "high",
    "reasoning": "Refuted on BOTH prongs, and I ran each. Prong 1 - the pin's claim is a SURFACE claim: test/audit64_hud.test.js:248 reads 'every SetMidScreenText caller speaks to the label, and the two PopupMessage siblings do not', F34 being about SetMidScreenText versus the popup queue. I mutated the thing it claims to hold - src/scenes/townTalk.js:612 `setMidScreenText(TOO_FAR_AWAY_TEXT)` to `pushPopup(TOO_FAR_AWAY_TEXT)` - and the pin REDDENED: `not ok 6 - AUDIT 64 F34 ...`, 17 pass / 1 fail. Prong 2 - the finder's `false &&` mutant falsifies the DISTANCE GATE, a different law, and test/audit23_ui.test.js:51 catches it (5 pass / 1 fail); I reproduced both. That line blames to a pre-delta commit and is the exact whole-line guard-anchored match the finder's proposedFix proposes to add.",
    "corrections": "The finder mutated a law the pin does not claim, then judged the pin by it. Its proposedFix ('anchor the fragment to its GUARD rather than to the bare call') is not a fix - it is a verbatim restatement of test/audit23_ui.test.js:51-52, which has held both townTalk guards, whole line including the guard, since before the delta. Applying it would put a third copy of the same source pin in the tree. The generalisation to 'every pin in test/fixc_firstStart.test.js, test/fixe_deathMenu.test.js and test/water.test.js:161' is asserted, not tested, and PN-7 (the one instance it does test) refutes it.",
    "fixShape": "No lane. If anything is worth doing it is the finder's FIRST suggestion, which is real and unrelated to the count pin: townTalk is constructible in node (test/audit63_quests_talk.test.js:56 builds one), so a behavioural arm for one of the three refusals would be worth having. That is a new-coverage request for the hud/talk lane, not a repair of a broken pin, and it should be filed as such.",
    "severity": "low",
    "laneKey": "test/audit64_hud.test.js"
  },
  {
    "id": "PN-7",
    "upheld": false,
    "confidence": "high",
    "reasoning": "Refuted on both prongs. Prong 2: I removed `rebuildRoadless();` from the `if (his)` arm at src/scenes/world.js:442 - test/fixc_firstStart.test.js passed 4/4 (not 3 as the finder reports), and test/roads.test.js killed it at :846, 38 pass / 1 fail ('ROADS 25: a pixel says whether a network was present, and the host rebuilds the ones painted without one'). `git blame` puts roads.test.js:846-847 at 6b3af1ea, which `git merge-base --is-ancestor` confirms is PRE-DELTA. Prong 1: the FIX-C pin at :32 claims only 'the network's arrival marks the sweep' and pins the whole one-line body `function rebuildRoadless() { roadsSweepDue = true; }` - mutate that body to a no-op and it reddens; the frame arm at :33 is tight, as the finder concedes.",
    "corrections": "The two arm pins the finder's proposedFix asks for ALREADY EXIST, verbatim and character-for-character: test/roads.test.js:846 `/terrainGen\\.setRoadsData\\(\\{ \\.\\.\\.his, \\.\\.\\.roadSwitches \\}[^\\n]*rebuildRoadless\\(\\); return; \\}/` ('the mod-data path') and :847 `/terrainGen\\.setRoads\\(settlementsOf\\(maps\\), logRoads, roadSwitches\\);\\s*\\n\\s*rebuildRoadless\\(\\);/` ('our own network'). Applying the fix would add a third copy of each. The finder's own evidence names roads.test.js as the killer and still files the finding; it also mis-reports the FIX-C file as 3 tests when HEAD has 4.",
    "fixShape": "No lane, no change. If the FIX-C page wants the arms visible in its own file, the correct move is a comment at test/fixc_firstStart.test.js:32 pointing at test/roads.test.js:846-847 as their home - not a duplicate pin, which is the ONE DFU MEMBER, ONE EXPORT law applied to tests.",
    "severity": "low",
    "laneKey": "test/fixc_firstStart.test.js"
  },
  {
    "id": "PN-8",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "Confirmed at HEAD: src/scenes/shared.js:1538 `DEATH_VIDEO_WATCHDOG_MS = 30000` and test/fixe_deathMenu.test.js:36 is the only assertion on it, a 15000..60000 band. But the finder filed this WITHOUT running it and the claim 'the number itself is free' is wrong. I ran both directions: 30000 to 300 - a watchdog that cuts every death video off - is KILLED by the band (3 pass / 1 fail); only 30000 to 59000 survives, and a 59 s bound does not falsify the pin's own stated claim ('a real death video ends well inside it'). The finding survives on a larger hole the finder missed: `endRunToTitleMenu(renderer, { watchdogMs = DEATH_VIDEO_WATCHDOG_MS, ... })` at :1544 has a default that NO test exercises - every test injects watchdogMs - while all four hosts call `endRunToTitleMenu(renderer)` with no options (world.js:1678, exterior.js:933, worldModes.js:329, dungeonContext.js:1693). I mutated that default to `watchdogMs = 5` and the whole FIX-E file stayed green, 4 pass / 0 fail: production cuts every death video off after 5 ms and the band still passes because the exported constant is untouched.",
    "corrections": "'the number itself is free' and 'a 45-second-wide band: any value in it ... passes' - only the upper half is free, and the lower half is exactly the half that has a consequence; a short watchdog truncates DIE.VID, a long one costs a stall on a video that was already hung. 'Same class as Home.md's `assert.ok(bows >= 8)`' overstates: that is a one-sided inequality over a DFU-derived law, this is a two-sided band over a port tunable with no DFU original (DFU is synchronous), and the WM row in Port-Ledger section A endorses structural pins for exactly this class. The load-bearing defect is the unexercised default binding, which is the producer/caller gap PN-7 wrongly alleges and this one actually has - and it reaches all four hosts.",
    "fixShape": "test/fixe_deathMenu.test.js, in the 'BOUNDED wait' test (:31-37). Add the literal beside the band at :36, keeping the band as the explanatory second line: `assert.equal(DEATH_VIDEO_WATCHDOG_MS, 30000, 'src/scenes/shared.js:1538 - ANIM0012 runs well under this');`. Then close the hole the band cannot see, with one call that omits watchdogMs so the default binding at src/scenes/shared.js:1544 actually runs: `let dflt = null; await endRunToTitleMenu({ canvas: null }, { play: () => new Promise(() => {}), setTimer: (fn, ms) => { dflt = ms; fn(); return 0; } }); assert.equal(dflt, DEATH_VIDEO_WATCHDOG_MS, 'all four hosts call endRunToTitleMenu(renderer) with no options - the production wait IS the exported constant');`. No src/ change.",
    "severity": "low",
    "laneKey": "test/fixe_deathMenu.test.js"
  }
]
```