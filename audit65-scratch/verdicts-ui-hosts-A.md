```json
[
  {
    "id": "UI-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived at HEAD: spellbookWindow.js:777 `click(vx, vy, now)` and :862 `const t = now ?? Date.now();`, while every slot that mounts it dispatches four positionals — townTalk.js:1123 `overlay.click?.(v[0], v[1], e.button === 2, e.button === 1)`, worldModes.js:7090, dungeonContext.js:4867 `activeOverlay.click(vx, vy, right, middle)`. None of the three windows defines `pointer`, so townTalk's `if (overlay.pointer) ... else overlay.click?.(...)` fork really reaches `click`. Probe (host shape, no clock): first click `selectedIndex=2, readied=0, _lastRowClick=false`; a second click on a DIFFERENT row, arbitrarily later, fired `useSelected()` — `false ?? Date.now()` keeps `false`, `false != null` is true, `false - false === 0 < 300`. DFU BaseScreenComponent.cs:54 + :681-692 tests `leftClickTime - lastLeftClickTime < doubleClickDelay` against realtimeSinceStartup alone. charsheet.js:420 forwards `this.child.click?.(vx, vy)`, so the nested spellbook does time correctly — the exact asymmetry the claim predicts. `git log -S _lastRowClick` puts the spellbook stamp in OT1 (88c7b911, in the delta).",
    "corrections": "Three. (1) The listPicker half is mis-stated: listPicker.js:311's THIRD slot (`font`) is already content-guarded — :343 `this.pickerFont ?? this._font ?? (font?.fnt ? font : null)`, and its own header at :328-337 names townTalk.js:1123 / worldModes.js:7090 / dungeonContext.js:4867 as passing a boolean there. Only the FOURTH slot is unguarded; probe confirms a bare-mounted ListPickerWindow (worldModes.js:1588/7283/7332, dungeonContext.js:1927/1988) picks on the second click. (2) listPicker's and nativeTalk's stamps are PRE-EXISTING (AUDIT 54, 6b3af1ea), not OT1's — the finding reads as if one slice minted all three. (3) The proposed fix is WRONG for listPicker: reordering to `(vx, vy, right, middle, now)` regresses its five NESTED callers, which all pass `(vx, vy, this._font)` — guildServiceWindows.js:158, itemMakerWindow.js:431, potionMakerWindow.js:342, travelMapWindow.js:1201, spellMakerWindow.js:845 — dropping the font seed into a `right` slot. Do not touch listPicker's signature.",
    "fixShape": "Content-guard the clock; never reorder. (a) src/ui/spellbookWindow.js:862 — add a `_now()` seam in listPicker.js:185's shape and make it `const t = this._now();`, dropping the `now` parameter at :777 entirely; update test/spellbookwindow.test.js:812-824 and the second OT1 test to drive `w._now = () => 1000` instead of a third positional. Minimum-diff alternative if the signature must stay: `const t = typeof now === 'number' ? now : Date.now();`. (b) src/ui/listPicker.js:360 — `const t = typeof now === 'number' ? now : this._now();`, signature untouched. (c) src/ui/nativeTalk.js:851 — same content guard (its 3rd slot is correctly `rightButton`; only the 4th takes `e.button === 1`). (d) SAME LANE, second divergence: BaseScreenComponent.cs:684-691 stores `lastLeftClickTime = leftClickTime` UNCONDITIONALLY — DFU never clears the stamp, so three fast clicks are click/double/double; delete `this._lastRowClick = null` at spellbookWindow.js:867, listPicker.js:365 and nativeTalk.js:855, and flip test/spellbookwindow.test.js:824 which pins the clearing. Pin: drive each window through its host's four-argument shape (`w.click(x, rowY(2), false, false)` twice) and assert nothing was used/picked.",
    "severity": "high",
    "laneKey": "src/ui/spellbookWindow.js"
  },
  {
    "id": "UI-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Same as HP-1 — judged in full under HP-1's id below, which is in this file. The mechanism, the three wired sites (world.js:4816, world.js:6721, worldModes.js:6824) and the two unwired hook literals (exterior.js:2168-2192, dungeonContext.js:4339-4395) are re-derived there; `grep -rn relock src/` returns those four files and no other. The commit body of MAC1 (6c017b5e) claims 'through a hooks.relock every host hands it', and Port-Ledger.md:173 records only the PITCH_FLOOR row for MAC1, so the gap is not a recorded deliberate.",
    "corrections": "Two beyond HP-1's. (1) Severity 'high' is inflated — the cost is one wasted click plus a 0.3 s SetClickDelay per resume, not lost state; medium. (2) The classic-skin half is real but mis-scoped: pauseWindow.js:389 `openClassicPauseFlow` indeed never reads `hooks.relock` and `_closeWith` (:186-189) runs inside the gesture, but the frame-late relock is `makeLookGate` (pointerLock.js:127-134) and it is the close path of EVERY canvas window in every host — inventory, spellbook, sheet, trade — not a pause-window property. Brief it as a separate item; folding it into the four-hosts fix makes the pause window look special when it is not.",
    "fixShape": "See HP-1. For the classic half only, if the lane takes it: src/ui/pauseWindow.js:186 `_closeWith()` and :191 `_confirmExit()` — call `this.hooks.relock?.()` after `this.done = true` and before the action hook, excluding the exit path, matching pauseDoor.js:165's order; but flag in the record that this closes one window of many and that the general fix is a gesture-scoped relock at the host drain sites (townTalk.js:1125, worldModes.js:7091, dungeonContext.js:4871).",
    "severity": "medium",
    "laneKey": "src/scenes/dungeonContext.js"
  },
  {
    "id": "UI-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "DFU re-read: DaggerfallControlsWindow.cs heads Joystick (:281), Mouse (:290), Defaults (:299), Continue (:321), CurrentBindings (:338) and KeybindButton (:361) with `if (waitingForInput) return;`, and :371-372 ANDs it with the unbound refusal. The port's classic twin carries it in one line (controlsWindow.js:298 `if (this.capture) return true;`); enhancedControls.js — a delta-new file (FIX-F, d2c35fe8) — carries none: :270 `key.onclick = () => arm(action)`, :271 oncontextmenu, :277 `clear.onclick`, :332-335 which/defaults/cont. I drove the pane headless on the suite's own fake document: after arming MoveForwards, `ctl-continue.onclick()` applied AND saved, `captureArmed()` stayed `'MoveForwards'`, and the very next document keydown (I sent Escape) landed as `currentDict(controlsStaging()).get('MoveForwards') === 'Escape'`; separately `ctl-which.onclick()` flipped `usingPrimary` true→false under the live capture, so the pending bind targets the other dict.",
    "corrections": "Two overstatements. (1) 'Re-arms a different row mid-capture' is a real divergence but leaks nothing — `arm()` calls `disarm()` first (enhancedControls.js:173), so exactly one capture listener exists at any time (my probe: listener count 1 before and after). (2) 'Silently rebinds' overstates: the repaint after applyAndSave still paints 'PRESS A KEY' on the armed row, and the stray key lands in the STAGED dict, so it needs a second Continue to reach the registry. The consequence is a wrong staged binding plus a swallowed keystroke, not a silent saved rebind.",
    "fixShape": "src/ui/enhancedControls.js — refuse every click surface while `armed != null`, citing :281/:290/:299/:321/:338/:361/:372 at the site: guard :270 `key.onclick`, :277 `clear.onclick`, :332 `which.onclick`, :333 `defaults.onclick`, :335 `cont.onclick`, and :202 `promptRemove`. Prefer one wrapper `const act = (fn) => (e) => { if (armed) return; fn(e); }` so a future control cannot be added without it. CORRECTION TO THE WRAPPER: :271 `oncontextmenu` must keep its own `e?.preventDefault?.()` BEFORE the refusal and still `return false`, or a refused right-click pops the browser's native context menu over the pane — a cost DFU's bare `return` does not have. Do NOT guard the prompt card's Yes/No: once :202 and :333 are guarded no prompt can be open while armed, and guarding the answer would strand an open card. Pin: arm a row, then click Continue / the Primary toggle / Defaults / a second row's key / its ✕ and assert after each that `captureArmed()`, `controlsStaging().usingPrimary` and the live registry are unchanged; each assertion must redden under deleting its own guard.",
    "severity": "medium",
    "laneKey": "src/ui/enhancedControls.js"
  },
  {
    "id": "UI-4",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "Probe against HEAD: `largeHudClick(bar, x, y, b)` for b in 0..4 over the inventory panel returns 'Inventory' for every button, and over interactionMode returns CycleModeForward for 0/1/3/4 and CycleModeBackward only for 2 — so buttons 1, 3 and 4 run the LEFT action. hudLarge.js:319 is `const action = (button === 2 && panel.right) ? panel.right : panel.action;` and :691-692 is the F42 sound gate `if (button === 0 || button === 2) audio.playOneShot(...)` sitting directly above an ungated `routeAction(hit.action, ctx)`. DFU HUDLarge.cs:170-232 registers OnMouseClick + OnRightMouseClick on all eleven panels and no OnMiddleMouseClick anywhere in the file; BaseScreenComponent.cs:681-724 dispatches left/right/middle in three independent blocks with no fallback. All four hosts pass the raw DOM button (world.js:5046, exterior.js:2426, dungeon.js:323, worldModes.js:7077).",
    "corrections": "Three. (1) WRONG AUTHOR: `git log -S` puts both the fall-through line and the self-contradicting header sentence at hudLarge.js:303-306 in AUDIT 54 (6b3af1ea), pre-delta; AUDIT 64 F42 (0de73325) added only the sound gate. The defect is real but it is not 'F42 applied the law to the sound only'. (2) A STANDING PIN CERTIFIES THE DIVERGENCE — test/hudlarge.test.js:213-214 `// a middle click takes the left handler - DFU binds only the two` / `assert.equal(at('map', 1).action, 'AutoMap')` — a Home.md 'A PIN MUST FAIL' case that would block its own fix; the lane must flip it. (3) Severity is lower than claimed: routeLargeHudClick returns false unless `largeHudEnabled() && !windowUp && cursorActive()` (:673), so the bar only takes clicks after the player frees the cursor with the ActivateCursor toggle.",
    "fixShape": "src/ui/hudLarge.js:314-320 — `if (button !== 0 && button !== 2) return null;` inside `largeHudClick` after the panel resolve; :691 — the F42 gate then collapses to an unconditional `audio.playOneShot(SOUND.ButtonClick, 1)`, which is DFU's own order (the sound is the first statement of all thirteen handlers). Amend BOTH headers rather than leaving them standing: :303-306 states the fall-through as the rule, and :665-671 ('A HIT IS CONSUMED WHETHER OR NOT ANYTHING ANSWERS IT') is written about unwired ACTIONS, not unbound BUTTONS — returning false changes the consumption answer and the sentence must say so. Falling through is safe: `isSwingButton` (input.js:301) resolves the swing from the bindings' MOUSE_CODES so an aux button cannot swing, and `requestLook` is a no-op while `cursorActive()` (pointerLock.js:84); the only residue is the `setClickDelay` arm every cursor-active miss already takes. Flip test/hudlarge.test.js:213-214 to `assert.equal(at('map', 1), null)` and add 3/4.",
    "severity": "low",
    "laneKey": "src/ui/hudLarge.js"
  },
  {
    "id": "UI-5",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "nativeInventory.js:354 seeds `this._mouse = [-1, -1]`; :1045 inside `hover(vx, vy)` is the only writer in the file (grep `_mouse` returns 354/956/1045); :954 `wheel(dir)` reads `const [vx, vy] = this._mouse` and every `scrollerHit` against (-1,-1) misses. The three wheel routers carry no position — townTalk.js:1183 `overlay.wheel?.(Math.sign(e.deltaY))`, worldModes' `wheel`, dungeonContext.js:4883 `overlayWheel(dir) { activeOverlay?.wheel?.(dir); }` — and `hover` is fed only from pointermove (townTalk.js:1176, worldModes.js:7189-7196), which does not fire when the lock is dropped and the cursor reappears in place. DFU BaseScreenComponent.cs:724-736 guards the scroll block on `mouseOverComponent`, recomputed each Update from the live scaled mouse position. F52 is in the delta (AUDIT 64).",
    "corrections": "One factual error: 'the same seeded-sentinel shape rides nativeTalk._mouse and itemMakerWindow._mouse' is wrong — nativeTalk.js:424 and itemMakerWindow.js:230 both seed `[0, 0]`, not the leave sentinel. The two that seed `[-1,-1]` are nativeInventory.js:354 and spellMakerWindow.js:444, and spellMaker is not this delta's. The symptom is effectively the same at [0,0] (top-left hits no scroller), but the stated mechanism — 'the hosts' pointer-LEAVE sentinel' — is true of one window only.",
    "fixShape": "Carry the point on the event, which is the only shape that matches BaseScreenComponent.cs:724-736's per-frame `mouseOverComponent`. src/ui/nativeInventory.js:954 — `wheel(dir, vx = this._mouse[0], vy = this._mouse[1])` and use the parameters below. Then the seams: src/scenes/townTalk.js:1181-1186 `wheel(e)` already has the WheelEvent and the canvas — compute the native point exactly as `hover` does at :1168-1176 and pass it; src/scenes/worldModes.js's `wheel` (the twin of `hover` at :7177-7196) likewise, for BOTH the interior slot and `dungeonCtx.overlayWheel`; src/scenes/dungeonContext.js:4883 `overlayWheel(dir, vx = -1, vy = -1)` forwarding to `activeOverlay?.wheel?.(dir, vx, vy)`. THE FOUR HOSTS RULE applies: name world.js, exterior.js, worldModes.js and dungeonContext.js (plus standalone interior.js/dungeon.js) in the record, each wired or flagged. If the seam is not to move this lane, seed `_mouse` at mount from the host's last free-cursor position — do NOT simply change [-1,-1] to [0,0], which is the sibling windows' bug rather than a fix. Pin: build the window through its real producer, call `wheel(1)` with no prior `hover` and a cursor over the local list, and assert `scroll` moved.",
    "severity": "low",
    "laneKey": "src/ui/nativeInventory.js"
  },
  {
    "id": "HP-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "`grep -rn relock src/ test/` returns exactly four source files: pauseDoor.js:165 `if (action !== 'exit') hooks.relock?.();` (the only reader), world.js:4816, world.js:6721 and worldModes.js:6824 `relock: host.relock`. I read both unwired hook literals in place: exterior.js:2166-2192's `togglePause` ends at `questMessages/questLog` with no `relock` key, and dungeonContext.js:4336-4396's `togglePause` carries quickSave/quickLoad/playerName/saveAs/loadKey/pushWindow/exitToMenu/textLines and no `relock`. Reachability re-derived: world.js:4928 gates its own ladder on `(modes?.mode ?? 'exterior') === 'exterior'`, so Escape underground falls to worldModes.js:7027 `routeKey(e, dungeonCtx, ...)` → input.js:524 → dungeonContext.togglePause — the classic-start host's pause door is the unwired one. The mechanism is real: `makeLookGate` (pointerLock.js:127-134) relocks on the close EDGE, i.e. the next frame outside any gesture. MAC1's commit body claims 'through a hooks.relock every host hands it'; Port-Ledger.md:173 rows only PITCH_FLOOR for MAC1, so nothing records this as deliberate. test/mac1_playreport.test.js:252-254 asserts the three wired sites and stops, under a title claiming every host.",
    "corrections": "One material error in the fix: 'dungeonContext already holds canvas' is false — dungeonContext.js:4666 states outright 'this context owns no canvas of its own', and the only canvas it sees is `_weaponCanvas` (:2465, :3555), a per-call argument of drawFoes. The relock must arrive through `opts` from each dungeon host, exactly as `onTeleport` / `useMagicItem` / `setupStaticNpc` already do. Minor: the 'six flows' framing double-counts exterior.js (one host, two arms); and exterior.js's recorded charter (:2575-2597, :1678) really does not list `relock`, so it is not covered by that host's exemptions.",
    "fixShape": "Four files. (1) src/scenes/exterior.js:2180 — add `relock: () => requestLook(canvas),` to the `openPauseFlow` hooks literal beside `savingPrevented` (`requestLook` is already imported at exterior.js:12). (2) src/scenes/exterior.js — add `relock: () => requestLook(canvas),` to the `createWorldModes({...})` host bag (~:2910-3120), or worldModes.js:6824's `host.relock` stays `undefined` in the ?exterior host's interior pause. (3) src/scenes/dungeonContext.js:4380 — add `relock: () => opts.relock?.(),` to `togglePause`'s hooks literal beside `quickSave`, and thread it in from BOTH dungeon hosts: src/scenes/dungeon.js:107's opts bag and src/scenes/worldModes.js:4892's opts bag, each `relock: () => requestLook(canvas)` (worldModes and dungeon.js both own the canvas and already import requestLook). (4) test/mac1_playreport.test.js:247-255 — extend past the three source regexes so the pin's own four-hosts title bites; deleting any one `relock:` line must redden. Do not relock on 'exit' anywhere (pauseDoor.js:164's law).",
    "severity": "medium",
    "laneKey": "src/scenes/dungeonContext.js"
  },
  {
    "id": "HP-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived line by line. worldModes.js:5107 opens the DUNGEON `_enemyArm` (`dungeonCtx ? tryMobileEnemyActivate(...)`) and :5111 is `modal: (t) => mountInterior(new ActionTextBox(...))`; `mountInterior` (:6638) pushes onto `interiorWindows`, whose `onTop` (:990) writes `interiorOverlay`. The dungeon arm of `frame()` ends `return true;` at :5724, above the only interiorOverlay tick/drain/draw block (:6038-6076); keydown is gated `mode === 'interior'` at :7022; the pointer seam returns at :7081; `overlayHeld` (:5355-5357) and `modeHudCovered` (:1029-1030) both omit the interior slot in dungeon mode. The sink really fires: probing `activateMobileEnemy(foe, 1.0, 'steal', player, {...})` with a forced success returned `modal=['You pinched 4 gold pieces.']` (talk.js:339/:341 set `modal:true`; mobileEnemyActivate.js:113 `if (r.modal) modal?.(r.message)`). DFU PlayerActivate.cs:1640/:1646 `DaggerfallUI.MessageBox(...)` inside `Pickpocket()` (:1611) pushes onto `uiManager.TopWindow` — the current scene's stack, which underground is the dungeon's. Audit-63.md:128-131 records F33 as 'Wired into all five activation ladders' with no flag on this sink.",
    "corrections": "Two small ones. `mountInterior` does not assign `interiorOverlay` directly — it goes through `interiorWindows.pushWindow` → the `onTop` callback at :990, which is what leaves the slot truthy and blocks `mountSpellWindow` (:1060) and `interiorKeyCtx.uiOverlayActive` thereafter; state it that way so the fix is not read as a slot assignment. And I checked the claim 'the only dungeon-mode-reachable mountInterior in the file': it holds — :301 is the interior exhaustion arm, :2280/:2298 merchant, :4630 the interior enemy arm, :4738 G5 loot, :6705-6912 the interiorKeyCtx bag routeKey reaches only in interior mode, and :408's `wireInfectionVideos` is re-registered by dungeonContext.js:1178 on dungeon mount, so it is NOT a second instance.",
    "fixShape": "src/scenes/worldModes.js:5111 — `modal: (t) => mountServiceWindow(new ActionTextBox(String(t).split('\\n'))),`. `mountServiceWindow` (:1112-1118) routes dungeon → `dungeonCtx.showOverlay`, which is the same stack dungeon.js:233 reaches through `ctx.hudBox` → dungeonContext.js:4274 `pushDungeonWindow`, and its interior arm is the identical slot so the interior ladder at :4630 needs no change and must not be touched. Pin: drive worldModes into dungeon mode with a stub dungeonCtx, run the dungeon activate ladder in steal mode against a class foe with a forced-success roll, and assert `dungeonCtx.showOverlay` took an ActionTextBox carrying the pinched-gold row AND that `interiorOverlay` is still null; restoring `mountInterior` must redden both. Add the standing structural assertion that no `mountInterior(` appears inside the `mode === 'dungeon'` ladder.",
    "severity": "high",
    "laneKey": "src/scenes/worldModes.js"
  },
  {
    "id": "HP-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "worldModes.js:5110 `hud: (t) => say(t)` sits in the DUNGEON `_enemyArm`, and `say` resolves to `townTalk.say`. Both queues are the same class and therefore the same anchor: dungeonContext.js:1346 `const hudText = new HudText();` and townTalk.js:129 `const hud = new HudText();`. Both paint on a world-hosted dungeon frame — dungeonContext.js:4152 `hudText.draw(...)` inside `drawFoes` (called at worldModes.js:5718), and world.js:7131 `townTalk.frame(dt)` runs UNCONDITIONALLY after `modes.frame` returns true at :7110, with townTalk.js:1079 `hud.draw(renderer, canvas, font, s)` inside it. The correct sibling is dungeon.js:232 `hud: (t) => ctx.hudSay?.(t)` → dungeonContext.js:4273 `hudSay: (t) => hudText.add(t)`. DFU PlayerActivate.cs:1651 is `DaggerfallUI.Instance.PopupMessage(...)`, one PopupText per HUD.",
    "corrections": "Two cite/scope fixes. `say` is defined at worldModes.js:372, not :360 (:356-372 is the comment block above it). And the claim that both columns 'anchor at the same native rows' is inferred from their being the same HudText class — I did not compare the two draw scales (townTalk uses `hudScale`, dungeonContext `hudScaleFor`), so the overprint is near-certain but not pixel-verified; say so in the record. Also worth noting for the lane: the too-far refusal is NOT affected — it goes through `midScreen`, whose default is `setMidScreenText` at mobileEnemyActivate.js:93-94, so only the Info line and the 'You are not successful.' line land on the wrong queue.",
    "fixShape": "src/scenes/worldModes.js:5110 — `hud: (t) => dungeonCtx.hudSay(t),` (unconditional is safe: :5107 already short-circuits the whole arm on a null `dungeonCtx`). Leave the interior arm at :4629 alone — worldModes.js:356-372 records that in interior mode townTalk's queue IS the host's queue, ticked and drawn at :6035. Pin: in HP-2's dungeon-mode harness, run the enemy arm in Info mode against a live foe and assert the line reached `dungeonCtx.hudSay` and NOT the townTalk stub's `say`; reverting to `say(t)` must redden. Pair it with a structural assertion that both halves of the dungeon `_enemyArm` name `dungeonCtx` sinks, so the family cannot split again.",
    "severity": "low",
    "laneKey": "src/scenes/worldModes.js"
  }
]
```