All findings re-derived at HEAD. Final verdicts:

```json
[
  {
    "id": "CV-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "TextProvider.cs:503 is verbatim `skillValueToken.text = string.Format(\"{0}%\", playerEntity.Skills.GetLiveSkillValue(skill));` inside GetSkillSummary (:489-529), and DaggerfallCharacterSheetWindow.ShowSkillsDialog (:276-325) builds EVERY row through that one member (:288, :296, :301) - the permanent value is never read on that path, and the dialog's own hand-to-hand row at :313-314 calls GetLiveSkillValue twice. The port's classic dialog at src/ui/charsheet.js:659 prints `e.skills?.[id] ?? 0` (raw permanent array - `skillValue` is imported at :33 and used four lines later at :663 for the damage row), and git blame puts the whole statement in the delta (42282fec8, AUDIT 63 F35). The enhanced twin is the same split inside one model: src/ui/enhancedCharSheet.js:118 `attributes: ... liveStat(e, key)` and :135-137 `handToHandDamage` off `skillValue`, against :126 `skill: (id) => e.skills?.[id] ?? 0`, consumed for both the number AND the meter at src/ui/enhancedMenu.js:1547-1548. skillValue (src/systems/skills.js:78-96) folds `_enchantMods.skillMods` (+15 EnhancesSkill) and racialOverride skillMods (LYCANTHROPE_SKILL_MOD 30 over seven skills incl. HandToHand, src/systems/lycanthropy.js:92-97), so a lycanthrope reads `Hand-to-Hand 45%` on one line and a damage range computed from 75 on the next. Nothing in Audit-63.md, Audit-64.md or Port-Ledger section A records this as deliberate.",
    "corrections": "Two small ones. (a) `e.skills?.[id] ?? 0` is not quite `permanentSkillValue` - it also bypasses `entity.skillOverrides`, so the divergence is wider than stated (irrelevant for the player, who has none, but it means the fix must use `skillValue`, not `permanentSkillValue`). (b) The finder did not name the enhanced skin's CONSUMER: enhancedMenu.js:1547-1548 draws both the value text and `pxMeter(m.skill(id), 100, 'thin')` off the accessor, so fixing :126 moves the meter too - which is correct (DFU's is a number, not a bar, and the bar should track the same figure the attributes bars do). Also note enhancedCharSheet.js:126 is PRE-delta (b8a13b73); only the classic :659 and the two hand-to-hand arms are added lines, so the delta created the mismatch rather than the enhanced literal.",
    "fixShape": "src/ui/charsheet.js:659 - replace `${e.skills?.[id] ?? 0}%` with `${skillValue(e, id)}%` (skillValue already imported at :33); leave the `getSkillRecentlyIncreased` colour arm at :661 untouched. src/ui/enhancedCharSheet.js:126 - replace `skill: (id) => e.skills?.[id] ?? 0` with `skill: (id) => skillValue(e, id)` (imported at :68). No change at src/ui/enhancedMenu.js:1547-1548 - it reads through the model. src/ui/charsheet.js:692 (the art-less `_drawFallback` pane) is NOT the member DFU's GetSkillSummary owns; convert it in the same edit for consistency or leave it, but do not let it hold the lane. The pin must be built from the real producer (chargen character -> applyLycanthropy round that writes the racialOverride skillMods), assert the row model equals permanentSkillValue+30 AND that the same page's damage row is computed from that same number; mutating either back to `e.skills[id]` must redden it.",
    "severity": "medium",
    "laneKey": "src/ui/charsheet.js"
  },
  {
    "id": "CV-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "I read both prefabs out of the object store myself: PlayerAdvanced.prefab CharacterController is `m_Height: 1.8 / m_Radius: 0.35 / m_SkinWidth: 0.06`, and `DaggerfallEnemy [Game Serializable].prefab`:442-446 is `m_Height: 1.8 / m_Radius: 0.4 / m_SkinWidth: 0.05`; SetupDemoEnemy.cs:99-114 touches only `controller.height`, never the radius. So src/systems/spellcast.js:257-261's comment - \"the player's is the same radius\" - is false at the prefab, and BODY_CAPSULE_RADIUS 0.45 is applied to the player by src/scenes/hostMagic.js:297 (the ONE AoE arm every explodeAt caller reaches) and :593, src/scenes/dungeonContext.js:2818 and :2907, and src/combat/arrowFlight.js:91 (whose own comment at :86 says \"MISSILE_COLLIDER_RADIUS + the 0.45 body\"). I reproduced the consequence in node against the live module: the port hits the player at 0.89 from the capsule axis and misses at 0.91, where DFU's `Physics.SphereCast(colliderPosition, ColliderRadius=0.45, ...)` (DaggerfallMissile.cs:339) meets a 0.35 capsule at 0.80; the AoE catch is 4.45 against DFU's 4.35 (OverlapSphereNonAlloc at :481). The 0.45 also feeds the axis inset `half = Math.min(BODY_CAPSULE_RADIUS, h/2)` at :307, so a crouched player (h 0.9) collapses to a sphere where a 0.35 body would keep a real segment - the fix must thread the radius through BOTH uses, not just the final comparison.",
    "corrections": "Three, and the first is the brief's question. (1) SKIN WIDTH DOES NOT BELONG. DFU reads the raw `controller.radius` at every geometry site I can find - PlayerHeightChanger.cs:530 `Physics.SphereCast(ray, controller.radius, distance)`, EnemyMotor.cs:679/:1144/:1154/:1191 - and never `radius + skinWidth`; skinWidth is PhysX contactOffset for the controller's own Move, and the only DFU read of it is as a ray LENGTH (DaggerfallActionCollision.cs:82). The port's own collider model already agrees: src/player/collider.js:439-453 sweeps the player at exactly CAPSULE_RADIUS 0.35 with no skin. So the player value is plain `CAPSULE_RADIUS` (0.35), NOT the finder's alternative 0.41. (2) The finder's \"0.45 is right for foes and only for foes\" is WRONG by the same argument - the foe's query radius is 0.40, and 0.45 is skin-inflated on that side too (the coincidence with MISSILE_COLLIDER_RADIUS 0.45 = DaggerfallMissile.ColliderRadius is what makes the error invisible). (3) The fix does NOT land at dungeonContext.js:2732/:2900/:2912 or in worldModes.js/exterior.js: those are explodeAt CALLERS that funnel into hostMagic.js:291-300, and arrows are the shared src/combat/arrowFlight.js, not per-host. Five direct call sites, not eight.",
    "fixShape": "src/systems/spellcast.js: give the body radius a parameter - `sphereOverlapsCapsule(pos, radius, feet, height, bodyRadius = FOE_CAPSULE_RADIUS)` using it at BOTH :307 (`Math.min(bodyRadius, h/2)`) and :309; same parameter on `missileHitsCapsule` (:287-288). Replace :262's `BODY_CAPSULE_RADIUS = 0.45` with `FOE_CAPSULE_RADIUS = 0.4` (enemy prefab m_Radius, skin excluded) and rewrite :257-261, which currently states the false premise; import `CAPSULE_RADIUS` from '../player/motor.js' rather than restating 0.35 (ONE DFU MEMBER, ONE EXPORT). Pass `CAPSULE_RADIUS` at the five player arms: src/scenes/hostMagic.js:297 and :593, src/scenes/dungeonContext.js:2818 and :2907, src/combat/arrowFlight.js:91 (and fix arrowFlight.js:86 and hostMagic.js:296, both of which spell \"0.45 body\" for the player). Update the pin at test/audit62_foes.test.js:206 (`assert.equal(BODY_CAPSULE_RADIUS, 0.45, \"m_Radius 0.4 + m_SkinWidth 0.05\")`) and the derived bounds at :212-213/:223-224 and test/roadh_tail.test.js:46-51. IF the lane judges the foe change too wide to take here, it must at minimum split player from foe and record the foe skin-width row - but do not keep the comment's claim. SAME ROOT CAUSE, worth the same edit: src/characters/enemyMotor.js:299/:301 derive an ENEMY's obstacle cast from the PLAYER's CAPSULE_RADIUS (0.35) where EnemyMotor.cs:1144/:1154 read the enemy's own `controller.radius` (0.4) - 0.2475 vs 0.2828 and 0.175 vs 0.2. Two named radii solve all of it.",
    "severity": "medium",
    "laneKey": "src/systems/spellcast.js"
  },
  {
    "id": "CV-3",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "Three literals confirmed at HEAD: src/render/waterSurface.js:66 `export const WATER_SCROLL_TILES_PER_SEC = 0.05` (added by WATER1, c03e047be, in the delta) under a docstring at :63-65 naming scenes/dungeon.js as the owner without importing it; src/scenes/dungeon.js:72 (consumed :907); src/scenes/worldModes.js:251 `DUNGEON_WATER_SCROLL = 0.05` (consumed :5722). Home.md's ONE DFU MEMBER, ONE EXPORT covers a class's constants explicitly - \"If two files legitimately need it, one exports and the other imports - never two literals\" - and the delta took it from two to three. What upgrades this above pure hygiene is the pin WATER1 shipped beside it: test/water.test.js:101 `assert.equal(WATER_SCROLL_TILES_PER_SEC, 0.05, 'scenes/dungeon.js WATER_SCROLL_TILES_PER_SEC')` labels itself as pinning scenes/dungeon.js's rate while asserting only the waterSurface export - change dungeon.js:72 to 0.06 and that pin stays green, which is A PIN MUST FAIL, at HEAD, in an added line.",
    "corrections": "The finder overstates the case twice. (a) There is no observable consequence at HEAD - all three read 0.05 and no runtime path can make them differ without an edit - which is the exact ground on which Audit-64.md:64-66 REFUTED the sibling `goldPieceWeightInKg` (\"a restated constant ... unobservable\"). It is distinguishable (DFU itself spells 0.0025 twice, so that port literal mirrored the reference; the water rate has no DFU original and its own docstring asserts a cross-module equality nothing enforces) but the finder should have argued that distinction rather than asserting the shape alone. (b) The finder's own coverage block misquotes this module - it lists \"WATER_F0 0.02, SHORE_SOFTNESS 0.16\" where waterSurface.js:60/:62 read 0.5 and 0.9 - so treat its unverified coverage claims with care. Severity stays low; this must not displace CV-1/CV-2 in a lane.",
    "fixShape": "Make src/render/waterSurface.js the one home (lowest module, already exports the water constant set): delete src/scenes/dungeon.js:72 and src/scenes/worldModes.js:251, import `WATER_SCROLL_TILES_PER_SEC` from '../render/waterSurface.js' in both hosts, and drop the `DUNGEON_WATER_SCROLL` alias at the two consumers (dungeon.js:907, worldModes.js:5722). Rewrite waterSurface.js:63-65 to state the rate rather than point at a module that no longer declares it. Then FIX THE PIN, which is the half that earns the lane: test/water.test.js:101 must assert the single source - a source-level scan of non-comment `src/**/*.js` for a water-scroll declaration asserting exactly one, in render/waterSurface.js - so that reintroducing a second literal reddens. The behaviour half (same `seconds` -> `waterUniforms().scroll` equals the dungeon host's drawWater scroll argument) is worth adding but the source scan is what cannot be satisfied by accident.",
    "severity": "low",
    "laneKey": "src/render/waterSurface.js"
  },
  {
    "id": "CV-4",
    "upheld": false,
    "confidence": "high",
    "reasoning": "I found Internal_Strings.csv in the reference clone's object store at `Assets/StreamingAssets/Text/Master Localization CSV Files/Internal_Strings.csv` and read the two records: line 36 `storeClosed,Store is closed. Open from %d1:00 to %d2:00.` and line 37 `guildClosed,Guild is closed. Open from %d1:00 to %d2:00.` The port's line at src/scenes/worldModes.js:4061-4062 produces `Store is closed. Open from 9:00 to 23:00.` / `Guild is closed. Open from ...` - byte for byte the shipped records with %d1/%d2 filled from OPEN_HOURS/CLOSE_HOURS. The text is not invented; the two records differ ONLY in the leading noun, so the port's one-template-with-a-swapped-noun is a faithful fold of them, not a shape DFU does not have. And the claim that the line \"carries none\" of the csv cites is false: the function's own docblock four lines above, at src/scenes/worldModes.js:4049, already reads \"(:473-483; Internal_Strings.csv:36-37 gives both literals, ':00' suffixes and all)\" - the correct file, the correct pair of lines. PlayerActivate.cs:477-480 and the hour tables at :91-92 check out exactly as the port has them.",
    "corrections": "The title, the claim and the evidence are all wrong on substance: the sentence is verbatim, the cite exists and is exact, and the C# guard (`!buildingUnlocked && buildingType < Temple && buildingType != HouseForSale`, PlayerActivate.cs:474-475) is mirrored line for line at worldModes.js:4059-4060. The one residue is stylistic: DFU picks between two whole localized records where the port folds them, which matters only under a localization layer the port does not have. The finder's proposed fix would have REWRITTEN a correct string into two constants for no behavioural gain, and its pin (\"assert the two differ by more than the first word\") asserts a property the reference records do not have - they differ by exactly the first word.",
    "fixShape": "No code change. If a lane touches this region at all, the most it should do is carry the cite down to the statement itself - append `// Internal_Strings.csv:36-37: storeClosed / guildClosed, which differ only in the leading noun` at src/scenes/worldModes.js:4061 - so a reader at the line need not walk up to the docblock at :4049. Do NOT re-word the string, do not split it into two constants in systems/buildingLocks.js, and do not add the proposed pin as specified.",
    "severity": "none",
    "laneKey": "src/scenes/worldModes.js"
  },
  {
    "id": "RC-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "src/ui/enhancedControls.js:29 cites `DaggerfallControlsWindow.cs:73` for \"ReservedKeys is empty in DFU\". I counted that file's lines: :68 `public override void Update()`, :69 `{`, :70 `base.Update();`, :72 `if (!AllowCancel && !waitingForInput && InputManager.Instance.GetBackButtonDown())`, :73 `{`, :74 `ShowMultipleAssignmentsMessage();`. Line 73 is an opening brace in the back-button arm. `grep -n ReservedKeys` on that file returns exactly one hit and it is a READ, not a declaration: :410 `if (code != KeyCode.None && InputManager.Instance.ReservedKeys.FirstOrDefault(x => x == code) == KeyCode.None)`. The declaration is in another file entirely - InputManager.cs:73 `KeyCode[] reservedKeys = new KeyCode[] { };`, exposed at :174-176 - so the address is wrong but the LAW is right (the array really is empty, the :410 gate never refuses a key, Escape binds), and the port's code follows the right reading. Comment-only.",
    "corrections": "Severity downgraded from medium to low: no code changes, no runtime consequence, and the law the pane rests on is correct. More importantly the proposed PIN is wrong twice over. (a) `test/citedrift.test.js` does NOT only resolve cites into the port's own files - it already imports `dfuFile`/`missingDfu` from test/dfuRoot.mjs at :38 and already resolves C# cites at :1019-1027 (`DaggerfallSpellBookWindow.cs:507 is no longer UpdateSelection`), so a new sibling file is redundant. (b) `tools/parity/dfu/` is GITIGNORED (.gitignore:24) and dfuRoot.mjs says so outright - \"CI has no DFU tree and Port-Doctrine keeps it an EXTERNAL reference, never vendored\" - so a resolver that only reads C# SKIPS in CI and is a pin that provably cannot redden there, precisely the shape this audit condemns.",
    "fixShape": "Comment only: src/ui/enhancedControls.js:29 - rewrite to \"ReservedKeys is empty in DFU (InputManager.cs:73 `new KeyCode[] { }`, exposed :174-176), so the capture gate that consults it (DaggerfallControlsWindow.cs:410) never refuses a key - Escape included\". Naming both the declaration and the consuming site is what makes it checkable at both ends. For the pin, extend test/citedrift.test.js following the CD8b pattern already there at :1008-1027, NOT a new file: a LITERAL half that runs everywhere (assert the comment's cite string reads `InputManager.cs:73`, which reddens in CI on any regression) plus a RESOLVE half gated on `missingDfu(...)` that checks `new KeyCode[] { }` is on that line of the real file. A generalized 'walk every .cs cite in src/' arm may be added, but brief the lane that it is a local-only (DFU_PATH) check and must be paired with a literal half or it can never redden.",
    "severity": "low",
    "laneKey": "src/ui/enhancedControls.js"
  },
  {
    "id": "RC-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Counted GameObjectHelper.cs myself: :914 `Quest quest = QuestMachine.Instance.GetQuest(link.questUID);`, :915-916 the null throw, :917 BLANK, :918 `// Get the Place resource referenced by this link`; the call the port's comments describe is at :926 `QuestResourceBehaviour[] resourceBehaviours = Resources.FindObjectsOfTypeAll<QuestResourceBehaviour>();` under DFU's own :923-925 comment \"Helps ensure a resource is not injected twice\". `grep -rn IsAlreadyPlaced tools/parity/dfu/Assets/Scripts` returns NOTHING; the real guard is `IsAlreadyInjected` at :978, called at :950. Five port sites carry the invented name - src/scenes/worldModes.js:1413, :1423, :1434 and the cites at :1412/:1434 (`:917`) - against src/scenes/dungeonContext.js:5141, which cites the RIGHT line (`:926`) with the same wrong name. The port's LAW is right and its code follows it (sceneBehaviours() unions the dungeon people at worldModes.js:1425), so a lane touches comments only. Delta membership confirmed: :1423 is c6ac15d1d and :1434 is eac56a711, both in 0fe2c05b..HEAD; :1412-1413 are pre-delta (6b3af1ea).",
    "corrections": "The finder's own list is slightly off (it writes \"worldModes.js:1412, :1414, :1423, :1434\"; the invented-name lines are :1413, :1423, :1434 and the `:917` cites are on :1412 and :1434) - minor, but the lane should work from the grep, not the list. It also missed the strongest piece of evidence: src/systems/quest/sceneMount.js:125 ALREADY has it right - `/** IsAlreadyInjected (GameObjectHelper.cs:978-991).` - so the port names the same member correctly in the quest subsystem and wrongly in both hosts, which is the ONE DFU MEMBER, ONE NAME violation stated at its full size. Severity downgraded from medium to low (comment-only, no runtime consequence), though this is first among the three cite fixes because an invented name is what makes a future grep for the real member miss every site that models it. The pin caveat from RC-1 applies: the 'unknown-member' arm reads only the C# tree and therefore skips in CI.",
    "fixShape": "Comments only, six edits: src/scenes/worldModes.js:1412 `:917` -> `:926`; :1413 `IsAlreadyPlaced` -> `IsAlreadyInjected (GameObjectHelper.cs:978, called :950)`; :1423 same rename; :1434 `:917` -> `:926` and the same rename; src/scenes/dungeonContext.js:5141 keep `:926`, rename only. Leave src/systems/quest/sceneMount.js:125 alone - it is the correct sibling and the model for the wording. Pin: add to test/citedrift.test.js (not a new file) a literal half asserting no `IsAlreadyPlaced` occurs anywhere under src/ - that arm needs no DFU checkout and reddens in CI on regression - plus, gated on `missingDfu`, a resolve half asserting `FindObjectsOfTypeAll<QuestResourceBehaviour>` is on GameObjectHelper.cs:926 and `IsAlreadyInjected` on :978.",
    "severity": "low",
    "laneKey": "src/scenes/worldModes.js"
  },
  {
    "id": "RC-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "All five off-by-ones reproduced against the on-disk C#, with `cat -n`. StaticNPC.SetLayoutData(DFBlock.RdbObject) is :149 the call, :150 XPos/YPos/ZPos, :151 Flags, :152 FactionOrMobileId, :153 TextureArchive, :154 TextureRecord, :155 `obj.Resources.FlatResource.Position`, :156 CurrentMapID, :157 LocationIndex, :158 `0);`, :159 `npcData.context = Context.Dungeon;` - and the nameSeed the comment is about is `data.nameSeed = (int)position ^ buildingKey + locationIndex;` at :218, built from the Position argument. So dungeonContext.js:546's `:154` should be `:155` (it cites TextureRecord) and :549's `:157` should be `:158` (it cites LocationIndex). DaggerfallStartNewGameWizard.SummaryWindow_OnClose's cancel arm is :566-577 with :569 startingSkills, :570 workingSkills, :571 startingStats, :572 workingStats, :573 `var bonusSkillPoints`, :574 SetBonusSkillPoints, :575 faceIndex - so chargen.js:1325's `:570-571` (the two STAT copies) should be `:571-572` and chargen.js:1407's `:575` (SetBonusSkillPoints) should be `:574`. SerializableEnemy.cs:116 is `data.questSpawn = enemy.QuestSpawn;` and :117 is mobileGender, so exteriorFoes.js:997 quotes :116's text while citing :117. I verified every LAW is right and every port behaviour follows the correct reading (position: f.flatPosition; buildingKey 0; shared stats/rolledStats; the six-item copy list with the stat bonus pool absent; questSpawn saved), so all five are comment-only, and all five lines are in the delta (c6ac15d1d, 8ce09bc3a, 45089ece2).",
    "corrections": "Two. The finder writes the nameSeed line as `StaticNPC.cs:212`; it is :218 (:212 is `// Store common layout data`), so the lane must not copy that number into the corrected comment. And its neighbour-exactness claim checks out where I sampled it - SerializableEnemy.cs:121/:124/:125/:126/:129-133 and :205-218/:225-228, CreateCharSummary.cs:125 `this.statsRollout.BonusPool = 0` - which is what makes 'counting slip inside an argument list', not revision drift, the right diagnosis. Severity low is correct. The proposed pin's strict single-line mode is the right rule but, per RC-1, it belongs as an arm of test/citedrift.test.js gated on `missingDfu`, and cannot redden in CI; wiring it into tools/citeShift.mjs is a separate and larger job than these five one-character edits and should not gate them.",
    "fixShape": "Comment-only, five edits, each a single number: src/scenes/dungeonContext.js:546 `StaticNPC.cs:154` -> `:155`; src/scenes/dungeonContext.js:549 `(:157)` -> `(:158)`; src/ui/chargen.js:1325 `DaggerfallStartNewGameWizard.cs:570-571` -> `:571-572`; src/ui/chargen.js:1407 `(through SetBonusSkillPoints, :575)` -> `:574`; src/scenes/exteriorFoes.js:997 `SerializableEnemy.cs:117` -> `:116`. Leave the bracketing cites in the same comments alone (dungeonContext.js:544 `:149-151`, :549 `:159`; chargen.js:1405 `:566-577`; exteriorFoes.js:998-1000 `:129-133`, `:205-218`) - all verified exact. Where a comment already quotes the C# text, keep the quote: it is what made the exteriorFoes slip self-correcting. Do NOT touch src/systems/biography.js:200/:264 (BiogFile.cs:215/:212 vs on-disk :216/:213) in this lane - that is consistent drift, a different judgement.",
    "severity": "low",
    "laneKey": "src/scenes/dungeonContext.js"
  }
]
```

Two notes outside the JSON. First, `git status --short` is not empty: `src/scenes/shared.js` carries a live mutant (`DEATH_VIDEO_WATCHDOG_MS` 30000 → 300) that appeared during my pass and is not mine — I made no edits and reverted nothing, since it is almost certainly the other refuter's in-flight probe. My own footprint on src/, test/ and tools/ is zero.

Second, the single highest-leverage correction across both lens files: `tools/parity/dfu/` is gitignored and `test/dfuRoot.mjs` already exists as the seam for reading it, with a C#-cite arm already live in `test/citedrift.test.js:1008-1027`. All three RC findings proposed new C#-resolving test files; any such pin skips in CI, so each needs a literal half (assert the cite string in the port comment) paired with the resolve half, or it is a pin that cannot redden.