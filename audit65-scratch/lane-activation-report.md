# Lane activation (HP-2, HP-3, MC-2) - commit bc0494a4 on a65/activation (Opus resume of WIP 3094607a; 296k tokens, 125 tool uses, 30 min)
Diff 583346e3..bc0494a4: 35 files, +731/-155 (src: activate.js, mobileEnemyActivate.js, world/exterior/worldModes/dungeonContext/dungeon/townTalk/corpseMarker/droppedLoot/interior.js, midScreenText.js + 5 cite-only; 10 tests; 7 bible).
WIP right: HP-2/HP-3 sinks (dungeonCtx.hudBox / hudSay unconditional - verdict B over A's mountServiceWindow), MC-2 per-family reach at every mint site, `reach` on pickActivatableHit, door/record split, F33 near/far collapse to one _enemyArm(RAY_DISTANCE, rival), nearerThan rival for townTalk, quest resource silent (recorded delta).
WIP wrong (fixed): no pins at all; mobileEnemyActivate.js docblock stated the retired TWICE law; REGRESSION `_dropPick = _lootPick ? null : pick(piles)` (a body across the room suppressed a pile at arm's length) -> decided by distance (_pileNearer); hudlarge D10 window 64->80 (world.js gap 71); a blank line in droppedLoot.js; midScreenText.js header counts 10 of 11 youAreTooFarAway callers (:330 + action record deliberate).
21 mutants, all red (incl. the refuter's `?? say(t)` double-fire; a stub bug found by mutation - hudSay spy must be void like hudText.add).
Test delta +7: activate.test.js 1->3, audit63_guilds_court.test.js 12->17. Suite 7038->7045 at the lane. Lint clean; full suite 7054 run, 2 fail = Suite line.
Record HP-2: the world-hosted dungeon's ActivateMobileEnemy arm mounted its pickpocket MessageBox into mountInterior (the INTERIOR slot the dungeon frame never draws/ticks/keys) - an ActionTextBox has no timer, so the box orphaned and surfaced on the next building entry. Now dungeonCtx.hudBox = pushDungeonWindow, the stack dungeon.js:233 already calls = DaggerfallUI.MessageBox's uiManager.TopWindow (PlayerActivate.cs:1640/:1646 -> DaggerfallUI.cs:1328-1330). Pinned by RUNNING the shipped arm off the source with spies on all four sinks + no mountInterior( in the dungeon ladder. Refuter correction: the orphan bites at the next building entry, not "for the rest of the session".
Record HP-3: the HUD half was say(t) (townTalk's outdoor queue) so the Info line and "You are not successful." opened a second seven-row column over the dungeon's own. Now dungeonCtx.hudSay(t), unconditional (no ?. no ??) - the pin asserts the townTalk spy records ZERO (kills the ?? form: hudText.add is void). Not pixel-verified: near-certain overprint.
Record MC-2: midScreenText.js names eleven DFU youAreTooFarAway sites and the port could reach five, because activate.js pre-gated the PICK. DFU rays once to RayDistance and gates inside each handler. Each family competes for one ray carrying its handler's reach; every ladder speaks the refusal and consumes - :503, :688, :852, :872, :940 ported; :330 the recorded delta; an action RECORD keeps the narrow pre-gate (:380-383 silent). No new export (correction against pickActivatableHitFar). F33's near/far pair is ONE call at the ray's reach (the far half ran with nearerThan Infinity). LEDGER A: the corpse's Info arm is absent (youSeeADead unported) - in Info mode a body past 3.75 now speaks the refusal where DFU says "You see a dead X".
Four hosts: world.js, exterior.js, worldModes.js (tryEnter's static door + both ladders), dungeonContext.js (piles/bodies + the hover plaque applies the reach), standalone dungeon.js wired; interior.js flagged by name (runs no activation ray - pinned).
townTalk person arm (left): BETTER - the door at arm's length now wins on distance over an unoccluded person beyond; the residual is rayPersonDistance taking no collider (a genuinely nearer person behind a wall still eats the click).
