Harnesses deleted, tree clean. Here are my verdicts.

```json
[
  {
    "id": "RS-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived at HEAD: src/render/renderTarget.js:59-66 `withTarget` calls `draw()` then restores with no try/finally; `withVolumeLayer` (:71-76) and `finishVolume` (:80-86) likewise. Proxy-GL harness (recording stub fed to the real exported functions): on the happy path the last `bindFramebuffer` arg is null and the last `viewport` is the caller's rect; inside `assert.throws` the call log ends `bindFramebuffer -> viewport -> drawArrays` with the last bindFramebuffer holding the target FBO and the last viewport [0,0,1024,1024]. The volume path is worse - a throw at layer 5 of 128 skips `finishVolume` entirely (no `generateMipmap` recorded) and leaves the volume FBO bound at viewport [0,0,128,128]. The mechanism is exactly as claimed, and renderer.js:2139-2142's panelFrame doctrine ('the return happens in a `finally`, so a throw inside the body cannot leave the session's one shared renderer holding a scissor') names this precise fault class, so it clears the brief's trap bar.",
    "corrections": "Severity is inflated. I enumerated every closure passed to these three functions (volumetricClouds.js:459 empty, :525-528 and :544-547 = `gl.viewport` + `gl.drawArrays`; cloudNoise.js:173-177 = `gl.uniform1f` + `gl.drawArrays`). Not one contains a JS operation that can throw - WebGL calls set the error flag, they do not throw, and a lost context makes them no-ops. The `restoreViewport` argument is never null either: both hosts pass `renderer.worldViewportPx ?? [0,0,w,h]` (world.js:7749, exterior.js:4032) and skyLab.js:119 passes a literal. So this is latent, not live - unlike RS-2, which has a real swallowing catch. Also the docstring is not strictly 'false': renderTarget.js:13-14 says it restores 'when it returns', and on a throw it does not return - the gap is the unstated throw case, which is what the panelFrame doctrine exists to close.",
    "fixShape": "src/render/renderTarget.js: in `withTarget` (:59-66) wrap `draw()` in `try { draw(); } finally { gl.bindFramebuffer(gl.FRAMEBUFFER, null); gl.viewport(restoreViewport[0], restoreViewport[1], restoreViewport[2], restoreViewport[3]); }`. In `cloudNoise.js:_fill` (:168-181) wrap the layer loop plus `finishVolume` so `finishVolume` always runs: `try { for (...) withVolumeLayer(...); } finally { gl.bindVertexArray(null); finishVolume(gl, volume, viewport); }`. Do NOT put a finally inside `withVolumeLayer` itself - it deliberately does not restore (renderTarget.js:69-70 says the caller restores once after the last layer), and adding one there would issue 128 redundant bind/viewport pairs per volume at boot. Extend the :12-18 docstring to state the throw case explicitly, citing the panelFrame doctrine. Mutation check: deleting a finally must redden the pin.",
    "severity": "low",
    "laneKey": "src/render/renderTarget.js"
  },
  {
    "id": "RS-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Confirmed and the finding UNDERSTATES it. Proxy-GL harness against a real `new Renderer(canvasStub)` with `drawCharacter` stubbed to throw and a docked-HUD rect `_worldViewportPx=[0,60,1280,500]`: the throw path's last `bindFramebuffer` is the sprite FBO (leaked), last `viewport` is [0,0,200,300] (the sprite corner, not the world rect), last `clearColor` is [0,0,0,0) while `_clearColor` still reads [0.53,0.7,0.92,1]. The happy path restores all three correctly, so the delta is purely the missing finally. The existing finally at renderer.js:1382-1386 does restore `_proj`/`_view`/`_fogMode`/`_cloudShadow` - exactly the JS-only scope claimed. Two things make this live rather than latent: fpArm.js:3410-3416 wraps the icon path in `try { ... renderGroundMesh(...) } catch { img = null; }`, a real catch that swallows the throw and lets the frame continue; and `setClearColor` (renderer.js:1907-1911) is idempotent off the JS shadow ('the shadow decides'), so I harnessed a host re-asking for sky blue after a swallowed throw and NO `gl.clearColor` was issued - GL stays transparent black indefinitely, which is AUDIT 26 F034's shipped bug restored permanently.",
    "corrections": "Two factual errors. The target is 1024x1024, not 512x512 - `CHAR_SPRITE_RT_SIZE = 1024` (renderer.js:590, raised at E3d and again at MW-D43). And `renderCharacterSpriteImage` does NOT 'put its whole restore in a finally' (:1429-1437): its finally covers only the studio light state; its own `bindFramebuffer`/`readPixels`/`bindFramebuffer(null)` at :1439-1443 sit outside any finally too, and it calls `renderCharacterSprite` inside a try whose finally restores lights but not GL. The finder also missed the amplifier, which is the part that turns a one-frame glitch into a persistent one.",
    "fixShape": "src/render/renderer.js:1377-1397: move `gl.bindFramebuffer(gl.FRAMEBUFFER, null)` (:1387), `this._restoreWorldViewport()` (:1395) and `const cc = this._clearColor; gl.clearColor(cc[0],cc[1],cc[2],cc[3])` (:1396-1397) INTO the existing finally at :1382-1386, ordered GL-first then the JS-cache restores, leaving `return cs.tex` after the try. Keep the AUDIT 26 F034 / ROAD-E E5 / VC5 comments attached to the lines they explain. Do not convert this to `panelFrame` - that method owns a scissor and a clear this path must not take. Pin with the Proxy-GL harness above plus the persistence assertion: after a swallowed throw, `setClearColor(SKY_CLEAR)` must re-issue `gl.clearColor`.",
    "severity": "medium",
    "laneKey": "src/render/renderer.js"
  },
  {
    "id": "RS-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Collision confirmed: dynamicSkies.js:432-435 `TEXTURE_SLOTS` has NINE entries with `_MoonTex` at index 7, and dynamicSkiesRenderer.js:822-829 binds them `gl.TEXTURE0 + i`, so unit 7 is written by the mod's pass; renderer.js:2612 binds the cloud-shadow map on `gl.TEXTURE7`; markForeignPass (:1125-1129) resets `_lastProgram`/`_lastVao` only. I swept every other foreign pass - enhancedSky, precipitation, labGrass, overworldRenderer - and none goes above unit 3, so the mod's pass is the only door. Proxy-GL harness replaying exterior.js's real order on a live Renderer: setCloudShadow(deck) -> drawCharacter (stamps `_csUploaded.char`, binds unit 7) -> the mod's nine-slot loop (unit 7 now holds _MoonTex) -> markForeignPass (`_csUploaded.char` untouched) -> setCloudShadow(SAME object, stamp does not move) -> a second drawCharacter issues NO `activeTexture(TEXTURE7)` and samples _MoonTex. Emptying `_csUploaded` at the mark fixes it in the same harness.",
    "corrections": "The finder's stated mask is wrong, and so is their conclusion that this is latent. drawTerrain being first after the sky heals only the 'terrain' key - I confirmed the 'char' key stays stamped from BEFORE the sky and never rebinds. The real reason exterior.js is exposed and world.js is not: world.js draws the sky first (7749) with nothing before it, while exterior.js uploads 'char' at :4028 before the sky at :4032. The second `setCloudShadow` at exterior.js:4038 does NOT rescue it - shared.js:371 `get cloudShadow()` returns the same `enhancedSky.cloudShadow`/`dynamicDeck` reference within a frame, so `d !== this._cloudShadow` (renderer.js:2602) is false and the stamp does not move. Offsetting that, the live path is narrower than 'the ground samples the moon': the exposed key is only 'char', and exterior.js's two char draws are near-exclusive - `mwViewDrawBody` needs `!riding` (:3869) while the post-sky rig sprite needs `riding || params.has('rig')` (:4072) - so reaching it today needs `?exterior&rig` + third person + dynamic-skies + clouds. The ground itself is never wrong. Reject fix (a): renumbering the mod's slots needs a FIRST_FREE_UNIT contract and touches a 1:1 ported pass for no gain.",
    "fixShape": "src/render/renderer.js:1125-1129: add `this._csUploaded = {};` to `markForeignPass()` and extend its docstring - a foreign pass may have moved any texture unit, which is the same thing that method already means for programs and VAOs. That one line covers every present and future foreign pass. Optionally also move the reserved unit off the collision (renderer.js:2612/2615, one constant `CLOUD_SHADOW_UNIT`), which WebGL2 permits (>=16 fragment units). Pin two ways: a Proxy-GL test that a simulated foreign bind to unit 7 followed by `markForeignPass()` makes the next `drawCharacter` re-issue `activeTexture(TEXTURE7)`; and a pure guard `assert.ok(TEXTURE_SLOTS.length <= CLOUD_SHADOW_UNIT)` - note this one FAILS today (9 <= 7) and only goes green with the unit move, so brief it as the fix's pin, not a regression pin.",
    "severity": "medium",
    "laneKey": "src/render/renderer.js"
  },
  {
    "id": "RS-4",
    "upheld": false,
    "confidence": "high",
    "reasoning": "The shape is real - world.js:7866 passes `{ ring, height: b.size?.h ?? 0, animated: b.frame != null }` to flatDistance.js:46's destructuring signature, inside the per-batch loop. But the cost claim is measurably false. I benchmarked the exact call shape against a positional twin with identical body over 5,000,000 calls with `--expose-gc`: 56.3 ms vs 55.3 ms, a delta of 0.193 ns/call, and heap deltas of 448 KB vs 855 KB - i.e. the positional form measured a LARGER heap delta, which is GC noise in both directions. V8 inlines the tiny callee and scalar-replaces the literal, so the object never reaches the heap. At the finder's own worst case of ~1000 batches a frame that is 0.0002 ms against a 16.7 ms budget. There is no observable consequence, no DFU divergence, and no port law broken.",
    "corrections": "'on the order of a thousand short-lived objects a frame' is wrong - escape analysis eliminates them; the measured allocation is indistinguishable from zero. EV2's sentence at world.js:7872 is also misapplied: it governs `_camRight`, a Float32Array that ESCAPES into `renderer.drawBillboards`, which is why it needs a scratch; this literal escapes nothing. And the shape is deliberately source-pinned verbatim by test/mac1_playreport.test.js:118, so MAC1 chose and recorded it - changing it means moving a pin for a measured zero.",
    "fixShape": "None. If a lane touches flatDistance.js for another reason it may switch to positional args as housekeeping, but it must then update both world.js:7866 and the regex at test/mac1_playreport.test.js:118 in the same commit. Do not open a lane for this.",
    "severity": "none (refuted)",
    "laneKey": "src/world/flatDistance.js"
  },
  {
    "id": "RS-5",
    "upheld": false,
    "confidence": "high",
    "reasoning": "The counts are real and slightly worse than claimed - I measured them on a live Renderer with the recording stub: `drawWaterSurface` issues 34 `uniform*` calls per surface steady-state (37 on the first, which uploads the water mask once), so 49 water pixels cost 1666 uniform calls and 147 `uniformMatrix4fv`, against ~274 if split. But there is no defect here. The 'One uniform set a frame' comment at world.js:7876-7881 does NOT assert what the finder says: it describes the `waterUniforms(...)` JS object `wu`, which world.js:7879 really does build once, outside the loop - the comment is accurate as written and says nothing about GL uniform uploads. With that hook gone there is no law, no DFU divergence, and no measured consequence; and drawTerrain has the identical shape at 21 uniform calls per surface (1029 for the same 49 pixels), so water is not even the largest existing instance by much.",
    "corrections": "The comment is misread - that is the finding's only claimed law and it does not say that. '~28 pass-constant uniforms / ~1300 calls that could be ~130' should read 34 per surface / 1666 that could be ~274. The `_pointColorData` sub-claim is half right: renderer.js:2355-2362 returns the cached `this._pointColors` with no allocation when the host supplies per-light colours, and only falls to `s.subarray(0, count*3)` otherwise - but both hosts call `setPointLights(data, color)` with two args (world.js:7732/7742, exterior.js:4016), leaving `colors = null`, so the subarray path is the live one and a fresh view per call is correct in practice.",
    "fixShape": "No lane. If a future perf lane measures water uniform traffic as material on real hardware, the split is `beginWaterPass(u)` + a slimmed `drawWaterSurface(surface, modelMatrix, arrayTex, tilemapTex, tileSize, tileDim)`, both hosts calling begin once (world.js:7878, exterior.js:4201); it must keep the GL state bracket inside the per-surface call (the bracket is pinned both ways at test/water.test.js:149-157) and would need `_pointColorData` hoisted too. Not worth opening on an unmeasured gain.",
    "severity": "none (refuted; optimization note at most)",
    "laneKey": "src/render/renderer.js"
  },
  {
    "id": "RS-6",
    "upheld": false,
    "confidence": "high",
    "reasoning": "Every allocation site is real as quoted (enhancedSky.js:802-803 two Float32Arrays and :811-819 the deck; volumetricClouds.js:485-488 `get shadow`, :437 `this.drift`, :440 `shadowOrigin` via :150-152; waterSurface.js:198-211; renderer.js:1080's spread getter). But this is roughly eight small objects per frame, not a hot-loop allocation - and RS-4's measurement on this same tree shows V8 absorbing 1000 such allocations a frame at 0.0002 ms. There is no observable consequence and no law: AUDIT 17e's 'every allocation has an owner' governs GPU objects and teardown (createBillboardBatch / uploadTexture), not short-lived JS literals, and nothing in the tree forbids a per-frame object. The one genuinely load-bearing observation - that `setCloudShadow`'s `d !== this._cloudShadow` identity test is the wrong stamp - is correct, but it is not a perf finding: it is the mechanism behind RS-3, and it belongs in RS-3's lane.",
    "corrections": "The framing inverts the value. The allocations are noise; the identity stamp is the real content, and turning the deck into a refilled scratch WITHOUT first replacing identity with a counter would actively break `setCloudShadow` - a reused object would stop bumping `_csStamp` at all and every program would keep a stale unit-7 binding for the life of the session. So the proposed fix is dangerous in the order given, and its safe half is already RS-3's.",
    "fixShape": "No standalone lane. Fold one sentence into RS-3's brief: when touching `setCloudShadow` (renderer.js:2602), consider replacing the `d !== this._cloudShadow` identity test with an explicit `gen` counter on the deck, which is both a truer stamp and the precondition for any future scratch-reuse of the six producer sites. Do not reuse the deck object before that lands.",
    "severity": "none (refuted)",
    "laneKey": "src/render/renderer.js"
  },
  {
    "id": "RS-7",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Proxy-GL counting harness constructing a real `VolumetricClouds` at each tier: 5 createTexture, 5 createFramebuffer, 7 createProgram, 2 createVertexArray, 2 createBuffer, 160 layer draws at boot - and 0 deleteTexture, 0 deleteFramebuffer, 0 deleteProgram. Neither `VolumetricClouds.prototype` nor `CloudNoise.prototype` carries dispose/destroy/free/teardown (checked reflectively). GPU footprint measured from the tiers' own constants: 8.9 MB at lo, 11.1 MB at default, 20.1 MB at hi, plus `this.white` at 0.25/1.0/4.0 MB of JS (`c.white.byteLength` confirms 4 MB at hi). `createSkyController` (shared.js:163) returns no teardown key. AUDIT 17e's 'EVERY ALLOCATION HAS AN OWNER ... and that teardown must be reachable from the path that ends the object's life' is named in the brief as a qualifying law, so this survives the trap bar on the law even though nothing misbehaves today.",
    "corrections": "The severity rests on a false premise. 'every host boot that builds a controller adds another set' is wrong: `createSkyController` is called exactly once per host (world.js:500, exterior.js:327), a page runs one host, and the only path that ends a run - `exitToTitleMenu` (shared.js:1708-1711) - does `location.href = location.pathname`, a full navigation that destroys the GL context and every object on it. So there is no accumulation and no leak growth in a session; the footprint is a one-shot that dies with the page. That makes this a doctrine gap to close on the tree's terms, not a memory bug.",
    "fixShape": "Add `dispose()` to src/render/volumetricClouds.js (deleteTexture/deleteFramebuffer over `map`, `shadowMap`, `shadowScratch` and the two volumes, deleteProgram x4 for march/shadow/composite/view, deleteVertexArray + deleteBuffer, `this.white = null`, then delegate to `this.noise.dispose()`), and `dispose()` to src/render/cloudNoise.js (its 3 programs, VAO, buffer, and both volumes' tex+fbo). Expose `dispose()` on the object `createSkyController` returns (src/scenes/shared.js, beside the `cloudShadow` getter at :371) calling the clouds' plus `enhancedSky`/`dynamicSky`'s if those gain one. Wiring it to a host teardown is optional given the reload exit - brief it as 'reachable', not 'called every frame path'. Pin with the counting harness: after dispose, deleteTexture/deleteFramebuffer/deleteProgram counts must equal the create/link counts.",
    "severity": "low",
    "laneKey": "src/render/volumetricClouds.js"
  },
  {
    "id": "RS-8",
    "upheld": false,
    "confidence": "high",
    "reasoning": "The finder self-declares this 'a trap, not a live bug', and I confirmed the sweep mechanically: all 12 `enable(gl.BLEND)` sites in src/ (volumetricClouds:567, precipitation:488/530/568, renderer:1702/2285/2676/2745/2855, labGrass:509, overworldRenderer:429/445) have a `blendFunc` within two lines, and `glBlendFunc` sets srcRGB=srcAlpha and dstRGB=dstAlpha, so it fully overwrites the composite's `blendFuncSeparate` residue. More importantly the claimed asymmetry is not one: the four cloud passes exit `depthMask(true); enable(DEPTH_TEST); enable(CULL_FACE)` and leave BLEND disabled, which restores the renderer's baseline exactly - depth on, cull on, blend OFF, with every pass that wants blending enabling it itself. Leaving BLEND off is the correct restore, not a missing one. No law names blend-state symmetry; the panelFrame doctrine is about throw-safety and the Audit-47 upload law constrains upload paths, not this.",
    "corrections": "'the restore is asymmetric with the save' is the central claim and it is backwards - the exit restores the baseline, and re-enabling BLEND there would be the actual bug, since it would leave blending on for whatever draws next with a func it did not choose. The proposed fix (have `draw()` restore `blendFunc(SRC_ALPHA, ONE_MINUS_SRC_ALPHA)`) would add a GL call per frame to paper over a residue that is provably unreadable, and there is no documented 'tree baseline' func to restore to.",
    "fixShape": "No lane. At most, one comment on volumetricClouds.js:568 noting that `blendFuncSeparate` is the tree's only user and that the standing func is left non-default deliberately because every BLEND enabler sets its own. If a future pass ever enables BLEND without a func, that pass is the bug.",
    "severity": "none (refuted)",
    "laneKey": "src/render/volumetricClouds.js"
  },
  {
    "id": "RS-9",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Settled in pure node by parsing the real exported shader sources. MARCH_FS has `for (int i = 0; i < 96; i++) { if (i >= uSteps)` and `for (int i = 0; i < 8; i++) { if (i >= uLightSteps)`; SHADOW_FS has `int steps = min(24, ...)` with `for (int i = 0; i < 24; i++)`. QUALITY tiers are lo 32/4/8, default 56/5/12, hi 80/6/16 - every tier is inside its ceiling today, so nothing truncates now, but the headroom is only 16 on steps and 2 on light. The asymmetry the finding rests on is exact: the shadow march CLAMPS in-shader with `min(24, ...)` so it can never truncate silently, while the sky and light marches use a bare `break` with no clamp, so a tier raised past 96 or 8 silently stops short. The port's own law applies - ONE DFU MEMBER, ONE EXPORT ('If two files legitimately need it, one exports and the other imports - never two literals') - and this very file already demonstrates the fix, interpolating EXT, SHAPE_M, DETAIL_M, VARIATION_M and MOTTLE_M from JS into the same shader string.",
    "corrections": "Severity is inflated to medium for a defect that is zero-consequence at HEAD - no tier is over, and the file is the only producer of both halves, so nothing can drift without editing QUALITY deliberately. It is a cheap pin to add, not a bug to fix. The finder's read of the existing test file is accurate: test/volumetricClouds.test.js:132-133 pins the shadow clamp and the shadowSteps range, :61 pins only the ORDER of `.steps`, and `light` appears nowhere.",
    "fixShape": "src/render/volumetricClouds.js: add `export const MARCH_MAX_STEPS = 96;` and `export const LIGHT_MAX_STEPS = 8;` beside QUALITY (~:64-68) and interpolate them into MARCH_FS at the two loops (:231-233 and :250) as `for (int i = 0; i < ${LIGHT_MAX_STEPS}; i++)` / `for (int i = 0; i < ${MARCH_MAX_STEPS}; i++)`, exactly as EXTINCTION and the four period constants already are. In test/volumetricClouds.test.js, beside the shadowSteps pin at :132-133, add `for (const q of Object.values(QUALITY)) { assert.ok(q.steps <= MARCH_MAX_STEPS); assert.ok(q.light <= LIGHT_MAX_STEPS); }`. Mutation check: `hi.steps = 128` must redden it. Do not add a `min()` clamp to the sky march - that would silently cap a raised tier instead of failing the pin, which is the weaker outcome.",
    "severity": "low",
    "laneKey": "src/render/volumetricClouds.js"
  },
  {
    "id": "RS-10",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "NEEDS A REAL CONTEXT to see the pixels, as the finder says, so I judged the source and it is unambiguous. A source-order probe over both hosts: world.js sky.draw@7749, markForeignPass@7781, setCloudShadow@7783, mwViewDrawBody@7784 (body after the sky); exterior.js setCloudShadow@4027, mwViewDrawBody@4028, sky.draw@4032, markForeignPass@4033 (body BEFORE the sky). The sky's contract is opaque and depth-blind in both skins: enhancedSky.js:827-829 `useProgram; depthMask(false); disable(DEPTH_TEST)` with no BLEND enable and `outColor = vec4(out3, 1.0)` at :748; dynamicSkiesRenderer.js:797-799 identical with `outColor = vec4(enc, 1.0)` at :688. Third person is genuinely wired in the exterior host (mwViewWheel bound at :2451, mwViewFrame at :3870 under `(!shotMode || walkMode) && !riding`), so the path is reachable in normal play, and `eye` at :3878-3882 resolves to `mwv.eye` exactly as world.js:7784 passes it.",
    "corrections": "The consequence is understated, not overstated. The sprite quad's FS is `if (t.a < 0.5) discard; outColor = vec4(..., 1.0)` and `drawCharacterSpriteQuad` (renderer.js:1451+) touches neither depthMask nor DEPTH_TEST, so it inherits the world pass's depth WRITE. The sky then overwrites colour without touching depth, and drawTerrain at :4040 runs with DEPTH_TEST on - so the terrain is depth-rejected inside the body's silhouette. The visible artifact is therefore a body-shaped patch of sky colour punched through the landscape, not merely a missing body. Also: the ordering is pre-existing, not delta-introduced - `git log -L 4026,4034:src/scenes/exterior.js` shows VC3 (201b3629) only added sky.draw's viewport argument and VC4c (a7851ddb) only inserted the setCloudShadow line above an already-existing body call. Brief it as a host-divergence fix, not a delta regression.",
    "fixShape": "src/scenes/exterior.js: move the `mwViewDrawBody(canvas, {...})` call from :4028 to immediately after `renderer.markForeignPass()` at :4033, and move the `setCloudShadow(sky?.cloudShadow ?? null)` at :4027 with it so the deck still precedes the body - which then makes the existing second `setCloudShadow` at :4038 redundant and removable. This lands the exterior host on world.js:7781-7784's exact order and, as a free consequence, removes RS-3's pre-sky 'char' upload entirely. Confirm with a GL run before shipping: `?exterior` in third person, frame-synced on the shot-mode `__frame` counter (never a sleep - bible/Home.md), reading the pixel at the body's projected centre against the sky colour. Pin with the audit18_hosts_outer.test.js source-sweep idiom: in each exterior host, the index of `mwViewDrawBody(` must exceed the index of the `markForeignPass()` that follows `sky.draw(`.",
    "severity": "medium",
    "laneKey": "src/scenes/exterior.js"
  },
  {
    "id": "RS-11",
    "upheld": false,
    "confidence": "high",
    "reasoning": "Every mechanical claim reproduces - I drove a real `VolumetricClouds` at the hi tier through a pixel crossing with the recording stub: 0 `texSubImage2D` and 0 `blitFramebuffer` on a non-crossing frame, 2 and 1 on a crossing frame, 512 KB uploaded inside that frame (256 KB per axis; a diagonal crossing does both, so the finder's 262 KB is the per-axis figure). The call window is `bindTexture -> texSubImage2D -> texSubImage2D -> bindTexture(null)` with no `activeTexture` anywhere in `_shiftShadowMap`, so it does act on whatever unit was last active and does leave that unit's TEXTURE_2D binding null. But the finder concedes neither is a correctness bug, and I confirmed why: both sky skins end their draw with `activeTexture(TEXTURE0)` (dynamicSkiesRenderer.js:831), and `clouds.draw()` rebinds unit 0's TEXTURE_2D immediately after `update()` returns (shared.js:597), so nothing reads a stale binding. Crossings occur once per 819.2 m (PIXEL_METRES), and no law covers this: Audit-47's upload law is one-directional - it forbids UPLOAD paths from drawing, binding a framebuffer, clearing, setting a viewport or leaving a binding, and says nothing about a DRAW path uploading.",
    "corrections": "'the unbind is a leak with no note' overstates it - leaving a sampler unbound is the same thing `march()`, `draw()` and `drawShadowView()` all do on exit as their documented discipline, and the finder's own coverage block credits that. The upload cost is also small and rare: 512 KB once per 819.2 m at the hi tier only, versus 8.1 MB of volume generation this same module already does at boot in one go.",
    "fixShape": "No lane. If volumetricClouds.js is opened for RS-7 or RS-9, two free hardening touches at :461-464: prepend `gl.activeTexture(gl.TEXTURE0)` before the `bindTexture(dst.tex)` so the path names the unit it acts on rather than inheriting one, and add a comment that the trailing `bindTexture(null)` is deliberate. Do not replace the strip upload with a scissored clear - the strips are written ALL LIGHT (255) and a clear would need its own clearColor borrow-and-return, trading two uploads for more state churn.",
    "severity": "none (refuted; hardening note)",
    "laneKey": "src/render/volumetricClouds.js"
  }
]
```