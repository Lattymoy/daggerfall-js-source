```json
[
  {
    "id": "XL-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "REPRODUCED with the real PlayerMotor over a real Collider floor in the hosts' own order (capture _wasSwimming -> applyMotorEffectFlags -> player.update(1/60) -> onExteriorWater -> exteriorSwimming): with OT1's write at world.js:7330 / exterior.js:3708 the swimmer travels dz=0.0000, dy=0.0000 over 60 steps; with the write removed dz=1.7722 == swimSpeed(walkSpeed(50),30)*1s exactly, and the dry baseline is 4.4304 == walkSpeed(50). The mechanism is the setter at motor.js:471-476 (`this._swimming=b; this.cancelMovement=true`): shared.js:888 writes false and :7330 writes true every render frame, so the frame's one fixed step always spends the cancel at motor.js:1131-1144 and returns. DFU is unambiguous that these are TWO members - PlayerEnterExit.cs:415-421 clears `levitateMotor.IsSwimming` with no tile test while keeping `isPlayerSwimming` on tile 0; PlayerMotor.cs:149-152 makes IsSwimming == levitateMotor.IsSwimming; and exteriorSurface.js:45-56 states that separation in the port's own words while exteriorSwimLatch (:261-267) returns `motorSwimming:false` beside `swimming` - the field the wiring ignored. Nothing in Audit-63/64.md or Port-Ledger.md A records this; Player-Arc.md:1535-1546 records the opposite law ('the outdoor swimmer is grounded on the lake bed with the replacement motor disengaged'). ot1_swim_latch.test.js is a pure-helper + source-grep pin and is green at HEAD, as are audit64_motor and mac1_playreport - none drives a motor across the seam.",
    "corrections": "Three of the finder's mechanism claims are wrong or overstated. (1) 'the swim/levitate branch returns above UpdateSpeed, so F0's `sunk` gate is never reached outdoors' is FALSE: applyMotorEffectFlags clears `swimming` BEFORE player.update, so motor.js:1190 is never entered outdoors and the measured `speed` field IS 1.7722 (the swim speed) - the only thing that makes F0's law unreachable is the cancelMovement return at :1131. (2) 'not forward, not up, not down' - the vertical half is a red herring: DFU gives an exterior swimmer no float control either (levitateMotor.IsSwimming is false outdoors), and `waterSurfaceY` is null outdoors by design. (3) The freeze is total only at ~1 step/frame; a 3-step hitch frame moves 2/3 of the correct distance (measured 1.1814 vs 1.7722 over 60 steps), so the honest sentence is 'frozen at every ordinary frame rate', not 'cannot move under any condition'. Also: the finder's fix leaves headBobber on the motor flag - WRONG, HeadBobber.cs:101 and :215 both read `playerEnterExit.IsPlayerSwimming`, so world.js:7054 / exterior.js:3492 MUST move. And there is no save-envelope swim member (grep of src/systems/save.js is empty) - drop that item.",
    "fixShape": "Split the member exactly as DFU has it. (a) Declare a plain data field `this.isPlayerSwimming = false` in the PlayerMotor constructor (src/player/motor.js:372-392, beside `sunk`/`onExteriorWater`) - a FIELD, no setter, so it can never arm cancelMovement. (b) src/scenes/shared.js:888 keeps `player.swimming = false` untouched (that is PlayerEnterExit.cs:421, verbatim). (c) src/scenes/world.js:7330 and src/scenes/exterior.js:3708 change the assignment target only: `player.isPlayerSwimming = exteriorSwimming({ wasSwimming: _wasSwimming, ... })`, and world.js:7243 / exterior.js:3633 read `_wasSwimming = !!player.isPlayerSwimming`. (d) REPOINT the IsPlayerSwimming readers at the new field, each named with its C#: world.js:7231 + exterior.js:3616 (playerTicker fatigue band + Swimming tally, PlayerEntity.cs:410); world.js:2256 + exterior.js:1400 (encounter suppression, PlayerEntity.cs:489); world.js:1609 + exterior.js:877 (exhaustionOutcome, PlayerEntity.cs:2406/2426); world.js:3100 + exterior.js:1531 (restDecision, DaggerfallUI.cs:661); world.js:7054 + exterior.js:3492 (headBobber style AND bouncing, HeadBobber.cs:101/:215). (e) LEAVE on `player.swimming`: motor.js:1190 (PlayerMotor.cs:323), motor.js:733/747/750 (_heightAction's forcedSwimCrouch, PlayerHeightChanger.cs:128/:551), world.js:7341 + exterior.js:3715 footsteps `swimming` (PlayerFootsteps.cs:230 reads playerMotor.IsSwimming), worldModes.js:1055 passiveSpecials `isSwimming` (PassiveSpecialsEffect.cs:136 reads PlayerMotor.IsSwimming), and the MW movement-vector bags (port-owned animation). (f) THE FOUR HOSTS RULE - this is the half the finder omitted and it is a regression if skipped: PlayerEnterExit.cs:385-393's dungeon branch writes BOTH members, so worldModes.js:5295 and dungeon.js:683 must write `player.isPlayerSwimming = player.swimming = <blockWaterLevel test>`; otherwise every reader moved in (d) goes dead in dungeons. Interior shells (worldModes' interior arm) leave both false, as applyMotorEffectFlags already does. (g) motor.js:1372 - leave `this.sunk && !this.waterWalking` as-is for this lane: DFU's UpdateSpeed (PlayerMotor.cs:383-389) reads IsPlayerSwimming, and `sunk` is the documented lockstep proxy (PlayerHeightChanger.cs:419-423/:374-377, Player-Arc.md:1530-1537) whose F0 pins are green; note in the record that re-pointing it at `this.isPlayerSwimming` once the field exists makes it verbatim and closes the <=1-frame dungeon-exit-onto-water gap. (h) PIN: a behavioural pin driving the real host order over a Collider floor on tile 0 asserting dz over 60 steps == swimSpeed(walkSpeed(50),30) to 1e-3 from the C# constants, plus `motor.cancelMovement === false` after a steady-state water frame, plus a source pin that neither exterior host assigns exteriorSwimming's result to `player.swimming`. Mutant: restoring the old target must drive the travel pin to 0.0000 (verified it does).",
    "severity": "critical",
    "laneKey": "src/scenes/world.js"
  },
  {
    "id": "XL-2",
    "upheld": false,
    "confidence": "medium",
    "reasoning": "The arithmetic is right and I re-derived all of it: managedArchivesForSeason (seasonsIliacBay.js:92-96) returns [] for Summer, so Summer really is classic-sized and the three other seasons re-skin 504/506/508/510 (Fall/Spring) and 505/507/509 (Winter); seasonalBillboardSize (:223-228) is fround(px*3.1)*0.025 against the classic (px + trunc(px*scale/256))*0.025, the pin at seasonsIliacBay.test.js:141-150 asserts the 3.1 ratio on the 79x156 winter pine, and world.js:1157-1160 passes `sib.size` straight into createBillboardBatch, which renderer.js:2460 stores verbatim as `batch.size` and world.js:7866 reads as `b.size?.h`. So 2.5 world units does become 32.26 texture pixels under the mod. What the finding does not have is a divergence. There is no DFU law here at all (DFU draws every flat and MAC1's own Ledger row 173 says the far rule is 'presentation, not law ... carries no row'), and the port's recorded criterion - flatDistance.js:20-23/:37, 'a tree, at or above TALL_FLAT_HEIGHT in world units (a walker is 1.8)' - is a WORLD-UNIT test that a 3.1-unit seasonal shrub genuinely satisfies: the mod really does draw it taller than a walker, so it really is as large on screen as a tree (at ring 2, 2.5 units subtends ~1.9 px at 1080p/60deg whether it got there classically or through the atlas). The rule is self-consistent in screen terms; what changed is the world, not the rule.",
    "corrections": "Two things are overstated and one proposed fix is actively harmful. (1) 'MAC1's fix silently reverts' overstates: it still culls 500-503, 511, every non-nature archive, all of Summer, and the unmanaged records of managed archives - and it never stopped answering its own written criterion. Severity 'high' is inflated for a cost that only exists when an optional third-party mod the player must supply themselves is installed. (2) The claim that 'a normalisation by BILLBOARD_SCALE' is a safe alternative is wrong twice over: the classic path takes the record's real recordScale (rmbFlats.js:54-59) while the seasonal path hardcodes zero scale, so dividing by 3.1 does NOT recover the classic height for any record with a non-zero scale; and TryBuildSeasonalAtlas puts record 1's TEXTURE in slot 0 (seasonsIliacBay.js:211), so record 0's normalised height would be record 1's, not its own. (3) Most importantly the proposed fix would make genuinely visible flats pop out of existence: under the mod a 40-px rock is 3.1 units tall and ~2.4 px on screen at ring 2, and culling it because its CLASSIC twin would have been small is a visual regression traded for fps.",
    "fixShape": "No code change. If the lane wants the record, it is a documentation row, not a fix: add one sentence to bible/07-Rendering/Seasons-Iliac-Bay.md beside the existing 3.1x line and to src/world/flatDistance.js's header - 'a seasonal atlas makes the managed archives 3.1x taller in world units, so the far-flat rule admits most of them in Fall/Spring/Winter; the flats really are that big, and the rule is a world-unit size test, so this is the mod's cost and not a hole in the rule.' If Mac later reports fps specifically with SIB installed, the lever is a SCREEN-SIZE criterion (height/ringDistance) at src/world/flatDistance.js:46-50 applied uniformly, or a per-ring budget - NOT a season-conditional height, and NOT a raised TALL_FLAT_HEIGHT. One real gap worth folding into whatever lane touches world.js:7866: farFlatVisible has exactly ONE call site in the tree; src/scenes/exterior.js does not take the rule at all, which is a THE FOUR HOSTS RULE omission in MAC1's record independent of seasons.",
    "severity": "low",
    "laneKey": "src/world/flatDistance.js"
  },
  {
    "id": "XL-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Same as HP-1 (judged here under XL-3's id). Re-derived at HEAD: pauseDoor.js:165 `if (action !== 'exit') hooks.relock?.();` is optional-chained, so an omitting host is silent. Four openPauseFlow call sites exist - world.js:4808, worldModes.js:6816, exterior.js:2168, dungeonContext.js:4339 - and `relock` appears in exactly two places in src/scenes: world.js:4816 and world.js:6721 (the latter reaching worldModes.js:6824 as `host.relock`). exterior.js contains the string 'relock' only inside a comment at :2423. Both un-wired hosts still run makeLookGate (exterior.js:12/:3444, dungeon.js:567) and both carry the identical SetClickDelay arm (exterior.js:2448, dungeon.js:338, world.js:5068), so the double-click resume is unchanged there. MAC1's record (Port-Ledger.md:173) names none of the four.",
    "corrections": "The gap is bigger than the finder counted, and one of the two proposed one-liners will not compile as written. (1) exterior.js passes no `relock` to createWorldModes either (grep of exterior.js for 'relock' returns only the :2423 comment), so the INTERIOR arm hosted by exterior.js - worldModes.js:6824's `host.relock` - is undefined too. That is a third un-wired surface the finder missed, and it is the one THE FOUR HOSTS RULE actually indicts. (2) `relock: () => requestLook(ctx.canvas)` is not available in dungeonContext: the module's own comment at :4666 says 'this context owns no canvas of its own' and it sees one only per drawFoes call (`_weaponCanvas`, :2465/:3555). The hook must ride in from the constructing host through buildDungeonContext's `deps` (dungeonContext.js:183), the way worldModes takes `host.relock`. (3) The touch second-order note is speculation, not a demonstrated defect - keep the guard as cheap insurance, do not write it up as a finding.",
    "fixShape": "src/scenes/exterior.js:2168 - add `relock: () => requestLook(canvas),` to the openPauseFlow options (requestLook is already imported at :12 and `canvas` is in scope, used at :2378/:2449). src/scenes/exterior.js:2910 - add `relock: () => requestLook(canvas),` to the createWorldModes deps bag so worldModes.js:6824's `host.relock` is live in the exterior-hosted interior arm. src/scenes/dungeonContext.js:183 - accept `relock` in `deps`; :4339 - pass `relock: deps.relock` into openPauseFlow. Feed it from both constructing hosts: src/scenes/dungeon.js (buildDungeonContext call, `canvas` in scope at :338) and src/scenes/worldModes.js (its buildDungeonContext call, forwarding `host.relock`). Guard the call for touch in ONE place - src/ui/pauseDoor.js:165, `if (action !== 'exit' && !hooks.touchActive?.()) hooks.relock?.();` - rather than in four hosts. Name all four hosts in the record. PIN: a census over the four openPauseFlow call sites requiring a `relock` key at each and the count of keys == the count of sites, plus a behavioural pauseDoor pin that resume/save/load each fire hooks.relock exactly once and exit never does; mutation - deleting any one host's key must redden the census.",
    "severity": "medium",
    "laneKey": "src/scenes/exterior.js"
  },
  {
    "id": "XL-4",
    "upheld": true,
    "confidence": "high",
    "reasoning": "MEASURED on the MAC1 staircase (riser 0.3, run 0.6, the real PlayerMotor over a real Collider), worst single-frame vertical delta: at 60 Hz the first-person eye through eyeAt is 0.0513 smoothed / 0.1530 with `_eyeSmoothing` off - reproducing MAC1's own 0.153 -> 0.051 to the digit - while the third-person focal, which is raw `player.pos[1]`, is 0.1530; at 144 Hz first person is 0.0211 and the third-person focal is still 0.1530, a 7.2x gap. I re-read mwCamera.js:198-233 line by line: `eye()` returns `fpEye` untouched in first person and in third builds `focal = [feet[0], feet[1] + FOCAL_HEIGHT*heightScale*u, feet[2]]` then `eye = focal - fwd*dist` - there is NO smoothing, no lerp and no previous-frame state anywhere in mwCamera.js or mwView.js (the only per-frame state is `pendingClicks` and `cameraDistance`), so the finder did not miss a filter. All four hosts pass the raw feet: world.js:7597, worldModes.js:5652, exterior.js:3870, dungeon.js:840, and mwViewDrawBody the same at world.js:7784, worldModes.js:5707/:5772, exterior.js:4028, dungeon.js:871 - while every one of them sets `cam.pos = player.eyeAt()` (world.js:7378 etc.), so fpEye is smoothed and feet is not. That contradicts eyeAt's own recorded law at motor.js:506-508 ('Only cameras read eyeAt'). mac1_playreport.test.js is green at HEAD and never touches third person.",
    "corrections": "One claim is empty: 'the body and the first-person eye disagree by up to one step too' - mwViewDrawBody returns false whenever mwCamera.thirdPerson() is false (mwView.js:82), so the body never draws in first person and there is nothing to disagree. Severity 'medium' is right but note the scope: this is an optional port-owned view mode (MW-D25) with no DFU counterpart, so the divergence is from EV1/MAC1's own stated law, not from Daggerfall Unity - the record should say so rather than cite a C# line.",
    "fixShape": "src/player/motor.js - add `feetAt(alpha = this._alpha)` directly beneath eyeAt (:516-535): the same span lerp and the same SNAP_SPAN guard, the same `_eyeFeetY` substitution for the height, but WITHOUT `_eyeLevel()` and WITHOUT `bobOffset` (a third-person camera must not inherit the head bob - OpenMW's camera tracks the scaled body node, not the bobbing first-person camera). Then pass it as `feet` at all five-plus sites: src/scenes/world.js:7597 and :7784, src/scenes/worldModes.js:5652, :5707 and :5772, src/scenes/exterior.js:3870 and :4028, src/scenes/dungeon.js:840 and :871. `player.pos` stays the simulation truth for the collider, the activation ray and the focal's own ceiling probe input, exactly as `eye` stays it in first person. Do NOT change mwCamera.js - the smoothing belongs on the producer, not in the camera, or the pins that drive mwCamera.eye with literal feet would have to learn about motor state. PIN: run the staircase harness in both modes and assert the worst third-person single-frame vertical delta is within a small factor of the first-person figure and below 0.06 at 60 Hz, beside a shape pin that no host passes `player.pos` as mwViewFrame's or mwViewDrawBody's `feet`; mutation - reverting one host to `feet: player.pos` must lift the third-person figure back to 0.1530 (verified that is the raw value).",
    "severity": "medium",
    "laneKey": "src/player/motor.js"
  },
  {
    "id": "XL-5",
    "upheld": true,
    "confidence": "high",
    "reasoning": "The shape defect is real and exactly as described: `this.standing` has three writers - motor.js:657 (_trackHalfSpeed, on the walk path below UpdateSpeed), :1139 (the cancelMovement block) and :1269 (the swim/levitate branch) - and `if (this._climbStep(dt, input, yaw)) return;` at :1182 sits above the last two, with _climbStep (:1023-1084) containing no `standing` or `movingLessThanHalfSpeed` write at all despite quoting ':322-326 zeroes moveDirection' at :1047-1049. A climb is entered with forward > 0, so the last walk-path write was `standing = false`, and it stays false for the climb's duration. DFU has no such hole: PlayerMotor.cs:113-125 is a live getter and :323-326 puts the climb disjunct in the SAME statement as the swim one the port did port - and because DFU RETURNS at :326 without updating `grounded`, DFU's IsStandingStill reads TRUE for the whole climb (grounded latched true from the approach, moveDirection zeroed) where the port reads false. The readers are new in the delta and real: world.js:7351 (footstep standingStill), :7939 (MAC1 H's townsfolk politeness gate), :8292 (riding animator), with twins in exterior.js/worldModes.js/dungeon.js.",
    "corrections": "The headline consequence is demonstrably FALSE and must not travel into the lane brief. 'the fatigue band's `running && !standing` arm keeps charging the running rate on top of ClimbingFatigueLoss' cannot happen: src/systems/worldTick.js:493-495 is climb-first - `if (activity.climbing) loss = FATIGUE_LOSS.Climbing; else if (activity.running) ...` - matching PlayerEntity.cs:405-408, so the running term is never consulted while `climbing` is true, and every host passes `climbing: !!player.climb?.isClimbing`. The footstep claim is near-empty too: footsteps.js:121's `if (!m.swimming) { if (!grounded) { ...return; } }` short-circuits above the standingStill gate, and the port's _climbStep writes `this.grounded = r.grounded` so a climber off the ground never reaches the stale term. What actually survives is the townsfolk gate at world.js:7939 and a narrow window where a wall-hug near the ground keeps grounded true. Severity drops from medium to low accordingly. The proposed `movingLessThanHalfSpeed = true` is also wrong: PlayerMotor.cs:168-181 returns true only when IsStandingStill, otherwise it compares GetBaseSpeed()/2 against the stale `speed` field - mirror the swim branch's shape, not a constant.",
    "fixShape": "src/player/motor.js - immediately before `return true;` at the end of _climbStep (:1083), add the swim branch's own two lines with its citation: `this.standing = this.grounded;   // PlayerMotor.cs:325 - moveDirection zeroed, so :113-125 collapses to grounded` and `this.movingLessThanHalfSpeed = this.grounded ? true : (walkSpeed(this.stats.speed) / 2 >= this.speed);` (the exact mirror of :1264-1267, NOT an unconditional true). Record the one knowing departure in-file: DFU returns at PlayerMotor.cs:326 without updating `grounded`, so DFU's climber stays grounded-true for the whole climb while the port's _climbStep writes the collider's live grounded - the port answers its own truth, which is the same choice the swim branch already made. Better long-term shape, if the lane has room: replace the field with `get standing() { return this._moveDirZeroed ? this.grounded : this._walkStanding; }` so no future early return can leave it stale - that is DFU's own shape. PIN: drive a real ClimbingState one step and assert `standing === grounded` on the first climbing step and every step after, plus a census over _step's early returns (:1131-1144, :1155, :1182, :1270) requiring a `standing` write in each branch body; mutation - deleting the swim branch's existing :1269 write must redden the same census.",
    "severity": "low",
    "laneKey": "src/player/motor.js"
  },
  {
    "id": "XL-6",
    "upheld": true,
    "confidence": "high",
    "reasoning": "MEASURED against a fake IndexedDB that counts operations: `registerMorrowindData()` over N stored Morrowind names issues exactly 1 getAllKeys + N value get()s, and for LEGACY (ArrayBuffer) records also N readwrite put()s - with a 6-file, 181 MB set that is 181 MB materialised out of the store and written back, serially, on the first call; a second call issues N more gets and 0 puts (the values are Blobs by then). The chain is exactly as cited: dataSource.js:717-722 -> storedMorrowindSizes (:706-715, `for (const n of [...names].sort()) { const v = await assetBlob(MW_STORE, n); }`) -> assetBlob (:371-388, `const blob = new Blob([stored]); ... os.put(blob, fileName)`). Both halves are in the delta and neither reviewed the other: `git log -S storedMorrowindSizes` gives 563f1e8a (MW-LOAD) and `git show 0fe2c05b:src/scenes/dataSource.js:537` is `const mwFingerprint = (names) => [...names].sort().join('\\n')` - names only; `git log -S registerMorrowindData -- src/ui/enhancedMenu.js` gives 6c017b5e (MAC1) as the only commit, and the call at :1994-1996 is the second caller beside shared.js:1129.",
    "corrections": "MAC1's share of the cost is smaller than the claim implies and the record should say so. The boot-menu call is gated on `morrowindDataFingerprint() == null`, so it fires once per page load; shared.js:1129 (loadMagicRegistries) is unconditional and would have paid the same migration at the first host boot. So MAC1 moved a one-time cost EARLIER (onto the title screen, and onto players who open enhanced settings and never start a game) - it did not create a recurring one. The root cause is MW-LOAD's size fingerprint, not MAC1's placement; 'a player ... now pays a full read-and-rewrite ... the moment the enhanced boot menu mounts' is true, but 'MAC1 A could safely add a second caller' equally means MW-LOAD could not safely add sizes to the first. 'low' is the right severity.",
    "fixShape": "Two steps, smallest first. (1) src/scenes/dataSource.js:706-715 - storedMorrowindSizes must not migrate: replace `await assetBlob(MW_STORE, n)` with `await assetValue(MW_STORE, n)` and read `v == null ? -1 : (v.size ?? v.byteLength ?? -1)`. That alone removes all N Blob wraps and all N readwrite puts from a counting call while keeping the sizes byte-exact, and it is a one-line change. The legacy-to-Blob migration stays where the file is actually USED - assetBlob's callers at :589 and :651 (loadMorrowindArchives / the record path) - which is where MW-LOAD's range reads need it. (2) To reach the finder's zero-value-read pin, add a per-domain metadata record written at storeAssets time (name -> byteLength) and read it in storedMorrowindSizes, falling back to (1) for names the metadata does not know; keep it additive so an existing attach is not invalidated. PIN as the finder shapes it - a fake IDB counting value gets: registerMorrowindData over N names issues ZERO value reads and ZERO puts after (2), and even after (1) alone must issue zero puts, while loadMorrowindArchives over the same store still issues N. Mutation: routing the size read back through assetBlob must lift the counts and redden.",
    "severity": "low",
    "laneKey": "src/scenes/dataSource.js"
  }
]
```