I have re-derived all four findings against the reference. Here are my verdicts.

```json
[
  {
    "id": "SL-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-read at HEAD: `restoreWeather` (src/systems/weatherSim.js:322-334) sets `_climateWeathersRolled=true`, `_updateFromClimateArray=false`, `_jumps++`, `_evolveHour=null` and never touches `_climateWeathersValid` (declared :147, raised at :185 setClimateWeathers and :348 importClimateWeathers, lowered only in resetWeatherSim :430), which the evolution guard at :398 reads. resetWeatherSim has no production caller (grep: tests only) and `worldQuickLoad` (src/scenes/world.js:4155) restores in-process, so on any in-session load the flag is still true from the outgoing session's boot roll (tickWeather's lazy roll, weatherSim.js:285). I reproduced it in Node against the real module: outgoing boot roll -> restoreWeather('rain') -> hourly evolve+tick drains to 'thunder' at hour 5; the same script with no prior roll (boot load) never drains and stays 'rain'. DFU persists one value (SerializablePlayer.cs:174 `data.weaponDrawn`-adjacent block; the weather line is :225) and WeatherManager.cs:540-542's startedFromLoadedSaveGame arm suppresses the array apply, and the port's own recorded law says the same thing twice - weatherSim.js:322-334's header and Port-Ledger.md:94 ('a load re-anchors and rolls nothing', 'the loaded array never evolved off'). The guard's own comment at :398 names this exact hazard, so this is a divergence from the port's recorded law, not a judgement call.",
    "corrections": "The fix line is right (`_climateWeathersValid = false;` beside `_evolveHour = null`) and needs no SAVE_VERSION change (module state, not envelope). Two things the finder did not check that the lane must not break: (1) THE CLASSIC IMPORT IS SAFE - world.js:4332 calls restorePlayer (which reaches restoreWeather at save.js:535) and world.js:4362 calls importClimateWeathers AFTER it, so the import re-validates; the pin should assert that ordering (a future reordering silently kills the imported array's evolution for a whole game day). (2) THE PIN MUST BE THE IN-SESSION SHAPE. test/clockArc.test.js:299-311 (CLK4 arm 2) already pins the boot-load case (`resetWeatherSim(); restoreWeather('rain')`) and stays GREEN under the bug - a new pin that reuses that shape is a pin that cannot fail. Roll the array first (tickWeather or rollClimateWeathersForDay), then restoreWeather, then walk the hours. Also worth one line in the pin that the DUNGEON host's load (dungeonContext.js:4675 restorePlayer) reaches the same seam.",
    "fixShape": "src/systems/weatherSim.js restoreWeather: add `_climateWeathersValid = false;` next to `_evolveHour = null;` with the CLK2 citation (the loaded array is not a rolled array in ANY session, not just a fresh process). New pin in test/clockArc.test.js that rolls the array BEFORE the restore and asserts the restored weather survives 24 hourly evolve+tick calls; mutation-check by deleting the line. Optionally a source-pin that importClimateWeathers runs after restorePlayer in world.js's classic-import arm.",
    "severity": "medium"
  },
  {
    "id": "SL-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Three live PlayerWeapon instances confirmed at HEAD: world.js:2406, worldModes.js:538 and dungeonContext.js:2477, each `new PlayerWeapon({})` inside createWeaponRig (combat/weaponRig.js:104); exterior.js:1588 is a fourth but that host has no save path at all (grep for quickSave/restorePlayer in scenes/exterior.js returns nothing), so it is legitimately flagged rather than a hole. The only writers of sheathed/usingRightHand outside playerWeapon.js are world.js:4259/:4272 and dungeonContext.js:4731/:4737 - nothing anywhere writes interiorWeapon.playerWeapon, and nothing syncs the rigs at a mode transition, so the interior rig's state is genuinely independent. worldModes.js:6919 and :6822 route BOTH the F9 key and the pause window's Save to `host.quickSave`, i.e. world.js's worldQuickSave, whose pose is composed at world.js:4115 from `weaponRig.playerWeapon` unconditionally, and applyPose (world.js:4255-4272) writes back into that same exterior rig. DFU has one WeaponManager for every WorldContext - SerializablePlayer.cs:174-176 writes `data.weaponDrawn = !weaponManager.Sheathed;` / `data.usingLeftHand = !weaponManager.UsingRightHand;` and :419-421 restores the pair - so a save taken inside a building records and restores the wrong rig: boot-load an indoor save and the interior rig is at its constructor default (playerWeapon.js:162 sheathed, :158 right hand), which is F25's own consequence (swinging the right-hand item, or fists) re-opened for the fourth host. AUDIT 63 F24 already reached this host for its enemy pools (Audit-63.md:196), which makes the weapon half a plain four-hosts miss rather than a design choice.",
    "corrections": "Three corrections to the fix. (1) applyWeaponPose MUST NOT be gated on the current mode. worldQuickLoad calls `modes.forceExitToExterior()` FIRST (world.js:4172) and only re-enters the building at :4207, and applyPose runs last at :4245 - so for a save taken OUTDOORS loaded while the player was indoors the mode is 'exterior' at apply time, and a mode-gated apply leaves the interior rig carrying the OUTGOING session's drawn weapon, which the player then meets at the next building entry. Write the pair into the interior rig unconditionally; DFU has ONE WeaponManager, so landing the same bit in every rig is the parity shape, not an over-reach. (2) The save-side gate is `mode === 'interior'` ONLY - do not widen it to 'dungeon'. worldModes.js:7028 routes dungeon-mode keys to `dungeonCtx`, and dungeonContext.js:4656 composes its own pose from its own playerWeapon, so there is no fifth hole and a mode-agnostic weaponPose() would shadow the dungeon host's correct answer. (3) NAME THE LARGER GAP IN THE RECORD, or the pin encodes the split as law: the four rigs never share state at all, so walking into a shop with a drawn left-hand weapon ALREADY finds the interior rig sheathed and right-handed, with no save involved. This seam narrows the save half of a divergence whose root is 'four PlayerWeapons where DFU has one' - say so in the slice record and in Ledger A, and note that composing from the live rig plus applying to all rigs is a step toward the one-manager law (after the fix, leaving the building carries the indoor state out, which is what DFU does).",
    "fixShape": "worldModes.js: export `weaponPose()` returning `mode === 'interior' ? { weaponDrawn: !interiorWeapon.playerWeapon.sheathed, usingRightHand: interiorWeapon.playerWeapon.usingRightHand } : null`, and `applyWeaponPose(pose)` writing interiorWeapon.playerWeapon presence-gated and UNGATED by mode - both beside interiorPoolSnapshot/restoreInteriorPools (worldModes.js:7859-7860), flag-only (no applyWeapon; syncWorn is DFU's UpdateHands+ApplyWeapon, per world.js:4260-4271). world.js:4115: `const wp = modes?.weaponPose?.() ?? null;` and take weaponDrawn/usingRightHand from `wp` else the exterior rig. world.js applyPose: keep the two exterior writes, then `modes?.applyWeaponPose?.(pose)`. Pins: a worldModes unit pin that weaponPose() reads the interior rig (set drawn + left hand, assert the pair) and answers null outside interior mode; a source pin that worldQuickSave's pose reads the mode-host seam and that applyPose forwards to it; a source pin naming all four hosts.",
    "severity": "medium"
  },
  {
    "id": "SL-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "The latch is real and the header's law is verbatim: src/player/mobileEnemyActivate.js:44-46 records 'it lives on the live foe entity and dies with the pool, as DFU's does', and :107-110 read and raise `entity.pickpocketAttempted`. DFU's field is EnemyEntity.cs:97-101 `PickpocketByPlayerAttempted`, written only by PlayerActivate.cs:829-838, and it appears nowhere in EnemyData_v1 (SerializableGameObject.cs:358-389); on load SerializableStateManager.RestoreEnemyData (:404-424) InstantiatePrefab's a fresh GameObject per record after RespawnPlayer has Destroy'd the dungeon (PlayerEnterExit.cs:456), so the flag is false for every enemy after a load. The port's re-minting pools match that - exteriorFoes.js:1014-1026 restoreWorld goes through spawnFoe and the interior pool is the same factory (worldModes.js:7258) - but dungeonContext.applyWorld (dungeonContext.js:3000-3060) patches `foes[i]` in place and assigns health/items/feet/yaw/hostile/encountered/magicka/maxHealth/fatigue/activeEffects/team/mobileTeam/wabbajackActive/specialTransformationCompleted/dead and never pickpocketAttempted. That host already carries explicit BACKWARD-rewind arms for exactly this reason (the wabbajack `!= null` gate at :3033, the un-kill at :3054, the Seducer clear at :3047), so the omission is a gap in an established pattern, not a deliberate stance. Observable: save-scum a failed pickpocket in a dungeon and the foe can never be attempted again where DFU allows it.",
    "corrections": "The fix location is right but the SCOPE is wrong. `applyWorld` walks `w.foes?.forEach((sf, i) => { const f = foes[i]; if (!f) return; ... })` - it only visits indices the RECORD carries, so a live foe beyond the record's length (a quest foe spawned after the save, or a shorter record) keeps its raised latch, where DFU destroys and re-instantiates the whole set. Put the clear as a pre-pass over the live pool at the top of applyWorld (`for (const f of foes) if (f?.entity) f.entity.pickpocketAttempted = false;`) rather than inside the per-record loop, and cite SerializableStateManager.cs:404-424 plus the module header at mobileEnemyActivate.js:44-46. Second correction: the pin should also SOURCE-pin that the exterior/interior pools re-mint through spawnFoe (exteriorFoes.js:1026) rather than merely asserting the dungeon case - if that restore ever becomes an in-place patch the hole re-opens silently in two more hosts. Nothing is needed for the 'different dungeon' arm (dungeonContext.js:4712 leaves the world as built and the dungeon is rebuilt on arrival).",
    "fixShape": "dungeonContext.js applyWorld: a pre-pass clearing `entity.pickpocketAttempted` on every live foe before the per-record loop, citing RestoreEnemyData's re-instantiation and mobileEnemyActivate.js's header. Pin in the dungeon suite: raise the latch on a live foe, run applyWorld with a record covering FEWER foes than the pool, assert the latch is falsy on both a recorded and an unrecorded foe; mutation-check by deleting the pre-pass. Add a source pin that exteriorFoes.restoreWorld routes through spawnFoe.",
    "severity": "low"
  },
  {
    "id": "SL-4",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "Both halves check out structurally. DFU is explicit: PlayerEntity.cs:128-129 `const int socialGroupCount = 11; int[] reactionMods = new int[socialGroupCount]; // Indices map to FactionFile.SocialGroups 0-10 - do not serialize, set by live effects`, and SerializablePlayer.cs:152-162 writes the eleven named SGroupReputations and nothing else - so save.js:110-118's REP_ARRAYS comment, which cites that range for 'the matching reaction modifiers', mis-cites DFU for a member DFU refuses to serialize. The width gap is real too: save.js:246 snapshots verbatim and :548-552 restores verbatim with no normalisation (contrast the skillsRecentlyRaised guard at :458-459), every later mint is `??=` or `if (!entity.reactionMods)` (enchantments.js:657/:825, artifactEffects.js:149, talk.js:282) and ClearReactionMods' `.fill(0)` preserves length, so a 5-wide array restored once stays 5-wide for the life of the character. I checked the exposure the finder did not bound: talk.js:292 length-guards so there is no NaN, applyRepMod (enchantments.js:653-661) only touches indices 0-4 anyway, and chargen mints 11 first (biography.js:102 ensureReactionState, run inside finishChargen per formulas.js:294) - so the only reachable damage is the Masque of Clavicus loop (artifactEffects.js:151, bounded by `mods.length`) buffing five groups instead of eleven, on a pre-2026-09-08 save whose array was minted by enchantmentMagicRound rather than by chargen. SAVE_VERSION is frozen at 1 by doctrine (save.js:53, :437-438 'the envelope version holds at 1'), so such a save is not rejected and the migration duty is real, but the in-game exposure is much narrower than 'permanently, on a pre-F6 save' suggests.",
    "corrections": "Prefer DROPPING the member over padding, and say why in the record. DFU's PlayerEntity.Reset() (PlayerEntity.cs:794-819) does NOT clear reactionMods and DFU never serializes it, so DFU's load leaves the outgoing live mods standing until the next DoMagicRound's ClearReactionMods (:1567-1570) re-derives them - which is EXACTLY what the port gets by removing 'reactionMods' from REP_ARRAYS (save.js:119): the restore loop already skips a missing key (`if (!snap[k]) continue;`), so no SAVE_VERSION change and legacy 5-wide envelopes stop reaching the entity at all, closing the width hole at the root instead of patching one symptom. Two things to carry with that: (i) the port's clear/re-apply pump is enchantmentMagicRound (enchantments.js:824) which runs for the player every magic round, so nothing permanent lives in reactionMods and nothing is lost - the one-round gap after a boot load is DFU's own; (ii) classicSave.js:797's `reactionMods: new Array(SOCIAL_GROUP_COUNT).fill(0)` becomes inert and its AUDIT 63 F6 comment must be rewritten or the line removed, not left claiming a purpose it no longer has (RETIRING A FLAG DELETES THE SENTENCE). If the lane prefers the minimal-risk shape instead, then pad on restore AND fix the comment AND open a Ledger A row - padding alone leaves a non-DFU member in the envelope with a wrong citation, which is the half of this finding that is certain. Do not describe the departure as harmless without naming the re-derivation that makes it so.",
    "fixShape": "Preferred: remove 'reactionMods' from REP_ARRAYS (save.js:119); rewrite the REP_ARRAYS header to cite SerializablePlayer.cs:152-162 for sGroupReputations alone and PlayerEntity.cs:128-129 for reactionMods' explicit non-serialization; retire classicSave.js:797's line or its comment; Ledger A row recording that the envelope used to carry it. Pin: restorePlayer over a snapshot carrying reactionMods [0,0,0,0,0] leaves the entity's array at SOCIAL_GROUP_COUNT (or absent, then minted 11 by the next magic round), and a source pin that REP_ARRAYS holds sGroupReputations alone. Fallback shape: pad in the REP_ARRAYS restore loop to SOCIAL_GROUP_COUNT with zeros, leading values preserved, mutation-checked by removing the pad.",
    "severity": "low"
  }
]
```

**Summary for the lane:** all four survive, but three of the four fix shapes needed correction — SL-1's pin must use the *in-session* shape (the existing CLK4 pin at `test/clockArc.test.js:299-311` stays green under the bug), SL-2's `applyWeaponPose` must be **ungated by mode** (the load path calls `forceExitToExterior` before `applyPose`) and must not be widened to the dungeon mode, SL-3's clear belongs as a pre-pass over the whole live foe pool rather than inside the record-indexed loop, and SL-4's parity fix is to **drop** `reactionMods` from the envelope (DFU's `PlayerEntity.Reset()` does not clear it either) rather than pad it.