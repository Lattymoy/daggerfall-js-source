# Review: motor-view (XL-4, XL-5) - verdict FIXUP (Opus, 2026-09-11 15:50 UTC, 197k tokens, 87 tool uses)
should 1: mac1_playreport.test.js:271-277 - the "without the bob" half of the feetAt shape pin is vacuous (fixture bob is [0,0,0]); add `m2.bobOffset = [0.1, 0.2, -0.3];` after the 30 update steps (idiom of audit28_headbob.test.js:137); both bob mutants then redden.
should 2: motor.js:1131-1135 "KNOWING DEPARTURE ... DFU returns at :326 without updating grounded" is FALSE (PlayerMotor.cs:278 refreshes grounded each FixedUpdate from the collisionFlags ClimbingMotor.cs:766-767 writes); the swim-branch tail is backwards (the PORT's swim branch latches - Player-Arc.md:1854-1856). Replace with the true statement; drop the departure from the record.
nit 3: the refuters' CENSUS pin is absent (climbing.test.js:266-271 ships only the negative half); deleting the swim branch's `this.standing = this.grounded` leaves climbing.test.js green (audit64_motor catches it). Add a source census over the three moveDirection-zeroing early returns, exempting the freeze return.
gates: lint pass; citedrift pass (all nine host edits same-line; motor.js +59 in four hunks - cross-file motor.js:N cites for N>511 were ALREADY stale at base); manifest Suite-only (7040/669); touched 16/16; full 7049 run, 2 fail = Suite line (manifest + landing U60).
mutants: 13 applied; M5/M5b (bob) SURVIVED (finding 1); M9 (swim write) survives climbing.test.js, caught by audit64_motor (finding 3); the rest RED.
RS-10: straightforward - the lane changed only the `feet:` argument; `setCloudShadow(...)` + `mwViewDrawBody(...)` remain an adjacent pair to move after `renderer.markForeignPass()` (main exterior.js:4201 inside the PERF2 sky block); test/volumetricClouds.test.js:145 asserts the pair adjacent in world.js AND exterior.js - move them together.
Swim overlap: motor.js auto-merges; behaviour overlap nil (a swimming climber takes the climb return's identical writes). Main-merge conflicts expected: PERF5/6 staticBatch lines beneath the three mwViewDrawBody calls (dungeon.js:876, worldModes.js:5710/:5781: keep main's PERF lines + the lane's feetAt()), PERF2's sky-block move in exterior.js:4033, Testing.md.
Record XL-4: as the lane wrote it, with the bob-exclusion claim made true by finding 1 (or softened). Scope: the port's own presentation law (EV1/MAC1 I) applied to one camera and not the other; MW-D25 has no C# counterpart - cite no C# line for the defect itself.
Record XL-5: rewrite the third paragraph (reviewer's 5 lines): standing/movingLessThanHalfSpeed are a port-side cache of PlayerMotor.cs:113-125/:168-181; _climbStep writes the swim branch's two lines before `return true` (the climb is the other disjunct of :322-326), mirrored half-speed line; NO departure: DFU refreshes grounded at :278 from ClimbingMotor.cs:767's flags; freezeMotor return stays bare (:296-306 does not zero moveDirection); the fatigue-band consequence is false (worldTick.js:493-494 climb-first = PlayerEntity.cs:406-408).
