I have everything I need. Here are my verdicts.

```json
[
  {
    "id": "SL-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived at HEAD: `restoreWeather` (src/systems/weatherSim.js:322-334) sets `_climateWeathersRolled = true`, `_updateFromClimateArray = false`, `_jumps++` and `_evolveHour = null` (:333) and never touches `_climateWeathersValid`, which is declared at :147 with the CLK2 sentence 'a loaded save stamps ROLLED without rolling ... the evolution must not move off that' and is raised at :185 (setClimateWeathers) and :348 (importClimateWeathers), lowered only in resetWeatherSim (:430). `evolveClimateWeathers` reads it at :398 and worldTick.js:560 calls the evolution every tick wherever the player is; `resetWeatherSim` has no production caller (grep: tests only), so after any in-session load the boot roll's `true` is still standing over the OUTGOING session's `_climateWeathers`. I reproduced it: reset -> setWeatherEvolution(true) -> tickWeather (boot roll) -> restoreWeather('rain') -> 24 hourly evolve+tick gives 'hour 6 DRAINED to sunny'; the same walk with no prior roll stays 'rain'. DFU serializes ONE weather value (SerializablePlayer.cs:225 verified) and WeatherManager.cs:538-542's else arm exists precisely so 'no weather update from climate array happens in case of loaded savegame', so the port breaks both DFU's law and its own recorded CLK2 law; the existing pin (test/clockArc.test.js:299-306) starts with `resetWeatherSim()` and therefore only covers the fresh-process load.",
    "corrections": "Cite drift only: `_climateWeathers` is declared at :144 and `evolveClimateWeathers` opens at :392-398 (the guard is :398). One thing the finder understates: the drain does not need the CHANGED zone to be the player's - `tickWeather` -> `applyFromArray` (:222-228) applies the PLAYER's zone slot from the outgoing array whenever any of the six zones flips, so a distant zone's turn is enough to replace the restored sky. Everything else in the claim re-derives exactly.",
    "fixShape": "Add `_climateWeathersValid = false;` to `restoreWeather` beside `_evolveHour = null` (src/systems/weatherSim.js:333), with the CLK2 citation on the same line ('a loaded save's array is not this session's - the evolution stays dormant until the next day roll re-rolls it'). Nothing else: leave `_climateWeathers`, `_rolledAtMinutes` and `_zoneChangedAtMinutes` alone (with valid down and `_updateFromClimateArray` down, no drain can reach the stale stamps before `rollClimateWeathersForDay` resets both, and the travel-arrival apply off the stale array is the existing Ledger A row 'TRAVEL WEATHER IS APPLIED ON EVERY ARRIVAL'). No SAVE_VERSION change. Pin: extend the CLK4 case at test/clockArc.test.js:299 with the IN-SESSION variant - reset; setWeatherEvolution(true); tickWeather(day0-200, Woodlands) (the boot roll, so the flag is true); restoreWeather('rain'); walk 24 hours of evolve+tick; assert currentWeather() === 'rain' and weatherJumpStamp() unchanged. Deleting the new line reddens it at hour 6 (verified by probe), so the pin fails under the one-character mutation.",
    "severity": "medium"
  },
  {
    "id": "SL-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Verified: three live PlayerWeapon instances reach the world host's save path - world.js:2406, worldModes.js:538 (`const interiorWeapon = createWeaponRig({`) and dungeonContext.js:2477, each minting its own `new PlayerWeapon({})` at combat/weaponRig.js:104. world.js:4115 composes `weaponDrawn`/`usingRightHand` from `weaponRig.playerWeapon` unconditionally and applyPose writes them back at :4259/:4272; worldModes routes the inside-a-building F9/F12 and the pause window's Save/Load to that same composer (worldModes.js:6919-6920, and :6822-6823 for the pause flow), and its returned seam list (:7857-7860 interiorSaveData / interiorPoolSnapshot / restoreInteriorPools) carries no weapon member - grep for `weaponPose|applyWeaponPose` over src/ and test/ is empty. Nothing syncs the pair at a transition either (enterInteriorCore, worldModes.js:4391-4560, touches no weapon state), and `interiorWeapon` is built once per host, not per entry, so its sheath/hand survive every interior visit. DFU has ONE WeaponManager for every WorldContext and writes/reads the pair in one place (SerializablePlayer.cs:175-176 and :420-421, both re-read), and never sheathes on a transition (the only SheathWeapons callers are WeaponManager.Reset and FPSWeapon.cs:364's no-arrows arm). Consequence at HEAD: a save taken inside a building records the exterior rig's pose, and a load into that building leaves the interior rig on whatever it happens to hold - a fresh-process load into an interior always stands you there with hands empty and right-handed however the save was written.",
    "corrections": "Two corrections. (1) DUNGEON mode is NOT part of this gap and must not be swept into the fix: worldModes hands buildDungeonContext its own `pose` seam (worldModes.js:4890-4956) and dungeonContext owns the whole composer (:4655 save, :4731/:4737 restore), so the world-hosted dungeon already round-trips the pair; the finder's own 'dungeonContext correctly wired' is right and the seam must stay interior-only. The finder's :4736-4744 is :4731-4738. (2) The finding is the SAVE symptom of a wider unrecorded divergence: because nothing hands the pair between rigs at a transition, drawing a weapon outside and walking into a shop already sheathes it, with no save involved - a save-only fix leaves that standing, and nothing in Ledger A / Audit-63 / Audit-64 records it (Home.md:50 only names the fourth rig's existence). exterior.js's fourth rig (:1588) is not in scope - that host has no quickSave at all.",
    "fixShape": "(a) Add to worldModes' returned object beside interiorPoolSnapshot/restoreInteriorPools (worldModes.js:7859-7860): `weaponPose()` answering `mode === 'interior' ? { weaponDrawn: !interiorWeapon.playerWeapon.sheathed, usingRightHand: interiorWeapon.playerWeapon.usingRightHand } : null` (null in exterior AND dungeon mode - dungeonContext owns its own composer), and `applyWeaponPose(pose)` writing the same two fields into `interiorWeapon.playerWeapon`, presence-gated with `!= null` exactly as world.js:4259/:4272 and dungeonContext.js:4731/:4737 are, and a no-op outside interior mode. (b) world.js:4115: read `modes?.weaponPose?.()` once and fall back to the exterior rig per field, so a pre-seam host and exterior mode compose exactly as today. (c) world.js applyPose (:4258-4272): keep the exterior writes and add `modes?.applyWeaponPose?.(pose)` - applyPose already runs at :4250, AFTER the restoreInterior re-entry at :4207, so the mode is interior by then; the classic-import caller shares applyPose and gets the same no-op. (d) In the record, either sync the pair at the interior enter/exit transitions (DFU's one-WeaponManager shape) or write a Ledger A row saying the port's four rigs each hold their own sheath/hand - do not close this leaving the transition half unnamed. Pin: a worldModes unit that mounts interior mode, sets interiorWeapon drawn + left hand and asserts `weaponPose()` is `{weaponDrawn:true,usingRightHand:false}` and null in exterior mode; a source pin that world.js's pose composer reads the mode seam and that applyPose's `applyWeaponPose` call sits after the restoreInterior await.",
    "severity": "low"
  },
  {
    "id": "SL-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Confirmed at HEAD: `entity.pickpocketAttempted` is raised at src/player/mobileEnemyActivate.js:110 and gates everything at :108, and the module header (:44-47) records 'it lives on the live foe entity and dies with the pool, as DFU's does'. The dungeon arms are live (worldModes.js:5107 for the world-hosted dungeon, dungeon.js:229 for the standalone), and dungeonContext.applyWorld (src/scenes/dungeonContext.js:3000-3050) patches `foes[i]` in place - health, items, feet, hostile, encountered, magicka, maxHealth, fatigue, activeEffects, team, mobileTeam, wabbajackActive, specialTransformationCompleted, dead - and never touches pickpocketAttempted, while the re-minting pools do clear it by construction (scenes/exteriorFoes.js:1014-1060 restoreWorld -> spawnFoe, and worldModes.restoreInteriorPools :7248-7260 delegating to the same). DFU's load re-instantiates: SerializableStateManager.cs:404-425 calls `GameObjectHelper.InstantiatePrefab` per EnemyData_v1 record and then RestoreSaveData onto it, and EnemyData_v1 carries no pickpocket field, so EnemyEntity.cs:57's `pickpocketByPlayerAttempted = false` default is what every loaded enemy gets. So a same-dungeon reload in the port keeps a latch DFU's rebuild clears - a failed pickpocket on a class foe cannot be retried after a load, and a save taken before the attempt loads back with the attempt still spent.",
    "corrections": "The claim and every cite re-derive (mobileEnemyActivate.js:108-110, dungeonContext.js:3000/:3004-3046, the four wiring sites at dungeon.js:229 / exterior.js:3796 / world.js:7443 / worldModes.js:4626 and :5107). One narrowing the finder did not state: applyWorld only walks the SAVED indices (`w.foes?.forEach((sf, i) => { const f = foes[i]; ...`), so a clear placed inside that loop still leaves the latch raised on any live foe with no saved record - and DFU's load leaves only the saved enemies standing at all. Also note the exteriorFoes path is src/scenes/exteriorFoes.js, not src/systems/.",
    "fixShape": "Clear it across the WHOLE live pool at the top of `applyWorld` rather than inside the indexed forEach: `for (const f of foes) if (f?.entity) f.entity.pickpocketAttempted = false;` - unconditional, NOT presence-gated on a saved field, citing SerializableStateManager.cs:404-425 (every restored enemy is a freshly instantiated prefab, so EnemyEntity.cs:57's default is the loaded truth) and the module header at mobileEnemyActivate.js:44-47 that this host was falsifying. One line, no envelope change, no SAVE_VERSION change. Pin in the dungeon save/load suite: raise the latch on a live foe, quickSave then quickLoad the same locationKey, assert `foe.entity.pickpocketAttempted` is falsy on a foe both with and without a saved record; deleting the clear reddens it.",
    "severity": "low"
  },
  {
    "id": "SL-4",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "Both halves re-derive. DFU: PlayerEntity.cs:128-129 is `const int socialGroupCount = 11; int[] reactionMods = new int[socialGroupCount]; // ... do not serialize, set by live effects`, and SerializablePlayer.cs:152-162 writes only the eleven named SGroupReputations - so save.js:110-119's REP_ARRAYS comment does hang reactionMods off a cite that covers the reputations alone. Port: every mint is now SOCIAL_GROUP_COUNT (enchantments.js:657 and :825, artifactEffects.js:149, talk.js:282's ensureReactionState, classicSave.js:797) and all of them are `??=`/`if (!...)` guards, while save.js:246 snapshots and :548-556 restores the array verbatim with no width normalisation - so a 5-wide array from an older save restores 5-wide and `ClearReactionMods`' `.fill(0)` (enchantments.js:825, run every magic round via worldTick.js:213) preserves the length forever. `git show eac56a71` confirms the three pre-F6 mints were `new Array(5)`. The only consumer that can be hurt is the Masque of Clavicus, whose loop is `for (let g = 0; g < mods.length; g++)` (artifactEffects.js:149-150): on such a save it buffs five groups instead of eleven, permanently - which is the same defect AUDIT 63 F6 fixed at the mints and left open at the restore arm. SAVE_VERSION is still 1 (save.js:53, :422), so those saves do load.",
    "corrections": "The finder's stated trigger is wrong twice over, in both directions. GoodRepWith/BadRepWith CANNOT be harmed by a short array - `applyRepMod` (enchantments.js:653-661) only ever writes indices 0-4, DFU's own five. And the real minting source is broader than 'a held effect ran a round before the first greeting': `enchantmentMagicRound` clears the array for any `entity.isPlayer` before it even looks at the item list (enchantments.js:825), and that runs every magic round from worldTick.js:213, so before ensureReactionState existed (talk.js, 6b3af1ea, 2026-09-03) essentially every character's array was minted 5-wide - the affected population is saves whose character predates 2026-09-03, not saves with an exotic effect. Conversely the harm is narrower than 'the Masque buffs five groups' implies: the restored VALUES are irrelevant (they are zeroed at the next magic round); only the LENGTH survives, and only the Masque reads past index 4. The 'unlike newSkillsRecentlyRaised at :243/:474' analogy is wrong: that helper is at save.js:109, used at :459, and it is a PRESENCE default (DFU's own guard at SerializablePlayer.cs:292), not a width normalisation - there is no width-padding precedent in this file to lean on.",
    "fixShape": "Prefer the finder's option (b) over (a), because the pad keeps a member in the envelope that DFU's own source forbids. DROP `reactionMods` from REP_ARRAYS (save.js:119), leaving `const REP_ARRAYS = ['sGroupReputations']` and keeping the SupernaturalBeings quirk arm at :552 untouched; an old snapshot's key is then simply ignored, which is back-compatible and removes the width hazard at the root. Justify it in the comment at save.js:110-118: re-cite SerializablePlayer.cs:152-162 for the reputations ALONE and add PlayerEntity.cs:129's 'do not serialize, set by live effects' with the reason it costs nothing - enchantments.js:825 clears the player's array unconditionally at the head of every magic round (worldTick.js:213) and the folds re-apply it, so DFU's fresh-PlayerEntity-plus-next-MagicRound is exactly what the port then reproduces. Three pins must move with it and must be REWRITTEN rather than deleted: test/audit17h.test.js:26, :68 and :85 - assert instead that the restore leaves the live array alone and that one magic round re-derives it. No SAVE_VERSION bump, no Ledger A row needed (the departure is being retired, not recorded). If the lane finds a reader of reactionMods across a load that I did not (I checked every site: enchantments.js:657/:825, artifactEffects.js:149, talk.js:282/:292 - all `??=`-guarded or length-guarded), fall back to padding the restored array to SOCIAL_GROUP_COUNT with zeros in the REP_ARRAYS loop AND open a Ledger A row for the serialized member.",
    "severity": "low"
  }
]
```