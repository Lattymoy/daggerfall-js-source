I've completed the cross-lane read. Here are my findings.

```json
[
  {
    "id": "XL-1",
    "lens": "cross-lane",
    "title": "OT1's exterior swim flag lands on the motor's LevitateMotor gate: the player is frozen solid in exterior deep water, and AUDIT 64 F0's swim speed becomes unreachable",
    "severity": "critical",
    "files": [
      "src/scenes/world.js:7243-7244,7266,7330",
      "src/scenes/exterior.js:3633-3634,3654,3708",
      "src/scenes/shared.js:886-892",
      "src/player/motor.js:470-476,1132-1144,1190,1372",
      "src/player/exteriorSurface.js:47-56,261-267,289-293",
      "test/ot1_swim_latch.test.js:33-80"
    ],
    "claim": "DFU carries TWO distinct swim members: PlayerEnterExit.isPlayerSwimming (the host flag the fatigue arm, the encounter roll and the rest refusal read) and levitateMotor.IsSwimming (== PlayerMotor.IsSwimming), which the exterior arm clears UNCONDITIONALLY every frame. The port has only ONE member - PlayerMotor.swimming - and exteriorSurface.js's own header says so and says the latch must be carried 'without letting it near the motor'. OT1 then wired `player.swimming = exteriorSwimming(...)` into both exterior hosts, i.e. straight onto the motor's LevitateMotor gate. Consequence per frame on tile 0: applyMotorEffectFlags writes false (an edge -> cancelMovement), player.update spends that cancel and returns from _step doing nothing, then OT1 writes true (another edge -> cancelMovement armed for the next frame). The exterior swimmer cannot move at all - not forward, not up, not down - and because the swim/levitate branch returns above UpdateSpeed, AUDIT 64 F0's `if (this.sunk && !this.waterWalking) speed = swimSpeed(...)` (motor.js:1372) and F1's water jump-cancel are never reached outdoors, which is exactly the law F0 was written to restore.",
    "evidence": "PORT, the frame order in world.js: :7243 `const _wasSwimming = !!player.swimming;` -> :7244 `applyMotorEffectFlags(player, playerEntity);` whose body is shared.js:888 `player.swimming = false;` -> :7266 `player.update(dt, ...)` -> :7330 `player.swimming = exteriorSwimming({ wasSwimming: _wasSwimming, sunk: !!player.sunk, unsunk: player.heightAction === 'unsink', tileIndex: _surf.tileIndex });`. exterior.js:3633/3634/3654/3708 is the same sequence. motor.js:471-476 `set swimming(v) { ... this._swimming = b; this.cancelMovement = true; }`, so BOTH writes are edges while swimming; motor.js:1134-1144 spends cancelMovement with `return;` (no move, `this.standing = this.grounded`, `groundKey = null`). motor.js:1190 `if (this.levitating || this.swimming) {` is the LevitateMotor branch that returns above UpdateSpeed.\n\nMEASURED (a harness driving the real PlayerMotor over a real Collider floor in exactly the host order above, Speed 50 / Swimming 30, 1/60 steps):\n  DRY walk, 30 steps -> dz = 2.2152, speed field 4.430\n  DEEP WATER with OT1's write, 60 steps of full forward -> dz = 0.0000, dy = 0.0000\n  DEEP WATER without OT1's write (F0's shape: swimming stays false, `sunk` carries the speed), 60 steps -> dz = 1.7722, speed field 1.772\n  Holding FloatUp over the sea for 300 steps -> dy = 0.000 (and `player.waterSurfaceY` is null outdoors - shared.js:887's default - so the LevitateMotor surface clamp at motor.js:1246-1249 would not have stopped a rise either).\n\nDFU, the law that decides it: PlayerEnterExit.cs:416-421 (the else arm, outdoors) - `if (StreamingWorld.PlayerTileMapIndex != 0) isPlayerSwimming = false; isPlayerSubmerged = false; levitateMotor.IsSwimming = false;` - the motor flag is cleared with no tile test at all. PlayerMotor.cs:149-152 `public bool IsSwimming { get { return (levitateMotor) ? levitateMotor.IsSwimming : false; } }`. PlayerMotor.cs:323-326 `if (levitateMotor && (levitateMotor.IsLevitating || levitateMotor.IsSwimming) || climbingMotor.IsClimbing) { moveDirection = Vector3.zero; return; }` - the return F0 explicitly reasons is NOT taken outdoors. And the readers OT1 wanted really do read the OTHER member: PlayerEntity.cs:410 `else if (GameManager.Instance.PlayerEnterExit.IsPlayerSwimming)` for the fatigue/Swimming tally. exteriorSurface.js:52-56 states the separation in the port's own words, and exteriorSwimLatch (:261-267) returns `motorSwimming: false` beside `swimming` for precisely this reason - the wiring ignored that field.\n\nThe OT1 pin cannot see any of this: test/ot1_swim_latch.test.js only calls the pure helper and greps the hosts for the assignment's source text; no test drives a motor across the seam.",
    "proposedFix": "Split the member, as DFU has it split. Add a host-side `player.isPlayerSwimming` (or a plain host local, e.g. `_exteriorSwimming`) that exteriorSwimming() writes, leave `player.swimming` (levitateMotor.IsSwimming) written false by applyMotorEffectFlags every exterior frame, and repoint the IsPlayerSwimming readers at the new flag: the worldTick fatigue/tally arm (world.js:7232, exterior.js's twin), the encounter-roll suppression (world.js:2256, StreamingWorld :488-491), the rest refusal (world.js:1609 -> restSession.js:185), and the dungeon-exit carry. Leave the motor-flag readers (the _step branch, headBobber's swim style, footsteps' motor term) on `player.swimming`. AUDIT 64 F0's `sunk` gate at motor.js:1372 then stays the live exterior swim speed, which is what it was built to be.",
    "pinShape": "A behavioural pin that drives the REAL host sequence (capture -> applyMotorEffectFlags -> player.update -> the exterior write) over a Collider floor on tile 0 and asserts the swimmer still TRAVELS: dz over 60 steps equals swimSpeed(walkSpeed(50), 30)*1s to 1e-3, computed from the C# constants, not the port's helpers. Plus: `motor.cancelMovement` is false after a steady-state water frame (no double edge), and an absence pin that neither exterior host assigns exteriorSwimming's result to `player.swimming`. Mutation: restoring `player.swimming = exteriorSwimming(...)` must drive the travel pin to 0.0000."
  },
  {
    "id": "XL-2",
    "lens": "cross-lane",
    "title": "Seasons of the Iliac Bay draws nature flats at 3.1x, so MAC1's TALL_FLAT_HEIGHT rule admits every shrub and the far-flat cull silently reverts for three seasons out of four",
    "severity": "high",
    "files": [
      "src/world/flatDistance.js:31-50",
      "src/scenes/world.js:7855-7870",
      "src/systems/seasonsIliacBay.js:216-228,331-338",
      "src/world/rmbFlats.js:47-60",
      "src/world/meshReader.js:20",
      "bible/07-Rendering/Seasons-Iliac-Bay.md:9,50-52"
    ],
    "claim": "MAC1's far-flat rule is a WORLD-UNIT height test calibrated against classic flat sizes ('a walker is 1.8', trees at 2.5+). SIB1/SIB2 changes the world size of exactly the archives that dominate the streamed flat count: TEXTURE.504-510 draw at 3.1x their classic size while a seasonal atlas is installed. The batch the far rule measures is the SEASONAL batch (world.js:1157-1163 passes `sib.size` into createBillboardBatch, and renderer.js:2460 stores it as `batch.size`), so under the mod the 2.5-unit threshold corresponds to a classic 0.806-unit flat - roughly a 32-pixel record - and effectively every plant, rock and shrub of those archives is re-classified as a tree and drawn at every ring. MAC1's fix therefore works in Summer (vanilla atlas, classic sizes) and stops working in Fall, Spring and Winter, on precisely the woodland/hills/haunted/mountain pixels the fps report was about. The same mechanism makes the drawn-flat count - and the frame rate - step on a month boundary with nothing else changing.",
    "evidence": "src/world/flatDistance.js:37 `export const TALL_FLAT_HEIGHT = 2.5;` with the header's calibration at :20-23 ('a tree, at or above TALL_FLAT_HEIGHT in world units (a walker is 1.8)'); :46-50 `farFlatVisible` returns `height >= TALL_FLAT_HEIGHT` for ring >= 2. world.js:7866 `if (!farFlatVisible({ ring, height: b.size?.h ?? 0, animated: b.frame != null })) continue;` - `b.size` is whatever built the batch. The seasonal branch at world.js:1148-1163: `const sib = seasonsActive ? seasons.lookup(archive, record) : null; ... renderer.createBillboardBatch(archive, rkey, sib.size, centers)`. seasonsIliacBay.js:337 `return { texture: t, size: seasonalBillboardSize(t.width, t.height) };` and :223-227 `seasonalBillboardSize(w,h) = scaledBillboardSize({ width: fround(w * BILLBOARD_SCALE), height: fround(h * BILLBOARD_SCALE) }, { width: 0, height: 0 })` with `BILLBOARD_SCALE = fround(3.1)` (:80). The classic path is world.js:1165 `scaledBillboardSize(t.getSize(record), t.getScale(record))`. With zero scale both collapse to `h_px * GLOBAL_SCALE` (rmbFlats.js:54-59, meshReader.js:20 GLOBAL_SCALE = 0.025), so the seasonal height is exactly 3.1x the classic height for the same record - which the port's own bible states outright: Seasons-Iliac-Bay.md:9 'draw at 3.1 times their classic size' and :50-52 'the size is 3.1x the texture's pixels through the same GetScaledBillboardSize every classic flat takes'. 2.5 / 3.1 = 0.806 world units = 32 classic pixels; the tallest walker MAC1 calibrated against is 1.8.",
    "proposedFix": "Feed the far rule a SEASON-INVARIANT height. Either carry the classic `scaledBillboardSize(t.getSize(record), t.getScale(record)).h` on the batch alongside the drawn `size` (the seasonal branch already has `t` in hand at world.js:1150) and test that, or normalise inside the flat walk by dividing a seasonally-skinned batch's height by BILLBOARD_SCALE. Do NOT raise TALL_FLAT_HEIGHT: that would break the vanilla case the constant was measured for. Whichever way, the rule must answer the same verdict for one record whether or not a seasonal atlas is installed.",
    "pinShape": "One record, two sizes: assert `farFlatVisible({ ring: 3, height: h_classic })` === `farFlatVisible({ ring: 3, height: h_seasonal })` for a record small enough to be culled classically, where h_classic = scaledBillboardSize(size, {0,0}).h and h_seasonal = seasonalBillboardSize(size.width, size.height).h off the SAME producer. Plus a host pin that world.js's flat walk does not read the seasonal `size.h` directly. Mutation: passing `b.size.h` through unchanged must flip the seasonal verdict and fail."
  },
  {
    "id": "XL-3",
    "lens": "cross-lane",
    "title": "MAC1's relock-on-resume is wired into two of the four hosts; exterior.js and dungeonContext.js still relock a frame later through the look gate, so the double-click resume and its stolen SetClickDelay survive there",
    "severity": "medium",
    "files": [
      "src/ui/pauseDoor.js:155-165",
      "src/scenes/world.js:4808-4816,6721,5068",
      "src/scenes/worldModes.js:6816-6824",
      "src/scenes/exterior.js:2167-2192,2448,3444,3499",
      "src/scenes/dungeonContext.js:4337-4345",
      "src/scenes/dungeon.js:338,567,625",
      "src/player/pointerLock.js:117-141"
    ],
    "claim": "MAC1 J's mechanism is that the look gate's relock runs on the NEXT frame, outside transient activation, so the browser refuses it and the first world click is spent re-acquiring the lock (and arms SetClickDelay with it). The cure - `hooks.relock` fired inside the resume click - is passed by world.js (:4816, and :6721 as `host.relock` for the interior arm) and consumed by worldModes.js (:6824). The other two of the four hosts that own a motor open the SAME pause flow and pass no `relock` at all: exterior.js:2168 and dungeonContext.js:4339. Both still run `makeLookGate` (exterior.js:3444/:3499, dungeon.js:567/:625), and both carry the identical SetClickDelay arm (exterior.js:2448, dungeon.js:338), so the reported bug is unchanged in the ?exterior and ?dungeon hosts. MAC1's record names neither, against THE FOUR HOSTS RULE.",
    "evidence": "pauseDoor.js:165 `if (action !== 'exit') hooks.relock?.();` - an optional hook, so a host that omits it is silent. Present: world.js:4816 `relock: () => requestLook(canvas),`; world.js:6721 `relock: () => requestLook(canvas),   // MAC1: the interior arm's pause door relocks through this host's canvas`; worldModes.js:6824 `relock: host.relock,`. Absent: exterior.js:2168-2191 `openPauseFlow((w) => townTalk.showOverlay(w), { at, openPack, openSpellbook, openChronicle, savingPrevented, exitToMenu, textLines, questMessages, questLog })` - no relock key; dungeonContext.js:4339-4380 likewise. Both hosts then take the refused path pointerLock.js:135-140 `else if (wasHeld) requestLook(canvas);` on the frame AFTER the close, which the module header itself calls out at :16-18. The consequence arm is identical in all three files: exterior.js:2448 / dungeon.js:338 / world.js:5068 `if (e.pointerType !== 'touch' && document.pointerLockElement !== canvas) setClickDelay(...)`.\nSecond-order note for the touch layer: before MAC1 the relock always fired OUTSIDE activation and was refused, so on a phone nothing happened; MAC1's relock now rides a trusted DOM click on the enhanced menu, so on any browser that grants pointer lock to a touch-driven click the resume can now actually acquire a lock that AUDIT 62 F6 (world.js:5057-5067, ui/touch.js) asserts 'a finger can never hold'. Worth a `pointerType`/coarse-pointer guard in the same fix.",
    "proposedFix": "Pass `relock: () => requestLook(canvas)` from exterior.js's togglePause (:2168) and `relock: () => requestLook(ctx.canvas)` from dungeonContext.js's togglePause (:4339), naming all four hosts in the record. Guard the call for touch (skip the relock when the closing gesture's pointerType is 'touch', or when the touch layer is mounted), so MAC1's fix cannot hand a phone a lock the AUDIT 62 F6 arm assumes is impossible.",
    "pinShape": "A census pin over the four openPauseFlow call sites (world.js, worldModes.js, exterior.js, dungeonContext.js): each options object must name `relock`, and the count of call sites must equal the count of relock keys - so a fifth host cannot land without one. Beside it, a behavioural pauseDoor pin that act('resume'), act('save') and act('load') each call hooks.relock exactly once and act('exit') never does. Mutation: deleting any one host's key must fail the census."
  },
  {
    "id": "XL-4",
    "lens": "cross-lane",
    "title": "The Morrowind third-person camera is built from the raw stepped feet, so it takes neither EV1's interpolation nor MAC1's step low-pass - the jitter MAC1 fixed is still there in third person, in all four hosts",
    "severity": "medium",
    "files": [
      "src/player/mwCamera.js:198-233",
      "src/scenes/world.js:7596-7599,7783",
      "src/scenes/worldModes.js:5651-5654",
      "src/scenes/exterior.js:3869-3872",
      "src/scenes/dungeon.js:839-842",
      "src/player/motor.js:495-538,961,966-987"
    ],
    "claim": "MAC1 I and EV1 both live read-side, inside `eyeAt()`: the interpolation between physics steps and the STEP_SMOOTH_TAU low-pass on the interpolated feet. The hosts hand mwViewFrame `fpEye: cam.pos` (which IS eyeAt's output) AND `feet: player.pos` - the raw, 60 Hz-quantised stepped feet. mwCamera.eye returns fpEye untouched in first person, but in third person it builds the focal from `feet` alone (`focal = [feet[0], feet[1] + FOCAL_HEIGHT*scale*u, feet[2]]`) and pulls the eye back from that. So the whole render-side smoothing arc - EV1's judder fix and MAC1's stairs/hills fix - is bypassed the moment the player switches to third person, in every host: the camera steps in 1/60 quanta horizontally and pops a whole rung per stair vertically, the exact two symptoms both slices were written for. mwViewDrawBody (world.js:7783) draws the body off the same raw feet, so the body and the first-person eye disagree by up to one step too.",
    "evidence": "mwCamera.js:198-206 `eye({ fpEye, feet, ... }) { if (firstPerson) { cameraDistance = 0; return { eye: fpEye, ... }; } const u = 1 / MW_UNITS_PER_METER; const focal = [feet[0], feet[1] + FOCAL_HEIGHT * heightScale * u, feet[2]];` ... :229 `eye: [focal[0] - fwd[0]*dist, focal[1] - fwd[1]*dist, focal[2] - fwd[2]*dist]`. All four call sites pass the raw feet: world.js:7597 `fpEye: cam.pos, feet: player.pos`, worldModes.js:5652 same, exterior.js:3870 same, dungeon.js:840 same; world.js:7783 `mwViewDrawBody(canvas, { proj, view, eye: mwv.eye, feet: player.pos, yaw: cam.yaw })`. The smoothing that these bypass is motor.js:516-537 (eyeAt: `q[1] + dy*a` and the `_eyeFeetY` substitution at :525) and motor.js:979-987 (`_smoothEyeFeet`, called once per render frame at :961). MAC1's own measurement - worst single-frame eye pop 0.153 -> 0.051 at 60 Hz - was taken through eyeAt, which third person never reads.",
    "proposedFix": "Give the motor a render-feet accessor that is exactly eyeAt's positional half without `_eyeLevel()`/bob (the interpolated x/z and the low-passed feet height, sharing the snap guard), and pass THAT as `feet` at all four mwViewFrame sites and at mwViewDrawBody. `player.pos` stays the simulation truth for the collider, the rays and the focal's own ceiling probe, exactly as `eye` does for first person.",
    "pinShape": "Run the MAC1 staircase harness twice - first person and third person - and assert the worst single-frame vertical camera delta is within a small factor of each other, and that the third-person figure is below MAC1's measured 0.051 at 60 Hz. Beside it a shape pin that no host passes `player.pos` as mwViewFrame's `feet`. Mutation: reverting a host to `feet: player.pos` must lift the third-person figure back to the raw rung (~0.153)."
  },
  {
    "id": "XL-5",
    "lens": "cross-lane",
    "title": "player.standing is a cached field with no writer on the climb return, and AUDIT 64 F3/F7 plus MAC1 H just made it a per-frame law in seven places - so a climb freezes IsStandingStill at its pre-climb value",
    "severity": "medium",
    "files": [
      "src/player/motor.js:640-660,1023-1105,1182,1132-1144,1256-1270",
      "src/scenes/world.js:7223,7351,7939,8292",
      "src/scenes/exterior.js:3608,3725,4221",
      "src/scenes/worldModes.js:5448,5490,5527",
      "src/scenes/dungeon.js:749,788"
    ],
    "claim": "DFU's PlayerMotor.IsStandingStill is a live GETTER over moveDirection (:113-125) and can never be stale; the climb/swim/levitate return zeroes moveDirection first (:325) so a climbing player reads standing-still = grounded. The port turned it into a cached field `this.standing`, written in three places - _trackHalfSpeed (the walk path, :657), the cancelMovement block (:1139) and the swim/levitate branch (:1269) - and the CLIMB return at :1182 sits above all of them and writes nothing. `_climbStep` even quotes ':322-326 zeroes moveDirection' at :1047-1049 and zeroes _airVelX/_airVelZ from it, but not `standing` (nor `movingLessThanHalfSpeed`). That was inert while the field had one lazy reader; AUDIT 64 F3/F7 and MAC1 H made it the term behind the footstep stride, the fatigue/Running band and the townsfolk politeness gate in all four hosts, so a player who runs at a wall and climbs it carries `standing === false` for the whole climb: the fatigue band's `running && !standing` arm keeps charging the running rate on top of ClimbingFatigueLoss, the footstep machine's standing-still gate is wrong for the climb's whole duration, and MAC1 H's new politeness gate reads a value the motor stopped maintaining.",
    "evidence": "PORT writers: motor.js:656-657 `const standing = this.grounded && !input.forward && !input.strafe; this.standing = standing;   // the hosts' IsRunning && !IsStandingStill read`; motor.js:1139 `this.standing = this.grounded;` (cancel block); motor.js:1269 `this.standing = this.grounded;` (swim/levitate). The gap: motor.js:1182 `if (this._climbStep(dt, input, yaw)) return;` returns above both remaining writers, and _climbStep (:1023-1105) contains no `this.standing` assignment - its own comment at :1047-1049 reads ':322-326 zeroes moveDirection before the climb/swim/levitate return, and airControl is false - so nothing of the momentum carried into the climb survives it' and it then zeroes only the air velocities. NEW READERS in the delta: world.js:7223 `running: player.isRunning && !player.standing,   // AUDIT 23 (entity-2): PlayerEntity.cs:408`; world.js:7351 and :8292 `standingStill: player.standing,`; world.js:7939 `_playerStill = walkMode && playerSpawned ? !!player.standing : ...` (MAC1 H); exterior.js:3608/:3725/:4221; worldModes.js:5448/:5490/:5527; dungeon.js:749/:788.\nDFU: PlayerMotor.cs:113-125 IsStandingStill is a getter, `if (grounded) return (new Vector2(moveDirection.x, moveDirection.z).magnitude == 0); return false;`. PlayerMotor.cs:323-326 `if (levitateMotor && (levitateMotor.IsLevitating || levitateMotor.IsSwimming) || climbingMotor.IsClimbing) { moveDirection = Vector3.zero; return; }` - the climb disjunct is in the SAME statement as the swim one the port did port. PlayerEntity.cs:408 `else if (playerMotor.IsRunning && !playerMotor.IsStandingStill)` is the reader AUDIT 64 F7 cites, and PlayerEntity.cs:406-407 puts ClimbingFatigueLoss ahead of it - so DFU's ladder is climb-first and never sees a stale running term.",
    "proposedFix": "Write the field on the climb return the way the swim return does - `this.standing = this.grounded; this.movingLessThanHalfSpeed = true;` (or route both through one `_zeroMoveDirection()` helper) immediately before `_climbStep` returns true, citing PlayerMotor.cs:325. Better still, make `standing` a getter over a single `_moveDirZeroed` flag so no future early return can leave it stale, which is the shape DFU has.",
    "pinShape": "Drive a real ClimbingState one step: a motor moving forward (standing false) that latches a climb must read `standing === grounded` on the FIRST climbing step and every step after, and `movingLessThanHalfSpeed === true`. Beside it a census over `_step`'s early returns: every `return` above the walk path must be preceded by a write to `standing` (grep the branch bodies, not a comment). Mutation: deleting the swim branch's existing `this.standing = this.grounded` must fail the same census, proving the pin bites on the shape and not on the climb alone."
  },
  {
    "id": "XL-6",
    "lens": "cross-lane",
    "title": "MAC1 moved registerMorrowindData onto the boot-menu mount right after MW-LOAD made it read every stored Morrowind file's value - so a pre-MW-LOAD attach migrates hundreds of MB through memory on the title screen",
    "severity": "low",
    "files": [
      "src/ui/enhancedMenu.js:1983-1996",
      "src/scenes/dataSource.js:371-390,706-715,717-740",
      "src/scenes/shared.js (loadMagicRegistries, the previous sole caller)"
    ],
    "claim": "Before MW-LOAD, registerMorrowindData was names-only - an assetNames/getAllKeys sweep - which is why MAC1 A could safely add a second caller on the boot door. MW-LOAD then made the fingerprint carry SIZES, and the only way it has to ask a size is `assetBlob(MW_STORE, n)`, which for any value stored before MW-LOAD (an ArrayBuffer, not a Blob) structured-clones the whole file into memory, wraps it and writes it back. So a player with a legacy Morrowind attach now pays a full read-and-rewrite of Morrowind.esm plus every .bsa the moment the enhanced boot menu mounts - before any host boots, on the surface most likely to be opened on a phone. The two slices are individually correct and neither reviewed the other's cost.",
    "evidence": "enhancedMenu.js:1994-1996 `if (morrowindDataFingerprint() == null) { registerMorrowindData().then(() => { if (app === host && host.isConnected) render(); }).catch(() => {}); }`. dataSource.js:717-722 `export async function registerMorrowindData() { const names = await storedMorrowindNames(); const next = ...; const sizes = await storedMorrowindSizes(names); const print = mwFingerprint(names, sizes);` - the `sizes` line is MW-LOAD's addition (the pre-delta body was `const print = mwFingerprint(names);`). dataSource.js:706-715 `async function storedMorrowindSizes(names) { for (const n of [...names].sort()) { const v = await assetBlob(MW_STORE, n); out.push(v ? v.size : -1); } }` - serial, one per file. dataSource.js:371-390 `assetBlob` - `const stored = await assetValue(...); if (isBlob(stored)) return stored; const blob = new Blob([stored]); ... os.put(blob, fileName)`, i.e. materialise + rewrite for every legacy record. dataSource.js:501 `storedMorrowindNames = () => assetNames(MW_STORE)` is the cheap half MAC1's reasoning was written against.",
    "proposedFix": "Give the fingerprint a size source that does not touch values: write a small per-domain metadata record (name -> size) at storeAssets time and read that in storedMorrowindSizes, falling back to the assetBlob path only for names the metadata does not know. Keep the legacy Blob migration where a file is actually going to be USED (loadMorrowindArchives / loadMorrowindArmRecords), not on a counting call. MAC1's boot-door count then stays as cheap as its own comment assumes.",
    "pinShape": "A fake IDB that counts value `get`s: registerMorrowindData over N stored Morrowind names must issue ZERO value reads (keys and metadata only), while loadMorrowindArchives over the same store issues N. Mutation: routing the size read back through assetBlob must lift the count from 0 to N and fail."
  }
]
```

**coverage**

```json
{
  "pairsRead": [
    "OT1 exteriorSwimming (both exterior hosts) x AUDIT 64 F0 `sunk` swim speed x A6's motor swim/levitate branch x shared.applyMotorEffectFlags x the sink/unsink height actions x levitate/water-walking x the encounter roll (StreamingWorld :488-491) x restSession's swim refusal x worldTick's fatigue+Swimming tally  -> XL-1",
    "MAC1 D far-flat rule x ROAD-H H3 / SIB1 / SIB2 seasonal atlas sizes -> XL-2",
    "MAC1 D far-flat rule x EV3's pixel/batch frustum boxes x FA1's `frame` flag x E3's npcBatches inside p.batches x WATER1's water pass (which walks p._visible, not p.batches) -> clean, one note: street-NPC batches are height-gated with the plants and vanish past ring 2 while their buildings stay; activation reads p.npcs, not the batch, so nothing but the draw changes",
    "MAC1 J relock x makeLookGate x U45 cursorActive precedence x AUDIT 62 F6's touch guard x the SetClickDelay arm in all three pointerdown handlers x the four pause-flow call sites -> XL-3",
    "MAC1 I render-eye low-pass x MW-D25 mwCamera third person x mwViewDrawBody -> XL-4",
    "MAC1 H `player.standing` x AUDIT 64 F3 footstep gate x AUDIT 64 F7 split Running tally x M3's climb early return x the stealth senses' movingLessThanHalfSpeed -> XL-5",
    "MAC1 A boot-door count x MW-LOAD's Blob storage + size fingerprint -> XL-6",
    "MAC1 I `_smoothEyeFeet` x AUDIT 28 W10 head bobber (bobOffset is added after the filter, world-space, order-safe) -> clean",
    "MAC1 I x the crouch/stand/mount/sink/unsink camera lerps (`_eyeLevel` is applied on top of the smoothed feet; the sink/unsink lerp is a separate latched pair) -> clean",
    "MAC1 I x teleports/loads/placements: spawn() always clears `grounded`, so the first step is the airborne arm and `falling` holds the filter on the raw height until landing; eyeAt's SNAP_SPAN guard covers the zero-step frame. Order-dependent but currently covered - a levitating/swimming placement is the one uncovered corner (the swim branch never raises `falling`)",
    "MAC1 I x offsetOrigin (shifts `_eyeFeetY` with both span ends, motor.js:542) and x ridePlatform (applied BEFORE player.update, so the delta is inside the latched span) -> clean",
    "MAC1 C PITCH_FLOOR x LookFilter residual (clamped on the target each tick, no accumulation) x lockOn's pitch payments into the same filter x the save/load pose restore (glides, does not snap) x MW-D30's PITCH_LIMIT ceiling -> clean",
    "MAC1 B unadjustedMovement x requestLook's cursorActive early return and the NotSupportedError-only retry -> clean",
    "OT1 d readZipEntries/readZip x the ARENA2 ingest's KEEP diet, arena2/ prefix rule, progress callback and EOCD/central-directory error paths -> clean (same picked set; progress now counts post-diet entries against a post-diet total, which is self-consistent; the 'never touches memory' comment is about inflate and was already true/false the same way before)",
    "AUDIT 63 saveload members (F24 interior pools, F28 orphaned items) x AUDIT 64's envelope additions x SAVE_VERSION's gate (held at 1, every member additive and presence-tested) x the classic-import path (F28's sweep is gated on `extras?.quest != null`, which a classic import does not carry) -> clean",
    "WATER1 water pass x EV4's restrideTerrain (WATER-AUDIT rebuilds p.water with the new stride, world.js:1355-1360) x destroyPixel's free order (:1373) x EV3's p._visible x the far-flat rule -> clean",
    "CLK1/CLK2 game-minute presentation clock x DS1/DS2's own wall-clock dt (`dtReal`, kept and clamped separately at shared.js:474) x the eased weather row and the front's stretch -> clean at the seam; the unbounded driftXZ integral under a multi-day jump was noted but not chased to a defect"
  ],
  "clean": [
    "MAC1's eye filter vs the head bobber, the height-action eye lerps, offsetOrigin and platform riding",
    "MAC1's PITCH_FLOOR vs lockOn, the look filter's residual and the save/load pose",
    "OT1's readZip rebuild vs the ARENA2 ingest",
    "AUDIT 63/64 save-envelope members vs each other, the classic import and the version gate",
    "WATER1's water pass vs EV4's restride, EV3's visibility and the pixel teardown order",
    "MAC1's far-flat rule vs the Detect feed (it walks foe pools and loot piles, never batches) and vs EV3's boxes",
    "CLK1's one clock vs DS1/DS2's retained wall-clock frame"
  ]
}
```