```json
[
  {
    "id": "XL-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "I reproduced it with a harness driving the real PlayerMotor (src/player/motor.js) over a real Collider floor in world.js's exact order (capture `_wasSwimming` :7243 -> applyMotorEffectFlags shared.js:888 `player.swimming = false` :7244 -> player.update :7266 -> onExteriorWater :7325 -> `player.swimming = exteriorSwimming(...)` :7330), stats Speed 50 / Swimming 30, dt 1/60: on tile 0 with the OT1 write dz = 0.1034 over 60 steps AND still 0.1034 over 600 steps (all of it spent in the two frames before the latch establishes; `cancelMovement` pending on 599 of 600 frames) - a permanent soft-lock; with the write removed, dz = 1.8165 over 60 and 17.7659 over 600, i.e. 1.777/s against swimSpeed(walkSpeed(50),30) = 1.7722. At sub-60Hz render rates the multi-step frames let every second step through (3 s of swimming: 2.81 units at 30Hz, 3.74 at 20Hz, vs 5.36-5.45 unfrozen), so the freeze is total at 60 Hz and a ~50% speed loss below it. The two-member split IS DFU's shape, read myself: PlayerEnterExit.cs:415-421 clears `isPlayerSwimming` only when `StreamingWorld.PlayerTileMapIndex != 0` but sets `levitateMotor.IsSwimming = false` with no tile test, while the dungeon arm (:381-393) sets BOTH together; PlayerMotor.cs:149-152 `IsSwimming => levitateMotor.IsSwimming`; PlayerMotor.cs:322-326 is the zero-and-return the port ports at motor.js:1190. Decisively, PlayerMotor.cs:383-389 UpdateSpeed reads `playerEnterExit.IsPlayerSwimming && !IsWaterWalking` for GetSwimSpeed - that is AUDIT 64 F0's `sunk` gate at motor.js:1372, which the OT1 write makes unreachable outdoors. Not recorded as deliberate: Audit-64.md:331-333 strikes F0's row and claims OT1 as the wiring; `git log -S` puts the write in 88c7b911 (OT1), inside the delta.",
    "corrections": "Two figures in the finder's evidence are harness artefacts, not wrong conclusions: the frozen dz is 0.1034 (not 0.0000) - the first two frames move before the latch closes - and the unfrozen dz is 1.8165 over 60 steps (not 1.7722), because the first water frame is still at land speed. The FloatUp datum is decoration, not evidence: dy = 0.000 over 300 steps with AND without the write, and DFU also never floats outdoors (levitateMotor.IsSwimming is always false above ground), so there is no divergence there. The freeze is not total at every frame rate - below 60 Hz a multi-step render frame lets the second step run.",
    "fixShape": "Split the member as DFU splits it. (1) Add a host-side flag that is PlayerEnterExit.isPlayerSwimming and is never read by the motor - e.g. `player.isPlayerSwimming` declared beside `sunk` at motor.js:387 with a comment that `_step` must not read it. (2) world.js:7330 and exterior.js:3708 write THAT flag instead of `player.swimming`; world.js:7243 / exterior.js:3633 capture `_wasSwimming` from it (this is what preserves the dungeon-exit carry). (3) `player.swimming` (levitateMotor.IsSwimming) keeps shared.js:888's unconditional false outdoors. (4) The dungeon/interior hosts must write BOTH members from the same block-water test (worldModes.js:5295, dungeon.js:683), matching PlayerEnterExit.cs:381-393. (5) Repoint the IsPlayerSwimming readers at the new flag, each with its C# cite: world.js:1609 + exterior.js:877 (exhaustion collapse, PlayerEntity.cs:2406/:2426); world.js:3100 + exterior.js:1531 (rest refusal, DaggerfallUI.cs:661); world.js:2256 + exterior.js:1400 (encounter roll, PlayerEntity.cs:489); world.js:7231 + exterior.js:3616 (worldTick fatigue/Swimming tally, PlayerEntity.cs:410); AND world.js:7054 + exterior.js:3492 (headBobber style/bounce - the finder got this one backwards: HeadBobber.cs:101 and :215 read playerEnterExit.IsPlayerSwimming, not the motor flag). (6) LEAVE on `player.swimming`: world.js:7341 / exterior.js:3715 -> footsteps.js:121, which is PlayerFootsteps.cs:230's `if (!playerMotor.IsSwimming)`, and motor.js's own :733/:747/:750/:1190/:1206-1235 reads. AUDIT 64 F0's `sunk` gate at motor.js:1372 then remains the live exterior swim speed, which is PlayerMotor.cs:387's own shape. Pin behaviourally through the real motor + Collider in host order (dz over 60 steps == swimSpeed(walkSpeed(50),30)*1s to 1e-3, `cancelMovement` false after a steady-state water frame), because test/ot1_swim_latch.test.js:33-80 only calls the pure helper and greps the host source for the assignment text and cannot see any of this.",
    "severity": "critical",
    "laneKey": "src/scenes/world.js"
  },
  {
    "id": "XL-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived through the producers at HEAD: world.js:1153-1159 passes `sib.size` into renderer.createBillboardBatch, renderer.js:2460 stores it verbatim as `batch.size`, and world.js:7866 tests `height: b.size?.h ?? 0` against TALL_FLAT_HEIGHT = 2.5 (flatDistance.js:37). Computed the seasonal height off seasonsIliacBay.js's own producer for the one real record the tree records - TempW's K1, 79x156 (test/seasonsIliacBay.test.js:143): classic scaledBillboardSize -> {w:1.975, h:3.9}, seasonalBillboardSize(79,156) -> {w:6.12249984741211, h:12.089999389648439}, ratio 3.0999998434995995 (BILLBOARD_SCALE = fround(3.1), zero recordScale, GLOBAL_SCALE 0.025 on both paths). Sweeping pixel heights, every record from 33 px to 99 px tall flips its ring>=2 verdict from culled to drawn under a seasonal atlas (classic threshold 100 px, seasonal threshold 32.26 px) - e.g. a 64-px shrub is 1.60 units classically and 4.96 units seasonally. So the rule's own recorded calibration at flatDistance.js:35-37 ('above the tallest walker (1.8) and below the shortest of Daggerfall's tree records') stops holding for archives 504-510 in Fall, Spring and Winter, and MAC1's draw-call cull loses its discrimination on exactly the nature flats that dominate the streamed count.",
    "corrections": "Severity 'high' is inflated. This is opt-in, not default: `seasonsActive` is only set at world.js:615 after `seasonsInstalled()` answers true, and the bible (Seasons-Iliac-Bay.md, 'The textures are the player's to supply') says the mod's textures come from the player's own copy - so a stock install is unaffected. There is also no DFU law here at all (MAC1's rule is the port's own presentation rule under Port-Doctrine), and strictly read the world-unit rule is behaving as written: under the mod those plants really ARE 5-8 units tall. The defect is that the constant's calibration, and the fps win it bought, silently evaporate - not that the rule computes the wrong number. Only ONE host is affected: `farFlatVisible` has exactly one call site in the tree (world.js:7866); exterior.js/worldModes.js/dungeonContext.js have no ring walk, so THE FOUR HOSTS RULE is satisfied by inspection, not violated.",
    "fixShape": "src/scenes/world.js:1155-1161 (the seasonal branch) already holds the vanilla texture `t`: stash the classic height on the batch beside `_box`, e.g. `batch._classicH = scaledBillboardSize(t.getSize(record), t.getScale(record)).h;` with a MAC1/SIB cite, and in the classic branch (:1165-1167) either set the same field or leave it undefined. Then world.js:7866 reads `height: b._classicH ?? b.size?.h ?? 0`. Do NOT touch flatDistance.js's TALL_FLAT_HEIGHT (it is calibrated for vanilla and is also the ROAD-H H3 re-skin key) and do NOT normalise by BILLBOARD_SCALE inside the flat walk (that hard-codes one mod's constant into the host). `_box` must keep the DRAWN `sib.size` - EV3's frustum test measures what is rendered, not what is measured for the height rule. Pin it through the real producers: one record, `farFlatVisible({ring:3, height: scaledBillboardSize(size,{0,0}).h}) === farFlatVisible({ring:3, height: seasonalBillboardSize(size.width,size.height).h})` for a 33-99 px record, plus a host pin that the flat walk does not read `b.size.h` directly; reverting the host line must flip the seasonal verdict.",
    "severity": "medium",
    "laneKey": "src/scenes/world.js"
  },
  {
    "id": "XL-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Same as HP-1 (judged here under XL-3's id since HP-1 is not in this lens file). Verified at HEAD: pauseDoor.js:165 `if (action !== 'exit') hooks.relock?.();` is optional, so a host that omits the key is silently unfixed; `grep -n relock` over the four hosts returns world.js:4816 and :6721 and worldModes.js:6824 and nothing else - exterior.js:2168-2192 and dungeonContext.js:4339+ pass no `relock` at all. Both of those hosts still relock a frame later through makeLookGate (pointerLock.js:126-133 `else if (wasHeld) requestLook(canvas)`), outside transient activation, and both carry the same SetClickDelay arm (exterior.js:2448, dungeon.js:338), so Mac's reported double-click survives there verbatim. The dungeon one is real play, not a test route: ui/input.js:524 routes Escape to `ctx.togglePause`, which for a dungeon inside ?world is dungeonContext.js:4336 - so every dungeon pause in the shipping host is still unfixed. MAC1's record names neither host, against THE FOUR HOSTS RULE.",
    "corrections": "The finder's second-order touch note is plausible but unverified and should not be briefed as a finding: makeLookGate's next-frame relock already fired on every close before MAC1, and I did not establish that any browser grants pointer lock off a touch-driven click. Brief it as a guard to consider, not as a defect. Also note MAC1 leaves TWO relock attempts live per close (the in-gesture hook and the look gate's next-frame edge); the lane should decide whether the gate's edge is now redundant rather than adding a third path.",
    "fixShape": "src/scenes/exterior.js:2168 - add `relock: () => requestLook(canvas),` to the openPauseFlow options (the import already exists for this host's look gate); src/scenes/dungeonContext.js:4339 - add `relock: () => requestLook(ctx.canvas ?? canvas),` resolved against whatever canvas this context's own makeLookGate was built with (dungeon.js:567/:625). Name all four hosts in the record. Pin as a census over the four openPauseFlow call sites (world.js:4808, worldModes.js:6816, exterior.js:2168, dungeonContext.js:4339): the count of call sites must equal the count of `relock` keys, so a fifth host cannot land without one; beside it a behavioural pauseDoor pin that resume/save/load each fire hooks.relock exactly once and exit never does. Deleting any one host's key must fail the census.",
    "severity": "medium",
    "laneKey": "src/scenes/exterior.js"
  },
  {
    "id": "XL-4",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived at HEAD: mwCamera.js:199-206 returns `fpEye` untouched in first person but in third builds `focal = [feet[0], feet[1] + FOCAL_HEIGHT*heightScale*u, feet[2]]` and :229 pulls the eye straight back from it, so the third-person eye's x/y/z deltas are the FEET's deltas. All five call sites hand it raw stepped feet while handing the smoothed eye for first person: world.js:7597 `fpEye: cam.pos, feet: player.pos` (cam.pos is `player.eyeAt()`, world.js:3731), worldModes.js:5652, exterior.js:3870, dungeon.js:840, and mwViewDrawBody at world.js:7784 / worldModes.js:5707,:5772 / exterior.js:4028 / dungeon.js:871. Measured in MAC1's own harness shape (test/mac1_playreport.test.js:142-173, riser 0.3 / run 0.6 staircase, `m.pos=[0,0,0]; m.grounded=true`): worst single-frame eye RISE is 0.0514 at 60Hz / 0.0338 at 90 / 0.0257 at 120 / 0.0214 at 144 for eyeAt (reproducing MAC1's 0.051), while the third-person focal's feet rise 0.1531 at every one of those rates - the full raw rung, MAC1's own pre-fix figure. Horizontally the first-person per-frame dz falls to [-0.0179, 0.0308] at 144Hz while the feet stay quantised at [-0.043, 0.074] (the 60Hz step) at every rate, so EV1's judder fix is bypassed too.",
    "corrections": "Severity 'medium' is inflated: third person is doubly opt-in - mwViewFrame (mwView.js:46-56) only enters it when the player has Morrowind data AND fpArm.setViewMode('third') succeeds, and it must be scrolled into. mwViewDrawBody early-returns unless third person (mwView.js:82), so the 'body and first-person eye disagree' half of the claim is unreachable: the body is never drawn in first person. Nothing here is a DFU divergence - it is the port's own presentation law (MAC1 I / EV1) applied to one camera and not the other.",
    "fixShape": "src/player/motor.js - add a render-feet accessor beside eyeAt (:517-531), e.g. `feetAt(alpha = this._alpha)` returning `[q[0]+dx*a, (this._eyeFeetY != null && alpha === this._alpha) ? this._eyeFeetY : q[1]+dy*a, q[2]+dz*a]`, sharing eyeAt's SNAP_SPAN guard and WITHOUT `_eyeLevel()` or `bobOffset` (the focal's own FOCAL_HEIGHT supplies the height, and the bob is a first-person term). Then pass it as `feet` at all five sites: world.js:7597 and :7784, worldModes.js:5652 and :5707 and :5772, exterior.js:3870 and :4028, dungeon.js:840 and :871. `player.pos` stays the simulation truth for the collider, the rays and the focal's ceiling probe (mwCamera.js:207-217 casts from the focal, which now rides the smoothed feet - check that the ceiling guard still clears a real dungeon ceiling by at most STEP_OFFSET). Pin: run the MAC1 staircase harness in both camera modes and assert the third-person worst single-frame vertical delta is at or under the first-person figure (0.0514 at 60Hz), plus a shape pin that no host passes `player.pos` as mwViewFrame's `feet`; reverting one host must lift the figure back to 0.1531.",
    "severity": "low",
    "laneKey": "src/player/motor.js"
  },
  {
    "id": "XL-5",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "The structural claim is right at HEAD: `this.standing` is a cached field written in exactly three places - motor.js:656-657 (_trackHalfSpeed, walk path), :1139 (the cancelMovement block) and :1269 (the swim/levitate branch) - and `_climbStep` (:1023-1105) writes `_airVelX/_airVelZ`, `jumping`, `falling`, `fallStart`, `velY` and `grounded` but never `standing` or `movingLessThanHalfSpeed`, while motor.js:1182 `if (this._climbStep(dt, input, yaw)) return;` sits above both remaining writers. DFU's PlayerMotor.cs:113-125 is a live getter `if (grounded) return moveDirection.xz.magnitude == 0; return false;` over the moveDirection zeroed at :322-326, and DFU's `grounded` IS refreshed during a climb (FixedUpdate :278 reads collisionFlags, which ClimbingMotor.cs:766-767 writes from its own controller.Move) - so a climber off the ground reads IsStandingStill false in DFU, which is what the port's stale value already says. The divergence is therefore confined to steps where the climb's own move reports grounded: a grounded forward climb start (climbing.js:121 only applies `tooCloseToGround` to back/slip/airborne-grasp, so a forward start from the floor is legal) and the slip that reaches the floor. In that window DFU reads IsStandingStill true / IsMovingLessThanHalfSpeed true (the :168-175 short-circuit) while the port reads false, which reaches footsteps.js:139's `if (m.standingStill) return null` bare return (PlayerFootsteps.cs:264-265), MAC1 H's politeness gate at world.js:7939, and the stealth feed's movingLessThanHalfSpeed at world.js:2841.",
    "corrections": "The headline consequence is FALSE and must not travel into a brief: the fatigue band cannot charge the running rate during a climb, because worldTick.js:494 is climb-first - `if (activity.climbing) loss = FATIGUE_LOSS.Climbing; else if (activity.running) ...` - exactly PlayerEntity.cs:405-410's ladder, so `running: player.isRunning && !player.standing` is unreachable while `climbing` is true (and AUDIT 26 F083 already recorded that). 'Carries standing === false for the WHOLE climb' is also overstated: DFU answers false for the whole airborne part of the climb too, so the two agree except in the grounded corner above. And the proposed census ('every return above the walk path must write standing') would pin a non-law: the freezeMotor return (motor.js:1157, PlayerMotor.cs:296-307) does NOT zero moveDirection in DFU, so DFU's getter there keeps reading the pre-freeze vector - demanding a write on that return diverges from DFU. `movingLessThanHalfSpeed` is separately near-harmless because DFU's :176-180 compares against the same STALE `speed` field the port's last walk step used.",
    "fixShape": "src/player/motor.js - prefer the getter, which is DFU's actual shape and closes every future early return: add `_moveDirZeroed`, set it true in the cancelMovement block (:1132-1144), in the swim/levitate branch (:1256-1270) and on the climb's `return true` (:1104), clear it on the walk path in _trackHalfSpeed (:656), and replace the field with `get standing() { return this.grounded && this._moveDirZeroed ? true : this._standingWalk; }` - or, minimally and with the same cite, write `this.standing = this.grounded; this.movingLessThanHalfSpeed = this.grounded ? true : this.movingLessThanHalfSpeed;` immediately before `_climbStep` returns true at motor.js:1104, citing PlayerMotor.cs:322-326 and :168-175. Leave the freezeMotor return alone and say so in the comment. Pin by driving a real ClimbingState from a GROUNDED forward start (not an airborne grasp - an airborne climber reads false in DFU too) and asserting `standing === grounded` and `movingLessThanHalfSpeed === true` on the first climbing step and every step after; a census pin over `_step`'s early returns must exempt the freeze return or it certifies the wrong law.",
    "severity": "low",
    "laneKey": "src/player/motor.js"
  },
  {
    "id": "XL-6",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "The mechanism is real and the ordering is confirmed: `git show 0fe2c05b:src/scenes/dataSource.js` line 544 is `const print = mwFingerprint(names);` - names-only before the delta - and `git log -S storedMorrowindSizes` puts the sizes in 563f1e8a (MW-LOAD), which lands before 6c017b5e (MAC1) in the same delta. At HEAD dataSource.js:717-722 calls `storedMorrowindSizes(names)`, which (:706-713) awaits `assetBlob(MW_STORE, n)` serially per file, and assetBlob (:371-388) structured-clones any non-Blob value whole (`assetValue` is a plain IDB get, :347-355) and rewrites it. enhancedMenu.js:1994-1996 fires that from the boot-menu mount, before any host boots, on the surface most likely to be a phone. So a player whose Morrowind attach predates MW-LOAD pays a serial read-and-rewrite of Morrowind.esm plus every .bsa on the title screen.",
    "corrections": "Three overstatements. (1) It is a ONE-TIME migration, not a recurring cost, and the code says so deliberately at dataSource.js:367-369 ('materialised whole this once - and REWRITTEN as a Blob behind the read'); once converted, `assetValue` returns a Blob handle and no bytes reach JS memory. (2) MAC1 did not create the cost: shared.js:1129 `registerMorrowindData().catch(() => 0)` has always called it at every host boot, so the same legacy player paid it seconds later anyway - MAC1 moved the moment earlier, it did not add a second full pass (the menu is gated on `morrowindDataFingerprint() == null`, and by the host's call the values are already Blobs). (3) The proposed metadata fix cannot avoid the legacy read at all: a pre-fix record has no metadata and an ArrayBuffer has no size without materialising it, so the fallback path is the only path for exactly the files the finding is about.",
    "fixShape": "Give the boot door a names-only count instead of a fingerprint. src/scenes/dataSource.js:717 - split `registerMorrowindData()` into a cheap `countMorrowindArchives()` that does only `storedMorrowindNames()` + the `.bsa` filter and sets `_mwCount`, and keep the full sizes+fingerprint pass for the host bootstrap at shared.js:1129. src/ui/enhancedMenu.js:1994-1996 calls the cheap one. CRITICAL HAZARD the lane must not trip: the cheap call must NOT write `_mwFingerprint` (dataSource.js:704, :738) - if the menu stored a names-only print and the host then computed a names+sizes print, `print !== _mwFingerprint` would read as a changed set and bump `_mwGeneration`, which is precisely the fpArm.unload() regression MW-D9g's comment block at :723-736 records; the guard condition and the boot-door gate at enhancedMenu.js:1994 must both be re-read against a null-fingerprint-after-a-count state. Optionally write a per-domain name->size metadata record at storeAssets time so future stores never need a value read, populating it during the existing one-time Blob migration. Pin with a fake IDB that counts value `get`s: the boot-door call issues ZERO value reads over N stored names, the host bootstrap issues N (or zero once metadata exists), and the menu path leaves `morrowindDataFingerprint()` null.",
    "severity": "low",
    "laneKey": "src/scenes/dataSource.js"
  }
]
```