```json
[
  {
    "id": "UI-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived and PROVEN by probe, not by reading. spellbookWindow.js:777 is `click(vx, vy, now)` and :862-868 is `const t = now ?? Date.now(); const wasDouble = this._lastRowClick != null && (t - this._lastRowClick) < DOUBLE_CLICK_DELAY_MS`. The window has NO `pointer` seam (only wheel :397, hover :407, release :446, click :777), so townTalk.js:1123 falls to `overlay.click?.(v[0], v[1], e.button === 2, e.button === 1)` and the third positional is a boolean; worldModes.js:7090 and dungeonContext.js:4867 (`activeOverlay.click(vx, vy, right, middle)`) hand the same shape, so all four hosts, not just townTalk. I built the real SpellbookWindow through the host's call shape (`w.click(x, y, button===2, button===1)`): click row 0, then click row 2 with no clock at all -> `readied` = ['C']. `false ?? Date.now()` keeps `false`, `false != null` is true, `false - false === 0 < 300`, so every second click in the list casts (buys in buy mode). DFU's law is BaseScreenComponent.cs:681-692 - MouseClick, then `lastLeftClickTime = leftClickTime; leftClickTime = Time.realtimeSinceStartup; if (leftClickTime - lastLeftClickTime < doubleClickDelay) MouseDoubleClick(...)` - a real clock and nothing else. Introduced by OT1 (git log -S '_lastRowClick' -> 88c7b911), inside the audited delta.",
    "corrections": "The listPicker half is RIGHT in substance but WRONG in scope and reason. The five NESTED forwards pass three arguments - guildServiceWindows.js:158, itemMakerWindow.js:431, potionMakerWindow.js:342, travelMapWindow.js:1201 (`click(vx, vy, this._font)`) and spellMakerWindow.js:845 (`click(vx, vy)`) - so `now` is undefined and `_now()` runs; I probed both shapes with a real 600 ms gap and the nested one correctly REFUSED while the host shape picked. The broken listPickers are the BARE mounts the finder never named: createUseMagicItemWindow through world.js:4776, dungeonContext.js:5123 and worldModes.js:6912, plus worldModes' bookshelf picker at :1588-1598 assigned straight into `interiorOverlay`. The finder also missed the strongest evidence in its own file: listPicker.js:328-336 already RECORDS that 'the three routers that mount a bare picker pass a right-button BOOLEAN in that slot (townTalk.js:1123, worldModes.js:7090, dungeonContext.js:4867)' and defends the `font` slot by content (`font?.fnt ? font : null`) - the `now` slot immediately beside it was left undefended, which is ROAD-U/AUDIT 58's bite landing twice on one signature. nativeTalk is confirmed (townTalk.js:777 `mount(new NativeTalkWindow(...))` where `mount = push ? pushOverlay : showOverlay`, and nativeTalk has no `pointer` seam either). The DFU secondary divergence is correct: :686-692 stores the stamp unconditionally, so DFU gives click/double/double where the port's `_lastRowClick = null` gives click/double/click.",
    "fixShape": "One shape, three files, plus the pins that mint the wrong shape. (1) src/ui/spellbookWindow.js:777 `click(vx, vy, now)` -> `click(vx, vy, right = false, middle = false)`; :862 `const t = now ?? Date.now()` -> `const t = this._now()`; add `_now() { return typeof performance !== 'undefined' ? performance.now() : Date.now(); }` beside it, verbatim from listPicker.js:187. (2) src/ui/listPicker.js:311 `click(vx, vy, font = null, now = null)` -> `click(vx, vy, font = null)` and :360 `const t = now ?? this._now()` -> `const t = this._now()`; KEEP the `font?.fnt` content test at :343 - five nested callers still pass a real font there, so the third slot cannot be re-typed. (3) src/ui/nativeTalk.js:774 `click(vx, vy, rightButton = false, now = null)` -> `click(vx, vy, rightButton = false, middle = false)`; :851 `const t = now ?? Date.now()` -> `const t = this._now()`; add the same `_now()`. Do NOT touch src/ui/charsheet.js:419 - `this.child.click?.(vx, vy)` is already correct and is the only path that works today. Same lane must rewrite the pins that hand a number to a positional the producer fills with a boolean (Home.md's TEST THE SHAPE THE PRODUCER MINTS): test/spellbookwindow.test.js:822 and :828 and the 'OT1 clicks: the gesture is TIME, not identity' test below them - drive them as `w.click(x, y, false, false)` with `w._now` stubbed, and add the mirror pins for the bare listPicker (useMagicItemWindow through a host's 4-arg dispatch) and for nativeTalk. Optional second law in the same file: drop `this._lastRowClick = null` at spellbookWindow.js:867 / listPicker.js:364 / nativeTalk.js:855 to match :689-692's unconditional stamp.",
    "severity": "high",
    "laneKey": "src/ui/spellbookWindow.js"
  },
  {
    "id": "UI-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "SAME AS HP-1 for the host half - judged there per the brief, and my verdict, severity and fix for the four-hosts half live in the HP-1 entry. The half that is UI-2's ALONE is the SKIN FORK, and it stands: `grep -rn relock src/` returns src/ui/pauseDoor.js:165 as the only reader and it sits inside `enhancedPauseOverlay` (:110-188); `openClassicPauseFlow` (src/ui/pauseWindow.js:389) never reads `hooks.relock`. The classic window has the identical bug, which the finder asserted but did not derive: world.js:1821 `gamePaused = () => townTalk.overlayActive || (modes?.overlayHeld ?? false)` is true while the classic panel sits in townTalk's slot, world.js:7061 `lookGate(gamePaused())` over makeLookGate (pointerLock.js:128-135) releases the lock while it is up and re-requests it on the NEXT frame's `wasHeld` edge - outside any transient activation - and world.js:5054 `if (townTalk.pointerdown(e)) return;` sits ABOVE the host's own `requestLook(canvas)` at :5069, so the Resume CLICK never reaches a relock either. DEFAULT_SKIN is 'enhanced' (systems/uiSkin.js:35), so the classic half is the opt-in skin and the host half is the default one.",
    "corrections": "The proposed fix is wrong where it lands. Do NOT call `hooks.relock?.()` from `pauseWindow.js`'s `_closeWith()`: that member is also called on the SAVE and LOAD arms under the replace fallback (:258 `if (!this.hooks.saveLoadPushes) this._closeWith(); this.hooks.openSave();` and :277 for load), so a relock there grabs the pointer at the exact moment the slot window is being mounted and the player loses the cursor they need for it; and the CONTROLS arm (:311-317) deliberately does `this.done = true` inline rather than `_closeWith`, so it would be missed anyway - correctly, because the rebinding grid needs the cursor. UI-2's claim that the pin 'names no fourth' is right but understates it: test/mac1_playreport.test.js:252-254 asserts three sites, not four, and all three by source regex. Severity high is inflated - the cost is one wasted click per resume, not lost state.",
    "fixShape": "Classic half only (the host half is HP-1's). Put the relock on the two RESUME exits alone, not on the shared `_closeWith`: src/ui/pauseWindow.js:242 `if (inRect(R.continue, vx, vy)) { this._click(); this._closeWith(); return true; }` -> add `this.hooks.relock?.();` after `_closeWith()`, and src/ui/pauseWindow.js:230-231 (the Escape `keyup` arm, `this._click(); this._closeWith();`) -> the same line after `_closeWith()`. Leave `_confirmExit()` (:180-185) alone - that is pauseDoor.js:165's excluded 'exit'. Leave the save/load/controls arms alone; their own windows re-release through the look gate on the next frame. Comment each with `// MAC1 J, the classic twin (ui/pauseDoor.js:153-170): the close runs inside this click/keyup, which is the activation requestPointerLock needs`. Pin: drive `openPauseFlow` with `isEnhanced()` false and a recording `relock`, assert it fires on the CONTINUE rect and on the deferred Escape keyup, and NOT on Yes-to-exit, NOT on SAVE/LOAD, NOT on CONTROLS.",
    "severity": "medium",
    "laneKey": "src/ui/pauseWindow.js"
  },
  {
    "id": "UI-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "DFU heads every handler with the guard: DaggerfallControlsWindow.cs:280-282 (Joystick), :289-291 (Mouse), :298-300 (Default), :320-322 (Continue), :337-339 (CurrentBindings), :359-362 (KeybindButton), :370-373 (right-click, ANDed with the KeyCode.None refusal), over `SetWaitingForInput` at :267-272 which also drops AllowCancel. The port's classic grid carries it in one line (src/ui/controlsWindow.js:298 `if (this.capture) return true;   // every tab ignores clicks mid-capture (:283 etc.)`). The enhanced pane carries none: enhancedControls.js:270 `key.onclick = () => arm(action)`, :271 `key.oncontextmenu`, :277 `clear.onclick`, :332 `which.onclick = switchDict`, :333 `defaults.onclick`, :334 `cont.onclick = applyAndSave`. `arm()` (:172-181) adds a capture-phase document keydown and nothing on a click removes it, so the two live consequences are real: `applyAndSave` (:226-238) saves through `saveKeyBinds(bindings())` and re-stages `unsaved` while `armed`/`armedHandler` are untouched; and `switchDict` (:241-247) flips `unsaved.usingPrimary`, which `setUnsavedBinding` (systems/controlsConfig.js:48-50, `currentDict(u).set(action, code)`) then follows, so the pending capture lands in the dict the player did not click. The file's own header claims the two faces are one law.",
    "corrections": "Two overstatements. (a) The stray keystroke after CONTINUE is STAGED, not saved - `applyAndSave` re-stages at :232 and `onCaptureKey` writes into that fresh copy; the observable is a swallowed keystroke (onCaptureKey stopPropagation's at :164) plus an unasked-for staged rebind, which only reaches the registry on a second CONTINUE. (b) The prompt card's Yes/No do not need the guard and it would be dead code: `paneControls` returns immediately after `promptCard()` when `prompt` is set (:318), so no key row exists to arm while a prompt stands, and once `clear.onclick`/`oncontextmenu` are guarded no prompt can be opened while armed. The port's pane has no Joystick/Mouse sub-window buttons, so DFU's seven handlers map to five port sites plus the right-click. No existing pin breaks: test/enhancedControls.test.js:180-195 arms then presses a key and :319-320 tears down; nothing clicks a second control while armed.",
    "fixShape": "src/ui/enhancedControls.js. Declare one wrapper beside `arm()` (after :181): `const act = (fn) => (...a) => { if (armed) return undefined; return fn(...a); };` - a single predicate so a future control cannot be added without it, which is the classic grid's posture. Apply at :270 `key.onclick = act(() => arm(action));`, :271 `key.oncontextmenu = (e) => { e?.preventDefault?.(); act(() => promptRemove(action))(); return false; };` (keep the preventDefault OUTSIDE the guard or the browser menu reappears mid-capture), :277 `clear.onclick = act(() => promptRemove(action));`, :332 `which.onclick = act(switchDict);`, :333 `defaults.onclick = act(() => { prompt = { kind: 'defaults' }; repaint(); });`, :334 `cont.onclick = act(applyAndSave);`. Cite `DaggerfallControlsWindow.cs:281/:290/:299/:321/:338/:361/:371` on the wrapper and note that `arm()`'s own leading `disarm()` (:173) becomes unreachable-by-click, which is DFU's shape. Leave `promptCard`'s Yes/No (:292-295) alone. Pin: arm 'MoveForwards' via `captureArmed()`, then click Continue / Primary / Defaults / a second row's key / its clear, asserting after each that `captureArmed()` is unchanged, `controlsStaging().usingPrimary` is unchanged and the saveKeyBinds spy is silent; mutation-check by deleting each `act(` individually.",
    "severity": "medium",
    "laneKey": "src/ui/enhancedControls.js"
  },
  {
    "id": "UI-4",
    "upheld": true,
    "confidence": "high",
    "reasoning": "DFU: HUDLarge.cs:170-232 registers only `OnMouseClick` and `OnRightMouseClick` on all eleven panels and the file contains no `OnMiddleMouseClick`; BaseScreenComponent.cs:679-724 dispatches left/right/middle in three separate blocks with no fallback. The port: src/ui/hudLarge.js:319 `const action = (button === 2 && panel.right) ? panel.right : panel.action;` sends buttons 1, 3 and 4 to the LEFT action, and routeLargeHudClick (:673-693) gates only the SOUND (`if (button === 0 || button === 2) audio.playOneShot(...)` at :692) before an ungated `routeAction(hit.action, ctx)` at :693. So a middle or aux click on a panel silently opens the pack, opens the spellbook, toggles the sheath, cycles the interaction mode or opens the pause door, in all four hosts (world.js:5046, exterior.js:2425, dungeon.js:323, worldModes.js:7078 all pass the raw `e.button`). The sibling lane took the opposite posture on the same law (nativeInventory.js:1177 `if (middle) return this._middleClick(vx, vy);`). Not recorded as deliberate: bible/10-UI/UI-Arc.md:11212-11216 says only 'a middle click reaches no handler in the reference and makes no sound here' and stops.",
    "corrections": "Three. (a) The delta claim is loose: `git log -S \"const action = (button === 2 && panel.right)\"` dates that line to 6b3af1ea (the AUDIT 54 combat lane), not to AUDIT 64. What the delta added is F42's sound gate (0de73325) and the record; the finding is 'F42 recorded the law and applied half of it', not 'F42 introduced it'. (b) THE FINDER MISSED A LIVE PIN THAT ENCODES THE BUG: test/hudlarge.test.js:213-214 asserts `at('map', 1).action === 'AutoMap'` under the comment 'a middle click takes the left handler - DFU binds only the two'. Any fix reddens it; the lane must rewrite that assertion, not route around it, and it is the reason this has survived two audits. (c) The proposed fix is wrong: returning `null` from `largeHudClick` breaks the module's OWN recorded law at hudLarge.js:665-671 ('A HIT IS CONSUMED WHETHER OR NOT ANYTHING ANSWERS IT ... or pressing REST in a host with no rest door would fall through to the world and swing the player's sword at the floor'), and the sibling the finder cites takes the opposite posture - `_middleClick` returns TRUE at nativeInventory.js:1123, :1126, :1140 and :1149 and only falls through at :1151 when nothing in the WINDOW was hit. Gate the action, keep the consumption. Severity medium is inflated: it needs LargeHUD enabled AND `cursorActive()` (Enter pressed) AND a non-left/right button.",
    "fixShape": "src/ui/hudLarge.js, inside `routeLargeHudClick` only - leave `largeHudClick`'s signature alone, it is a pure hit-test with direct test callers at test/hudlarge.test.js:177 and :198. After :675 `if (!hit) return false;` insert: `// HUDLarge binds OnMouseClick + OnRightMouseClick on all eleven panels (HUDLarge.cs:170-232) and BaseScreenComponent.cs:710-724 sends the middle button to MiddleMouseClick alone, with no fallback - the bar CONSUMES the click (the law above) and runs nothing.` / `if (button !== 0 && button !== 2) return true;`. The F42 sound gate at :692 is then redundant and simplifies to `audio.playOneShot(SOUND.ButtonClick, 1);`, which is DFU's own order (first statement of all thirteen handlers). Fix the docstring at :303-306, which currently states the opposite of the law it cites ('anything else takes the left handler, because DFU binds only those two'). Rewrite test/hudlarge.test.js:213-214: drop the `at('map', 1).action === 'AutoMap'` assertion and replace it with a routeLargeHudClick pin over a recording ctx - `routeAction` NOT called for buttons 1, 3 and 4, called for 0 and 2, and the return value true in all five cases so the host still withholds its fall-through. Mutation-check `button !== 0 && button !== 2` -> `||`.",
    "severity": "low",
    "laneKey": "src/ui/hudLarge.js"
  },
  {
    "id": "UI-5",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "src/ui/nativeInventory.js:354 seeds `this._mouse = [-1, -1]`, :1045 in `hover(vx, vy)` is the sole writer, and :956 `const [vx, vy] = this._mouse;` is what every `scrollerHit` in `wheel(dir)` (:954-972) is tested against - so before the first mousemove both scroller tests miss and the notch does nothing. The hosts feed hover from mousemove alone (townTalk.js:1176 `overlay.hover(v ? v[0] : -1, ...)`), and the wheel seam carries no position (townTalk.js:1182-1185, worldModes.js:7166-7172, dungeonContext.js:4883 `overlayWheel(dir) { activeOverlay?.wheel?.(dir); }`, interior.js:249), so the window can only remember one. Opening the pack with the Inventory key releases the lock through makeLookGate without moving the cursor, so no mousemove fires and the wheel is dead until the player nudges the mouse. DFU has no such state: BaseScreenComponent.cs:724-736 guards the scroll block on `mouseOverComponent`, recomputed each Update from the live scaled mouse position.",
    "corrections": "The sibling claim is wrong. nativeTalk.js:424 and itemMakerWindow.js:230 seed `[0, 0]`, not the leave sentinel, as do mouseControlsWindow.js:263 and automapWindow.js:579 - only nativeInventory.js:354 uses `[-1,-1]`. The 'hosts' pointer-leave sentinel' framing is therefore decorative: `[0,0]` misses every scroller rect just as `[-1,-1]` does, so the dead-until-first-move class is real for those windows too but by a different mechanism, and the fix must not be sold as 'retiring the sentinel'. AUDIT 64 F52's record (bible/10-UI/UI-Arc.md:11540-11543) states the sentinel choice deliberately but records only 'hits no rect' - it never records the open-window-before-first-move consequence, so this is not an approved departure. Low severity is right.",
    "fixShape": "Carry the point on the event; five seams, one shape. src/scenes/townTalk.js:1182-1185 `wheel(e)`, src/scenes/worldModes.js:7166-7172, src/scenes/interior.js:249, and src/scenes/dungeonContext.js:4883 `overlayWheel(dir)` with its two callers src/scenes/dungeon.js:362 and src/scenes/worldModes.js:7168: compute the native point exactly as each file's own `hover` already does (`const v = pointToNative(nativeMetrics(canvas), px, py)`) and pass it - `overlay.wheel?.(Math.sign(e.deltaY), v ? v[0] : -1, v ? v[1] : -1)`, `overlayWheel(dir, vx, vy)`. Then src/ui/nativeInventory.js:954 `wheel(dir)` -> `wheel(dir, vx = this._mouse[0], vy = this._mouse[1])` and delete the destructure at :956. Every other `wheel(dir)` in src/ui ignores the extra args, so no other window changes; do NOT touch src/ui/automapChrome.js:314 `wheel(nx, ny, dir)`, which is a nested seam with its own argument order and is fed by automapWindow, not a host. Pin: build the window through its real producer, call `wheel(1, vx, vy)` over the local list with no prior `hover`, assert `scroll` moved; mutation-check by dropping the two new parameters so it falls back to `[-1,-1]`. Do NOT take the fallback option the finding offers ('seed `_mouse` from the host's last known cursor position') - no host tracks the cursor position, so there is nothing to seed from.",
    "severity": "low",
    "laneKey": "src/ui/nativeInventory.js"
  },
  {
    "id": "HP-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived at HEAD. src/ui/pauseDoor.js:165 `if (action !== 'exit') hooks.relock?.();` is the ONLY reader in the tree (`grep -rn relock src/` returns it plus three wiring lines and comments). The wired sites are src/scenes/world.js:4816, src/scenes/world.js:6721 and src/scenes/worldModes.js:6824 `relock: host.relock`. NOT wired: src/scenes/exterior.js:2167-2192 (`togglePause`'s hooks literal - openPack/openSpellbook/openChronicle/savingPrevented/exitToMenu/textLines/questMessages/questLog, no relock), src/scenes/exterior.js:2910's `createWorldModes({...})` bag (`grep -n relock src/scenes/exterior.js` returns one comment at :2423 and nothing else), and src/scenes/dungeonContext.js:4337-4396 (at/openPack/openSpellbook/openChronicle/questMessages/questLog/quickSave/quickLoad/playerName/saveAs/loadKey/pushWindow/exitToMenu/textLines, no relock). The bug is unchanged in those flows: pointerLock.js:128-135's `makeLookGate` is the frame-late relock, exterior.js:3499 and dungeon.js:625 call it, and world.js:7061 covers worldModes' dungeon arm through `gamePaused()`/`overlayHeld` (worldModes.js:5355). The dungeon flow is the most-played one - world.js:4928 gates the world host's Escape on exterior mode, so underground it falls to worldModes.js:7027 `routeKey(e, dungeonCtx, ...)` -> ui/input.js's Escape case -> dungeonContext.togglePause. Not recorded as deliberate anywhere: bible/01-Overview/Port-Ledger.md:173 mentions MAC1's relock only as a presentation call carrying no row, and exterior.js's own 'deliberately absent here' charter at :2575-2597 does not list it.",
    "corrections": "Three. (a) HP-1's count of 'six pause flows' is a reachability count, not an edit count: there are FIVE hook literals (world.js:4806 exterior, world.js:6721's bag -> worldModes.js:6808's one literal, exterior.js:2167, exterior.js's bag -> that same worldModes literal, dungeonContext.js:4337), and worldModes' interior arm is one literal fed by two host bags. Three edits close it. (b) dungeonContext CANNOT do `requestLook(canvas)` itself - the file states at :4666 'this context owns no canvas of its own' - so the opts-fed dep HP-1 sketches is required, not a stylistic preference, and UI-2's 'pass relock: () => requestLook(canvas) into buildDungeonContext' is the same thing said loosely. (c) The pinShape's source-regex half is exactly what let MAC1 ship three-of-five; worse, a source regex on worldModes.js:6824 PASSES for the ?exterior host today while `host.relock` resolves to undefined there. Only the behavioural half - compose each host's bag and assert `typeof hooks.relock === 'function'` - can see that case.",
    "fixShape": "Three edits, all in this lane. (1) src/scenes/exterior.js, inside `hudCtx.togglePause`'s `openPauseFlow` hooks literal at :2168-2191, beside `savingPrevented: () => true` (:2178): `relock: () => requestLook(canvas),   // MAC1 J: the pointer comes back with the resume gesture (ui/pauseDoor.js:153-170)` - `requestLook` is already imported at exterior.js:12 and the canvas is already in scope (:2378, :2449). (2) src/scenes/exterior.js:2910's `createWorldModes({...})` bag: add the same `relock: () => requestLook(canvas),` so worldModes.js:6824's `relock: host.relock` resolves in the ?exterior host's interior pause instead of reading undefined. (3) src/scenes/dungeonContext.js: take the dep from `opts` in `buildDungeonContext` (:183) and add `relock: () => opts.relock?.(),` to the hooks literal at :4337, beside `quickSave: () => ctx.quickSave?.()` (:4383) - the same threading `hudMessageSink` already uses; then hand it in as `relock: () => requestLook(canvas)` from BOTH dungeon hosts' opts bags, src/scenes/dungeon.js:106-119 and src/scenes/worldModes.js:4890. Pin (behavioural, not regex): compose each host's hooks bag headless and assert `typeof hooks.relock === 'function'` for world-exterior, world-interior, exterior-exterior, exterior-interior, standalone dungeon and world-hosted dungeon, plus the pauseDoor behaviour pin (fires on resume/save/load, silent on exit). Deleting any one `relock:` line must redden its own assertion.",
    "severity": "medium",
    "laneKey": "src/scenes/dungeonContext.js"
  },
  {
    "id": "HP-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Every cited line re-derived. src/scenes/worldModes.js:5107-5115 is the DUNGEON `_enemyArm` and :5111 is `modal: (t) => mountInterior(new ActionTextBox(String(t).split('\\n')))`. `mountInterior` (:6638-6643) pushes through `interiorWindows`, whose `onTop` callback sets `interiorOverlay` (:989 `makeWindowStack({ onTop: (w) => { interiorOverlay = w; } })`) - so this really does load the interior slot in dungeon mode. That slot is dead there: frame()'s dungeon arm returns at :5722, the only `interiorOverlay` tick/draw is at :6038 below it; the keydown arm is gated `if (mode === 'interior')` at :7022; the pointer seam returns at :7081 `if (mode !== 'interior' || !interiorOverlay) return false;`; `overlayHeld` (:5355) and `modeHudCovered` (:1029) both fold the interior slot only under interior mode. ActionTextBox has no timer (src/ui/actionText.js clears `done` only in input()/click()). The sink is reachable: src/player/mobileEnemyActivate.js:113 `if (r.modal) modal?.(r.message); else hud?.(r.message);` inside the steal arm, against a class enemy in a world-hosted dungeon - the classic-start context. DFU's owner is PlayerActivate.cs:1640/:1646 inside Pickpocket(), and DaggerfallUI.MessageBox builds on `uiManager.TopWindow`, which underground is the dungeon's stack. The sibling standalone host does it right (dungeon.js:233 -> dungeonContext.js:4274 `hudBox`), the interior arm at :4630 is correctly gated, and both outdoor hosts use townTalk's slot.",
    "corrections": "Two. (a) Consequence (c) is overstated: `mountSpellWindow`'s `if (interiorOverlay) return false` (:1062) is inside its `mode === 'interior'` arm - in DUNGEON mode it routes to `dungeonCtx.showOverlay` and is unaffected, so the refusal and `interiorKeyCtx.uiOverlayActive` bite on the next BUILDING entry, not 'for the rest of the session' underground. The orphan does survive to that point, which the finder did not show: both `interiorWindows.clear(...)` sites (:4851-4852 and :7658-7659) sit inside `if (interiorCtx)` teardown arms and `interiorCtx` is null in dungeon mode. (b) The proposed fix WORKS but is not the best one: `mountServiceWindow`'s dungeon arm is `dungeonCtx?.showOverlay?.(win)` and dungeonContext.js:4799 is `showOverlay(win) { return pushDungeonWindow(win); }`, so it is the same push, no replace hazard. But dungeonCtx already publishes the sink NAMED for exactly this at dungeonContext.js:4274 (`hudBox: (rows) => pushDungeonWindow(new ActionTextBox(rows)),   // AUDIT 63 F33 ... for the enemy arm's success boxes`), and dungeon.js:233 already calls it; routing worldModes through the same named door satisfies ONE DFU MEMBER, ONE EXPORT and keeps the ActionTextBox construction in one place instead of two.",
    "fixShape": "src/scenes/worldModes.js:5111 — `modal: (t) => mountInterior(new ActionTextBox(String(t).split('\\n'))),` becomes `modal: (t) => dungeonCtx.hudBox(String(t).split('\\n')),   // AUDIT 63 F33: DaggerfallUI.MessageBox is uiManager.TopWindow's stack, which underground is the dungeon's (dungeonContext.js:4274, the sink dungeon.js:233 already calls)`. The bare `dungeonCtx.` is safe: the whole `_enemyArm` is already `(dungeonCtx ? tryMobileEnemyActivate(...) : false)` at :5107. Do NOT touch the interior arm at :4630 or the two outdoor hosts. Pair this edit with HP-3's one-line change to :5110 in the same lane - they are the two halves of one sink family. Pin: drive worldModes into dungeon mode with a stub dungeonCtx, run the dungeon activate ladder against a class enemy in steal mode with a forced-success roll, assert (a) `dungeonCtx.hudBox` was called with the pinched-gold rows and (b) the interior slot is still null; mutation-check by restoring `mountInterior`, which must redden both. Add the standing structural assertion the file's own header at :7069-7073 already argues for: no `mountInterior(` may appear between :5080 and :5240 (the dungeon ladder).",
    "severity": "high",
    "laneKey": "src/scenes/worldModes.js"
  },
  {
    "id": "HP-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "src/scenes/worldModes.js:5110 is `hud: (t) => say(t),` inside the DUNGEON `_enemyArm`, and `say` is the outdoor host's queue - worldModes.js:372 `const say = (l, delay) => { if (townTalk?.say) townTalk.say(l, delay); else console.warn('[interior]', l); };`. Every other HUD line in a world-hosted dungeon goes through `dungeonCtx.hudSay` -> `hudText.add` (dungeonContext.js:4273), which is what the sibling standalone host uses (dungeon.js:232 `hud: (t) => ctx.hudSay?.(t)`). Both queues genuinely paint on a dungeon frame: dungeonContext.js:4152 `hudText.draw(...)` inside drawFoes (called from worldModes.js:5718), and src/scenes/world.js:7131 `townTalk.frame(dt);` runs UNCONDITIONALLY after `modes.frame(dt, now)` returns true, with the host's own note at :7124-7129 stating that `townTalk.frame` ticks and draws the HUD TEXT LAYER. So the 'You see a <foe>' line and the 'You are not successful.' line land on a second, independent column. DFU has one PopupText per HUD (PlayerActivate.cs:1651 `DaggerfallUI.Instance.PopupMessage(...)` -> `dfHUD.PopupText.AddText`). The interior arm at :4629 is correctly `say(t)` because townTalk's queue IS the interior host's queue (ticked at :6035).",
    "corrections": "THE PROPOSED FIX IS A BUG AND MUST NOT BE BRIEFED AS WRITTEN. `hud: (t) => dungeonCtx?.hudSay?.(t) ?? say(t)` fires BOTH sinks on every line: `hudSay` is `(t) => hudText.add(t)`, a void sink, so the call evaluates to undefined and `??` falls straight through to `say(t)` - the finding's own symptom (two columns) would be made unconditional by its own fix. Use the unconditional form the arm already guarantees. Minor cite drift: `say` is defined at worldModes.js:372, not :360.",
    "fixShape": "src/scenes/worldModes.js:5110 — `hud: (t) => say(t),` becomes `hud: (t) => dungeonCtx.hudSay(t),   // AUDIT 63 F33: PopupMessage is the HUD's ONE PopupText (PopupText.cs:123); underground that is this context's hudText, the same queue dungeon.js:232 speaks to`. No `?.` and no `??` - the enclosing `_enemyArm` is already `(dungeonCtx ? ... : false)` at :5107, and any optional chain here re-opens the double-fire. Land it in the same commit as HP-2's :5111, since they are the two host-supplied sinks of one call (the third, `midScreen`, needs nothing: src/player/mobileEnemyActivate.js:93 defaults it to the scene-free `setMidScreenText`). Pin: in HP-2's dungeon-mode harness, run the enemy arm in Info mode against a live foe and assert the line reached `dungeonCtx.hudSay` and NOT the townTalk stub's `say` - and assert the townTalk stub recorded ZERO calls, which is the assertion that would have caught the `??` fix. Add the structural pin that both halves of the dungeon `_enemyArm` name a `dungeonCtx` sink.",
    "severity": "low",
    "laneKey": "src/scenes/worldModes.js"
  }
]
```