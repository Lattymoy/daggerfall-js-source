I've completed the sweep. Here are my findings.

```json
[
  {
    "id": "CV-1",
    "lens": "constants-vs-reads",
    "title": "The skills dialog prints the PERMANENT skill array where GetSkillSummary reads GetLiveSkillValue - beside the hand-to-hand line the same delta gave the LIVE value",
    "severity": "medium",
    "files": [
      "/home/user/daggerfall-js-source/src/ui/charsheet.js",
      "/home/user/daggerfall-js-source/src/ui/enhancedCharSheet.js"
    ],
    "claim": "AUDIT 63 F35 rewrote `CharSheet._drawSkillPage` (the whole statement is an added line in the delta) and kept the row's value as the entity's own `skills` array; AUDIT 63 F34 then added the hand-to-hand damage row three lines below it off `skillValue(e, SKILLS.HandToHand)` - the port's GetLiveSkillValue. DFU builds EVERY row of that dialog from one member, `TextProvider.GetSkillSummary`, whose value token is `playerEntity.Skills.GetLiveSkillValue(skill)`. So a lycanthrope (`LYCANTHROPE_SKILL_MOD = 30` over seven skills, `src/systems/lycanthropy.js:92-97`, HandToHand among them) or anyone wearing an EnhancesSkill enchantment (`entity._enchantMods.skillMods`, +15) opens the Miscellaneous page and reads `Hand-to-Hand 45%` on one line and a damage range computed from 75 on the next. The enhanced skin has the same split inside ONE model object: `attributes` are `liveStat(e, key)` (live) and `skill` is the raw array.",
    "evidence": {
      "portLiteral": "src/ui/charsheet.js:659 - `shadowText(renderer, font, `${SKILL_NAMES[id]} ${e.skills?.[id] ?? 0}%`, ...)` (added by the delta); the live reader is imported and used one block later at src/ui/charsheet.js:663 `const v = skillValue(e, SKILLS.HandToHand);`. The enhanced twin: src/ui/enhancedCharSheet.js:126 `skill: (id) => e.skills?.[id] ?? 0,` sitting directly above the delta-added src/ui/enhancedCharSheet.js:135-138 `handToHandDamage: { min: handToHandMinDamage(skillValue(e, SKILLS.HandToHand)), ... }`, and above src/ui/enhancedCharSheet.js:118 `attributes: STAT_KEYS_ORDER.map((key) => ({ key, value: liveStat(e, key) }))`.",
      "csharpRead": "tools/parity/dfu/Assets/Scripts/Utility/TextProvider.cs:503 - `skillValueToken.text = string.Format(\"{0}%\", playerEntity.Skills.GetLiveSkillValue(skill));` inside GetSkillSummary (:489-529), the ONE member DaggerfallCharacterSheetWindow.cs:288/:296/:301 builds every row of ShowSkillsDialog from. The permanent value is never read on that path.",
      "portsOwnLiveReader": "src/systems/skills.js:78-96 `skillValue()` (\"DaggerfallSkills.GetLiveSkillValue (:135-143): the permanent value PLUS the effect mod\") vs src/systems/skills.js:68-74 `permanentSkillValue()`, which is what `e.skills[id]` is."
    },
    "proposedFix": "In `src/ui/charsheet.js:659` replace `e.skills?.[id] ?? 0` with `skillValue(e, id)` (already imported at :33). In `src/ui/enhancedCharSheet.js:126` replace `skill: (id) => e.skills?.[id] ?? 0` with `skill: (id) => skillValue(e, id)` (skillValue already imported at :68). Do NOT change `getSkillRecentlyIncreased` - that is a separate read and already correct. Leave the classic sheet's fallback pane (`_drawFallback`, :692) alone or convert it in the same edit for consistency, but the dialog is the member DFU's GetSkillSummary owns.",
    "pinShape": "Build the entity through the real producer (a chargen character, then `applyLycanthropy`/the racialOverride round that writes `skillMods`), then assert the dialog's row model deepEquals `[SKILL_NAMES[HandToHand], skillValue(e, HandToHand)]` with skillValue(e,HandToHand) === permanentSkillValue(e,HandToHand) + 30, AND assert the same page's hand-to-hand damage row is computed from that same number. Mutation check: flipping `skillValue` back to `e.skills[id]` must fail, and so must changing LYCANTHROPE_SKILL_MOD by one."
  },
  {
    "id": "CV-2",
    "lens": "constants-vs-reads",
    "title": "The PLAYER's blast/contact capsule is measured at the ENEMY prefab's 0.45 radius where the player's own CharacterController is m_Radius 0.35 - and the port already spells 0.35 in motor.js",
    "severity": "medium",
    "files": [
      "/home/user/daggerfall-js-source/src/systems/spellcast.js",
      "/home/user/daggerfall-js-source/src/scenes/hostMagic.js",
      "/home/user/daggerfall-js-source/src/scenes/dungeonContext.js",
      "/home/user/daggerfall-js-source/src/player/motor.js"
    ],
    "claim": "ROAD-H H2 (in this delta) replaced hostMagic's old point test against the player (`playerFeet[1] + 0.9`) with a real capsule test, and routed it through `sphereOverlapsCapsule`, whose body radius is the single module constant `BODY_CAPSULE_RADIUS = 0.45`. That number is the ENEMY prefab's `m_Radius 0.4 + m_SkinWidth 0.05` - the module's own comment asserts \"the player's is the same radius\", and the prefab says otherwise. DFU's AoE is `Physics.OverlapSphereNonAlloc(position, ExplosionRadius, ...)` and its contact is `Physics.SphereCast(colliderPosition, ColliderRadius, ...)`: both meet whatever collider each entity actually wears, i.e. each entity's OWN CharacterController radius, never one shared number. The port therefore gives the player a body 0.10 wider than DFU's on every missile, arrow and blast: the missile contact is 0.45+0.45 = 0.90 where DFU is 0.45+0.35 = 0.80 (12.5% wide), and the AoE catch is 4.45 where DFU is 4.35. The port already carries the right number one module over.",
    "evidence": {
      "portLiteral": "src/systems/spellcast.js:262 `export const BODY_CAPSULE_RADIUS = 0.45;` with the comment at :257-261 (\"the player's is the same radius under PlayerHeightChanger's live height\"), consumed for the player by the delta-added src/scenes/hostMagic.js:297 `if (playerFeet && sphereOverlapsCapsule(pos, EXPLOSION_RADIUS, playerFeet, playerHeight))` and by src/scenes/dungeonContext.js:2732/:2900/:2912's `explodeAt(..., playerFeet, ..., { playerHeight })`; `sphereOverlapsCapsule` is src/systems/spellcast.js:304-307. The port's own player radius is src/player/motor.js:99 `export const CAPSULE_RADIUS = 0.35;`.",
      "csharpRead": "tools/parity/dfu/Assets/Scripts/Game/DaggerfallMissile.cs:481 `Physics.OverlapSphereNonAlloc(position, ExplosionRadius, aoeBuffer, layerMaskDefault, QueryTriggerInteraction.Ignore)` and :339 `Physics.SphereCast(colliderPosition, ColliderRadius, direction, out hitInfo, displacement.magnitude + ColliderRadius, layerMask)` - the query meets the target's collider, not a constant. The player's collider: Assets/Prefabs/Player/PlayerAdvanced.prefab (read with `git show HEAD:...` in tools/parity/dfu) lines 81-85 `m_Height: 1.8 / m_Radius: 0.35 / m_SkinWidth: 0.06`; the enemy's: Assets/Prefabs/Scene/DaggerfallEnemy [Game Serializable].prefab:442-446 `m_Height: 1.8 / m_Radius: 0.4 / m_SkinWidth: 0.05`. SetupDemoEnemy.cs:99-114 adjusts the enemy controller's HEIGHT only - the radius is never touched at runtime, so 0.45 is right for foes and only for foes."
    },
    "proposedFix": "Give `sphereOverlapsCapsule` / `missileHitsCapsule` the body radius as a parameter instead of a module constant, defaulting to the foe value: `sphereOverlapsCapsule(pos, radius, feet, height, bodyRadius = BODY_CAPSULE_RADIUS)`. Export `PLAYER_CAPSULE_RADIUS = CAPSULE_RADIUS + 0.06` (0.41) - or plain `CAPSULE_RADIUS` if the port's collider model excludes skin width, which is the choice motor.js already made - from ONE home (import motor.js's CAPSULE_RADIUS rather than restating 0.35), and pass it at every player-side call: hostMagic.js:297, hostMagic.js:592-596, dungeonContext.js's three explodeAt/`missileHitsCapsule` player arms, and worldModes.js/exterior.js's arrow contacts. Fix spellcast.js:257-261's comment, which currently states the false premise.",
    "pinShape": "Two-sided distance pin at the player: with playerFeet at origin and playerHeight 1.8, a blast centred at 4.36 from the capsule axis must NOT reach the player and one at 4.34 must; a missile at 0.81 must miss and at 0.79 must hit. Assert separately that the foe half is unchanged at 0.45 (a foe at 0.89 still hit). Mutation check: changing the player body radius by 0.01 in either direction must fail one of the four assertions, and re-pointing the player arm back at BODY_CAPSULE_RADIUS must fail."
  },
  {
    "id": "CV-3",
    "lens": "constants-vs-reads",
    "title": "The water scroll rate is now spelled a THIRD time - waterSurface.js re-declares the dungeon pool's 0.05 and its own docstring promises they match",
    "severity": "low",
    "files": [
      "/home/user/daggerfall-js-source/src/render/waterSurface.js",
      "/home/user/daggerfall-js-source/src/scenes/dungeon.js",
      "/home/user/daggerfall-js-source/src/scenes/worldModes.js"
    ],
    "claim": "WATER1 added `export const WATER_SCROLL_TILES_PER_SEC = 0.05` to `render/waterSurface.js` whose comment reads \"the dungeon water's own rate (scenes/dungeon.js), so a river and a dungeon pool crawl alike\" - i.e. it names the module that already owns the number and then restates the literal instead of importing it. There were already two spellings before the delta (`scenes/dungeon.js:72 WATER_SCROLL_TILES_PER_SEC = 0.05` and `scenes/worldModes.js:251 DUNGEON_WATER_SCROLL = 0.05`, the same value under two names); the delta made it three, across two names, in three modules. This is the AUDIT 64 goldPieceWeightInKg shape and it is REPORTABLE rather than refutable because the value is a port tunable, not an immutable DFU number: the divergence a tune would produce - a river or a pond crawling at a different rate from the dungeon pool three metres away, or from itself under the classic skin - is exactly what the docstring claims cannot happen, and nothing enforces the claim. Home.md's ONE DFU MEMBER, ONE EXPORT: \"If two files legitimately need it, one exports and the other imports - never two literals.\"",
    "evidence": {
      "portLiteral": "src/render/waterSurface.js:63-66 (added by WATER1) `/** The classic texel's slow flow, in tiles per second - the dungeon water's own rate (scenes/dungeon.js), so a river and a dungeon pool crawl alike. */ export const WATER_SCROLL_TILES_PER_SEC = 0.05;` against the pre-existing src/scenes/dungeon.js:72 `const WATER_SCROLL_TILES_PER_SEC = 0.05;` (consumed at :907) and src/scenes/worldModes.js:251 `const DUNGEON_WATER_SCROLL = 0.05;` (consumed at :5722).",
      "csharpRead": "No C# read is claimed for this number - it is the port's presentation constant (Ledger A). The law being broken is the port's own doctrine (bible/Home.md, ONE DFU MEMBER, ONE EXPORT), and the finding is that a value whose module doc asserts cross-module equality is stored as three unlinked literals."
    },
    "proposedFix": "Make `render/waterSurface.js` the one home (it is the lowest module of the three and already exports the rest of the water constants): delete `src/scenes/dungeon.js:72` and `src/scenes/worldModes.js:251` and have both hosts `import { WATER_SCROLL_TILES_PER_SEC } from '../render/waterSurface.js'`, dropping the `DUNGEON_WATER_SCROLL` alias. Rewrite waterSurface.js:63-65's comment so it states the rate rather than pointing at a module that no longer declares it.",
    "pinShape": "A source-level single-source test (the shape settings.test.js already uses to re-derive claims from code): scan non-comment `src/**/*.js` for a water-scroll declaration and assert exactly ONE exists, in render/waterSurface.js. Plus a behaviour pin: with the same `now`, assert `waterUniforms({seconds}).scroll` and the dungeon host's `drawWater` scroll argument are the same number. Mutation check: reintroducing a second literal, or changing one of the two call sites' rate, must fail."
  },
  {
    "id": "CV-4",
    "lens": "constants-vs-reads",
    "title": "The closed-building line is an invented English sentence where PlayerActivate reads guildClosed/storeClosed from the string table and substitutes %d1/%d2",
    "severity": "low",
    "files": [
      "/home/user/daggerfall-js-source/src/scenes/worldModes.js"
    ],
    "claim": "AUDIT 64 F11's `activateBuilding` (an added block) speaks the closed-shop refusal as a hand-written template string with the word 'Guild' or 'Store' substituted into ONE sentence. DFU picks between TWO WHOLE localized records and then substitutes only the two HOURS into whichever it picked: `buildingClosedMessage = (buildingType == GuildHall) ? GetLocalizedText(\"guildClosed\") : GetLocalizedText(\"storeClosed\")`, then `.Replace(\"%d1\", openHours[...])` and `.Replace(\"%d2\", closeHours[...])`. The two hour tables ARE read correctly (`OPEN_HOURS`/`CLOSE_HOURS` in systems/buildingLocks.js:25-26 match PlayerActivate.cs:91-92 byte for byte) - it is only the carrier sentence that was invented, and it is invented in a shape DFU does not have (one string with a swapped noun rather than two independent records). Every neighbouring string in this delta carries an `Internal_Strings.csv:NNN` cite (`NO_ACCOUNT_TEXT = 'None'` at banking.js, the two loan-reminder lines at worldTick.js, `YOU_SEE_A_TEXT`/`YOU_SEE_AN_TEXT` at mobileEnemyActivate.js); this one carries none.",
    "evidence": {
      "portLiteral": "src/scenes/worldModes.js:4059-4062 - `const which = bd.buildingType === BUILDING_TYPES.GuildHall ? 'Guild' : 'Store'; townTalk?.say?.(`${which} is closed. Open from ${OPEN_HOURS[bd.buildingType]}:00 to ${CLOSE_HOURS[bd.buildingType]}:00.`);` (added line, delta.diff:12967).",
      "csharpRead": "tools/parity/dfu/Assets/Scripts/Game/PlayerActivate.cs:477-480 - `string buildingClosedMessage = (buildingType == DFLocation.BuildingTypes.GuildHall) ? TextManager.Instance.GetLocalizedText(\"guildClosed\") : TextManager.Instance.GetLocalizedText(\"storeClosed\"); buildingClosedMessage = buildingClosedMessage.Replace(\"%d1\", openHours[(int)buildingType].ToString()); buildingClosedMessage = buildingClosedMessage.Replace(\"%d2\", closeHours[(int)buildingType].ToString()); DaggerfallUI.Instance.PopupMessage(buildingClosedMessage);`"
    },
    "proposedFix": "Lift the two shipped strings out of Internal_Strings.csv (the same source the delta's other lines cite by line number) into `systems/buildingLocks.js` beside OPEN_HOURS/CLOSE_HOURS as `GUILD_CLOSED_TEXT` / `STORE_CLOSED_TEXT`, each carrying its csv cite, and add `buildingClosedText(buildingType)` that picks by `buildingType === BUILDING_TYPES.GuildHall` and does the two `%d1`/`%d2` replaces off the existing tables. Call it from worldModes.js:4062. If the csv rows cannot be read, say so in the comment and keep the literal - but keep DFU's SHAPE (two independent whole strings, hours substituted), not one sentence with a swapped noun.",
    "pinShape": "deepEqual the produced line against the record text with %d1/%d2 filled from OPEN_HOURS/CLOSE_HOURS for a named buildingType (e.g. Tavern, open 9 close 23) and separately for GuildHall, asserting the two differ by more than the first word. Mutation check: swapping the guild/store pick, or shifting either hour table index by one, must fail."
  }
]
```

```json
{
  "coverage": {
    "read": [
      "All 13,358 numeric added lines of `git diff 0fe2c05b..HEAD -- src` (199 files), reduced to ~1,100 non-comment numeric added lines and read file by file.",
      "Whole modules read against their C#: player/exteriorSurface.js vs PlayerMotor.cs:499-603 (the tile lists and GetOnExteriorWaterMethod verbatim); world/cityGate.js vs DaggerfallCityGate.cs:19-51; world/staticBuildings.js vs DaggerfallStaticBuildings.cs:54-92 + RMBLayout.cs:864-882; ui/midScreenText.js vs DaggerfallHUD.cs:25/:33/:50-51/:175-177/:259-267/:353-372 and all 22 SetMidScreenText call sites; systems/holidays.js vs FormulaHelper.GetHolidayId + PlayerEnterExit.cs:78-80/:355-368/:565-585/:1404-1409; systems/banking.js bankingStatusRows vs DaggerfallBankingWindow.cs:519-577; ui/charsheet.js _drawSkillPage vs DaggerfallCharacterSheetWindow.cs:276-325 + TextProvider.cs:489-529; world/locationEntrance.js pickLocationSide vs StreamingWorld.cs:1470-1525; characters/enemyTargets.js aim arms vs DaggerfallMissile.cs:515-590; systems/spellcast.js missile constants vs DaggerfallMissile.cs:35-38/:333-347/:481; world/terrainSampler.js vs DefaultTerrainSampler.cs:28-29; systems/theft.js + talk.js pickpocket vs FormulaHelper.cs:254-279 + PlayerActivate.cs:1611-1673; systems/biography.js macro source vs BiogFileMCP.cs:90-148/:610-640.",
      "Settings lane: systems/settingsDefaults.js (171 keys) and settings.js's LIVE/stored/unavailable tiers cross-checked against every settings-shaped literal in the delta; the DFU settings reads in DaggerfallHUD / DaggerfallInventoryWindow / DaggerfallCharacterSheetWindow / PlayerActivate / DaggerfallTradeWindow enumerated and matched to port consumers.",
      "Time lane: every `1440`, `3600`, `* 60`, `/ 60`, `dt`, `seconds`, `MINUTES_PER_*` and `deltaTime` occurrence in added lines; the four hosts' `say(line, delay)` sinks and the four `createPlayerTicker` wirings; HolidayTextTimer's dt; LightningFlash's real-second clock and MAX_DELTA_SECONDS.",
      "Units/sign lane: every `Math.PI`, `180`, degree/radian conversion in added lines; the keyboard-look sign chain (InputManager.cs:1854-1865 -> :1510-1511 -> PlayerMouseLook.ApplyLook) against ui/input.js's kb axes and the hosts' lookFilter.add.",
      "World-scale lane: every `0.025`/GLOBAL_SCALE, `819.2`, `6.4`, `1.01`, `128` in added lines; drawTerrain/drawWaterSurface tile-size and tileDim arguments in both exterior hosts and the lab.",
      "Live-vs-permanent sweep: every added line touching `.stats`, `.skills`, `liveStat`, `skillValue`, `permanentSkillValue`; every `?? <number>` default in added lines (54x `?? 0`, 4x `?? 1`, 1x `?? 0.25`), each traced to its producer.",
      "Restated-constant sweep: all ~120 `export const NAME = ...` added by the delta, each grepped for a second declaration elsewhere in src/.",
      "Cross-checked against bible/01-Overview/Audit-63.md and Audit-64.md 'What was refuted' - no refuted number re-raised (goldPieceWeightInKg, AmbientLitInteriors, the interior editor markers, the prop-RMB door record index, the chargen confirm click, bonusPointsRows, JournalNote's variant draw, enemy weapon poison)."
    ],
    "notFound": [
      "LITERALS DFU ALSO HARDCODES - checked and NOT findings: LOAN_REMINDER_HUD_DELAY 3 (LoanChecker.cs:15 `const float loanReminderHUDDelay = 3`); HOLIDAYS_START_ID 8349, HOLIDAY_TEXT_PRIME_DELAY 2.5, HOLIDAY_TEXT_REARM 10 (PlayerEnterExit.cs:567/:1406/:584, all consts); MID_SCREEN_TEXT 146 / 1.5 / 7 (DaggerfallHUD.cs:25/:51/:360, and every one of the 22 SetMidScreenText callers takes the default delay); DOOR_TEXT_HUD_DELAY_S 2.0 (DaggerfallAction.cs:875); PARRY_VOLUME 1.1 (EnemySounds.cs:139); SOUL_BOUND_MONSTER_ID_COUNT 43 (SoulBound.cs:29); BANKING_STATUS_COLUMNS [0,60,120,180] and shortenName's 12 (DaggerfallBankingWindow.cs:537/:559-576); the skills dialog's second-column x 136 (DaggerfallCharacterSheetWindow.cs:301); ARROW_ORIGIN_FORWARD 0.6, height/3, ARROW_CROUCH_DIP 0.05, PLAYER_ARROW_DOWN 0.11, PLAYER_ARROW_SIDE 0.15 (DaggerfallMissile.cs:534/:537/:546-549/:584-585); MISSILE_SPEED 25, MISSILE_COLLIDER_RADIUS 0.45, EXPLOSION_RADIUS 4.0 (DaggerfallMissile.cs:35-38); staticBuildings' 1.01 (DaggerfallStaticBuildings.cs:78); OPEN_HOURS/CLOSE_HOURS (PlayerActivate.cs:91-92, byte for byte); onShallowWaterTile / onPathTile record sets (PlayerMotor.cs:551-574, verbatim); CITY_GATE 446/447 (RMBLayout.cs:959-963); the biography seeds base/+123/+9543 and the six imperial names (BiogFileMCP.cs:145/:621/:629/:635); the lunar 32-day ratio and the +3/-1 offsets; the potion textureRecords (PotionRecipe.cs:34 default 11 plus the seventeen effect-class overrides); SCALED_OCEAN/BEACH_ELEVATION (DefaultTerrainSampler.cs:28-29 - the delta actually IMPROVED these to Math.fround, matching C# float32).",
      "DECLARED LEDGER A DEPARTURES with an owner and a recorded row - not findings: PITCH_FLOOR 75 degrees (MAC1, Mac's number, DFU clamps at 90); KEYBOARD_LOOK_UNITS_PER_SECOND 60 (FIX-F, per-second where DFU's keyboard turn is frame-bound, recorded as a departure of kind); FAR_FLAT_RING 2 / TALL_FLAT_HEIGHT 2.5 (MAC1 flatDistance, presentation is the port's under Port-Doctrine); EVOLVE_CHANCE_PER_HOUR 0.12 and EVOLVE_SEED (CLK2, the enhanced lane's own evolution); wind.js CALM_MIN/CALM_SPAN/DRIFT_MEAN/DRIFT_SWING; WATER_LIFT 0.08, WATER_TINT, WATER_F0 0.02, SHORE_SOFTNESS 0.16, WATER_OPACITY 0.82 (WATER1); the whole volumetricClouds/cloudNoise constant set (VC arc); combatVisuals BLEND/SHADE/REVEAL (ECV1); STEP_SMOOTH_TAU 0.06 (MAC1).",
      "CHECKED AND REFUTED IN PLACE: `target.level ?? 0` in talk.js pickpocket and `entity?.level ?? 1` in shared.js doorSpellFor and `hooks.level?.() ?? 1` in bankWindow - all dead fallbacks; every producer (makeEnemyEntity sets level for class enemies from playerLevel exactly as EnemyEntity.cs:299-301 does; the bank window's host wires `level: () => playerEntity.level`). `m.casterLevel ?? 1` in hostMagic - dead too: enemyCasting.js:332 always passes `f.entity.level`, and dungeonContext's `casterLevel: 1` for trap bundles is CalculateCasterLevel(null) verbatim. `personality ?? 50` at ten sites - all now `liveStat(...)` with the 50 only guarding a stats-less test entity; AUDIT 63 F10/F11 fixed exactly this shape in the delta. The template-vs-item's-own-field shape is likewise FIXED by the delta (AUDIT 63 F20/F21 in itemTemplates.inventoryItemImage) rather than introduced.",
      "The port's `CLASSIC_MINUTES_PER_SECOND = 12/60` (worldTick.js) and `WIND_SECONDS_PER_MINUTE = 60/12` (enhancedSky.js) ARE two spellings of a value DFU reads live (`WorldTime.cs:32 public float TimeScale = 12f`, read at WorldTime.cs:67 and DaggerfallSky.cs:336). Deliberately NOT raised: nothing in the port or in the trimmed reference ever writes TimeScale, so the divergence is not observable - the brief's own bar.",
      "Not raised because they are another lens's shape, but they are real and the fix lanes should know: (a) `PITCH_LIMIT` is imported and never used in all four hosts (world.js:119, exterior.js:38, dungeon.js:33, interior.js:10) - dead imports left by MW-D30; (b) the keyboard-look arm added by FIX-F is wired in world.js:7032, exterior.js:3470 and dungeon.js:593 but NOT in worldModes.js, the interior host - THE FOUR HOSTS RULE; (c) WATER-AUDIT lane A's water-only index set (`buildWaterIndices`) is used by world.js:838/:1359 and the lab, but exterior.js:4200 passes the whole `groundSurface` to `drawWaterSurface`, so the fixed-city host still draws the entire ground grid for the water pass the audit narrowed - a seam one lane built and another host bypassed.",
      "src/tools/skyLab.js and src/tools/waterLab.js hardcode `65 * Math.PI / 180` where the game reads `fieldOfView()` (ui/viewSettings.js:23, `getInt('Video','FieldOfView',60,120)`). NOT reported as a finding - they are dev harnesses, not the game - but noted because a probe that does not sample the game's own FOV is the class of mistake bible/Home.md warns about."
    ]
  }
}
```