All eight findings re-derived and every proposed fix written, run green on HEAD, and mutation-checked. Work was done in an isolated `git worktree` at HEAD (`scratchpad/a65/wtB`, now removed) — see the operational note at the end.

```json
[
  {
    "id": "PN-1",
    "upheld": true,
    "confidence": "high",
    "severity": "medium",
    "laneKey": "test/water.test.js",
    "greenOnHead": "yes",
    "redUnderMutant": "yes - all six, individually",
    "claimPromisesTheNumber": "PARTLY. test/water.test.js:79 names 'the sky's two colours' — DEFAULT_SKY_ZENITH/HORIZON are promised by the claim, so those two are a finding. For WATER_LIFT / WATER_OPACITY / WATER_F0 / SHORE_SOFTNESS the claim says only 'the uniforms', and the file header (:1-5) promises the table, the draw state, the hosts and the record — not the tuning. Those four are a note under the strict reading.",
    "reasoning": "Re-derived at HEAD: src/render/waterSurface.js:52/54/60/62/69/70 hold the six constants; test/water.test.js:93/98 are the only assertions that touch them and each compares waterUniforms()'s output to the constant waterUniforms() read (src/render/waterSurface.js:206-210). `grep -rn` over test/ and tools/ returns no other reference — nothing in the tree holds any of the six values. I applied WATER_LIFT 0.08->0.8 and ran the FULL suite: 7047 tests, 0 fail. Confirmed the finder's six mutants each survive `node --test test/water.test.js` (10 pass / 0 fail).",
    "corrections": "Two. (1) The finder calls the identity lines a tautology and nothing more; they are in fact honest WIRING pins — I mutated `lift: WATER_LIFT` -> `lift: 0` at waterSurface.js:206 and test/water.test.js went red (9 pass / 1 fail). So the lane must KEEP them, not replace them; they are the only thing proving waterUniforms forwards at all. (2) Severity: these are port aesthetic policy invented by WATER1 (Mac's request), not DFU-referenced numbers, so a wrong value is a visual regression, not a logic/save defect — medium, not high.",
    "fixShape": "test/water.test.js — insert immediately AFTER line 93, verbatim:\n  assert.equal(WATER_LIFT, 0.08, 'the z-fight lift above the ground the water is built from');\n  assert.ok(WATER_LIFT > 0 && WATER_LIFT < 0.2, 'above the ground but below a step');\n  assert.equal(WATER_OPACITY, 0.82, 'the surface\\'s base opacity, before Fresnel raises it toward grazing');\n  assert.equal(WATER_F0, 0.02, 'Schlick\\'s F0 for water (n = 1.33)');\n  assert.equal(SHORE_SOFTNESS, 0.16, 'half-width of the shore feather, in coverage');\nand immediately AFTER line 98, verbatim:\n  assert.deepEqual([...DEFAULT_SKY_ZENITH], [0.17, 0.35, 0.72], 'the plain day the surface reflects when no sky says otherwise');\n  assert.deepEqual([...DEFAULT_SKY_HORIZON], [0.66, 0.78, 0.92]);\nThe spread is required — both are Object.freeze'd arrays and deepEqual against a plain array needs it. Lines 93 and 98 STAY. WATER_TINT (waterSurface.js:57) is the seventh constant and is not held either; add `assert.deepEqual([...WATER_TINT], [0.62, 0.78, 0.86]);` while the lane is in the file."
  },
  {
    "id": "PN-2",
    "upheld": true,
    "confidence": "high",
    "severity": "medium",
    "laneKey": "test/volumetricClouds.test.js",
    "greenOnHead": "yes",
    "redUnderMutant": "yes - all three, individually",
    "claimPromisesTheNumber": "SPLIT. :63 'the map, the sweep, the drift - the NUMBERS the rest of the outdoors already keeps' names the map and the sweep and says 'numbers' — QUALITY and SWEEP_FRAMES are promised, so that half is a finding. :19 'the profile - one row per weather, eased on the weather ease's own exponential' promises the ROW SET (pinned at :22) and the EASE (pinned at :30-35), not the row values — the VC_PROFILE half is a note.",
    "reasoning": "Re-derived: src/render/volumetricClouds.js:62-67 (QUALITY), :70 (SWEEP_FRAMES), :110-118 (VC_PROFILE). test/volumetricClouds.test.js:60-62 holds QUALITY only by `lo.width < default.width < hi.width`, `lo.steps < default.steps < hi.steps` and `q.height % SWEEP_FRAMES === 0`; :23-24 holds VC_PROFILE only by `base>0 && top>base` and 0..1 range. The two other constraints I found that the finder did not name (:114-117's whole-texels-per-pixel and :133's `shadowSteps >= 8 && <= 24`) also survive the halved-default mutant. I ran all three mutants together against the FULL suite: 7047 tests, 0 fail.",
    "corrections": "None material. Note the finder's own suggested keeps are right: the inequalities say WHY, and must stay.",
    "fixShape": "test/volumetricClouds.test.js — insert immediately BEFORE line 60 (`assert.ok(QUALITY.lo.width < ...`), verbatim:\n  assert.deepEqual(QUALITY.lo, { width: 512, height: 128, steps: 32, light: 4, shadow: 256, shadowSteps: 8 });\n  assert.deepEqual(QUALITY.default, { width: 1024, height: 256, steps: 56, light: 5, shadow: 512, shadowSteps: 12 });\n  assert.deepEqual(QUALITY.hi, { width: 2048, height: 512, steps: 80, light: 6, shadow: 1024, shadowSteps: 16 });\n  assert.equal(SWEEP_FRAMES, 8, 'a full re-march every eight frames');\nand immediately BEFORE line 27 (`assert.ok(VC_PROFILE.overcast.flat > ...`), verbatim:\n  assert.deepEqual(VC_PROFILE.sunny,    { base: 1400, top: 3200, density: 0.60, dark: 0.00, flat: 0.10, shear: 0.35 });\n  assert.deepEqual(VC_PROFILE.cloudy,   { base: 1200, top: 3000, density: 0.70, dark: 0.10, flat: 0.35, shear: 0.40 });\n  assert.deepEqual(VC_PROFILE.overcast, { base: 800,  top: 1600, density: 0.80, dark: 0.30, flat: 0.90, shear: 0.20 });\n  assert.deepEqual(VC_PROFILE.fog,      { base: 150,  top: 600,  density: 1.00, dark: 0.20, flat: 1.00, shear: 0.05 });\n  assert.deepEqual(VC_PROFILE.rain,     { base: 600,  top: 2600, density: 0.90, dark: 0.50, flat: 0.70, shear: 0.30 });\n  assert.deepEqual(VC_PROFILE.snow,     { base: 600,  top: 2000, density: 0.80, dark: 0.30, flat: 0.85, shear: 0.20 });\n  assert.deepEqual(VC_PROFILE.thunder,  { base: 500,  top: 4200, density: 1.00, dark: 0.70, flat: 0.50, shear: 0.50 });\ndeepEqual works directly on the frozen objects (no spread needed). All seven rows, not the finder's four — the table is deepEqual'd whole or not at all."
  },
  {
    "id": "PN-3",
    "upheld": true,
    "confidence": "high",
    "severity": "medium",
    "laneKey": "test/combatVisuals.test.js",
    "greenOnHead": "yes",
    "redUnderMutant": "yes - all six, individually",
    "claimPromisesTheNumber": "NO, strictly. :38 'the law - plain, hidden, and the three concealed draws with invisible over blending over shade' promises the PRECEDENCE (pinned at :53-54). :56 'fading over REVEAL_SECONDS' promises the fade's SHAPE (pinned at :60-64, linear in REVEAL_SECONDS), not 0.35. Read strictly these are notes. They still matter because nothing anywhere in the tree holds the values and the whole slice is about how visible a concealed foe is.",
    "reasoning": "Re-derived: src/systems/combatVisuals.js:53/54/55/58/59/61/62. Every assertion about visibility (test:43/45/48/51/59/62) reads the constant back from the module. I mutated ALL SIX free constants at once (BLEND_ALPHA 0.22->0.95, BLEND_SHIMMER 0.08->0.3, BLEND_HZ 1.3->4, SHADE_ALPHA 0.55->0.02, REVEAL_ALPHA 0.8->0.3, REVEAL_SECONDS 0.35->9) and ran the FULL suite: 7047 tests, 0 fail.",
    "corrections": "Three. (1) It is SIX of seven free, not five of six — the finder missed BLEND_SHIMMER, BLEND_HZ and REVEAL_ALPHA, which are equally free (I proved each). (2) The finder says SHADE_DARK 'already contains the fix shape'. It does not: test/combatVisuals.test.js:166 BUILDS its regex from SHADE_DARK (`new RegExp(...${String(SHADE_DARK)...}`) — it is the same tautology. It only reddens because the value 0.12 is DUPLICATED as a bare GLSL literal at src/render/renderer.js:379 (`if (uConceal.x == 2.0) lit *= 0.12;`), so the mutant desynchronises the two. That is an accidental cross-check AND a ONE DFU MEMBER, ONE EXPORT violation the lane should record. (3) Severity high -> medium: ECV1's alphas are port aesthetic policy, not DFU values.",
    "fixShape": "test/combatVisuals.test.js — insert immediately AFTER line 43, verbatim:\n  assert.equal(BLEND_ALPHA, 0.22, 'a chameleon shimmers at a fifth opacity');\n  assert.equal(BLEND_SHIMMER, 0.08);\n  assert.equal(BLEND_HZ, 1.3);\n  assert.ok(BLEND_ALPHA + BLEND_SHIMMER < 0.5, 'a concealed foe is never more than half visible');\nimmediately AFTER line 51, verbatim:\n  assert.equal(SHADE_ALPHA, 0.55, 'a shade is a silhouette: half again as solid as the chameleon, and pulled to black');\n  assert.equal(SHADE_DARK, 0.12);\nimmediately AFTER line 59, verbatim:\n  assert.equal(REVEAL_ALPHA, 0.8);\n  assert.equal(REVEAL_SECONDS, 0.35, 'a third of a second, not a tell that outlives the swing');\n  assert.ok(REVEAL_SECONDS < 1, 'the flash is shorter than a swing');\nSecond, smaller change in the same lane: src/render/renderer.js:379's bare `0.12` should be interpolated from SHADE_DARK the way the rest of the shader takes its constants, and test:166's regex should then be spelled with the literal `0\\.12` instead of `${String(SHADE_DARK)}` — otherwise the only surviving cross-check disappears the moment someone tidies the duplicate."
  },
  {
    "id": "PN-4",
    "upheld": true,
    "confidence": "high",
    "severity": "low",
    "laneKey": "test/audit64_travel.test.js",
    "greenOnHead": "NO for the finder's fix as written; YES for the corrected form",
    "redUnderMutant": "yes (corrected form)",
    "claimPromisesTheNumber": "YES, twice over. The file header at test/audit64_travel.test.js:18-20 says 'Every assertion below states the REFERENCE's value ... so a revert of any one fix turns it red rather than merely reshaping it', and the assertion's own message at :134 says 'a tenth of a block OUTSIDE the rectangle'. This is a finding, not a note.",
    "reasoning": "Re-derived: src/world/locationEntrance.js:45 `EXTRA_DISTANCE = RMB_SIDE * 0.1`, consumed at :171/:173. test/audit64_travel.test.js:133 puts EXTRA_DISTANCE on the expected side. Confirmed: EXTRA_DISTANCE -> RMB_SIDE * 0.5 leaves `node --test test/audit64_travel.test.js` at 7 pass / 0 fail, and is killed only when test/prisonrelease.test.js joins (its :54 `assert.equal(EXTRA_DISTANCE, RMB_SIDE * 0.1)`). I confirmed prisonrelease.test.js:54 is PRE-DELTA (`git log -L 54,54` -> b8a13b73, and `git merge-base --is-ancestor b8a13b73 0fe2c05b` succeeds); three other pre-delta files (testroom, tr4_ship, and locationEntrance's own callers) use the constant too.",
    "corrections": "IMPORTANT — the finder's FIRST proposed form does not compile green. `assert.deepEqual(at.pos, [origin[0] - tenth, ...])` FAILS on HEAD: the production expression is `centre[0] + side.dx * (halfWidth + EXTRA_DISTANCE)` (locationEntrance.js:171), so at.pos[0] is 989.7600000000001 while `origin[0] - tenth` is 989.76 — a float-associativity mismatch, not a value mismatch. Only the finder's SECOND alternative (keep the associativity, spell the tenth) works. Brief the lane with that one only. Also: the consequence is overstated — the number IS held in the tree, so a disarmed EXTRA_DISTANCE does NOT ship green; `npm test` reddens. Severity medium -> low.",
    "fixShape": "test/audit64_travel.test.js — REPLACE lines 133-134 with, verbatim:\n  assert.equal(EXTRA_DISTANCE, RMB_SIDE * 0.1, 'StreamingWorld.cs:1528-1530 - \"extra distance places player a little bit outside location area\"');\n  assert.deepEqual(at.pos, [origin[0] + half - (half + RMB_SIDE * 0.1), origin[1], origin[2] + half],\n    'a tenth of a block OUTSIDE the rectangle, not its centre');\nRMB_SIDE is already imported at :31. Do NOT collapse the `+ half - (half + ...)` into `- RMB_SIDE * 0.1`."
  },
  {
    "id": "PN-5",
    "upheld": true,
    "confidence": "high",
    "severity": "low",
    "laneKey": "test/audit63_windows.test.js",
    "greenOnHead": "yes",
    "redUnderMutant": "yes - 4 of 19 tests redden",
    "claimPromisesTheNumber": "YES. The test name at :471 says 'shopliftAttempt is DoSteal's ARITHMETIC', and the file header at :1-5 says 'Every pin here holds the REFERENCE's value, and dies under the mutation that reverts the fix.' A finding.",
    "reasoning": "Re-derived: src/combat/formulas.js:833 `const chance = (100 - pickpocketSkill) + shopQuality + weightAndNumItems;` clamped 5..95 at :834. test/audit63_windows.test.js:485 and the three roll-threshold helpers (:376, :400, :463) all compute the expected value with calculateShopliftingChance itself. Confirmed the sign-flip mutant survives `node --test test/audit63_windows.test.js` (19 pass / 0 fail) and is killed only by pre-delta test/theft.test.js:43 (`calculateShopliftingChance(40, 10, 6) === 76`; `git log -L 43,43` -> b8a13b73, pre-delta).",
    "corrections": "Two. (1) The mutated law is NOT in the audited delta: `git diff 0fe2c05b..HEAD -- src/combat/formulas.js` shows no change to calculateShopliftingChance. The delta's own new law — shopliftingLoad's `(int)` truncation, src/systems/theft.js:66 — IS properly pinned (I confirmed Math.trunc -> Math.round reddens the file). So this is duplicate/weak coverage of a pre-delta formula, not an unpinned new law. (2) The finder's suggested `const chance = 62` is wrong for the actual fixture: the three helpers stage one weightless good, so load = 1 and chance = 61. Severity medium -> low.",
    "fixShape": "test/audit63_windows.test.js — REPLACE line 485 with, verbatim:\n  assert.equal(out.chance, 74, '(100 - 40) + 12 + 2 - DaggerfallTradeWindow.cs:912, clamped 5..95');\nand insert immediately AFTER each of the three `const chance = calculateShopliftingChance(50, 10, ...)` lines (:377, :400, :463), verbatim:\n  assert.equal(chance, 61, '(100 - 50) + 10 + one weightless staged good - DaggerfallTradeWindow.cs:912');\nVerified: 19 pass / 0 fail on HEAD; 15 pass / 4 fail under the `+ weightAndNumItems` -> `- weightAndNumItems` mutant and under dropping shopQuality."
  },
  {
    "id": "PN-6",
    "upheld": true,
    "confidence": "high",
    "severity": "medium",
    "laneKey": "test/audit64_hud.test.js",
    "greenOnHead": "yes",
    "redUnderMutant": "yes - three different mutants, including one the count pin structurally cannot see",
    "behaviouralPinBuildable": "YES. townTalk is fully constructible in node — createTownTalk is exported and test/audit63_quests_talk.test.js:56 already builds one with a throwing fetchBytes. `tryActivate` is on the returned API (townTalk.js:1188), rayPersonDistance is exported (:92), midScreenText is a module singleton already imported by this very test file (:14), and setInteractionMode comes from player/interactionMode.js. I built and ran the pin: green on HEAD.",
    "claimPromisesTheNumber": "YES — the claim is about the WIRE, not a number. :246 'every SetMidScreenText CALLER SPEAKS to the label'. A dead call site does not speak. A count over file text cannot distinguish the two, so the pin does not hold what its name promises. A finding.",
    "reasoning": "Re-derived: src/scenes/townTalk.js:612/:619/:630 are the three `setMidScreenText(TOO_FAR_AWAY_TEXT)` sites; test/audit64_hud.test.js:253 counts them in the file's TEXT. Confirmed `if (false && getInteractionMode() !== 'steal' && ...)` at :612 leaves audit64_hud at 18 pass / 0 fail, and across the full 7047-test suite exactly one test dies — `AUDIT 23 ui-native-3: the talk ray reaches 76.8` in the PRE-DELTA test/audit23_ui.test.js. The behavioural pin I wrote reddens all three of: arm 1 disarmed, arm 2 (steal) disarmed, and the AUDIT 26 F048 NESTING removed (an already-attempted townsperson starts speaking again) — the third is invisible to any source-text count, which is the strongest argument for the fix.",
    "corrections": "One addition. Arm 3 (townTalk.js:630, inside `activate()`) is NOT behaviourally reachable from a test harness: it sits behind `ensureLoaded().then(...)`, and ensureLoaded needs ARENA2 bytes. Brief the lane to cover arms 1 and 2 behaviourally and leave arm 3 to the count. The finder's alternative (a guard-anchored regex) is a sound fallback for arm 3 and for the file's other host pins (:494-503, :369-378, :547-552) where no door exists.",
    "fixShape": "test/audit64_hud.test.js — add three imports after line 34 (`import { playerDamageFlash } ...`), verbatim:\nimport { createTownTalk } from '../src/scenes/townTalk.js';\nimport { TOO_FAR_AWAY_TEXT, MOBILE_NPC_ACTIVATION_DISTANCE, PICKPOCKET_DISTANCE, RAY_DISTANCE } from '../src/player/activate.js';\nimport { setInteractionMode } from '../src/player/interactionMode.js';\nthen insert immediately AFTER line 253 (the count pin, which STAYS), verbatim:\n  // ...and the ARM, not just the call site: the refusal RUNS, with its\n  // guard live (a `false &&` in front of the distance test leaves the\n  // three call sites standing and this line silent).\n  const talker = createTownTalk({\n    renderer: { uploadTexture: () => ({}) },\n    canvas: { width: 640, height: 400 },\n    fetchBytes: async () => { throw new Error('this pin loads no ARENA2'); },\n    playerEntity: { name: 'T', stats: { personality: 50 }, skills: 30, skillUses: [] },\n    regionIndex: 0,\n  });\n  const far = MOBILE_NPC_ACTIVATION_DISTANCE + 1;\n  assert.ok(far < RAY_DISTANCE, 'inside the ray, outside the mode\\'s reach - PlayerActivate.cs:76/:780');\n  try {\n    midScreenText._reset();\n    setInteractionMode('info');\n    assert.equal(talker.tryActivate([0, 1, 0], [0, 0, 1], [{ person: {}, pos: [0, 0, far] }]), true,\n      'the activation is CONSUMED by the refusal - E does not fall through to the door behind them');\n    assert.equal(midScreenText.text, TOO_FAR_AWAY_TEXT, 'PlayerActivate.cs:780 - SetMidScreenText, not the popup queue');\n    // ...and the steal arm's own shorter reach (:790), which the same\n    // guard nests inside `!PickpocketByPlayerAttempted`\n    midScreenText._reset();\n    setInteractionMode('steal');\n    assert.equal(talker.tryActivate([0, 1, 0], [0, 0, 1], [{ person: {}, pos: [0, 0, PICKPOCKET_DISTANCE + 1] }]), true);\n    assert.equal(midScreenText.text, TOO_FAR_AWAY_TEXT, 'PlayerActivate.cs:790');\n    // ...and an already-attempted townsperson is SILENT at any range (F048)\n    midScreenText._reset();\n    assert.equal(talker.tryActivate([0, 1, 0], [0, 0, 1], [{ person: { pickpocketAttempted: true }, pos: [0, 0, PICKPOCKET_DISTANCE + 1] }]), true);\n    assert.equal(midScreenText.text, '', 'the nested arm prints nothing');\n  } finally { midScreenText._reset(); setInteractionMode('info'); }\nThe `finally` is required: setInteractionMode and midScreenText are both module singletons shared with the rest of the file."
  },
  {
    "id": "PN-7",
    "upheld": true,
    "confidence": "high",
    "severity": "low",
    "laneKey": "test/fixc_firstStart.test.js",
    "greenOnHead": "yes",
    "redUnderMutant": "yes - both arms, individually",
    "behaviouralPinBuildable": "NO, and I proved it rather than assuming it: `node -e \"import('./src/scenes/world.js')\"` fails outright with `(intermediate value).glob is not a function` — world.js uses Vite's import.meta.glob, so the module cannot even be LOADED under node, let alone booted. A source-text pin is the only instrument available for this file, and that is a correct engineering call, not a shortcut.",
    "claimPromisesTheNumber": "PARTLY. The test name at :22 is about sweepRoadless's body ('the roads sweep RE-QUEUES what it tears down'), which is genuinely pinned at :24-30. But the assertion's own message at :32 says 'the network's ARRIVAL marks the sweep' — that sentence is about the callers, and the pin holds only the one-line declaration. A finding, narrowly.",
    "reasoning": "Re-derived: src/scenes/world.js:424 declares rebuildRoadless, :442 (the Basic Roads / `if (his)` arm) and :445 (the generated-network arm) call it. test/fixc_firstStart.test.js:32 matches only the declaration. Confirmed: deleting `rebuildRoadless();` from the `if (his)` arm leaves fixc_firstStart at 4 pass / 0 fail.",
    "corrections": "Two, both material. (1) The finder's count is WRONG: `world.match(/rebuildRoadless\\(\\);/g).length` is 2, not 3 — the declaration reads `function rebuildRoadless() { roadsSweepDue = true; }`, which has no `();`. A lane briefed with 3 would ship a test that is red on HEAD. (2) The consequence is REFUTED at suite level: both arms are ALREADY pinned, with essentially the finder's own two regexes, at test/roads.test.js:846-847 ('the mod-data path' / 'our own network'), and `git diff 0fe2c05b..HEAD -- test/roads.test.js` shows those two lines as untouched CONTEXT — they are pre-delta, and the FIX-C commit edited the hunk around them without disturbing them. roads.test.js is also the file that legitimately owns the roads arc. So the fix cannot be silently disarmed; `npm test` reddens. This is a locality/duplication note, not a hole. Severity medium -> low.",
    "fixShape": "test/fixc_firstStart.test.js — insert immediately AFTER line 32 (the `function rebuildRoadless() { roadsSweepDue = true; }` match), verbatim:\n  // ...and BOTH arms of loadModRoads().then arm it - the producer alone\n  // is no fix: a retail Basic Roads install lands its network down the\n  // first arm and never returns to the second.\n  assert.equal((world.match(/rebuildRoadless\\(\\);/g) ?? []).length, 2, 'both arms of loadModRoads().then, and no third caller');\n  assert.match(world, /road pixels \\(Hazelnut\\)[^\\n]*\\)\\); rebuildRoadless\\(\\); return; \\}/, 'the Basic Roads arm arms the sweep before it returns');\n  assert.match(world, /terrainGen\\.setRoads\\(settlementsOf\\(maps\\), logRoads, roadSwitches\\);\\s*\\n\\s*rebuildRoadless\\(\\);/, 'and so does the generated-network arm');\nCOUNT IS 2. Verified 4 pass / 0 fail on HEAD; 3 pass / 1 fail with the call removed from either arm. If the lane would rather not duplicate roads.test.js:846-847, the honest minimum is to change the :32 message from 'the network's arrival marks the sweep' to 'the producer; the ARRIVAL arms are pinned in roads.test.js ROADS 25' — the sentence is the part that is currently untrue."
  },
  {
    "id": "PN-8",
    "upheld": false,
    "confidence": "high",
    "severity": "low (note)",
    "laneKey": "test/fixe_deathMenu.test.js",
    "greenOnHead": "yes",
    "redUnderMutant": "yes - DEATH_VIDEO_WATCHDOG_MS 30000 -> 59000 (inside the band) survives the HEAD pin and reddens the literal one",
    "claimPromisesTheNumber": "NO. The test name at :29 is 'a video that never settles is a BOUNDED wait - the watchdog navigates'. Both halves of that promise are pinned BEHAVIOURALLY and tightly, with an INJECTED value: :31-34 passes `watchdogMs: 1234` and a `setTimer` spy, then asserts `armed === 1234` and `frameHeld() === false`. The claim never promises 30000, and the band's own message ('a real death video ends well inside it') is exactly what a band is for. Per the lens rule, this is a note, not a finding.",
    "reasoning": "Re-derived: src/scenes/shared.js:1538 `DEATH_VIDEO_WATCHDOG_MS = 30000` with the reason in the comment ('ANIM0012 runs well under this'); :1544 takes it as a DEFAULT parameter, which is the seam the test exercises with 1234. test/fixe_deathMenu.test.js:36's band is indeed 45 s wide and 59000 passes it. But the value is port POLICY with no DFU referent, the surrounding behaviour (hold / release / watchdog-fires / finally) is genuinely pinned, and Home.md's 'A PIN MUST FAIL' governs assertions 'claiming to pin a DFU law'. The finder self-graded this low and reported it without running; I ran it and the mechanics check out, but the finding does not clear the bar the lens sets.",
    "corrections": "The finder's proposedFix is a placeholder ('<the chosen value>'); the value is 30000. Otherwise: the framing 'a value that makes a dead player stare at a frozen frame for a minute passes' overstates it — the watchdog is the UPPER bound on a hung video, and the real cost of a longer bound is bounded by how often a video hangs at all, which is never in the shipping path.",
    "fixShape": "Optional, one line. test/fixe_deathMenu.test.js — insert immediately BEFORE line 36 (the band, which STAYS as the explanatory second assertion), verbatim:\n  assert.equal(DEATH_VIDEO_WATCHDOG_MS, 30000, 'thirty seconds - ANIM0012 runs well under it, and a video that has not ended by then is not going to');\nVerified 4 pass / 0 fail on HEAD, 3 pass / 1 fail at 59000. Cheap and harmless; not worth opening a lane for on its own — fold it into whichever lane already touches this file."
  }
]
```

**Operational note for the parent — a shared-tree collision.** My first mutant run reported a failure on HEAD that was not mine: `src/render/waterSurface.js` in the primary working directory already carried all six PN-1 mutants (`WATER_LIFT = 0.8`, `WATER_OPACITY = 0.12`, `WATER_F0 = 0.5`, `SHORE_SOFTNESS = 0.9`, sky `[1,0,0]`/`[0,1,0]`) — refuter A working in the same checkout. `git status --short` was empty when I started and dirty two calls later. I reverted only my own edit, moved all mutation work into an isolated `git worktree` at HEAD, and did the rest there; refuter A's tree later came back clean on its own. Two refuters mutating one checkout will produce phantom "green on HEAD" and "red under mutant" results in both directions — give each refuter its own worktree next time.

`git status --short` for `src/`, `test/` and `tools/` in `/home/user/daggerfall-js-source` is **empty**. My worktree has been removed (`git worktree list` shows only the primary and the eight pre-existing `a65/lane-*` trees).