```json
[
  {
    "id": "MC-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived at HEAD: `grep -rn 'hudMessageSink' src` returns src/scenes/world.js:6438, src/scenes/worldModes.js:4937 (the dungeonContext option), src/scenes/dungeonContext.js:1349/:1354 and the setter at src/scenes/townTalk.js:1246 — src/scenes/exterior.js is absent, while the sibling line `townTalk.notebookSink = ...` is present at src/scenes/exterior.js:2896 in the same post-questBridge block whose comment says 'world.js:6422's line for this host'. I read exterior.js's charter at :2575-2600 as instructed: its 'What is deliberately absent here, and why' list names exactly two families — THE TALK SEAMS (addQuestTopics, the six rumor doors, the four %f-faction getters, removeNpcQuestor) and THE ITEM SEAMS plus videos/faces/disease/cure — and a struck third (CreateFoe's outdoor arm). The message sink is NOT on that list, and the charter explicitly says the notebook-bearing families 'DO have producers here, so they are wired'. The consumer is live on this route: exterior.js:1094 `notebook: () => questBridge.notebook ?? null` feeds questJournalHooks, so the Messages page exists and cannot be filled. DFU files unconditionally off a global (DaggerfallHUD.cs:371 inside SetMidScreenText :353-372; PopupText.cs:123 inside AddText), so no host there can fail to file.",
    "corrections": "Two overstatements. (1) 'permanently empty' is too strong for the mid-screen half: `midScreenText` is a MODULE SINGLETON (src/ui/midScreenText.js:135 `export const midScreenText = new MidScreenText()`), so the first time the player enters an interior/dungeon from ?exterior, dungeonContext.js:1354 assigns `midScreenText.onMessage` and never clears it — after that visit the surface files again (into the same questBridge notebook, since worldModes.js:4937 closes over this host's bridge). The permanently-dead half is `hud.onMessage`, which is per-townTalk-instance (townTalk.js:129 `const hud = new HudText()`). (2) The lens calls it a 'four-hosts miss of the delta's own new law' — accurate, but the sink predates AUDIT 64 F34 (F34 only widened the setter to arm the second surface); the missing exterior line is the AUDIT 63 F5-era gap that F34 doubled the cost of.",
    "fixShape": "One line in src/scenes/exterior.js, immediately above :2896 (before `townTalk.notebookSink = ...`, so the two sinks read as the pair world.js:6438/:6443 makes them): `townTalk.hudMessageSink = (t) => questBridge?.notebook?.addMessage(t);`. Nothing else — townTalk.js:1246's setter already arms both surfaces. While the lane is in there it should also close the singleton-lifetime hole the finder did not check: src/scenes/dungeonContext.js:1354 writes the module singleton's `onMessage` and no teardown path clears it (EVERY ALLOCATION HAS AN OWNER), so a dungeon left behind keeps its sink installed on the one global label; null it in dungeonContext's dispose beside :1349/:1354. Pin: keep the finder's half (b) — read src/scenes/world.js AND src/scenes/exterior.js and assert each contains an assignment to `townTalk.hudMessageSink`, naming the file in the message — and add the singleton-clear assertion rather than the behavioural double, since townTalk.js:1246 is already exercised by world.js's suite.",
    "severity": "medium",
    "laneKey": "src/scenes/exterior.js"
  },
  {
    "id": "MC-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "I re-read all eleven C# sites myself: PlayerActivate.cs:76 `RayDistance = 3072 * MeshReader.GlobalScale`, :81-88 the 128/150/256 reaches, :314 the ONE `Physics.Raycast(ray, out hit, RayDistance, playerLayerMask)`, then :328-331 (quest resource, a method-level `return`), :501-504 (ActivateStaticDoor), :686-689 (ActivateActionDoor), :709-712 (bulletin board), :761-763/:778-780/:788-791 (static/mobile NPC), :832-836 (ActivateMobileEnemy steal), :850-853 (ActivateLaddersAndShelves), :868-873 (ActivateLootContainer) and :935-945 (the corpse arm, whose Info branch at :935-941 speaks `youSeeADead*` with NO distance gate before the else-branch's CorpseActivationDistance test). Port side: `grep -rn 'TOO_FAR_AWAY_TEXT' src` gives exactly five speaking sites (worldModes.js:2131 via bulletinBoard.js's re-export, townTalk.js:612/:619/:630, mobileEnemyActivate.js:109), and src/player/activate.js:246 `if (d > (target.distance ?? DEFAULT_ACTIVATION_DISTANCE)) continue;` drops an out-of-reach target before any host can see it — so on the door/action-door/ladder/loot/corpse families the port answers a too-far click with silence where DFU speaks. Crucially, DFU's Hit Checks order is NOT a precedence ladder: :314 returns exactly ONE hit (the nearest collider), and every check below dispatches on that same hit, so 'a far door pre-empting a near foe' is impossible there — which is precisely what the proposed fix would introduce in the port.",
    "corrections": "Three. (1) The header list is overread. src/ui/midScreenText.js:9-12 enumerates DFU's SetMidScreenText callers to justify minting the surface ('every SetMidScreenText caller ... was folded into the popup queue'); it is not a claim that eleven port sites exist, and bible/10-UI/UI-Arc.md:11015-11021 records exactly which were re-pointed (:780, :790, :834, :711, plus the mode line, the lockpick ladder and youHaveNoArrows). (2) It is FIVE unported refusals, not six: :330 is already RECORDED AS A DELIBERATE DELTA in two places — src/scenes/worldModes.js:4676-4679 ('DELTA (recorded): C# prints \"You are too far away.\" and aborts the whole activation ... the port's picker simply does not select it, so a too-far click falls through to whatever is behind it and says nothing') and bible/06-Systems/Quest-Arc.md:2519-2523. Strike :330 from the finding. (3) THE PROPOSED FIX IS WRONG AND WOULD CHANGE ACTIVATION ORDER. A second `pickActivatableHitFar` run only 'where the near pick answers null' sits at the exact ladder slot now occupied by `_enemyArm(RAY_DISTANCE)` (dungeon.js:254, worldModes.js:4689/:5147, world.js:7503, exterior.js:3847), so a door 20 units off would consume the click and silence the un-gated Info line DFU speaks for a foe at 5 — DFU's single ray would have hit the FOE, never the door. The tree already owns the correct idiom and writes down why: src/scenes/worldModes.js:3979-3986 gives bulletin boards `distance: RAY_DISTANCE` at :3986 so they compete in the ONE pick by distance, and puts the reach test inside `activateBulletinBoard` (:2131), 'exactly as C# does'.",
    "fixShape": "Do it the bulletin board's way, not with a second pick. (a) Raise the reach of the affected target families to the RAY's in the place each is minted, so one pick still decides by distance: src/player/activate.js:133 `activationTargets(objects, distance = DOOR_ACTIVATION_DISTANCE)` gains a RAY_DISTANCE default at its call sites (dungeon.js:238, worldModes.js:4655/:5117), worldModes.js:3956-3958's exterior door targets, worldModes.js:4666-4669's ladder/shelf targets, and the corpse/pile target builders (hostCombat.js's lootTargets, world.js:7459-7461, exterior.js:3810-3811). (b) Put the reach test in the HANDLER that dispatches the key, where DFU puts it: the door handler (worldModes.js:4101-4110 tryEnter's `activateStaticDoor` arm = :503, and the ActionSystem's action-door arm = :688), the `ladder:`/`shelf:` arms (worldModes.js:4691+ = :852), the `loot:`/`droppedLoot:` arms (dungeon.js:248-252, world.js:7474-7490, exterior.js:3826-3843 = :872) and the `corpse:` arm (= :940, keeping DFU's exemption at :868-869 and the un-gated Info branch at :935-941 verbatim), each speaking `setMidScreenText(TOO_FAR_AWAY_TEXT)` and consuming. (c) MANDATORY companion, or (a) regresses AUDIT 63 F33: the far foe arm must now compete by distance too — replace the two calls with ONE `_enemyArm(RAY_DISTANCE, _pick?.distance ?? Infinity)` at dungeon.js:241/:254, worldModes.js:4682/:4689 and :5142/:5147, world.js:7472/:7503, exterior.js:3822/:3847; that collapse is literally DFU's one ray plus one ActivateMobileEnemy and removes the near/far double-call the pre-gate forced. (d) Leave :330 alone — recorded. Two things the finder did not check that this lane must not reproduce: src/scenes/townTalk.js:598-610's person arm ALREADY has the un-unified far shape (it consumes at RAY_DISTANCE from world.js:7472 / exterior.js:3822 ahead of the loot and door picks, and `rayPersonDistance` at townTalk.js:92 takes no collider), so a townsperson on the ray line beyond a wall swallows a click on a door at arm's length — fold it into the same single distance comparison; and dungeonContext.js:3593's `pickActivatable(eye, dir, api.lootTargets(), collider)` is a fifth ladder on the same seam.",
    "severity": "medium",
    "laneKey": "src/scenes/worldModes.js"
  },
  {
    "id": "MC-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Confirmed at HEAD: src/render/dynamicSkiesBridge.js:60 `export const dynamicMoonlight = (moons) => (moons ? moonlightTerm(moons) : null);` and `grep -rn 'dynamicMoonlight' src test tools` returns that one line only. src/scenes/shared.js:441 `return dynamicMoons ? moonlightTerm(dynamicMoons) : null;` is the byte-for-byte body, inside the `moonlight()` the helper's doc names, and shared.js:18 imports only the two siblings `cloudsStateUnderMod, dynamicMoonState`. `git log -S'dynamicMoonlight'` puts it in the delta (d2c35fe8, the FIX-C/D/E/F + DS2 commit). It is a port-side helper, not a DFU member, so the cite is Home.md 17e's closing sentence ('If two files legitimately need it, one exports and the other imports - never two literals') rather than a C# line — the finder is honest about that.",
    "corrections": "The pinShape is wrong in two ways and the fix has an unstated dependency. (1) 'assert `moonlightTerm(` appears exactly once in shared.js' cannot hold: the name also appears in the import list at shared.js:16 and in the enhanced-dome arm at :437, so the count after the fix is two, not one. (2) More importantly, test/moonlight.test.js:101 ALREADY TEXT-PINS the inline form — `assert.match(shared, /moonlight\\(\\)\\s*\\{\\s*if \\(enhancedSky\\?\\.state\\) return moonlightTerm\\(enhancedSky\\.state\\);[\\s\\S]*?return dynamicMoons \\? moonlightTerm\\(dynamicMoons\\) : null;/)` — and it is green today (`node --test test/moonlight.test.js` passes). Rewriting :441 without rewriting that regex turns a live pin red, which the finding does not mention. Severity 'low' is right; this is a one-home tidy with no player-visible consequence, since the two expressions are identical.",
    "fixShape": "Wire it, don't delete it. src/scenes/shared.js:18 — add `dynamicMoonlight` to the existing `import { cloudsStateUnderMod, dynamicMoonState } from '../render/dynamicSkiesBridge.js';`. src/scenes/shared.js:441 — `return dynamicMoonlight(dynamicMoons);`. THEN test/moonlight.test.js:101 — replace the tail of the regex `return dynamicMoons \\? moonlightTerm\\(dynamicMoons\\) : null;` with `return dynamicMoonlight\\(dynamicMoons\\);` and keep the 'classic answers null' assertion message. Pair it with a behavioural pin on the helper in the same file (`dynamicMoonlight(null) === null`, and a real dynamicMoonState output returns exactly what `moonlightTerm` returns for it) so the export is not merely name-checked. Do not touch src/render/enhancedSky.js:332.",
    "severity": "low",
    "laneKey": "src/scenes/shared.js"
  },
  {
    "id": "MC-4",
    "upheld": true,
    "confidence": "high",
    "reasoning": "The mechanical claim holds: `grep -rn 'keyboardLook' src` gives world.js:7031-7032, exterior.js:3469-3470, dungeon.js:592-593 and nothing in src/scenes/interior.js, whose import line at :27 is `import { lookScale, lookInvert } from '../ui/lookSettings.js';   // AUDIT: the FOURTH host the SETT slice missed`. interior.js does own the machinery — `new LookFilter()` at :109, `const keys = new Set()` at ~:168 with `keys.add(e.code)` in its keydown, `lookFilter.add(e.movementX * lookScale(), ...)` at ~:256 and `lookFilter.tick(dt, cam)` at :290 inside `if (!gamePaused())`. I confirmed it IS a live route, as asked: src/main.js:72 `if (params.has('interior')) { await ensureData(); return bootInterior(...); }`, alongside ?dungeon/?world/?exterior at :73-75. DFU's side is as cited (InputManager.cs:1854-1865 sets keyboardLookX/Y; UpdateLook :1510-1511 hands them to PlayerMouseLook).",
    "corrections": "Three, and together they cut the severity. (1) 'the four look hosts' is not THE FOUR HOSTS RULE: Home.md names scenes/exterior.js, scenes/world.js, scenes/worldModes.js and scenes/dungeonContext.js. interior.js is a standalone dev scene (main.js:78 'Dev scenes stay one param away'), as dungeon.js is; the real four-of-four set here is the four LookFilter owners (world/exterior/dungeon/interior), which is a different set. Call it a look-filter-owner miss, not a four-hosts miss. (2) interior.js has NO player motor — its W/A/S/D at ~:296-300 drives the camera directly at 3/12 units per second, a free-fly inspection cam — so the consequence is a dev-scene convenience, not a parity gap a player can hit; the InputManager argument has no counterpart in a scene DFU does not have. (3) THE PROPOSED FIX WOULD TURN A LIVE PIN RED. test/fixf_keybinds.test.js:37-39 already text-pins the three-line inline arm across exactly `['src/scenes/world.js', 'src/scenes/exterior.js', 'src/scenes/dungeon.js']` with a regex anchored on `else lookFilter.tick(dt, cam);` followed by `const kb = keyboardLook(keys); if (kb.x || kb.y) lookFilter.add(...)`. Extracting `applyKeyboardLook` into lookSettings.js breaks that regex in all three hosts at once. The pin is green today.",
    "fixShape": "Cheapest correct shape, and the one the shipped pin already wants: add the SAME two lines verbatim to src/scenes/interior.js immediately after `else lookFilter.tick(dt, cam);` at :290 — `const kb = keyboardLook(keys);` / `if (kb.x || kb.y) lookFilter.add(kb.x * keyboardLookRate() * dt, kb.y * keyboardLookRate() * dt * lookInvert());` — and extend the import at interior.js:27 to `import { lookScale, lookInvert, keyboardLookRate } from '../ui/lookSettings.js';` plus `keyboardLook` from '../ui/input.js' (interior.js already imports actionOf from there). Then add `'src/scenes/interior.js'` to the host array at test/fixf_keybinds.test.js:37; the existing regex matches interior.js's `if (false) lookFilter.settle(); else lookFilter.tick(dt, cam);` tail unchanged. Do NOT do the `applyKeyboardLook` extraction unless the lane also rewrites that regex — and if it does, it must rewrite test/fixf_keybinds.test.js:58's and :73's host arrays' sibling assumptions in the same pass. Alternatively, if the lane judges a fly-cam scene out of scope, FLAG interior.js by name in FIX-F's record (THE FOUR HOSTS RULE accepts wired-or-flagged); today it is neither.",
    "severity": "low",
    "laneKey": "src/scenes/interior.js"
  },
  {
    "id": "MC-5",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "Same finding as CV-3 in findings-constants.md — judged under CV-3 and reported here for completeness. Evidence re-derived at HEAD (after a concurrent agent's constant mutants in src/render/waterSurface.js were reverted; `git status --short` is empty now): `git show HEAD:src/render/waterSurface.js` carries `export const WATER_OPACITY = 0.82;` at :54 and `export const WATER_SCROLL_TILES_PER_SEC = 0.05;` at :66 under a docstring naming scenes/dungeon.js as the origin; src/scenes/dungeon.js:71-72 declares `const WATER_COLOR = [1, 1, 1, 0.82];` and a locally shadowing `const WATER_SCROLL_TILES_PER_SEC = 0.05;`; src/scenes/worldModes.js:250-251 declares `DUNGEON_WATER_COLOR`/`DUNGEON_WATER_SCROLL` with the same two numbers. Neither dungeon host imports from render/waterSurface.js, so three names carry one pair of values.",
    "corrections": "The two 0.82s are not obviously one law. WATER_OPACITY is the ENHANCED water surface shader's base opacity before Fresnel raises it (waterSurface.js:53-54), while the dungeon hosts' 0.82 is the alpha of a classic textured water QUAD — different pass, different member, agreeing by coincidence of taste. Only the scroll rate carries a written single-source claim ('the dungeon water's own rate (scenes/dungeon.js), so a river and a dungeon pool crawl alike'), and that one is a genuine 17e violation. Also neither is a DFU member, so the cite is 17e's closing sentence, not the rule's main clause — the finding's own framing concedes this. Severity 'low' is right.",
    "fixShape": "Split it. (a) The scroll rate is one law and has a written claim: delete src/scenes/dungeon.js:72 and src/scenes/worldModes.js:251 and import `WATER_SCROLL_TILES_PER_SEC` from '../render/waterSurface.js' at both, updating the draw sites at dungeon.js:905-907 and worldModes.js:5720-5722. (b) The opacity is a judgement call — either export a `DUNGEON_WATER_COLOR = Object.freeze([1, 1, 1, WATER_OPACITY])` from waterSurface.js beside :54 and import it at both dungeon hosts (both want the identical array), or leave the two 0.82s in place and write down at waterSurface.js:53 that the classic dungeon quad's alpha is a SEPARATE number that happens to match, in the shape the AUDIT 64 PARRY_VOLUME row uses. Keep the finder's pin advice: assert the hosts no longer carry the literals and DO import the names, and do not pin `assert.equal(WATER_SCROLL_TILES_PER_SEC, 0.05)` — that restates the port and survives the drift.",
    "severity": "low",
    "laneKey": "src/render/waterSurface.js"
  },
  {
    "id": "MC-6",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Both exports are caller-less in production at HEAD. `grep -rn 'tilemapHasWater|waterCoverage' src test tools` shows src/render/waterSurface.js:115 and :168 as the only src occurrences outside test/water.test.js; the two hosts took other routes exactly as claimed — src/scenes/exterior.js:678 `tilemapRectHasWater(tilemapBytes, tilemapDim, loc.width * GROUND_TILE_DIM, loc.height * GROUND_TILE_DIM)` (WATER-AUDIT L1, because the zero padding converts to water) and src/scenes/world.js:838/:1359 `buildWaterIndices(...)`, which answers null at waterSurface.js:160 when there is no water. The file is delta-fresh (c03e047b WATER1, then 22e19bd5 WATER-AUDIT lane A), so it postdates OT1 c's F301 sweep as claimed. The hollow-pin half is sharper than the finding states: `waterCoverage` (:168-173) is a JS twin of the GLSL `coverage()` at waterSurface.js:309-314 IN THE SAME FILE, so a one-character mutation of the shipping shader reddens nothing — A PIN MUST FAIL, exactly.",
    "corrections": "One real correction to the deletion. `tilemapHasWater` is not merely superseded dead weight: test/water.test.js:302-303 uses it as the deliberate CONTRAST that documents WATER-AUDIT L1 — `assert.equal(tilemapHasWater(padded), true, 'the whole map says yes (the padding)')` against `assert.equal(tilemapRectHasWater(padded, 32, 16, 16), false, 'the town says no')`. Deleting the export deletes the only place the bug's shape is written in an executable form. The finding's 'delete both and drop them from the import list' would quietly retire that law. Also note :73-76 gives tilemapHasWater four table-behaviour assertions that the rect twin does not duplicate. Severity 'low' is right; there is no player-visible consequence either way.",
    "fixShape": "src/render/waterSurface.js — delete `waterCoverage` (:167-173) and remove it from test/water.test.js:12's import list along with its five assertions at :67-72; if the shader's bilinear law is wanted pinned, replace them with a framebuffer read-back against the water program at known tile fractions (Home.md's screen-projection ground-truth method), not another CPU twin. For `tilemapHasWater` (:113-118): do NOT delete it blind — either keep the export and record at :114 that its one live reader is the WATER-AUDIT L1 contrast in test/water.test.js:302-303, or delete it and rewrite that contrast as `tilemapRectHasWater(padded, 32, 32, 32)` vs `tilemapRectHasWater(padded, 32, 16, 16)` so the padding law survives the removal. Pin, as the finder says, is the sweep and not an assertion about a deleted body: extend the F301/OT1 c dead-export sweep to assert every `export function`/`export const` in src/render/waterSurface.js has at least one non-test reader under src/ — and note that the sweep must whitelist or resolve tilemapHasWater whichever way the lane goes.",
    "severity": "low",
    "laneKey": "src/render/waterSurface.js"
  }
]
```