```json
[
  {
    "id": "MC-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived at HEAD: `grep -rn hudMessageSink src/` returns exactly four sites — the setter at src/scenes/townTalk.js:1246, the feeders at src/scenes/world.js:6438, src/scenes/worldModes.js:4937 (inside `tryEnterDungeon`, into `buildDungeonContext`'s opts) and the consumers at src/scenes/dungeonContext.js:1349/:1354. src/scenes/exterior.js:2896 sets `notebookSink` in the post-questBridge block and never sets `hudMessageSink`; world.js does both at :6438/:6443. exterior.js:1094 does wire `notebook: () => questBridge.notebook ?? null` into questJournalHooks, so the Messages page is reachable on that route. DFU reads the Notebook off the `GameManager.Instance.PlayerEntity` global in both tails (DaggerfallHUD.cs:371 inside SetMidScreenText :353-372, PopupText.cs:123 inside AddText), so no host in DFU can fail to file. Nothing in Audit-63.md, Audit-64.md or Port-Ledger.md section A records this as deliberate; Audit-64's own HUD entry (:199-201) records the opposite intent.",
    "corrections": "Two overstatements. (1) 'on the ?exterior route hud.onMessage AND midScreenText.onMessage both stay null' is half wrong: `midScreenText` is a MODULE SINGLETON (src/ui/midScreenText.js:135 `export const midScreenText = new MidScreenText();`) and src/scenes/dungeonContext.js:1354 overwrites `midScreenText.onMessage` on every dungeon build, fed from worldModes.js:4937 on BOTH outdoor hosts. So on ?exterior the mid-screen label begins filing the moment the player first enters a dungeon and keeps filing after exit (last writer wins; nothing ever clears it). What stays permanently unfed on ?exterior is `hud.onMessage` alone — townTalk's own HudText — i.e. every PopupText line the outdoor host AND the interior arm speak (worldModes drives townTalk's hud directly for interiors, townTalk.js:1256-1260). (2) 'the Messages page is permanently empty' follows from (1) and is therefore wrong; it is empty of everything the outdoor and interior arms say, which is still a real, observable divergence. Separately the finder's pinShape (b) already exists in the tree and should be extended, not re-invented — test/audit24_wave22.test.js:42-52 is literally titled 'the two hosts hand their HudText the notebook sink' and pins world.js alone.",
    "fixShape": "src/scenes/exterior.js: insert one line immediately ABOVE the existing `townTalk.notebookSink = ...` at :2896, inside the same post-questBridge block, mirroring world.js:6438/:6443's order — `townTalk.hudMessageSink = (t) => questBridge?.notebook?.addMessage(t);` with the same lazy `questBridge?.` guard the sibling line already uses (the closure is evaluated per message, so ordering against questBridge's construction is a non-issue, exactly as for notebookSink). Nothing else changes: the setter at townTalk.js:1246 already arms both surfaces. PIN: extend the EXISTING assertion at test/audit24_wave22.test.js:42-43 from `assert.match(rd('src/scenes/world.js'), /townTalk\\.hudMessageSink = .../)` into `for (const h of ['src/scenes/world.js', 'src/scenes/exterior.js']) assert.match(rd(h), ..., h)` — same `test()` call, so bible/09-Testing/Testing.md needs no row/total edit and test/manifest.test.js stays green (it pins per-file `^test\\(` counts AND the suite total, so any NEW test() call reddens it unless the doc moves in the same commit). Regression sweep done: no other host reads `townTalk.hudMessageSink`; the getter at :1247 returns `hud.onMessage` and has no caller; the singleton re-point by dungeonContext is pre-existing and unchanged by this line. Verified green at HEAD: `node --test test/audit24_wave22.test.js` 7/7.",
    "severity": "medium",
    "laneKey": "src/scenes/exterior.js"
  },
  {
    "id": "MC-2",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "The underlying defect is real: src/player/activate.js:246 `if (d > (target.distance ?? DEFAULT_ACTIVATION_DISTANCE)) continue;` drops an out-of-reach target, so no caller can speak DFU's refusal, while PlayerActivate.cs:314 rays to RayDistance (:76, 76.8) and gates INSIDE each handler. I probed it: a door AABB 8 units down the ray at `distance: DOOR_ACTIVATION_DISTANCE` returns null, and the SAME target at `distance: RAY_DISTANCE` returns `{key:'door:1', distance:8}` — so the refusal is reachable with the existing helper. But the finding is materially wrong on scope, on the host count, and on one of the six, and its proposed fix is not DFU's shape. Nothing in Audit-63/64 or Ledger A records it, but worldModes.js:4677-4680 and bible/06-Systems/Quest-Arc.md:2521-2524 record ONE of the six explicitly.",
    "corrections": "SIX corrections, the last three fix-regression. (1) THE HEADER CLAIMS NOTHING. src/ui/midScreenText.js:9-12 enumerates DFU's eleven SetMidScreenText call sites to justify why the surface exists ('The port had only the first, so every SetMidScreenText caller ... was folded into the popup queue'); it never asserts the port has eleven callers. The delta's own record (bible/10-UI/UI-Arc.md:11015-11024) lists exactly the callers re-pointed and claims no more. (2) THE DEFECT PREDATES THE DELTA. `git log -S` puts activate.js:246's pre-gate at 6b3af1ea (AUDIT 54), and `git merge-base --is-ancestor 6b3af1ea 0fe2c05b` succeeds. The finder's own coverage block excludes other pre-delta gaps by exactly this rule ('arrestFlow's positionPlayerAtLocationEntrance ... outside this audit's delta'; 'makeScrollerBar ... all predate the delta'). Severity drops to low on that ground alone. (3) FIVE, NOT SIX. PlayerActivate.cs:330 (quest resource) is a RECORDED delta, stated twice — in the code at src/scenes/worldModes.js:4677-4680 ('DELTA (recorded): C# prints \"You are too far away.\" and aborts the whole activation; the port's picker simply does not select it, so a too-far click falls through to whatever is behind it and says nothing') and in bible/06-Systems/Quest-Arc.md:2521-2524. A lane must NOT port it. (4) PER-FAMILY REACHABILITY, the brief's question. :330 quest resource — family EXISTS (questFlatTargets, worldModes.js:1314-1325, pushed at :4680 interior and :5125 dungeon); recorded, skip. :503 static door — EXISTS (doorTargets at worldModes.js:3956-3958, no `distance` so 3.2; handled at :4110 activateStaticDoor); PORTABLE. :688 action door — EXISTS (`activationTargets(ctx.actions.objects)` at worldModes.js:4655, :5118, dungeon.js:238); PORTABLE but ONLY for objects `isActionDoorObject` answers true for (src/world/actionSystem.js:106, the port of ActionDoorCheck) — the SAME pool carries DFU's action-RECORD family, and PlayerActivate.cs:380-383 gates those `hit.distance <= DefaultActivationDistance` with NO refusal, so a blanket widening would make every lever and moving platform speak a line DFU never speaks. :852 ladders and shelves — the finder says 'the ladders in dungeon.js:240, worldModes.js and both exterior hosts'; that is WRONG on three of four. `grep -rn 'ladders\\|climbLadder' src/` finds ladders in exactly one place — src/scenes/interiorContext.js:285/:362 → `ladder:` targets at worldModes.js:4656-4658 → climbLadder at :4690-4707 — and shelves likewise only there (`shelf:` at :4642 → openShelf at :1600). dungeon.js, dungeonContext.js, world.js and exterior.js have NO ladder or bookshelf family at all; that leg is reachable in one host, not four. :872 loot container — EXISTS (`loot:`/`container:`/`shelf:`/`droppedLoot:`); PORTABLE. :940 corpse — EXISTS (src/scenes/corpseMarker.js:132-149, `distance: CORPSE_ACTIVATION_DISTANCE`); PORTABLE for the non-Info arm only. (5) THE CORPSE/INFO QUESTION: NO, the too-far line is never spoken for a corpse in Info mode. ActivateLootContainer's gate at PlayerActivate.cs:866-869 exempts `LootContainerTypes.CorpseMarker` by name, and the Info arm at :927-935 speaks youSeeADeadPerson/youSeeADead and RETURNS before the :938 CorpseActivationDistance gate — a corpse is readable in Info at the full RayDistance. The finder got this right in its proposedFix but it is moot: `grep -rn 'youSeeADead' src/` is empty, the port has no corpse Info arm, so there is no DFU exception to preserve and the lane must not invent one. (6) 'consume the activation (DFU's return/break)' — only :330 returns from Update; :503/:688/:852/:872/:940 return from their own helper and the Update ladder continues, harmless only because every check below reads the same `hit.transform`. (7) THE PROPOSED `pickActivatableHitFar` EXPORT IS THE WRONG SHAPE and unnecessary. My probe shows the existing helper already answers once the target carries DFU's ray reach, and that is ALREADY the tree's pattern for this exact law — worldModes.js:3977-3986 gives bulletin boards `distance: RAY_DISTANCE` with the comment 'Their reach here is the RAY's, not the board's ... whose failure speaks the refusal instead of falling through. A board the player can see but not reach therefore consumes the click, exactly as C# does.' A second pick export walks the target list twice per activation and mints a rival seam under ONE DFU MEMBER, ONE EXPORT. (8) The pinShape passes 'a null collider'; activate.js:255 calls `collider.raycast(...)` unconditionally and throws `TypeError: Cannot read properties of null (reading 'raycast')`. Use `{ raycast: () => Infinity }`, the shape test/audit63_guilds_court.test.js:355 already uses.",
    "fixShape": "NO new export in src/player/activate.js. Optionally make the return additive — `return { key: bestKey, distance: bestDist, reach: bestReach };` at :265 — so ladders can compare; the only two tests that read the return (test/audit63_guilds_court.test.js:360-362 and test/audit24_wave38.test.js:306) read `.key`/`.distance` and stay green. WIDEN PER FAMILY AT THE TARGET BUILD SITES, each to RAY_DISTANCE while carrying its real reach: worldModes.js:3956-3958 (static doors, real reach DOOR_ACTIVATION_DISTANCE); worldModes.js:4655 / :5118 / dungeon.js:238 (widen ONLY entries where `isActionDoorObject(o)` — src/world/actionSystem.js:106 — leave action RECORDS at DEFAULT_ACTIVATION_DISTANCE and silent, PlayerActivate.cs:380-383); worldModes.js:4642 and :4656 (`shelf:` / `ladder:`, real reach DEFAULT_ACTIVATION_DISTANCE); src/scenes/corpseMarker.js:144-148 (real reach CORPSE_ACTIVATION_DISTANCE); src/scenes/droppedLoot.js:284 and interiorCtx containers (TREASURE_ACTIVATION_DISTANCE). THEN SPEAK IT in each ladder, `setMidScreenText(TOO_FAR_AWAY_TEXT)` and stop, placed ABOVE the far enemy arm and above the fall-through: worldModes.js:4681-4690 (interior), worldModes.js:5140-5150 (dungeon), dungeon.js:240-246, worldModes.js:4097-4110 (tryEnter's static-door tail), exterior.js:3810-3851 and world.js:7460-7500 (the corpse/drop picks). DO NOT touch the `questflat:` reach at worldModes.js:1322 — recorded delta. FOUR-HOSTS REGRESSION the finder did not check: `exteriorActivationDistance` (worldModes.js:3993-3996) is the same pick and feeds `_rivalDist` in BOTH exterior hosts (exterior.js:3816-3821 and world.js's twin), so widening the door reach changes what it returns from Infinity to the far door's distance. The NEAR enemy arm still behaves (its reach is 3.2), but the FAR arm — exterior.js:3849, world.js's twin, worldModes.js:4691 and :5148, dungeon.js:256 — runs with `nearerThan = Infinity`, so a foe 20 units off would still eat a click DFU gives to a door at 5. Order the refusal BEFORE the far enemy arm and pass the refusing pick's distance into it as `nearerThan`, or that pre-existing mis-order gets louder under the widening. TEST CHURN: test/audit24_wave38.test.js:306 pins the literal text `pickActivatableHit(cam.pos, useFwd, corpseTargets, collider)` in BOTH exterior hosts and :308 pins its negative — keep that call site's text byte-identical (widen inside corpseMarker.js instead). Any NEW `test(` call needs a bible/09-Testing/Testing.md row + suite total in the same commit or test/manifest.test.js reddens.",
    "severity": "low",
    "laneKey": "src/scenes/worldModes.js"
  },
  {
    "id": "MC-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Confirmed at HEAD: `grep -rn 'dynamicMoonlight' src test tools` returns exactly one line, the declaration at src/render/dynamicSkiesBridge.js:60 `export const dynamicMoonlight = (moons) => (moons ? moonlightTerm(moons) : null);`. src/scenes/shared.js:441 is `return dynamicMoons ? moonlightTerm(dynamicMoons) : null;` — byte-for-byte the helper's body, inside `moonlight()`, the function the helper's own doc names ('The world's moon term off the mod's moons - DS1's moonlight()'). shared.js:18 imports `cloudsStateUnderMod` and `dynamicMoonState` from that module and not the third export, so the DS2 lane (d2c35fe8, in the delta) wired two of three and left the inline twin standing. This is port-side presentation with no C# member, so the citation is Home.md 17e and that is the right one. Not recorded in Audit-63.md, Audit-64.md or Ledger A.",
    "corrections": "The pinShape is arithmetically wrong and would be red before AND after the fix. `grep -n moonlightTerm src/scenes/shared.js` returns THREE lines today — the import at :16, the enhanced-dome arm at :437, the mod arm at :441 — so 'assert `moonlightTerm(` appears exactly once' fails now and still fails after (import + :437 remain, and :437 legitimately needs it). Count the CALL form and expect 2 today / 1 after, or better, assert the literal `return dynamicMoonlight(dynamicMoons);` at the mod arm. Second correction: the finder's fallback 'delete the export (OT1 c's treatment of F301's eleven dead exports)' leans on a sweep that does not exist — `grep -rn F301 test/` is empty, and test/flagsweep.test.js walks src/ for FLAGGED/INTERIM sentences, not for exports. See MC-6.",
    "fixShape": "Wire, do not delete — the null guard is the arm a future host forgets. src/scenes/shared.js:18: add `dynamicMoonlight` to the existing `import { cloudsStateUnderMod, dynamicMoonState } from '../render/dynamicSkiesBridge.js';`. src/scenes/shared.js:441: replace with `return dynamicMoonlight(dynamicMoons);`. LEAVE `moonlightTerm` on the enhancedSky import at :16 — the enhanced-dome arm at :437 still calls it directly, and dropping it from the import list would break that line. Regression sweep: `moonlight()` is a method on the sky object shared.js returns; its callers read a term or null and are unaffected because `dynamicMoonlight` is the identical expression. PIN: put both assertions (the source shape, plus `dynamicMoonlight(null) === null` and identity against `moonlightTerm` for a non-null state) inside an EXISTING `test()` body in the sky/dynamic-skies suite rather than adding a new `test(` call, or bible/09-Testing/Testing.md's per-file row and suite total must move in the same commit (test/manifest.test.js:50-62).",
    "severity": "low",
    "laneKey": "src/scenes/shared.js"
  },
  {
    "id": "MC-4",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Confirmed at HEAD: `grep -rn 'new LookFilter' src/` returns exactly four owners — world.js:1551, exterior.js:2090, dungeon.js:146, interior.js:109 — and `grep -rn keyboardLook src/` shows the arm at world.js:7031-7032, exterior.js:3469-3470 and dungeon.js:592-593 only. interior.js has every dependency and no consumer: `keys` Set at :169 filled with `e.code` at :195, `lookFilter.add` from pointer lock at :256 and from touch at :260, `lookFilter.tick(dt, cam)` at :290 inside `if (!gamePaused())`, `lookScale/lookInvert` imported at :27 under the comment 'AUDIT: the FOURTH host the SETT slice missed'. DFU has one InputManager for the whole game (InputManager.cs:1854-1865 sets keyboardLookX/Y, :1510-1511 hands them to the look), so no scene can lack the arm. Not recorded in Audit-63/64 or Ledger A.",
    "corrections": "THE PROPOSED FIX WOULD REDDEN A LIVE PIN — this is the finding's real hazard and the finder did not check it. test/fixf_keybinds.test.js:37-39 asserts the EXACT inline text in all three hosts: `/else lookFilter\\.tick\\(dt, cam\\);\\s*\\n(?:\\s*\\/\\/[^\\n]*\\n)*\\s*const kb = keyboardLook\\(keys\\);\\s*\\n\\s*if \\(kb\\.x \\|\\| kb\\.y\\) lookFilter\\.add\\(kb\\.x \\* keyboardLookRate\\(\\) \\* dt, kb\\.y \\* keyboardLookRate\\(\\) \\* dt \\* lookInvert\\(\\)\\);/`. Extracting `applyKeyboardLook(lookFilter, keys, dt)` into ui/lookSettings.js and replacing the three copies turns that assertion RED for all three files. I ran it at HEAD: `node --test test/fixf_keybinds.test.js` is 3/3 green today. Two smaller corrections: (a) the law cited is wrong — interior.js is NOT one of THE FOUR HOSTS (Home.md names exterior.js, world.js, worldModes.js, dungeonContext.js), and worldModes/dungeonContext own no LookFilter at all; the real host set for this seam is 'every file that owns a LookFilter', which is four files and which FIX-F's own pin enumerates as three. Good finding, wrong law. (b) 'it is a full look host by the port's own reckoning' is overstated: ?interior has no player motor — interior.js:294-299 moves `cam.pos` directly off W/A/S/D. It is a fly-cam probe route, which is why the severity is low, not why the arm should be skipped.",
    "fixShape": "MINIMAL, NO REFACTOR. src/scenes/interior.js:27 — add `keyboardLookRate` to the existing `import { lookScale, lookInvert } from '../ui/lookSettings.js';`. src/scenes/interior.js:37 — add `keyboardLook` to the existing `import { swallowBrowserKey, actionOf } from '../ui/input.js';`. Then paste world.js:7031-7032 verbatim immediately after `else lookFilter.tick(dt, cam);` at src/scenes/interior.js:290, inside the existing `if (!gamePaused())` block (interior.js already carries the pin's own anchor line, so the regex matches unchanged). Finally add `'src/scenes/interior.js'` to the host array at test/fixf_keybinds.test.js:37 — same `test()` call, so bible/09-Testing/Testing.md and test/manifest.test.js are untouched. If a later lane still wants the three-copy extraction, it must rewrite that regex in the SAME commit; do not let this lane carry both changes.",
    "severity": "low",
    "laneKey": "src/scenes/interior.js"
  },
  {
    "id": "MC-5",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Judged under CV-3 in findings-constants.md, as the brief directs — MC-5 is CV-3 plus an opacity half. The scroll-rate half is confirmed and is CV-3's: src/render/waterSurface.js:66 `export const WATER_SCROLL_TILES_PER_SEC = 0.05;` (whose doc names scenes/dungeon.js as the origin), src/scenes/dungeon.js:72 `const WATER_SCROLL_TILES_PER_SEC = 0.05;` (spent at :907) and src/scenes/worldModes.js:251 `const DUNGEON_WATER_SCROLL = 0.05;` (spent at :5722) — three literals, two names, three modules, no import between them; `grep` confirms the only src/ references to the export outside its own file are none. The opacity half I refute: the two 0.82s are not the same quantity.",
    "corrections": "SAME AS CV-3 for the scroll rate. The OPACITY half is MC-5's addition and is wrong. `WATER_OPACITY` (waterSurface.js:54) is the WATER1 surface's Fresnel FLOOR — the shader spends it as `float alpha = (uOpacity + (1.0 - uOpacity) * F) * edge;` (waterSurfaceFs) beside Schlick's F and the shore feather. `WATER_COLOR = [1, 1, 1, 0.82]` (dungeon.js:71) and `DUNGEON_WATER_COLOR` (worldModes.js:250) are a FLAT vertex alpha handed to `renderer.drawWater`, a different pass with no Fresnel, no feather and no wave field. They agree numerically and mean different things; merging them means a future Fresnel tune to WATER_OPACITY silently repaints every dungeon pool three metres away. That is the false single-source Home.md 17e is not asking for. Also correcting the citation for both halves: 17e's ONE DFU MEMBER, ONE EXPORT is scoped to DFU members and their constants ('Before porting a DFU class or method, grep the tree for its name AND its constants'); neither number is DFU's — Port-Ledger.md section A's WATER1 row records the whole surface as the port's own departure — so the law carries by analogy for the scroll rate (where the module doc itself asserts cross-module equality and nothing enforces it) and does not carry the opacity at all.",
    "fixShape": "EXACTLY CV-3's, scroll rate only. Delete `const WATER_SCROLL_TILES_PER_SEC = 0.05;` at src/scenes/dungeon.js:72 and `const DUNGEON_WATER_SCROLL = 0.05;` at src/scenes/worldModes.js:251; import `WATER_SCROLL_TILES_PER_SEC` from `../render/waterSurface.js` in both and spend it at dungeon.js:907 and worldModes.js:5722 (dropping the DUNGEON_WATER_SCROLL alias). Rewrite src/render/waterSurface.js:63-65's comment so it STATES the rate rather than pointing at a module that no longer declares it. LEAVE both `[1, 1, 1, 0.82]` arrays exactly as they are and add one line at each site saying why (flat alpha for drawWater, not the surface's Fresnel floor). Drop the finder's pin clause 'assert neither file contains a `[1, 1, 1, 0.82]` literal' with the opacity half. Two regression notes: (a) the import drags src/render/waterSurface.js — two large GLSL template literals at :216 and :249 — into the standalone ?dungeon entry graph; harmless for a bundler but if a lane wants it avoided, hoist the one number into a tiny shared module instead of importing the shader file; (b) test/water.test.js:103 already asserts `WATER_SCROLL_TILES_PER_SEC === 0.05` with the message 'scenes/dungeon.js WATER_SCROLL_TILES_PER_SEC' — that message goes stale the moment dungeon.js stops declaring it, so update it in the same commit.",
    "severity": "low",
    "laneKey": "src/render/waterSurface.js"
  },
  {
    "id": "MC-6",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "Both dead-export claims verified at HEAD: `tilemapHasWater` (src/render/waterSurface.js:115) has zero references in src/ outside its declaration and zero in tools/ (excluding the DFU mirror), and both routes that superseded it are real — src/scenes/exterior.js:678 `tilemapRectHasWater(...)` and src/scenes/world.js:838 `waterOn ? buildWaterIndices(tilemapBytes, stride) : null` against waterSurface.js's `return out.length ? Uint32Array.from(out) : null`. `waterCoverage` (:168) likewise has no src/ caller; its only reader is test/water.test.js:12 (import) and :67-72 (six assertions). Both are delta-scoped: `git log 0fe2c05b..HEAD -- src/render/waterSurface.js` returns c03e047b (WATER1) and 22e19bd5 (WATER-AUDIT lane A), i.e. the lane that minted `tilemapRectHasWater` left its own predecessor standing. Neither export is recorded in Audit-63/64, Ledger A, or bible/07-Rendering/Water-Arc.md.",
    "corrections": "THE PINSHAPE IS UNUSABLE AS DRAFTED AND WOULD PUSH A LANE INTO A BAD DELETION — the central fix-regression here. (1) There is no 'existing dead-export sweep (the F301/OT1 c style)' to extend: `grep -rn F301 test/` is empty, and test/flagsweep.test.js walks src/ but for FLAGGED/INTERIM sentences, not exports. The pin must be CREATED, not extended. (2) If created as drafted — 'every `export function`/`export const` in src/render/waterSurface.js is referenced by at least one non-test file under src/' — it is RED TODAY for ELEVEN MORE exports, not two: WATER_LIFT, WATER_OPACITY, WATER_TINT, WATER_F0, SHORE_SOFTNESS, DEFAULT_SKY_ZENITH, DEFAULT_SKY_HORIZON, SHORE_FAMILIES, buildWaterMaskTable, waterCorners, windStrength01 (I counted callers for all 21 exports). Every one is a named constant or helper consumed by `waterUniforms`/`buildWaterMaskTable` INSIDE the file and read by the test — a legitimate shape — so the sweep would either need an eleven-entry allowlist or would brief a lane to delete eleven named constants. Do not add it in this lane. (3) 'Delete waterCoverage' is the wrong call, and the brief's oracle question cuts both ways. It is NOT an oracle today — the finder is right: `grep -rn 'coverage' test/*.js` returns only prose about execution coverage, nothing greps the GLSL body at waterSurface.js:309-313, and `waterCoverage` has no production caller, so a swap of `f.x`/`f.y` inside the shader's `coverage()` reddens nothing. That is a genuine A PIN MUST FAIL instance. BUT the six assertions at water.test.js:67-72 are the only EXECUTABLE statement of the corner-bit order (bit0=(0,0), bit1=(1,0), bit2=(0,1), bit3=(1,1)) that `WATER_MASK_TABLE`/`waterCorners` mint and the shader consumes; deleting them deletes that, against TEST THE SHAPE THE PRODUCER MINTS. Keep it and make it the oracle it claims to be.",
    "fixShape": "TWO EDITS, both inside existing `test()` bodies so bible/09-Testing/Testing.md and test/manifest.test.js stay untouched. (a) DELETE `tilemapHasWater` at src/render/waterSurface.js:113-118; remove it from test/water.test.js:12's import list and delete the four assertions at test/water.test.js:73-76. I re-grepped src/ and tools/ at HEAD for the caller count before briefing this (zero), which is OT1 c's own order of operations. (b) KEEP `waterCoverage` at src/render/waterSurface.js:165-174 and give it the missing half: in the SAME `test()` at test/water.test.js:61 ('the pack, the coverage and the has-water gate'), beside the six value assertions, add a source assertion tying the JS body to the shader's — `const fs = waterSurfaceFs(''); assert.match(fs, /float coverage\\(uint m, vec2 f\\) \\{[\\s\\S]*?return mix\\(mix\\(c00, c10, f\\.x\\), mix\\(c01, c11, f\\.x\\), f\\.y\\);/)` — so a one-character swap in EITHER half reddens. That is the cheapest honest fix; the framebuffer read-back the finder proposes is the better oracle and belongs on bible/07-Rendering/Water-Arc.md as its own item, not in this lane. (c) DO NOT add a general dead-export sweep here. Note for the lane: another agent was running a mutation probe on src/render/waterSurface.js and test/water.test.js during this pass (since reverted; `git status --short` is clean now) — re-read both files before editing.",
    "severity": "low",
    "laneKey": "src/render/waterSurface.js"
  }
]
```