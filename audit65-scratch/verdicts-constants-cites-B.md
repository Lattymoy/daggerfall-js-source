All checks complete; working tree verified clean (`git status --short` empty). Here are my verdicts.

```json
[
  {
    "id": "CV-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived at HEAD: TextProvider.cs:503 `skillValueToken.text = string.Format(\"{0}%\", playerEntity.Skills.GetLiveSkillValue(skill))` inside GetSkillSummary (:489-529) is the ONE member DaggerfallCharacterSheetWindow.cs:288/:296/:301 builds every row of ShowSkillsDialog from, and GetLiveSkillValue is DaggerfallSkills.cs:135-143 `GetPermanentSkillValue(skill) + mods[...]`. The port draws the row off the PERMANENT array at src/ui/charsheet.js:659 (`e.skills?.[id] ?? 0`, blame 42282fec = AUDIT 63 windows lane, confirmed not an ancestor of 0fe2c05b) while the delta-added damage line four lines down at :663 uses `skillValue(e, SKILLS.HandToHand)`; the enhanced model has the same split inside one object (enhancedCharSheet.js:126 raw vs :136-137 live, with :118 `liveStat` for attributes). No double-apply: src/systems/skills.js:78-96 adds `_enchantMods.skillMods` plus every non-ended `racialOverride` entry's `skillMods` on top of `permanentSkillValue`, and lycanthropy.js:233-234 / vampirism.js:194-195 write ONLY `entry.skillMods` - `grep '\\.skills\\b'` over lycanthropy.js, vampirism.js and enchantments.js returns nothing, so nothing is folded into `e.skills`. Probe: a racialOverride entity at permanent 45 with LYCANTHROPE_SKILL_MOD prints `skill(H2H) = 45` and `handToHandDamage {min:8,max:16}` (the 75 figure) from the SAME model call - the two lines of one page disagree on screen.",
    "corrections": "Three. (1) The claim understates the readers: the enhanced model's `skill()` has exactly one consumer, src/ui/enhancedMenu.js:1547-1548, and it feeds BOTH the number and `pxMeter(m.skill(id), 100, 'thin')` - the meter fill moves too, which the finding never says. (2) The finder calls charsheet.js:659 'the whole statement is an added line' and implies enhancedCharSheet.js:126 is delta work; :126 blames to the boundary commit b8a13b73 and is PRE-delta - only the H2H line beside it is new, so the lane is fixing one delta-added line and one pre-existing one. (3) Missed in the same statement the lane will edit: DFU's row is THREE tokens - name at 0, value at startPosition+85, and `GetAbbreviatedStatName(DaggerfallSkills.GetPrimaryStat(skill))` at +112 (TextProvider.cs:505-508, :521-527). The port draws two; `grep -n 'AbbreviatedStat\\|primaryStat'` over charsheet.js, enhancedCharSheet.js and skills.js returns nothing. That is a second divergence in the cited member and the lane should be told, not left to rediscover.",
    "fixShape": "src/ui/charsheet.js:659 - `${SKILL_NAMES[id]} ${e.skills?.[id] ?? 0}%` -> `${SKILL_NAMES[id]} ${skillValue(e, id)}%` (skillValue already imported at :33). src/ui/enhancedCharSheet.js:126 - `skill: (id) => e.skills?.[id] ?? 0,` -> `skill: (id) => skillValue(e, id),` (imported at :68; safe on the empty-entity path because sheetModel guards `const e = entity ?? {}` at :88 and `skillValue({}, id)` returns 0 - verified, `skillValue(undefined, id)` would throw and is never reached). Leave `getSkillRecentlyIncreased` alone. DO NOT convert `_drawFallback` at charsheet.js:692 in the same edit without deciding the blank case: it prints `?? ''` today (nothing when a skill is absent) and skillValue would print 0 there - DFU has no fallback pane, so either keep it or write the blank explicitly. VERIFIED NO REGRESSION: I applied exactly this two-line change and ran test/charsheet.test.js, charsheetnav, enhancedCharSheet, audit63_windows, audit39_uiwindows, advancementui, audit23_ui, audit24_ui (88 pass, 0 fail) plus startwindow, audit18_ui_native, audit24_wave40, audit26_magicsave, chargenart, audit26_uiwindows3, audit64_bank_misc, enhancedSpellbook, audit39_entity_magic, overlayminus, enhancedPause, audit21_hosts, enhancedChronicle (137 pass, 8 skipped, 0 fail), then reverted. The only live-value pin in range, test/audit63_windows.test.js:666-669, builds its fixture with no activeEffects and no _enchantMods so `skills[id] === skillValue(e,id)` there and it stays green; nothing in test/ pins the source text of either expression. The new pin must therefore build the entity through the real producer (applyLycanthropy's racialOverride round) or it re-creates that blindness.",
    "severity": "medium",
    "laneKey": "src/ui/charsheet.js"
  },
  {
    "id": "CV-2",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "The DFU reading holds: DaggerfallMissile.cs:339 `Physics.SphereCast(colliderPosition, ColliderRadius, ...)` and :481 `Physics.OverlapSphereNonAlloc(position, ExplosionRadius, ...)` meet whatever collider each entity wears. Read from the object store, PlayerAdvanced.prefab's CharacterController is m_Height 1.8 / m_Radius 0.35 / m_SkinWidth 0.06 and it is the prefab's ONLY collider (grep for CapsuleCollider/SphereCollider/BoxCollider/MeshCollider returns just `CharacterController:` at :71); DaggerfallEnemy [Game Serializable].prefab:442-446 is 1.8 / 0.4 / 0.05. Neither PlayerHeightChanger nor SetupDemoEnemy (which touches only `controller.height`, :70/:104/:110) ever writes a radius. The port measures both bodies with one constant, src/systems/spellcast.js:262 `BODY_CAPSULE_RADIUS = 0.45`, consumed at :307/:309, while its own player collider is src/player/motor.js:99 `CAPSULE_RADIUS = 0.35` (used by player/collider.js:439-469 with no skin term). The divergence is real but small: the player's blast rim is 4.45 where DFU's is 4.35 and the missile contact 0.90 where DFU's is 0.80.",
    "corrections": "Four, and the caller list is the important one. (1) THE PLAYER-SIDE SEAM IS FIVE SITES, NOT THE FINDER'S LIST: src/scenes/hostMagic.js:297 (AoE) and :593 (missile contact), src/scenes/dungeonContext.js:2818 and :2907, and src/combat/arrowFlight.js:91. There is no arrow contact in worldModes.js or exterior.js - grep for sphereOverlapsCapsule/missileHitsCapsule across src/ shows the hosts never call either; arrowFlight.js is the one shared flight all four import (world.js:193, exterior.js:28, worldModes.js:68, dungeonContext.js:61), so 'worldModes.js/exterior.js's arrow contacts' is wrong and would send the lane hunting code that does not exist. dungeonContext has TWO player arms, not three - its `explodeAt(..., playerFeet, ..., { playerHeight })` calls route into hostMagic:297, which is already counted. (2) DELTA FRAMING IS OVERSTATED: spellcast.js:262 and dungeonContext.js:2907 blame to 5b0885a08 (AUDIT 61), which IS an ancestor of 0fe2c05b - the shared-radius decision is PRE-delta and is written up in bible/04-Characters/Characters-Arc.md:3245-3275 ('sweeps the player's own capsule ... exactly as the foe arm does'). Only hostMagic:297, dungeonContext:2818 and arrowFlight:91 are new. It is not recorded as a deliberate departure anywhere, so it survives, but this is an inherited divergence the delta widened, not one it created. (3) THE '+SKIN WIDTH' PREMISE IS THE FINDER'S, NOT UNITY'S: a CharacterController's query surface is m_Radius; skinWidth is a penetration allowance. Do NOT invent `PLAYER_CAPSULE_RADIUS = 0.35 + 0.06`. Import motor.js's CAPSULE_RADIUS, which is the convention the port's own collider already chose, and leave the foe's recorded 0.45 alone (changing it is a separate finding against AUDIT 62's record and test/roadh_missiles.test.js:316 / audit62_foes.test.js:206). (4) Severity medium is inflated - the band is 0.10 m wide and one-sided.",
    "fixShape": "src/systems/spellcast.js: add `bodyRadius = BODY_CAPSULE_RADIUS` as a 5th parameter to `sphereOverlapsCapsule` (:304, using it at :307 and :309) and a 4th to `missileHitsCapsule` (:287-289, passed through); export `PLAYER_BODY_RADIUS` from `import { CAPSULE_RADIUS } from '../player/motor.js'` (verified acyclic - motor.js does not import spellcast.js) rather than restating 0.35; rewrite the false premise at :257-261. Pass it at all five player sites: hostMagic.js:297, :593; dungeonContext.js:2818, :2907; arrowFlight.js:91. I BUILT AND RAN THIS MUTANT AGAINST THE FULL SUITE (7047 tests) AND THEN REVERTED. Result: every BEHAVIOURAL pin survives - including test/roadh_tail.test.js:51's `rim` bracket, which is body-radius-independent because `h - r + (missileR + r)` cancels, and roadh_missiles.test.js's three explodeAt player-height assertions. Exactly five SOURCE-TEXT pins redden and the lane must update them in the same commit: test/audit62_foes.test.js:184 (the `matchAll(/missileHitsCapsule\\(m\\.pos, playerFeet, playerHeight\\)/g).length === 2` count), test/audit62_foes.test.js:267, test/roadh_missiles.test.js:355-356 (pins the exact 3-line body of missileHitsCapsule), test/roadh_missiles.test.js:422, test/roadh_tail.test.js:266. The foe-side regexes (roadh_missiles.test.js:417, incident_ceiling_bats.test.js:219/:233) are untouched by an optional trailing parameter. TWO MORE FELL that the lane MUST NOT mistake for its own bug: test/citedrift.test.js CD4 (:689) and CD8 (:918) went red purely because my import lines shifted dungeonContext.js by one - any lane that inserts a line into dungeonContext.js/hostMagic.js must keep the edit line-count-neutral or run `node tools/citeShift.mjs --apply` and re-run citedrift.",
    "severity": "low",
    "laneKey": "src/systems/spellcast.js"
  },
  {
    "id": "CV-3",
    "upheld": true,
    "confidence": "medium",
    "reasoning": "The three literals are on disk as claimed: src/render/waterSurface.js:66 (consumed :205), src/scenes/dungeon.js:72 (consumed :907), src/scenes/worldModes.js:251 (consumed :5722, under the alias DUNGEON_WATER_SCROLL). The two host consumers are the identical expression `(now / 1000) * RATE` handed to `renderer.drawWater`, so importing is behaviour-identical. THE CYCLE QUESTION THE BRIEF ASKED: safe - I walked waterSurface.js's transitive import graph (it imports only world/terrainTiles.js, world/terrainSurface.js and systems/wind.js) and it reaches 8 modules, NONE under src/scenes/; world.js:21 and exterior.js:20 already import from it, so dungeon.js and worldModes.js can too. What tips this over the observability bar is not the triple spelling but test/water.test.js:101, `assert.equal(WATER_SCROLL_TILES_PER_SEC, 0.05, 'scenes/dungeon.js WATER_SCROLL_TILES_PER_SEC')` - a pin whose message asserts cross-module equality while reading only the waterSurface export, so retuning dungeon.js:72 or worldModes.js:251 can never redden it. The docstring's claim and the pin's message both assert a link that nothing holds.",
    "corrections": "Two. (1) The finder's distinction from AUDIT 64's refuted goldPieceWeightInKg is backwards as written. That one was refuted as 'unobservable' because DFU itself spells the same 0.0025 twice, so the port was 1:1; here no DFU member exists, which makes the port's own doctrine the only law - argue it that way, not 'it is a tunable therefore reportable'. Behaviour at HEAD is identical in all three modules, so if the audit is budget-limited this is the first of my four to drop. (2) The proposed source-scan pin is weaker than what is already available: prefer extending test/water.test.js:92/:101 to read BOTH host modules' scroll argument through the shared export, since that file already owns the claim and its message is the thing that is currently false.",
    "fixShape": "Make src/render/waterSurface.js the one home (it already exports WATER_LIFT/WATER_OPACITY/WATER_TINT/WATER_F0/SHORE_SOFTNESS and sits below both hosts). Delete src/scenes/dungeon.js:72 and src/scenes/worldModes.js:251; import `WATER_SCROLL_TILES_PER_SEC` from '../render/waterSurface.js' in both and drop the DUNGEON_WATER_SCROLL alias at its one consumer worldModes.js:5722. Rewrite waterSurface.js:63-65's comment so it states the rate instead of pointing at scenes/dungeon.js, which will no longer declare it. Fix test/water.test.js:101's message in the same commit - it names a module that no longer holds the constant. REGRESSION WARNING, PROVEN THIS SESSION: this fix is NOT line-count-neutral in worldModes.js (97 inbound `worldModes.js:N` cites across src/test/bible) or dungeon.js, and I watched test/citedrift.test.js CD4/CD8 go red from a one-line shift in a sibling host. The lane must run `node tools/citeShift.mjs --apply` after the edit and re-run test/citedrift.test.js, or citedrift fails for reasons that have nothing to do with water.",
    "severity": "low",
    "laneKey": "src/render/waterSurface.js"
  },
  {
    "id": "CV-4",
    "upheld": false,
    "confidence": "high",
    "reasoning": "I extracted the record text the brief asked for. `Internal_Strings.csv` is not checked out but is in the object store: `git show 'HEAD:Assets/StreamingAssets/Text/Master Localization CSV Files/Internal_Strings.csv'` gives line 36 `storeClosed,Store is closed. Open from %d1:00 to %d2:00.` and line 37 `guildClosed,Guild is closed. Open from %d1:00 to %d2:00.` (the same file's :1553 is `hthDamageFormatString,{0} dmg: {1}-{2}`, which matches the port's other cite, so the numbering convention checks out). The port's line at src/scenes/worldModes.js:4061-4062 produces `Guild is closed. Open from 9:00 to 19:00.` / `Store is closed. Open from ...` - CHARACTER FOR CHARACTER the two records, because DFU's two records differ only in that first word and PlayerActivate.cs:477-480 substitutes only %d1/%d2. There is no observable difference on either branch, and OPEN_HOURS/CLOSE_HOURS (buildingLocks.js:25-26) are byte-identical to PlayerActivate.cs:91-92.",
    "corrections": "The load-bearing evidence claim is false. 'Every neighbouring string in this delta carries an Internal_Strings.csv:NNN cite ... this one carries none' - it does: the docblock over `activateBuilding`, at src/scenes/worldModes.js:4048-4050, reads '...the store/guild-closed popup with the opening hours substituted (:473-483; Internal_Strings.csv:36-37 gives both literals, \":00\" suffixes and all)', and 36-37 are the exactly correct lines. So the finding reduces to a SHAPE preference (one template with a swapped noun vs two whole records) with provably identical output, which is the AUDIT 64 goldPieceWeightInKg disposition - 'unobservable' - and below the brief's bar. If a lane touches this line at all it should be for the real divergence the finder walked past: DFU calls `DaggerfallUI.Instance.PopupMessage` (PlayerActivate.cs:481), a HUD popup, where the port says it through `townTalk?.say?.()` - a different sink, and a hosts/UI lens question, not a constants one.",
    "fixShape": "None. If a cosmetic lane runs anyway, the only defensible edit is moving the two sentences to `systems/buildingLocks.js` beside OPEN_HOURS/CLOSE_HOURS as GUILD_CLOSED_TEXT/STORE_CLOSED_TEXT with `%d1`/`%d2` intact and a `buildingClosedText(buildingType)` that does the two replaces, purely so a future localization pass has one door - it changes no output byte. Do not brief this as a defect.",
    "severity": "none",
    "laneKey": "src/scenes/worldModes.js"
  },
  {
    "id": "RC-1",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Re-derived both ends. DaggerfallControlsWindow.cs:68-76 is `public override void Update()` / `{` / `base.Update();` / blank / `if (!AllowCancel && !waitingForInput && InputManager.Instance.GetBackButtonDown())` / `{` / `ShowMultipleAssignmentsMessage();` - line 73 is that opening brace, exactly as claimed. `grep -n ReservedKeys` on that file returns ONE hit and it is a read: :410 `if (code != KeyCode.None && InputManager.Instance.ReservedKeys.FirstOrDefault(x => x == code) == KeyCode.None)`. The declaration is InputManager.cs:73 `KeyCode[] reservedKeys = new KeyCode[] { };`, exposed at InputManager.cs:174-177. The port's sentence is src/ui/enhancedControls.js:29 verbatim, blame d2c35fe81 (FIX-C/D/E/F), confirmed in the delta. The law is right - the array really is empty so the :410 gate never refuses a key - only the file is wrong, and the two files coincidentally both have something at :73, which is exactly how this slip survives a glance.",
    "corrections": "Two, and the first changes the lane's size. (1) THE WRONG ADDRESS IS AT FOUR SITES, NOT ONE: src/ui/enhancedControls.js:29 ('DaggerfallControlsWindow.cs:73'), src/ui/enhancedMenu.js:1912-1913 ('ReservedKeys is empty in DaggerfallControlsWindow (:73)', delta, same commit), test/enhancedControls.test.js:207 ('ReservedKeys is EMPTY in DaggerfallControlsWindow (:73)', delta, same commit), and src/ui/controlsWindow.js:36-37 ('ReservedKeys is EMPTY in DFU (:73)', PRE-delta, inside a docblock whose sibling cites :135-141/:337-343/:383-427 are all DaggerfallControlsWindow - which is the source the delta says it 'mirrored from'). Fixing only enhancedControls.js:29 leaves three copies of the same false address, including the one the next slice will copy again. (2) Severity medium is inflated: the port's behaviour is correct and nothing but a reader is misled - low.",
    "fixShape": "Comment-only, four sites, and keep each edit within its existing line count so no cite into these files shifts (I confirmed there are no inbound `enhancedControls.js:N` cites, but enhancedMenu.js and controlsWindow.js are widely cited). At each site write the pair: 'ReservedKeys is empty in DFU (InputManager.cs:73 `new KeyCode[] { }`, exposed :174-177), so the capture gate that consults it (DaggerfallControlsWindow.cs:410) never refuses a key - Escape included.' Naming both the declaration and the consumer is what makes it checkable at both ends. PIN SHAPE CORRECTED - DO NOT ADD test/csCiteDrift.test.js: test/citedrift.test.js ALREADY has the C# arm this asks for. CD8b (test/citedrift.test.js:996-1027) resolves C# cites through test/dfuRoot.mjs's `dfuFile`/`missingDfu`, and its recorded precedent is this exact shape - `UpdateSpellsList` 'is not a member of DaggerfallSpellBookWindow - nor of anything else in the reference tree'. Extend CD8b; and note its two-part structure is mandatory, not stylistic: `missingDfu(...) return` means the LINE half SKIPS in CI (Port-Doctrine keeps DFU external and CI has no checkout, dfuRoot.mjs:1-30), so the half that actually holds is the text assertion. 'The assertion must fail on today's tree' is true only where a DFU checkout exists.",
    "severity": "low",
    "laneKey": "src/ui/enhancedControls.js"
  },
  {
    "id": "RC-2",
    "upheld": true,
    "confidence": "high",
    "reasoning": "Both halves verified. `sed -n '917p' GameObjectHelper.cs` prints an empty line (:914 GetQuest, :915-916 the null throw, :918 `// Get the Place resource referenced by this link`); the call the comment describes is :926 `QuestResourceBehaviour[] resourceBehaviours = Resources.FindObjectsOfTypeAll<QuestResourceBehaviour>();`, under DFU's own :923-925 comment 'Helps ensure a resource is not injected twice'. `grep -rn 'IsAlreadyPlaced' tools/parity/dfu/Assets/Scripts` returns NOTHING; the real member is `static bool IsAlreadyInjected(...)` at :978, called at :950. Port sites on disk: worldModes.js:1412 (cites :917) and :1413 (the invented name) are PRE-delta (boundary b8a13b73); worldModes.js:1423 and dungeonContext.js:5141 are delta (c6ac15d1d, AUDIT 64 layout lane); worldModes.js:1434 is delta (eac56a711, AUDIT 63). dungeonContext.js:5141 has the right line (:926) and the wrong name, so the two sibling comments really do disagree with each other.",
    "corrections": "Three. (1) The finder's own fix list mis-cites the port: the sites are worldModes.js:1412, :1413, :1423, :1434 - there is nothing at :1414. (2) The blame attribution is wrong in both directions: :1434 is AUDIT 63's commit, not 'the AUDIT 64 F13 lane', and :1423/:5141 are AUDIT 64's; :1412-1413 are pre-delta, so the delta inherited the name and spread it rather than inventing it. (3) Severity medium is inflated - the port's code is right (sceneBehaviours() already unions the dungeon people in) and nothing but a future grep is harmed; low.",
    "fixShape": "Comment-only at five sites: worldModes.js:1412 (:917 -> :926), :1413, :1423, :1434 (:917 -> :926) and dungeonContext.js:5141, renaming IsAlreadyPlaced -> IsAlreadyInjected and, at least once per file, naming where it lives ('GameObjectHelper.cs:978, called at :950'). CRITICAL CONSTRAINT, PROVEN THIS SESSION: `IsAlreadyInjected` is two characters longer than `IsAlreadyPlaced` and these are wrapped ~72-column comment blocks, so a naive rewrite will push a line and shift worldModes.js (97 inbound `worldModes.js:N` cites) and dungeonContext.js (53). I inserted ONE line into dungeonContext.js during an unrelated probe and test/citedrift.test.js CD4 (:689) and CD8 (:918) both went red. Re-wrap inside the same line count, or run `node tools/citeShift.mjs --apply` and re-run test/citedrift.test.js before committing. For the pin, extend test/citedrift.test.js's CD8b arm rather than adding a file - its `assert.equal(/UpdateSpellsList/.test(text), false, ...)` line IS the unknown-member arm this finding asks for, and it is the half that survives CI's missing DFU checkout.",
    "severity": "low",
    "laneKey": "src/scenes/worldModes.js"
  },
  {
    "id": "RC-3",
    "upheld": true,
    "confidence": "high",
    "reasoning": "All five off-by-one cites confirmed on disk, and all five port lines are in the delta. StaticNPC.cs:149-159 numbers as the finder says - :154 TextureRecord, :155 `obj.Resources.FlatResource.Position`, :157 LocationIndex, :158 `0)`, :159 `npcData.context = Context.Dungeon;` - so dungeonContext.js:546's ':154 seeds the name off the FLAT RESOURCE's stream position' should be :155 and :549's 'buildingKey stays 0 (:157)' should be :158 (the port reads `f.flatPosition` and sets no buildingKey, so the code is right). DaggerfallStartNewGameWizard.cs:569 startingSkills / :570 workingSkills / :571 startingStats / :572 workingStats / :573 var bonusSkillPoints / :574 SetBonusSkillPoints / :575 faceIndex, so chargen.js:1325's ':570-571' for the stat VALUES is :571-572 and chargen.js:1407's ':575' for SetBonusSkillPoints is :574. SerializableEnemy.cs:116 is `data.questSpawn = enemy.QuestSpawn;` and :117 `data.mobileGender = ...`, while exteriorFoes.js:997 QUOTES the :116 text and cites :117 - its siblings :126 and :129-133 in the same comment are exact. Blame: c6ac15d1d (AUDIT 64 layout), 8ce09bc3a (AUDIT 64 chargen), 45089ece2 (AUDIT 63 saveload) - all in the delta.",
    "corrections": "Two evidence slips of the finder's own, in a cite lens. (1) It cites the nameSeed build as 'StaticNPC.cs:212'; on disk `data.nameSeed = (int)position ^ buildingKey + locationIndex;` is at :218 (:212 is `TextFile.Token lastToken = new TextFile.Token();` in a different method). Do not copy that number into the lane brief. (2) The contrast case it waves off is not as clean as stated: biography.js:200 cites BiogFile.cs:215 for ExpandMacros (on disk :216) and biography.js:264 cites :212 for BirthRaceTemplate (on disk :213) - consistent -1 drift, agreed, but the SENTENCE at :199-201 also says ExpandMacros is 'one line after' the BirthRaceTemplate assignment when the on-disk gap is three (:213 -> :216), so that pair is a live near-miss a resolver WILL flag and the lane needs a decision on it, not a footnote.",
    "fixShape": "Digit-only edits, which keeps every line count intact and avoids the citeShift fallout that bit my CV-2 probe: dungeonContext.js:546 `:154`->`:155`; dungeonContext.js:549 `(:157)`->`(:158)`; chargen.js:1325 `:570-571`->`:571-572`; chargen.js:1407 `:575)`->`:574)`; exteriorFoes.js:997 `SerializableEnemy.cs:117`->`:116`. THE BRIEF'S TOOLING QUESTION, ANSWERED. tools/citeShift.mjs would NOT be affected and must NOT be the home for a C#-cite resolver: `git ls-files tools/parity/dfu` returns ZERO files (the clone is untracked, kept external by Port-Doctrine), so a .cs file can never be one of its targets, and `citeSpellings()` (citeShift.mjs:89-103) builds its regex from the target's full basename INCLUDING extension, so a `.js` target can never eat a `.cs` cite - there is no collision to fix and no diff to map, because the reference is pinned at 81e89e9 and its lines do not move between audits. The tool's own header already says C# continuations are 'REPORTED with their mapped value and never applied'. test/citedrift.test.js IS affected and is the right home: its CD8b test (:996-1027) already resolves DaggerfallSpellBookWindow.cs:507 and SpellIconPickerWindow.cs:249 through test/dfuRoot.mjs. Generalize CD8b; gate the line half on `missingDfu` as it does (CI has no checkout, so that half skips) and carry the name/text half unconditionally. Budget for a skip list: by the finder's own count 7 of 312 cites do not resolve, the two Dynamic Skies mod cites (systems/dynamicSkiesRuntime.js:100) have no source on disk at all, and the biography.js pair above will fail until it is decided.",
    "severity": "low",
    "laneKey": "src/scenes/dungeonContext.js"
  }
]
```