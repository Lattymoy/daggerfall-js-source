# Systems

ACTIVE - see `Systems-Arc.md` for the live record. S1-S22 SHIPPED (effects
and the spell/effect library, chargen, biography, items and equip, loot,
containers, talk, crime and the court, rest and recovery, diseases, poisons,
the Cure family, concealment, FreeAction) plus the economy sub-arc E1-E3
(shop templates and stock, the shelf mount, selling, the guild bookshelf, the health status box), plus ORL1's OBLIVION-REMASTER-LIKE LEVELING - the alternative leveling system a character chooses at creation, plus the DWARVEN THUNDERLOCK (`thunderlock.js`) - the port's own weapon, the first item in this port that Daggerfall does not have, with its two custom templates and the Dwemer Pellet it spends, and `appRoot.js` - the SITE ROOT off a module's own URL, which MAP-FIELD found the hard way on the held map and AUDIT-THUNDERLOCK F7 found again on the weapon's art, plus FIELD-GUN-MW2's pair for the port's OWN Morrowind assets - `urlArchive.js` (the `{has, get, load, loaded}` duck over files that ship with the build, moved out of `weaponSheathing.js` once it had a second caller) and `ownMwAssets.js` (the Thunderlock's NIF and DDS, mounted after the player's loose files so their own replacer still wins). `gravestoneLore.js` (GRAVE1: the epitaph pool an Info-mode activation that hit nothing reads from while the player stands in a Graveyard location - location-gated rather than per-headstone, because the stones are block models with no record of their own).  `mapTabs.js` (EM1: the tab law of the one held map - which sheets a place offers, DERIVED off the flags every host keeps rather than declared) and `automapFloors.js` (EM2: the storeys a Daggerfall dungeon does not record, derived off the reveal index's own triangles - a flat floor votes, a ramp only gets drawn). `characterId.js` (CHARID1: a character is an ID rather than a name, so a new character can never overwrite an old one's save) and `saveTransfer.js` (SP1: saves move between the website and the app - a whole set exported as one zip, and a zip or a Saves folder imported back). and `notify.js` (ENH-NOTICE3, 2026-09-21: THE ONE DOOR EVERY MESSAGE GOES THROUGH - DaggerfallUI's static `MessageBox` written once (`AddHUDText`/`PopupMessage` have a door there too, unwalked by any shipping producer yet - AUDIT ENH-NOTICE3 F4; SetMidScreenText stays `ui/midScreenText.js`'s); the hosts register a PRESENTER for their one overlay slot and their PopupText, a box is offered by priority then recency - the dungeon context, the modal modes, the outdoor slot, which is world.js's showQuestBox ladder written once - and falls to the HUD line rather than vanishing; the door never forks on the skin, the draw does), and `worldHover.js` (WORLD-HOVER, 2026-09-21: WHAT THE CROSSHAIR IS ON, as a record - World Tooltips 1.1's naming ladder over the SAME activation race the press walks, so the plaque and the button can never disagree; pure of the page, with `ui/worldPlaque.js` owning the node, the dress and the skin gate). and `worldTooltips.js` (WORLD-HOVER: World Tooltips 1.1's NAMING LADDER - jefetienne's own source ported arm by arm and cited line by line: the sixteen Daedra by billboard record, the action models, the house containers by full model id, the doors and what they say is shut. The WORDS only, because the ray, the pick and the reach are `worldHover.js`'s and the face is the plaque's). `cloudSaves.js` (ACC2: the cloud is a THIRD DESTINATION for the carrier SP1 already built - a slot pushed to the account service's R2 under (characterId, saveName), and one pulled back down through `saveTransfer.js`'s own import law, so there is no second merge rule and the cloud is a backup rather than the truth). and `quickLoot.js` (QUICK-LOOT, 2026-09-22: TAKING FROM THE PLAQUE, WITHOUT A WINDOW - Arc A's list of what a pile or a body holds made a way to TAKE. A DEPARTURE, and recorded as one: DFU has no quick loot and no mod under `vendor/` carries one, so the shape is the Skyrim QuickLoot mod's - the wheel moves a highlight through the rows, the activate key takes the lit row, `QuickLootAll` takes the lot and `QuickLootOpen` asks for the window anyway, and the cursor is never freed. The HIGHLIGHT's law is `worldHover.js nextSelection`, the MOVE is `inventory.js takeOneInto` - DoTransferItem's first statement, so gold goes to the counter and never the item list - and the take goes through the same loot hooks the inventory window is handed as its remote target. It owns the selection itself so that no take has to live in a draw and no systems module has to import `ui/`). and `statusReadout.js` (STATUS-LIVE, 2026-09-22: THE LIVE STATUS READOUT AND THE LAW THAT YIELDS IT - one player, one status panel, so which box is up, the host's own door back out of its slot and the `STATUS_YIELD_ACTIONS` set live here rather than four times over in the hosts. A LEAF on `systems/` for the reason `hudShortcuts.js` is one: `ui/input.js`'s `routeAction` is the single door every host's window keys and the large HUD's eleven panels go through, and it already sits inside `ui/actionText.js`'s import ring - reaching for the BOX from there put its class body in a temporal dead zone, a real crash on a real import order, found by running it), plus QUEST1's `questShare.js` (a quest shared with the party through the catalog's own gates) and TRADE1's `tradePack.js` (the real pack under a trade: reserve, release, apply). And `itemPowers.js` (MACRO-3, 2026-09-22: an item's POWERS, the %mpw under "Item powers:" - DaggerfallUnityItemMCP.MagicPowers ported arm by arm, DFU's own words for the box, which the port had no source for at all). And `affiliations.js` (GUILD-REP, 2026-09-22: the AFFILIATIONS BOOK - ShowAffiliationsDialog's affiliation, rank title and live reputation per membership, out of the classic sheet so the enhanced pause menu's Standing page draws the same guilds without importing the classic window). And `partyRestLaw.js` (AUDIT PARTY-REST, 2026-09-23: the PURE HALF of the party rest - a wire stamp read against the shared clock, a seat present only while the hub says so, a vote fresh and cast after the last rest, a follower's cancel its sender's own marker, one mirror per nap - lifted out of world.js's closures so a pin can RUN it, where fourteen regex pins had passed with the feature broken). And `allyCast.js` (ALLY-CAST, 2026-09-23: A SPELL CAST ON A PARTY MATE - the beneficial families a friend may put on you, the frame the caster sends, the reach by range type; the port's own targeting, a recorded departure, since DFU has no other players to cast on). And HORSE CART AND CARGO (HCC, 2026-09-23, Mac: "Next mod I want to implement 1 to 1 and also enhance its online integration functionality"; `06-Systems/Horse-Cart-And-Cargo.md`): `horseCartLaw.js` (the enums, the constants, the save record, every predicate, the billboard, wheel and cargo arithmetic, the scene<->world conversions - TrailingWagonRuntime's pure half off the IL), `horseFollow.js` (the following horse's path, speeds, detours, stuck recovery and combat evasion; the wagon's trail and grounded pose), `wagon41214.js` (the classic wagon welded into its five pieces so the wheels turn; the twelve cargo pieces by tier), `horseCart.js` (TrailingWagonRuntime's state and order of operations over the hosts' seams, with the parked wagon and the standing horse as pure state) and `horseCartWire.js` (HCC-ONLINE: a peer's horse and wagon as the cell's foes frame says them, validated at the door). And SNDREP1's `soundReplacer.js` (a player-attached sound pack's WAVs over classic clips, DFU's StreamingAssets/Sound, and the crickets/howl switches). And `weatherTable.js` (WEATHER3, 2026-09-22: DFU's weather odds - the Chronicles table, its climate dispatch and the roll - moved whole out of `weatherSim.js` so the sim and the world weather map read ONE table without an import cycle; the sim re-exports every name) and `weatherMap.js` (WEATHER3a: THE WORLD WEATHER MAP - the enhanced lane's weather as SYSTEMS on the land, a thunderstorm, a rain band, a fog bank each born, drifting on the wind, growing and dying; `weatherAt(x, z, minutes)` a pure function of place and minute, the same offline and online, its births a Poisson process solved backwards from the table so each climate still gets the Chronicles' share of each weather). And `distantStorms.js` (WEATHER3d: a thunderstorm on the horizon strikes on its own seeded schedule - its own cloud lights in its direction, its thunder heard its distance over the speed of sound later, quieter the further; the storm overhead stays DFU's). And JOURNAL1's `graphemes.js` (the characters as a reader counts them - EMOTE1's law, lifted out of the chat bubble so the notebook's line break and the bubble's cut read one: a joined family never parted, a face never halved; whole code points where the runtime has no segmenter). And CURSE-REPAIR1's `curseRepair.js` (a curse the magic-round clock pruned before CURSE-PERSIST1 - its tagged spells outliving it - given back on load, without replaying its onset). And DISC19-F's `townWatch.js` (the watch defends the town: when a monster hunting the player inside a town brings the watch as the player's allies, how many squads an incident brings, and when they walk away - the decision and the host's frame, `runTownWatchFrame`, pure so the pins run both (AUDIT DISC19); `scenes/cityGuards.js` mints and dismisses them). And OVH1/OVH2's `overhauls.js` (the three looks on the main menu's Overhauls screen - Texture, Sound, UI - the registry and its one writer) and `uiPack.js` (a UI pack worn over the classic skin, GrimoireUI first: its files by the name DFU asks for, off the archive's own listing). And DISC22-C's `classicLootFrame.js` (the classic skins' quick-loot frame and its lit row, left by the hover resolve for `ui/hud.js` to draw - a LEAF with no imports, because hud.js reaching for `quickLoot.js` closed an import ring that initialised `itemTransfer.js` before its own constants, found by the suite). And HT-WAIST's `lanternSwing.js` (2026-09-24: the lantern hung at the waist swings as you move - two damped pendulums on the hip's own acceleration, its stride sway and a turn's pull, one law fed by both bodies that draw it, the Morrowind body and Eye Of The Beholder's sprite; `06-Systems/Handheld-Torches.md` HT-WAIST). And RETRO1's `retroMode.js` (DFU's retro mode, the half that reads: the five `[Video]` keys, the retro targets and their docked-HUD twins, SetRetroAspectViewport's pillarbox to the cast, the Shift-F11 toggle's flag, and `retroFrameConfig` - the renderer's source, so `render/` never reads the store; `07-Rendering/Retro-Mode.md`). And DISC21-A's `conditionRepair.js` (a wearable that never had its condition minted - the biography's items until DISC21, and Roleplay & Realism's questions' dagger worn to 20% of nothing - minted on load by the law it missed; the questions' dagger and its wear, one home). And FPS-CAP1's `frameCap.js` (2026-09-25, Mac: "Add FPS limiter to settings": DFU's `Video/TargetFrameRate`, read by nothing since the settings screen shipped - the gate the four hosts ask at the top of their frame, StartGameBehaviour's `>= 30` floor over SettingsManager's GetInt(0, 300), one decision per rAF stamp so the FPS counter counts what the game drew; `10-UI/Features-Arc.md` FPS-CAP1) and FT18's `featureLanes.js` (2026-09-25: the condensed Features rows' lanes over the port's own prefs - the wind, the quick slots and the blood, each one bar over keys whose consumers read them as they always did; `10-UI/Features-Arc.md` FT18). And UXB1-E's `onlineSync.js` (2026-09-25, the UX backlog's "sync from server": THE ROOM'S RULES, COPIED HOME - every pref, DFU setting and mod key the online lane forces, plus the full-size dungeon online always plays, written into the offline stores in one press with the replaced values kept for Undo; there is no host to ask because a relay publishes no rules - they are the lane's, this build's, the same on every server). And PERF-SCALE's `renderScale.js` (2026-09-25: the RENDER SCALE's half that reads - the Features row's tiers, the player's answer and the `?renderscale` door, as `renderScaleSetting`, the renderer's source beside `retroFrameConfig`, so `render/` never reads the shelf for it; `07-Rendering/Rendering.md` PERF-SCALE). And TRAVEL-NAV1's `travelSteer.js` (2026-09-25, Mac: "Improving travel options navigation to properly route around objects and stopping before running into buildings": the Travel Options journey's own steering between the autopilot and the motor - a corridor of feelers through the host's collider, a fan frozen at each detour's start, the pocket rule, the pursuit back onto the leg's line, the stand-off at every acceleration and the stop in the mod's own manner; pure over a probe function so the pins fly it on a table; `06-Systems/Travel-Options.md` TRAVEL-NAV). And PARTY-TRAVEL's pair (2026-09-25, Mac: "Implementing a prompt for online to travel to party leader and the option for party members to ready up and travel together"; `06-Systems/Online-Arc.md` PARTY-TRAVEL): `partyTravelLaw.js` (the party's journey, its pure half - a round is its shared-clock stamp and stands while fresh, a vote names its round, the tally over the members gathered by the rest law's own near, where the journey to the leader goes or why not, the leader's feet as a landing only inside the pixel paid for, the spot beside them clear of walls on their own floor) and `partyTravel.js` (its session over the host's seams - the leader's proposal, a member's answer, the departure and the follow, the offer to travel to the leader - so a pin RUNS a leader and a member against each other through the wire's own law). And RENOWN1's `renownLayer.js` (Renown's health and magicka, ON TOP of the character's own while it plays online - two plain fields the live maximums read over everything they already sum, a save's vitals taken back through them at their fraction, and never on the save's whitelist; `06-Systems/Accounts-And-Cloud-Saves-Arc.md` RENOWN1). And PSCALE1's `partyScale.js` (a fight weighs what its fighters weigh online - a shared foe's fighters, the players who struck it in thirty seconds, its toughness by them taken at the authority's damage door with the remainder carried, its heals weighed the same, a kill never divided, its hit on a player, and the foes more an outdoor encounter stands; `06-Systems/Online-Arc.md` PSCALE1, AUDIT PSCALE1), plus SIGIL1's `sigil.js` (some Magic-or-better weapons won online carry a sigil - a per cent of damage at a foe in its tier's band, more often and stronger in a bigger fight, grown through five stages by the Renown XP earned with it in hand and held by the wielder's Renown, dormant offline; `06-Systems/Online-Arc.md` SIGIL1), plus HUB1's `regionHubs.js` (every populated region's main city is its hub online - the best kind of settlement, then the one named for its region, then the largest, over the game's own rows; Daggerfall, Wayrest and Sentinel the capitals; `06-Systems/Online-Arc.md` HUB1), plus HOME1's `onlineHomes.js` (the online homes as the client knows them - the account service's registry a town at a time, and the door's one answer: nobody's, mine, open to me, shut; the offer, the sale, and a home's own scene; `06-Systems/Online-Arc.md` HOME1), plus DECOR1's `decorCatalogue.js` (everything Daggerfall furnishes - every prop model and flat its own rooms hold, kinded and named where the game names them - and the decorate panel's filters; `06-Systems/Online-Arc.md` DECOR1), `decorScan.js` (DECOR1d: that catalogue read out of BLOCKS.BSA a few blocks a frame, and each piece's size measured for its price) and `decorPlacer.js` (DECOR1d: where a new piece stands for the point the free camera looks at - lifted by its own bottom, turned, raised, scaled, snapped to a quarter-metre grid, and priced; DECOR1e: or a placed one moved, from its own turn and scale), plus DECOR2a's `decorItems.js` (the player's own things in a room - what of the pack can stand and as which of Daggerfall's own world pictures, the game's own numbers an online home's piece names it by, and that name; DECOR2c: and which weapons and shields hang, as their own pack pictures, and the dye they wear; `06-Systems/Online-Arc.md` DECOR2), plus DECOR2b's `decorFurnish.js` (the furnisher's pieces - Daggerfall's Furniture group, which kinds of the catalogue each takes its look from, the panel's row for a delivered piece, and what is said when it comes and comes back; `06-Systems/Online-Arc.md` DECOR2).  And RARE-BREAK1's `rarityTier.js` (2026-09-25): the rarity ladder's rolled tiers as a leaf, so the combat formulas can ask whether a piece came off the ladder without reading it (`05-Combat/Combat.md` RARE-BREAK1). And DS1's `detailedShips.js` (2026-09-25: Detailed Ships' thirteen pictures on the texture door as stand-ins of the mod's archives 1210 and 1230 - four the author's files, nine built from the player's own classic records - and its six xml scales, behind the mod's one switch; `03-World/Detailed-Ships.md`). And WA1's `warmAshesShips.js` (2026-09-25: Warm Ashes - Ships' script off its IL - the sea voyage's ambush armed before a fast travel, the boarding after it, the quest's "Leave Ship" action; `03-World/Warm-Ashes-Ships.md`) and `modSaveData.js` (DFU's per-mod save slot as a registry - every save writes each registered mod's record beside HCC's, a load hands each back or its NewSaveData). And DW-D's `deepWaterPlayer.js` (2026-09-25: Iliac Puddle No More's public player API - the swim state the driver publishes and announces once a frame, the subscribers that may turn the outdoor swim off, the player's own water column - a leaf, so the mods that listen import it without the world host; `03-World/Deep-Waters.md`). And DW-E3's `deepWatersFishItems.js` (2026-09-25: Iliac Puddle No More's fish as items - the mod's seven templates registered, each drawn from an archive of its own, their icons built on the texture door with their aspect restored, the item a fish carries and the migration that keeps them in UselessItems2; `03-World/Deep-Waters.md`). And WB5's `gateSpoils.js` (2026-09-25, Mac: "on death the boss would physically spew out per player loot": a fallen boss's spoils for one player, rolled off the relay's receipt seed with the game's own makers and Loot Rarity's own ladder - gold by the level, a Rare-or-better and two Magic-or-better pieces, each with its condition and known, and the Sigil Stone on its own template row, 570, which never stacks into a gem; `11-Multiplayer/World-Bosses.md` section 7). And WB7's `gateScore.js` (2026-09-25, Mac: "Proper boss audio during the boss fight": the Burning Court's own music, written as notes - four songs in the HMI song's shape for the game's own player and FM bank, registered with the music service by names of their own - and the law of which plays: the war song of the boss's phase, the third in the Wrath's last minute, his fall's fanfare, then quiet; `08-Audio/Audio.md` WB7). 265 modules live under
`src/systems/`. Items still routed here are collected in
`01-Overview/Port-Ledger.md` section C; scope in
`01-Overview/Port-Doctrine.md` phase plan.

AUDIT 18 rewrote this page: its opening paragraph declared the arc
unstarted, with the Readers/World prerequisites its only news, through the
whole S and E arcs. A section
index nobody opens goes stale silently, so
`test/audit18_bible_docs.test.js` now fails if a section index says "Not
started" while its own `*-Arc.md` records a SHIPPED slice.

## HT1 - HANDHELD TORCHES, THE MOD, 1:1 (2026-09-14, Mac's call) - SHIPPED

Mac: "Next mod to integrate 1:1 is this." The whole account is
`Handheld-Torches.md` beside this file; the ledger row is Port-Ledger A.
The short of it: RedRoryOTheGlen's Handheld Torches 1.4.1 - four
MonoBehaviours in a compiled DLL read off the IL method by method -
ported into `systems/handheldTorches.js` (the component the weapon rig
runs beside the Weapon Widget clone: the hand law over the player's
light, the ignite / drop / throw keys as Unity KeyCode names through
`systems/keyCodes.js`, the first-person hand sprite on Weapon Widget's
Bob and Inertia laws) and `scenes/droppedTorches.js` (the pool each of
the five hosts owns: the dropped and thrown torches burning on the
world clock, picked back up, saved; the foe set alight with DFU's own
Continuous Damage-Health and the mod's light and flame riding it). The
mod's 39 textures are vendored - the author's own art, not a render of
ARENA2. TextKey and the two Tuple kinds are new to the Mods pane. Under
the Morrowind arms the law runs and the held torch is the classic
lane's; a Morrowind LIGH mesh on the `torch` animation group pends
Mac's word. Pins: 19 in `test/ht1_handheldtorches.test.js`. Not
filmed: a game.
