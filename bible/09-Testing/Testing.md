# Testing

Runner: `node --test` (bare - a trailing `test/` path breaks discovery on
Node 22). Suite: 480 tests across 105 files.

| File | Tests | Covers |
|---|---|---|
| action.test.js | 6 | door lifecycle/verbatim constants, move tween + chain gate, activation picking; U6 text actions (the 8600/5400/7700 bases + the verbatim type-12 answers, ShowText firing, ShowTextWithInput skipping the up-front cascade with only a case-insensitive answer match firing ActivateNext, DoorText remapping 7701->7705 + holding the door on first activation + the axisRaw>5 trespass gate); P10 locks (lock value 16 / magic threshold 20, the clamp(5..95) lockpicking chance, all five LookAtInteriorLock text tiers, locked-door refusal firing onLockedDoor while solid, Lock/Unlock/Open/Close delegates through door-riding action nodes incl. only-when-unlocked and StartingLockValue restore) and P10 teleport (resolved next-object warp, null destination refuses, actionless-destination cascade no-op) + the block-instance key namespace (two instances of one RDB position both register and play independently - the 3108-dungeon repeated-block collision fix) |
| talktopics.test.js | 7 | T3c building names + Where-is: GenerateBuildingName verbatim (the seeded B-then-A draw order reproduced by hand, seed-stable, Bank/GuildHall/House forms, the macro carriers), the 8-way compass bands over (east,north), the NPC-stable reaction tier + the 30-record answersToDirections shape (knows-Commoners tier 0 = 7261), the named-building pool merge over the REAL Daggerfall city (named>20, taverns>3, classic two-part tavern names through the directory); the 17c audit pin - the pool merge BOUNDED by the subrecord count (header garbage past it never draws); T3d - the answer invariant under a pure frame translation (the streaming host's location-frame doctrine); T3e - the knowledge roll (rollToBeat 15 for Commoners, MakeBuildingKey incl. the 1<<24 sentinel, hand-reproduced seeded rolls, seed-stable, BOTH table halves reachable and whereIsAnswer drawing the matching half); T3f - the tone tiers (the three verbatim mod tables, the neutral wrapper identity, the Dice100 skill roll separating pass/fail bands, the Merchants sgroup fold, the per-session reaction cache beating a fresh skill roll, the first-use-only tally) |
| court.test.js | 3 | G2 arrest + court verbatim: reputationLossPerCrime charging LegalRep per region + the surrender gates (SetHealth(1) BEFORE any refusal, < -20 involuntary refusal, the DFRandom coin, dead never surrender), the penalty math (base +- perRep*rep clamp [min,max] /40, the coin split 40 gold / 3 days, the gold cap converting to days) + the guilty plea halving-and-deducting, the not-guilty pleas (Etiquette/Streetwise + Personality chance clamp 5..95, free vs guilty with the fine roll x2/half) + THE NEVER-CHARGED VERDICT QUIRK (DeductGoldAmount lives only in the guilty PLEA - a failed defense adjusts but never charges the fine, preserved 1:1), sentence rep raise = half loss - 1 |
| cityguards.test.js | 5 | G1 the city watch (SpawnCityGuards verbatim over real CLASS18.CFG): the constants (146/max 5/77.5/105.469), immediate spawn converting the in-range wandering guard FIRST + disabling the source NPC (out-of-range ignored), the behind-player civilian 1/4 conversion, the 2-5 ring fallback when nobody converts, the over-max gate refusing further spawns; the 17c audit pin - the verbatim seen-by-guard MASS conversion (once any guard sees, every REMAINING pool NPC converts, gate-independent; the pre-seer stays); G3 corpse loot - KILLED guards are loot targets (walk-aways vanish with their items), takeLoot transfers once into the player entity and empties the corpse; G4 murder/assault - a civilian in weapon reach dies to ONE hit (disable + Murder + the response), out-of-reach and walled strikes are no-crimes, a wandering guard NPC converts on the spot (Assault) with the swing carried onto the fresh foe, and killing the watch through the real death path is Murder |
| shopstock.test.js | 5 | E1 the shop foundation: the 288-row baked template table with spot pins (Ruby/Horse/Book), the group enum mapping (Armor 102-112, Dagger first weapon), ItemBuilder's material value laws (weapon/plate basePrice*3*mult, chain x2, leather flat); StockShopShelf verbatim (all-templates at always-pass rolls, none at always-fail, the rarity<=quality gate, general-store horse+cart + the NO-dice book ladder both branches (q7 -> 4, q20 step-down -> 5), the gender clothing swap, the verbatim pair tables, every item carrying a value); CalculateCost verbatim (the 2x@q10 anchor, +-percent in C# toward-zero truncation, the value>=1 clamp, the regional adjustment FIRST, the lazy sticky 750..1250 region band); E2 - CalculateTradePrice verbatim fixed-point (the hand-reproduced 144/256 anchor, sell < buy, Mercantile lowering buys and raising sells, quality raising prices); E3 - storeBuysItemType verbatim (Bookseller Books-only, WeaponSmith Armor+Weapons, PawnShop 10 / Alchemist 9, the gate refusing off-list groups + Currency + non-shops) |
| nativetrade.test.js | 3 | U8c the native trade window: the composed DFU geometry (inventory lists 163/261,48,59x152, the SHOP00I0 cost strip, the INVE08I0 action panel, 38px slots x4), the click trade machine over fake hooks (remote click buys / local click sells with the item crossing lists, outside-rect not consumed, exit + digits + Esc); HOTFIX (Mac's catch) - icon draws V-FLIP the record texture (getColor32 stores bottom-up GL rows, drawScreenQuad samples v0 at the top - the shelf books rendered upside down; the flipped source rect pinned); AUDIT 17d - the ItemListScroller law VERBATIM (itemListPanelRect 9,0,50,152: the 50x38 buttons at x=9, the LEFT 9px rail = up arrow 0,0,9,16 / down arrow 0,136,9,16 / the bar between; rail clicks scroll and never trade), icons ScaleToFit MaxAutoScale 1 - never upscaled - centred in the BUTTON at x9, the V-flipped record source rect pinned, stack counts the only cell text (FONT0004; no names - the info seam pends) |
| audit17e.test.js | 13 | AUDIT 17e Wave 0 ship-blockers, each pin mutation-proven: the shop round-trip invariant (buy never undercuts sell, both sides resolving value the same way and multiplying by the stack - the old asymmetry was an unbounded gold loop); the OnExitLocationRect crime clear (first-sync/wilderness/same-key are not exits, town->wilderness and town->town are); save/load rebuilding the equip table from the restored items and re-deriving armor from it (fresh load, same-session load dropping a stale bonus, duplicate slot claims); IsEnchanted DERIVED from the enchantment array (the dead `enchanted` flag means nothing, the FP magic anim set promotes, all three stacking clauses bite on real shapes); an unequip returning the armor it granted; and the MODAL CONTRACT - worldModes.frame() carries a value from every exit (a bare return read as "not handled" and made the hosts draw the town over the dungeon); WAVE 1 - the paperdoll click mode (PaperDoll_OnMouseClick unequips in EQUIP/Select, REMOVE is inert), the clothing armor branch (leather boots 3*5 on Feet for both genders' 147..149 / 186..188 windows, Sandals outside it granting 0 as DFU has it), arrows valued at basePrice with no material multiplier, delayScrollUp's late snap (in-range and partly-filled columns untouched, past-the-end snaps), the INVENTORY icon law (player texture not world sprite, the Katana +1 bump, arrows/ingredients/MiscItems keeping the world sprite, variants advancing the record, and the templates now carrying the FULL DFU row), refreshPaperDoll coalescing instead of dropping, and the worn-weapon bind living in the RIG so all four hosts inherit it; WAVE 2 - the Where-is category list (CheckBuildingTypeInSkipList dropping Palaces/Furniture stores/houses/ships, DFU's enum ORDER, the three corrected captions), the three GL leaks each having an owner (the paperdoll's per-refresh composite, emptied loot piles freeing at WINDOW CLOSE as DFU does - not on the empty transition, which would leave an invisible-but-activatable ghost on refill - and the dungeon's per-foe batches), and dropped piles carrying STABLE ids that survive a cleanup splice while following the floating origin by REBUILDING their batches (the centers are baked into a STATIC_DRAW buffer and cannot be shifted in place) |
| equipmechanics.test.js | 5 | U8f/U8g the equip mechanics + the live paperdoll: the EquipItem swap machine over string-group bag items (Either right-then-left, 2H clearing both hands, shield bumping a held 2H, the 2H-replace rule, arrows/wands refused, slot swap, SplitStack) + FilterLocalItems hiding worn items; the GetItemImage forPaperDoll laws (armor archive 249+Human 2, the SetVariant material-family clamps incl. chain greaves 6, plate-iron dye 15 vs leather Unchanged 18, the Either-worn-RIGHT record+1, clothing archive+morphology with Blue default, jewellery only past slot 11, books none); the COMPOSITE doll over real art (plate cuirass + longsword composed 110x184, the layout carrying exactly both slots, GetEquipIndex walking every panel pixel finds exactly the two worn layers, unequip + recompose empties it); U8h - the armor value table verbatim (the 100-per-part baseline, leather chest 85 / steel greaves legs 55 via material*5, the DAEDRIC buckler material-BLIND at 5, the tower shield covering Head, swaps + unequips restoring, weapons/clothing never touching it, the (100-av)/5 display law, the verbatim armourLabelPos) + the starting seed (the interim dagger IN the bag worn, idempotent, probe bags untouched) and the worn-weapon binding shape (slots[RightHand] ?? null -> bare hands take the unarmed path) |
| droppedloot.test.js | 2 | U8e dropped loot: the verbatim treasure flat (archive 216, the 20-entry randomTreasureIconIndices, upload + batch at the drop position, the corpse-shape activation box) + EMPTIED piles vanishing from batches/targets (SerializableLootContainer's removal law) + empty drops minting nothing; the inventory window's remote side - Remove-mode local clicks feed droppedItems, remote clicks transfer back to the player (both Equip and Remove - RemoteItemListScroller verbatim), closing with session drops fires onDrop (the OnPop world mint) and loot-target opens default to REMOVE (the anti-accidental-equip law) with pile clicks transferring in |
| nativeinventory.test.js | 3 | U8d the native inventory window: the verbatim DaggerfallInventoryWindow rects (four tabs, the six 31x14 action buttons at x226, lists/exit), the tab+mode click machine (WeaponsAndArmor/Equip defaults, wagon/gold consumed no-ops, the interim info popup + clear), the AddLocalItem four-way filter law verbatim (weapon/armor not enchanted; magic = enchanted or Spellbook 132; ingredients = templates 0..77 exactly per ItemTemplates.txt; the rest to clothing & misc); the SHARED ItemListScroller module's rail law (up/down/page kinds at the verbatim rects, clamped paging, slot mapping) |
| nativetalk.test.js | 2 | U8b the native talk window: DaggerfallTalkWindow rect spot pins (whereIs/topicList/conversation/goodbye/tone radios), the click state machine over fake hooks (Where is -> categories -> buildings -> the answer appended to the conversation, tone radios, outside-rect not consumed, goodbye + onClose), the keyboard accelerators mirroring the session keys (W/digits/T/Esc); AUDIT 17d ListBox pins - topic rows 7px (FONT0003 fixedHeight, RowSpacing 0), conversation lines 11px (RowSpacing 4), DaggerfallQuestionTextColor (0.698,0.812,1) in the player-says panel (123,8,124,38), DaggerfallAnswerTextColor (227,223,0), the NPC name centred in its 197-wide panel |
| nativepanel.test.js | 2 | U8a the native 320x200 panel foundation: integer-scale centered metrics (1400x900 -> s4 letterbox 60/50, sub-native floors at 1), the screen->virtual point mapping round trip + outside-null, the DFU default label color (243,239,44); INFO00I0.IMG loading 320x200 through the real IMG reader + palette into the upload seam |
| townpopulation.test.js | 5 | T1 towns (CityNavigation/MobilePersonMotor/PopulationManager verbatim): tile weights (water 0/stone 4/dirt 6/grass 12/road 15/default 7) + the automap carve with the per-block row flip matching the rendered tilemap + occupancy flags + the spawn probe; the person motor (road-following seek via the 80% downgrade-leave, the 1.6-unit march, the politeness idle record 5 / guard 15); the pool laws (clamp(blocks/16,1,4)*24 sizing, the verbatim race/gender texture tables + guard 399, one spawn per 10Hz tick, the anti-skate first-move hide, the night recycle); the 2026-08-17 audit pins (single tick per frame with the timer RESET, the rect+62.5 spawn range gate, RandomiseNPC re-rolling at every spawn with the 1/32 guard branch, the self-target place() + nav rederive, the N/S/E-only best scan - DFU's Range(0,3) quirk) |
| animalambience.test.js | 2 | A4 the shared PlayRandomlyIfPlayerNear pass (dungeon + both exteriors): the verbatim AddAnimalAudioSource record table (0/1 horse, 3/4 cow, 5/6 pig, 7/8 cat, 9/10 dog, 2 silent) + the 768-unit distance + rand()<=100; the 16Hz cadence with the range gate BEFORE the roll (classic-stream sequence preservation) and the over-100 silent roll |
| ambient.test.js | 6 | A3 scene ambience (AmbientEffectsPlayer verbatim): the 14 dungeon one-shot clip ids 63-76 in enum order + storm 348-350 + sunny 437/438 + the rain 389/crickets 6 loops + water 439/114, the SERIALIZED scene wait windows (Dungeon 5/28, WeatherAmbientEffects 5/25 - overriding the script's 4/35), the WeatherManager preset mapping (rain/thunder divert, snow/fog/overcast fold to day/night), one-shots firing after the Next(min,max) wait somewhere-around (distance 10..20, min 13/max 104) with doNotPlayInCastle silent, the EXCLUSIVE ambient channel skipping while a clip plays, preset switches stopping loops (rain->storm restarts the rain loop, clearNight crickets, dispose stops), storm shots on the horizon ring (|xz| 0.94 / +0.34, min 3000), and the classic-cadence water (rand<50 WaterGentle AT the surface beside the player min 8; submerged bubbles sharing - and here losing - the channel; dry blocks silent) |
| anims.test.js | 2 | Directional attacks: verbatim DFU direction->strike mapping pins; delta-clip well-formedness (start/end 0, sorted keys); sampler continuity + keyframe hit + null past dur |
| pointerlock.test.js | 3 | requestLook never throws - swallows a rejecting requestPointerLock promise (the sh/< crash + lock:N frozen-yaw bug), tolerates the void-returning API, survives a synchronous throw |
| player.test.js | 11 | verbatim speeds/constants (audit 16e F1: the walk DRAG term 0.5*(100-max(30,SPD)) with its SPD-30 floor and SPD-100 zero pinned, the UNDRAGGED run base, the crouch-base run), collider ground/slide/step, motor gravity/jump, strafe = true camera-right, wall-ladder regression (step-up needs a clear raised path); P11 swim/levitate (GetSwimSpeed base*(skill/200)+base/4, levitate 4.0 look-directed with prior fall velocity dying, swim zeroing the look's vertical + float-up + the can't-surface clamp, waterWalking normal speed, the .7071 diagonal limit landing on the grounded path, the per-frame jumped flag); P12 crouch (controllerCrouchHeight 0.9 + the 0.8 eye, toggle edge, crouchSpeed for crouched walks and the crouch-base run for crouched RUNS (GetRunSpeed's branch, audit 16e F1), crawling under a 1.2 ceiling the standing capsule cannot enter, the CanStand probe refusing to stand under it and allowing it back in the open) and P12 breath (MaxBreath = LiveEndurance/2 int-div fortify-aware, the (END>>3)+4 short threshold + HUDBreathBar colors/width pinned, WaterBreathing (30,255) in the buff kinds both subType spellings, currentBreath riding the save envelope); the FIXED physics timestep (08-17 live hotfix: update() accumulates render dt and steps at 1/60 with the 0.25 jank clamp - a 10 fps jump reaches the SAME apex as 60 fps; raw-dt integration stole g*dt from the takeoff and collapsed phone jumps to ~0.1) |
| motorStairs.test.js | 10 | P14/P15 movement parity, NUMERIC traces (restores the reverted b9e9aa6 harness): a classic 0.3-riser staircase summits grounded, riser 0.45 climbs while a 78-deg ramp blocks and a 60-deg climbs (slopeLimit 70 is THE block criterion - a lone tall box crests via its ~45-deg edge contact, as Unity), a flat wall never ladders (the P9 facade pin), a 2.2 stairwell ceiling CAPS the lift ladder (stands tread 2, never wedges the head into the plane) while a 2.05 ceiling (no legal stand) refuses cleanly, jump apex = (4.5 x (1 + Jumping x 0.5/100))^2 / 2g (skill 30 -> +15%, not the one-frame 0.069 kill), the 0.1 s grounded-time gate blocks the first-frame re-jump, crouched jump x0.8 + moving-jump forward boost 0.05 with airborne momentum FROZEN through reversed input (airControl false), and a landed fall reports its distance (8-drop) while slowfall falls at -105*dt with the fall start re-anchored (lands billing ~0); P15 sneak - applied speed = walk/2 - 1/39.5 with IsMovingLessThanHalfSpeed TRUE while moving (the P13 stealth payoff - the subtracted classic unit lands it under the half line), plain walking not qualifying, running beating the held sneak, and the grounded-only latch (a sneaking jump stays sneaking through a mid-air release until the landing frame re-latches) |
| arrowflight.test.js | 3 | C13 host arrows (exterior/interior hosts, the no-targets subset of the S5 missile): constants single-sourced from spellcast.js + the oriented arrowMatrix law (nose-down r12 = -1), flight at MISSILE_SPEED with the swept wall kill and the 8s lifespan retire, the terrain landing via the collider heightAt floor + the late-resolving function-collider form + the all-dead list sweep |
| weaponrig.test.js | 5 | C9 the FP-weapon host rig (the non-dungeon hosts' surface, mirroring the audited dungeon laws): classic starts sheathed with DrawWeapon 78 only on unsheathing a REAL weapon (sheathing back silent), sheathed = no attack processing while an unsheathed right-drag past the threshold enters a Strike state with the pitch-matched swing sound on the entering edge and reaches its hit frame (paralysis holding the machine eventless), the zero-arrow bow auto-sheathe with the classic line from the draw path, and the WeaponEnvDamage ray - a door in reach BASHES and consumes the swing, other action objects Receive(Attack) and the swing continues, a wall in front occludes; C10 clickAttack carries the sheathed gate (the inline touch tap had bypassed it) |
| talksession.test.js | 4 | T3b mobile talk: the verbatim greeting thresholds (0/10/30 -> 7207/7208/7209, below 0 -> 7206) with the -20 refusal EDGE (< -20 refuses via 7205; exactly -20 still talks), the macro set (%pcf first token, %oth drawn only when the greeting carries it, oath = TEXT.RSC 201+FactionRace - the DFU oath fix, Nord 201/Redguard 203/Breton 204), the activation ray (cylinder hit at the controller radius 0.45/height 1.8, misses beyond radius/behind/above), greedy word-wrap; NextInteractionMode's verbatim Steal>Grab>Info>Talk wrap (the touch cycle button) |
| faction.test.js | 3 | T3a talk foundation: the real FACTION.TXT parse (exactly 366 factions, the Daggerfall province + its People-of child via relinkChildren, ruler power bonus 20..70, flat round-trip), the verbatim reaction sum (rep + biography + reactionMods[sgroup] + sGroupReputations[sgroup], out-of-range sgroup guarded) + the PlayerActivate distances (6.4/3.2), pickpocket (chance clamp 5..95 + the 5/level enemy modifier, the three outcomes: gold 1-6 onto the Currency stack / found-nothing on Dice100(33) / failure landing CrimeCommitted=Pickpocketing with guards FLAGGED) |
| freeaction.test.js | 3 | S22 FreeAction (26,255): the key both subType spellings + the Restoration (20,8) cost + AddState stacking; the AssignBundle hard-immune DROP (incoming Paralyze sheds silently - no stack, no chance roll, no message, throwing-roll proven); the READ-TIME fold (casting over a live paralysis frees NOW without curing - the bundle ticks underneath and RESUMES when FreeAction expires first) |
| concealment.test.js | 4 | S21 the concealment family (ConcealmentEffect/DaggerfallEntity/EnemySenses verbatim): the classic keys land as kinds - Invisibility (13,0/13,1), Shadow (24,0/24,1), Chameleon true (23,1) - with the Illusion duration costs pinned (40/120, 60/140, 20/80, 40/120, 40/120; skill 24); IsInvisible/IsBlending/IsAShade FOLD normal + true powers; a cast lands the buff and says the start line ONCE on new incumbency (You are invisible./blending./a shade.) while a re-cast STACKS rounds silently (ConcealmentEffect.AddState); and the P13 gate consumes the folded flags end to end - invisible always blocks with NO roll (the 13 seers exempt), shade rides the 4% branch, blending true the 8%, unconcealed rolls nothing, and expiry re-opens the gate |
| save.test.js | 2 | the snapshot/restore round-trip (entity fields, deep-copied stats/items/actives, spells re-resolved by index, extras + the S12 world/locationKey riding the envelope, the version gate refusing, storage round-trip + corrupt-JSON null), F12 piercing overlays while F9 stays gated |
| sky.test.js | 3 | SKY reader pins, panorama mirror law, night mapping |
| smoke.test.js | 1 | runner sanity |
| clock.test.js | 4 | hour gates, LightCurve pins, sun sweep, flicker determinism |
| restwindow.test.js | 3 | U7 the rest session machine: the sub-tick law (audit 16f: waitPerHour / minutesPerTick VERBATIM - the divisor is the constant 10, so a rested hour passes in 0.45 real seconds, loiter 0.75; the first cut divided by the 6 ticks-per-hour and rested ~1.7x slow), the 0-hour quirk (DFU tests hoursRemaining < 1 only AFTER an hour - resting 0 rests ONE full hour, vitals ticked), completions (timed wakes on TEXT 353 after its vitals ticks, full rest ends healed on 350 - instantly when starting already healed with NO time passed, loiter ends on 349 with vitals NEVER ticking - a throwing tickVitals proves it) and the 3-hour classic loiter cap constant, interrupts (enemies break at the hour boundary on 354 BEFORE vitals - a throwing tickVitals proves the order - with the hour's time still passed; death mid-rest ends at once with no text - the death screen owns the flow), and endEarly's per-mode finish texts |
| rest.test.js | 2 | S20 rest/exhaustion: HealingRateModifier floor(END/10)-5 (classic's negative-mod bug deliberately not recreated, following DFU), the health rate's Medical+60 base with the RapidHealing 100 upgrades (Always; InLight only when day AND outside; InDarkness otherwise) and its floor of 1, the fatigue rate maxFatigue/8 in stored x64 units, the spell-point rate zeroed by the NoRegenSpellPoints ability bit (the low-byte cast pinned), and the exhaustion outcome - a safe collapse rests one hour (all three rates + the 1071 text), enemies nearby die on 1072, water dies on the drowning line |
| riding.test.js | 2 | Platform riding (groundKey identity - the mover wins over the static floor within skin; the frame delta carries the capsule with ZERO lateral ejection; stepping off returns 'dungeon') + ClickToAttack (the DFU Range(UpRight, DownRight+1) six-direction table at roll boundaries, busy-machine refusal) |
| audio.test.js | 6 | A1 pure parts: pcm8->float32 verbatim, SoundClips indices vs SoundClips.cs, GetSwingSound pitch table + PlayHitSound roll families (boundaries MEASURED), the restored enemy sound columns (rat/imp rows, 61-entry count); A2 ambient data verbatim (IsTorchFlat 210/{0,1,6,16..20} + 5m/0.7 + Burning 420, the 201 animal record->clip pairs with gap records silent + 19.2m + the rand()<=100 classic cadence) and the action Play sound seam (soundIndex > 0 fires from effect origin / mover matrix, 0 stays silent) |
| datasource.test.js | 2 | ARENA2 key rule: uppercase basename across path styles (unix/windows/bare); readZip mobile ingest on a crafted archive - store + deflate-raw byte-exact round-trips, the arena2/ prefix filter dropping siblings, directory entries skipped, flat archives ingesting whole (real-zip witness: both DaggerfallGameFiles.zip and a self-zipped arena2 round-tripped ART_PAL.COL byte-identical in the arc probe) |
| bsa.test.js | 6 | BSA containers, closure invariant, FOO quirk |
| palette.test.js | 10 | 8 palettes, MAP.PAL x4, embedded reads |
| textrsc.test.js | 2 | the verbatim record table on crafted bytes (headerLength/6 - 1 count, id->offset walk, raw bytes INCLUSIVE of the 0xFE terminator, missing ids null), plainText flattening (NewLine, SubrecordSeparator variants, the two one-operand prefixes consuming without leaking printable operands, justify/control drops) |
| texture.test.js | 6 | 472-archive corpus, per-codec checksums |
| imgcif.test.js | 8 | IMG table, palettized files, CIF/RCI/weapons |
| arch3d.test.js | 5 | 10251-mesh corpus, UV rules, patch table, model 456 |
| blocks.test.js | 4 | 1295-block corpus, resource closure, FixRdbData |
| dungeon.test.js | 14 | dfRandom LCG, texture tables, RDB matrix order, action records, overlap removal, Privateer's Hold, 187-RDB closure, full 4232-dungeon sweep, R6 light collection pins, per-light flicker bounds, R7 water corpus (32/187) + Maorn pins, the P10 teleporter-resolution corpus gate (84 teleports, 82 resolve, N/W0000003 verbatim-dead like DFU's null NextObject) |
| monstercareer.test.js | 1 | ENEMY{nnn}.CFG careers from a crafted MONSTER.BSA through the shared BsaFile+ClassFile readers: fields real on the entity, session cache proven (throwing re-fetch), missing-index null path |
| actionchain.test.js | 9 | the Play cascade through EVERY flag (relays: Teleport/Activate/text/quest/Unknowns), chained doors keyed act:<position> running their OWN verb (OpenDoor unlock+open, CloseDoor close+relock-to-starting, Lock 16-if-unlocked, Unlock 0), special doors (verbatim DaggerfallActionDoorSpecial swing, C# `OpenDoor || CloseDoor && !door` precedence pinned), ExecuteActionOnToggle on the player toggle through the Door trigger gate (Direct-gated records stay silent), None-flag chained doors cascade without swinging, AttemptBash verbatim (open bash-closes, lock >= 20 magically held, d100 under 20 - lock bursts + clears) |
| activate.test.js | 1 | activationTargets single source: effect objects target on their precomputed aabb (crash regression - scenes read o.cpu on cpu-less traps), relays excluded, cpu path transformed through the matrix, door reach default |
| fpsweapon.test.js | 7 | the TRUE classic FP weapon (pivot 2026-08-17): WeaponBasics animation tables verbatim (records/frames per state incl. the bow draw-hold-loose), ItemHelper template-index -> WeaponType (enchanted promotion), WEAPON*.CIF corpus pins on real ARENA2 (frame counts/dims/offsets), the metal dye band, the 320x200 alignment math, the dye-parity pins (Steel skips as native, Silver DYES through the aliased 18 - DyeColors Silver = Unchanged = 18 routes into the Silver table), ToggleSheath verbatim (starts sheathed, the draw sound only on unsheathing a real weapon) |
| playerweapon.test.js | 3 | verbatim reach 2.25+0.25 + view/LOS hit rule + the swing-mod table, drag-gesture -> mapped strike on the shared machine (threshold + release reset), hit-frame kill through the full chain (deterministic rolls) + reach/dead gating |
| weaponPose.test.js | 2 | sampleClip UNITS regression (audit 2026-08-16): FP pose frames 2-3 sample the clip (phase * dur mapping pinned against a direct seconds sample), every enemy 1H clip samples at frame 3 (short durs died under phase-as-seconds) |
| formulas.test.js | 6 | FormulaHelper verbatim: Dice100/DamageModifier/H2H (sheet rule)/weapon min-max tables/material mods/body parts, career attack-modifier bits (bonus + phobia ADDITIVE) + enemy-type bonus, to-hit chain (dodging/4 bug preserved, the flat -50 + the +40 monster adjustment, weapon material x10 on the weapon branch, roll ordering, 3..97 clamp), damage paths (str-after-skeletal ordering, silver x2, material gate), the monster multi-attack loop (basics damage spans 1-3, the DFRandom reflex gate 50-10*(reflexes-2), per-hit enemy-type bonus, class enemies staying on the H2H path), the classic 0.25/MeleeDistance/35.156deg hit gate |
| spellcost.test.js | 2 | the verbatim component math (offset + A*starting + B*trunc(inc/per), averaged magnitude, per-effect skill scaling gold*(110-skill)/400, the zero-component fudge 60/100/160), target multipliers on the SUMS (x1.5/x2.5 pinned, the table's x2.0) + the castCostFloor 5 at mastery; the 2026-08-16d byte-cast pin (a real subType -1 Slowfall hits its table row, not the fudge) + the S19 Paralyze (28/100 duration+chance) and S19c cure rows (8/100 disease+poison, 20/140 paralyzation) pinned |
| spellcast.test.js | 5 | verbatim saving throw (tolerance precedence Resistant>Immune, immunity->0, DF-Chronicles proration edges at exact rolls, 5..95 clamp + MagicResist), magnitude roll (base+plus x floor(level/per), per-0 guard) + damage-family gate + full resolve skipping non-damage, the CastSpell 45.454546 cooldown firing through the sink with origin + the verbatim 1000 reset (fires at activations 1/23/45), TARGET_TYPES verbatim + applySpell (the one door) vs a fire-immune foe (nothing lands) and a soft target, skipped-family count, the ranges-II pure targeting (touch reach + nearest-live + LOS gate + corpse skip, the 4.0 sweep in/out/dead) |
| magicitems.test.js | 3 | MAGIC.DEF verbatim 62-byte records (index = stream position, -1 unfilled enchantment slots, artifact types read), CreateRegularMagicItem routing (regular-only filter, group-byte tables, the arrow re-roll, magic name swap + condition = uses, -1 slots filtered), the MI loot halving loop firing with the registry set and skipping without |
| magicka.test.js | 3 | SPELLS.STD verbatim 89-byte walk on crafted records (subType ALWAYS read - the dead 0xFF sbyte branch documented; invalid all -1 record gated), the 0x1C00>>8 multiplier decode table + SpellPoints floor, DrainMagicka max(1, flat?mag:axis) + 0-floor through the action system |
| advancement.test.js | 3 | the 35-entry advancement-multiplier table + uses-needed/level formulas, the raise flow (360-min gate, reflexes bit math 0x10000-((r-2)<<13) incl VeryHigh 1.25x, uses reset, the 95+/mastery cap), the level-up sum shape (primaries + majors - min + max minor) + headless leveling applying HP and the 4..6 pool |
| arrows.test.js | 2 | the ranged-attack gate (bow foes strike from sight via events at full speed, melee foes stay distance-gated with the timer untouched), Short/Long Bow -> Archery mapping + recoverable landed arrows stacking on the target |
| chargen.test.js | 4 | verbatim stat rolls (base + 0..10 inclusive, pool 6..14), skill tiers (28/18/13 + 0..3, defaults 3..6), HP (25 + hpPerLevel; per-level floor 1 + endurance mod), the interim lowest-first pool policy + full create (totals pinned), skillValue dual shape driving formulas (weapon skill hits where HandToHand misses on the same roll), the verbatim starting-spell sets (Mage/Sorcerer shared, Bard single, >6 none, resolution skipping missing records loudly) |
| charsheet.test.js | 3 | raiseSkills with a sink sets ready+pending WITHOUT applying (headless path unchanged), applyLevelUp (HP roll + endurance mod pinned, hand distribution, flags cleared, idempotent), the level-up screen sharing the verbatim clamps (pre-level floor, pool-0 block, confirm gate) with the hand-built stats landing |
| chargenui.test.js | 3 | the four verbatim pool clamps (stat max 100/pool 0/floor-at-rolled with points returning; skill pool-only up/floor-at-rolled), the flow end to end (name/gender/class/stats/skills, pool-0 confirm gates, stat-total conservation pinned, group pools landing on their skills), reroll replacing the working set |
| effects.test.js | 16 | HealHealth (10,8) instant + the verbatim duration arithmetic (base + mod x floor(level/per) with the multiplier CLAMPED AT 1, per-0 guard), the S15 GetMagnitude save gate (CasterOnly skips the save entirely; a RANGED heal on a magic-immune target saves to nothing), continuous (1,0) firing its INITIAL round at cast with saves rolled FRESH every round in the verbatim magnitude-then-save order, the DFU expiry shape (a 0-round entry survives its final pass and Ends the next), immune targets joining at zero damage, empty lists safe, the S8 buff kinds (slowfall/waterWalking/chameleonNormal keys, incumbent rounds STACKING on re-cast, expiry clearing the query); the 2026-08-16d subType byte-cast parity pin (real records read 0xFF as -1 - buffKind/isRegenerate accept both spellings); S19 Paralyze (0,255) - both key spellings, ChanceValue base+plus*floor(level/per), CasterOnly skipping the save, a chance fail consuming ONE roll and reaching no save, the ranged full-save immunity vs a failed save landing, the AddState-first quirk (a failed-chance re-cast still stacks rounds and rolls no save), expiry restoring the query; S19c the Cure family (3,0..2) - chance-gated IMMEDIATE bundle removal (a cure lifts the disease statMods NOW while a drain survives, Cure Poison strips poisons only, Cure Paralyzation lifts the paralysis instantly), chance fails counted with the disease intact, a ranged cure refused on a full save; S13 the SpellPoints/magicka family - DamageSpellPoints (4,2) instant saving-throw scaled DecreaseMagicka, ContinuousDamageSpellPoints (1,2) draining from the cast round with per-round saves; S15 the fatigue stat + families - HealFatigue (10,9) x64 (the parity FIX of S13's HealSpellPoints key: DFU Heal-SpellPoints is potion-only with no classic key), MaxFatigue (Str+End) x64 fortify-aware, DamageFatigue (4,1) + ContinuousDamageFatigue (1,1) x64 through the sink; S14 FortifyAttribute (type 9, subType=stat) - a live additive stat mod via liveStat, like-kind (stat + settings) stacking rounds, different-settings instances coexisting with mods summing, expiry restoring the base; S15 DrainAttribute (7,s) permanent-until-healed (incumbent by stat, magnitude ADDING per re-cast, the never-below-1 clamp vs the permanent value, ticks neither count nor act), HealAttribute (10,s) healing drain damage only (partial, overheal clamped at base + Ending the drain for next-pass removal, undrained no-op), the Transfer family (11,s attribute steal healing the caster's drained stat by the pre-clamp roll + separate incumbency from Drain; 11,8 TransferHealth hurt/heal both directions with the caster REQUIRED; 11,9 TransferFatigue x64 both ways), Regenerate (18,255) healing per round from the cast round with settings-keyed incumbent stacking, and fortified/drained stats moving combat statsToHit through the formulas |
| effectactions.test.js | 4 | Hurt21 every-20th gate + verbatim exclusive Range * level (min<=max guard), Hurt22-25 flat/axis every activation + Poison as DFU's own empty delegate, chain cascade lever->spike (ActivateNext-first order, activationCount), the verbatim Receive trigger gate (Collision03 blocks Direct/passes WalkInto, chains always valid, MultiTrigger's exact trio, undefined flags never fire) |
| enemyattack.test.js | 3 | verbatim reset-timer arithmetic (Range/level/reflex terms, /980, 0-floor), the floored-speed >>3 roll gate, strike gating (range+sight+22.5deg yaw) + hit event at HIT_FRAME_MELEE |
| enemyentity.test.js | 3 | ClassFile verbatim 74-byte parse (incl the (a<<16)|(c<<8)|b shuffle) on a crafted record, class entity rules (level, HP roll bounds, skills clamp, career Speed, city-watch +3..6), monster rules (predefined level, inclusive HP range, armor*5) |
| fnt.test.js | 2 | verbatim FNT layout on crafted bytes (header, 240-entry table, the L/R half SWAP with MSB-first expansion - x0 from the odd byte, x15 from the even, bounds null), the white atlas (256x240, pixel spot-checks) + measure (space rule, 1px classic spacing) + row-1 cell UVs |
| hudtext.test.js | 1 | the popup queue (4-line cap dropping oldest, per-line 2s life with independent lifetimes across a late add, expiry, empty ticks safe) |
| hud.test.js | 3 | compass scroll verbatim (trunc(258 x heading), wrap both directions, the 64 window never exceeds the 322 strip), bottom-anchored bar fill (v-window + clamps + max-0 guard) + integer scale flooring at 1, indexed->RGBA with the classic index-0 transparency |
| inventory.test.js | 5 | the verbatim stackable rule (equipped/enchanted never), AddItem merge-vs-append incl material split, CalculateWeightForMaterial verbatim (quarter-kg quantized, banker half-to-even: iron and daedric daggers BOTH 0.5kg) + leather formula + transferAll, the house-container predicate (418xx group, the 13-index list, shop-shelf 5 excluded, record = id%100) , removeOne (stack decrement / singleton splice / absent false - the per-loose Arrow consume) |
| inputmap.test.js | 3 | the binding tables (overlay chars/controls/null, F5/F6/Backspace/KeyC/none), routeKey precedence (overlay wins so Backspace edits not opens, toggles fire, the cast dir threads, unconsumed falls through), the one-shot click-cast latch (unarmed nothing, fires once, double-arm single fire) |
| inventoryui.test.js | 2 | the inventory window (cursor wrap, weapon equip via callback, arrows refused, gold not equippable, close), the spellbook (the interim known-list from the file excluding caster-only/non-damage, an entity's own book preferred, ready callback, null-map empty) |
| loot.test.js | 3 | 22 matrix rows exact (key C pinned) + brace-bounded group lists (no cross-enum bleed), gold x level + the WP halving loop at exact roll consumption + the '-'-key empty path, level-split ingredients (C1 scales/C3 flat), arrows stack/material, book template+variant, gender clothing, unknown-key -> '-' |
| enemyequipment.test.js | 4 | verbatim material walk ([64,128,...] modifier table, level clamps), armor materials (70/90 split, plate+weapon-material), variant-0 loadout + the init-100/subtract/class-60-clamp armor pass, variant-2 + monster keep-better + city-watch itemLevel-1 + weapon-vs-weaponless averages; G3 - equipmentItems mirrors DFU's Items.AddItem (weapon + shield armor item, no marker duplicate) |
| poisons.test.js | 7 | S19b poisons: the enum + timing tables pinned (weapon poisons 0-7 vs drugs 8-11, Arsenic 10/10/20-1000, Moonseed instant), Start rolling both Range(min,max+1) spans with an incumbent same-poison no-stack that still consumes the discarded instance's rolls, the Waiting countdown ticking its FIRST active minute at 0, minute catch-up running straight to Complete with negative statMods persisting until healed (heal-all cures on the NEXT CompletePoison round - never directly), drugs pushing POSITIVE mods stripped ONCE at Complete (Sursum's +str crash pinned) with Somnalius/Quaesto Vil fatigue x64 vs Aegrotat's RAW magicka, InflictPoison (career Poison-immunity veto, the full-save resist, the classic level-1 immunity, bypassResistance skipping the save), the ItemHelper weapon-poison roll (level gate, Orc/Centaur/OrcSergeant + class eligibility, Assassin 60%, Range(128,136) weapon poisons only) and the formulas seam inflicting ONCE on a damaging hit then clearing the blade (a miss keeps it), plus cure + the save round-trip carrying the entry |
| diseases.test.js | 13 | S18 diseases: the 17-row DiseaseData table byte-pinned (multiplier columns, damage spans, only Caliron's Curse/Blood Rot/Wizard Fever finite at 3-18 days) + the 0xff/0xfe markers and Diseases ids, startDisease gates (player-only, level >= 2, AddState's no-double-catch, the finite Range(min, max+1) day roll), InflictDisease (level-1 classic immunity consuming NO rolls, the full-save resist, uniform list pick), the daily tick (infection day = silent incubation, one damage roll per elapsed day through the multiplier matrix with statMods accumulating negative, daysPast catch-up rolling each day but alerting once per round, HEA hurting while FAT/SPL drain RAW units - DecreaseFatigue WITHOUT the x64, bug-for-bug), a finite disease's final day landing damage then ending with mods lifting when the same round's tick removes the entry, Heal{Attribute} healing disease statMods clamped at 0 WITHOUT ending the disease (it drains again next day), the OnMonsterHit rider table (rat 5%/bat 2% listB, zombie 2%/mummy 5% listC, nymph/lamia fatigue damage x2 x64, the vampire 0.6/2.0 float branch with vampirism ROUTED, werewolf consuming its roll, spider paralysis FLAGGED to S19), the multi-attack loop firing onMonsterHit per LANDED hit at the FormulaHelper.cs:662 site, and the save round-trip detaching statMods both directions; the S19 spider/scorpion rider (free-cast Spider Touch 66 through the castParalyze closure with no rolls, scorpion sharing the row, the incumbent-paralysis veto) |
| mobileunit.test.js | 10 | C11 classic sprite mobiles (DaggerfallMobileUnit/EnemyBasics verbatim): the record layout tables (Move 0-4/Attack 5-9/Hurt 10-14/Idle 15-19, back diagonals mirrored, speeds 6/10/4/4) + the quirk sets (rat idle inversion, ghost/wraith patterns, slaughterfish bounce) + GetStateAnims branch order; the signed-angle orientation law (front 0/back 4/symmetric flanks, yaw-relative); the attack chance ladder + the -1 damage marker (skipped, one hitFrame) + iterator-exhaust revert to idle; state transitions (hasIdle=false frame carry across idle<->move, hurt one-shot, the bounce reverse at n-2); the Giant Scorpion flip INVERSION + the orientation-switch frame RESCALE (frame*newN/oldN - the Ancient Lich 8-vs-4-frame overflow); the regenerated EnemyBasics anim fields (rat/imp sequences verbatim, all 42 monsters carry frames, >=5 with chance variants); AUDIT 08-17: the LEADING -1 (Frost Daedra's 50% [-1,4,5,0] variant - hitFrame flags at attack start, display advances to the second entry, exactly one damage moment) + the DFU priority (the attack EDGE overrides hurt, knockback-hurt never interrupts PrimaryAttack) + the FlyAnimSpeed 10 clock on flyers' move/idle (walkers hold at 6/4); C14 the Spell state (HasSpellAnimation -> records 20-24 for the Orc Shaman alone, everyone else casts over the primary records with NO ghost/wraith special, the shaman one-shot [0,0,1,2,3,3,3] playout to idle, and the interrupt laws: attack cuts a cast, hurt cuts a cast, a cast never cuts an attack); C16 the -1 frames ARE the damage moments (the Frost Daedra BASE swing lands TWO); C17 humanoids (FemaleThiefIdleAnims record-11 diagonals verbatim + the 483-female route, the ranged one-shot on records 20-24 with the -1 as shootFrame NOT hitFrame - one loose per draw - and the class extraction pins: mage 486/485, the shared archer sequence, thief 483, the 475 cast-scale class, corpse 380) |
| enemyspells.test.js | 5 | S16 enemy spellcasting: the verbatim EnemyEntity spell lists (13 monster casters spot+count pinned, EnemyClassSpells order, element cast sounds 352/353/350/351/349), SetEnemySpells (MaxMagicka 10xlevel+100, the six magic skills pinned at 80 over the flat fill via skillOverrides, missing SPELLS.STD records skipped loudly), the SetEnemyCareer assignment tail (monster lists, class level/3 buckets capped at 6, non-casters empty), EffectsAlreadyOnTarget (all-live vetoes, instants never count, per-stat identity), and the classic AI decisions (DoTouchSpell resetting the SHARED melee timer, the ranged 1/40 classic-update roll inside the 6..51.2 band, bow precedence, magicka-0 refusal, CasterOnly counting as touch) |
| enemymotor.test.js | 13 | C15 knockback (the WeaponManager formula exact + the 15-classic floor, monster/class weights 40/240/350, the motor shove along the attack ray INSTEAD of pursuit with the grounded y-drop (SimpleMove), hurtKnock riding the 5-classic threshold, the 5-per-classic decay to 0 with pursuit resuming after, and a knocked FLYER taking the full 3D ray AND falling - flyerFalls), C12 behaviour motors (flying 3D face-pursuit + gravity-free hover + MeleeDistance stop at striking height, aquatic WaterMove - the 2.5 surface margin cap, beached and waterless-block freezes with NO gravity, horizontal close - and the paralysis triad: flyers FALL, swimmers freeze, walkers stop pursuing but keep gravity), P17 fixed stepping (a 10fps foe pursues to the BIT-IDENTICAL spot/yaw as a 60fps foe - same 1/60 decomposition - and a 10s hitch integrates at most the 0.25 clamp), verbatim classic AI constants (sight/hearing/FOV/melee/turn/update-rate/system-divisor/walk-base), 11.25deg turn clamp, LOS wall + FOV gates, pursue-and-stop at MeleeDistance on the classic cadence; P13 stealth (the 2*((dist*Stealth)>>10) chance pins, the classic spawn/despawn bands with the 384-unit y hysteresis + the 1094 cap, the illusion gate - sees-through exemption, invisible always blocked, blending 8%/shade 4% with NO roll for unconcealed targets - and the flow: a quiet unseen player inside 25 units stays HIDDEN (hearing gates on prior detection), a failed stealth roll detects + sets hasEncounteredPlayer, earshot then holds it, the once-per-classic-minute cache, the odd-minute sneak skip and fast-move auto-detect both consuming NO rolls, ONE Stealth tally per minute across all foes, and chameleon blocking a plain-sight detection until the see-through roll pierces) |
| enterexit.test.js | 6 | verbatim landing offsets, door transforms, landing selection, dungeon exit, ladder climb, FixStanding floor snap |
| interior.test.js | 8 | ModelDoor extraction, static doors, interior layout, 6832-interior corpus, R8 light offsets + MAGEAA00 pins |
| people.test.js | 2 | AddPeople position/data verbatim, 14174-people corpus (6724/6832 interiors, archives 176-184) |
| poses.test.js | 1 | Static pose table (POSES): melee1H well-formed, sagittal angles within joint ranges |
| weapons.test.js | 2 | Verbatim DFU weapon data pins (enums, damage table, material mods/dyes, ApplyWeaponMaterial math incl. half-to-even weight round); Longsword piece well-formed on the armR fist |
| names.test.js | 5 | Region->bank verbatim, deterministic name composition on DFRandom, Redguard female stream parity, 76-NPC exterior corpus (SENT7 lamp quirk); S17 MonsterName (the engine-PRNG bank pick vs the forced bankType on one DFRandom stream, Monster2-female D set, both Monster3 branches, seed determinism) |
| enemies.test.js | 6 | 45x20 encounter tables, classic pick replay, fixed/passive/gender/water rules (gender FIXED-only: random flags = slot), the LIVE playerLevel banding (level 18 vs 1 differ on the same stream), Privateer's Hold pin (42 enemies / 25 fixed) |
| rig.test.js | 2 | Vendored Rewrite rig byte-parity vs canonical (16 bare-humanoid hashes), 710-face shape |
| rigmath.test.js | 5 | Two-bone IK invariants: exact bone lengths, reach/clamp, pole side, degenerate-target guard |
| paperdollart.test.js | 3 | Variant/cloak record resolution, material addressing (bases+morphology+clamps), plate-vs-leather band pins on 251 |
| piece.test.js | 2 | Sprite-shell geometry round-trip (synthetic + real plate-cuirass 1:1 witness + dye resolve). DAGGER_SPEC profile/pin cases removed with the trace rig. |
| neutral.test.js | 2 | Neutral paperdoll (buildNeutralBody): well-formed mesh (quad/normal/rgb shape, unit normals, on-ramp colours, feet-grounded ~7.5-head height), deterministic + snapped (blocky) shading |
| clothSim.test.js | 4 | Draped-garment cloth sim (verlet): stable (no NaN/explosion over 400 steps) for skirt/robe/cape; no clipping - every free particle stays outside the measured body collider after 500 steps under gravity + wind |
| relief.test.js | 4 | Relief identity witness + field arithmetic pins (synthetic + whole classic body), offsets pin (222,41 / 237,44), back shell mirrors z on the shared silhouette |
| charsprite.test.js | 1 | shared pixelize pass: projection-exact ph/pw at CHAR_PIXEL, hand-computed center/half-extents |
| backstab.test.js | 3 | verbatim 8-orientation back-facing wheel (records 3/4, Unity half-to-even at 112.5/157.5, sign-symmetric), backstab channels (chance on chanceToHitMod, x3 post-calc roll), FP pose composition (base+clip, couplings zeroed) |
| charmesh.test.js | 2 | Face packing (fan order, color/normal interleave), bare-humanoid pack + bounds |
| paperdoll.test.js | 3 | 27-slot EquipSlots verbatim, 288-template DB (index resolution, Short Shirt pin), BlitItems order |
| dyes.test.js | 3 | Clothing dye range shifts (Blue identity, Red 0xEF), 11 metal tables extraction-pinned, band-only swap |
| equip.test.js | 5 | Slot-rule extraction pins (94 cases), hands verbatim, paired first-open, 2H right-hand rule, all-wearables corpus |
| maps.test.js | 9 | 62 regions, converters, climate, city + Privateer's |
| snd.test.js | 3 | 459 sounds, byte-exact header, zero-length record 5 |
| climate.test.js | 5 | applyClimate verbatim rules, texture-info classification, exterior-window table, nature/ground archives, 8820-combo corpus sweep (735 pairs, 0 missing) |
| spectral.test.js | 2 | SetSpectral constants + gray remap (eyes 14->247, 96-index), the V^1.9 emission lerp (red eyes, body toward black) on a hand-built albedo |
| streaming.test.js | 7 | world-coord conversions, nearest-first 7x7, crossing offsets + column swaps, two-pixel jumps, vertical recenter, 30-crossing floating-origin invariant, 2000-step invariant fuzz |
| terrain.test.js | 14 | WOODS.WLD reader + corpus pins, perlin, cubic, sampler pins, umRandom (float + int), marching-squares lookup, blend flattening, Daggerfall-on-terrain integration, 15251-location corpus sweep, terrain key, nature scatter rules + integration |
| weather.test.js | 4 | verbatim fog tables, mapping, offsets, scales, fog math, lightning strobe |
| window.test.js | 2 | MaterialReader style constants, real glass-texel mask pins |
| world.test.js | 13 | mat4, meshReader, rmbLayout, location grid, flats, nature-quirk pin, city lights, R9 tilemap conversion + grid pins |
| manifest.test.js | 1 | drift guard: this table and the total against the real suite |

Two tiers per module:
1. **Synthetic fixtures** - in-memory data built inside the test. Always run;
   CI stays green with no game data (88 pass, 49 skip).
2. **Real-data validation** - gated on `ARENA2_PATH`; skip cleanly when
   absent. Pin observed counts, names, ids, checksums, and structural
   closure invariants.

Sourcing data in a fresh session: `sh tools/fetch-data.sh`, then
`ARENA2_PATH=/home/claude/dfdata/arena2 npm test`.

Visual proof: `npm run shot [out.png]` boots vite in-process and drives the
provisioned Chromium (playwright@1.56.0 <-> /opt/pw-browsers chromium-1194;
bump both together). CONTRACT: the scene query rides the SHOT_QUERY env
var, NOT argv (`SHOT_QUERY='shot&world&tod=22:00' npm run shot out.png`) -
argv[2] is only the output path. SHOT_TIMEOUT overrides the 30s ready
wait (streaming world wants 90000); SHOT_EVAL runs a JS snippet in-page
after ready (promise-returning expressions are awaited - top-level
`await` fails). Shot-mode probe hooks in the dungeon and world scenes:
window.__move(dx, dy, dz), window.__pose(x, y, z, yaw, pitch),
window.__player {pos, warp}, and in the dungeon __activate/__activateKey/
__ray/__actions. SwiftShader renders heavy scenes at ~7 fps headless -
time-based proofs must poll state, never trust wall-clock. `?shot` in main.js fixes the vantage and raises
`window.__shotReady`. Manual proof, not a suite gate.

Pre-push gate: `npm run check` (test + build).

Drift guard: `test/manifest.test.js` pins the total line and every row of
the table above against the real suite. Recalculate this doc in the same
commit as any test change.


