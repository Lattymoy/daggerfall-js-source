Warning: truncated output (original token count: 463379)
... 804938 bytes omitted ...

# Testing

Runner: `node --test` (bare - a trailing `test/` path breaks discovery on
Node 22). Suite: 8925 tests across 900 files.

**THE ARENA2-GATED PINS ARE HALF-BLIND, AND THAT IS A KNOWN COST.** A
pin behind `{ skip: skipReal }` never runs on CI, so a law change that
contradicts it goes unnoticed until someone runs the suite WITH game
data. Two did (2026-08-23, found while merging the input arc):
audit18_hosts_dungeon's swing-fatigue pin still asserted the drain
AUDIT 24 wave 42 had deliberately moved to the hosts, and
cityguards.test.js's stub renderer lacked the `destroyBillboardBatch`
wave 6's batch-free calls - it THREW rather than failed. Both are
fixed, and both by making the pin say MORE, not less. When you change
a law, grep the ARENA2-gated files for it too.

**AND THEY ARE ENGLISH-RETAIL-LOCKED, WHICH NOTHING SAID.** 2026-08-25:
the gated pins ran in a working environment for the first time, against
a real ARENA2 pulled from the DaggerfallSetup installer. 35 of them
failed, and NONE of the 35 is a port defect. 8 fail on FILENAME CASE
alone - retail data was written on a filesystem that did not care, and
that install carries `INVE00I0.img` beside `INVE04I0.IMG` and
`ANIM0001.vid` beside `ANIM0002.VID`, 76 lowercase names among 1,598.
The shipping data path already knew (`dataSource.normalizeName`
uppercases on both ingest and lookup, so the browser is fine); the test
harness and the vite dev middleware read the folder case-sensitively,
so `npm run dev` 404'd against files that were sitting right there.
The dev server is case-insensitive now; the pins still read the folder
literally and a mixed-case corpus still reddens them.

The other 27 are LOCALISATION. That install is the German edition, and
these pins encode the English retail corpus by value: FACTION.TXT has
374 records against the pinned 366 and names faction 41 Kriegergilde,
the map directory has no location called "Privateer's Hold" so five
dungeon pins dereference null, SPELLS.STD's indices move, CLASS*.CFG
carries different career flags (a Mage there CAN wear plate), and the
IMG corpus is 198 files against 263. Every one of those is the data
being legitimately different, not the port being wrong.

Two port-relevant facts fell out of the same run and are recorded
rather than fixed, because both change shipped behaviour: the
CLASS*.CFG NAME FIELD IS ENCIPHERED in that edition (CLASS00 reads
"vfu", CLASS15 reads "dfhdfh" - a repeated three-gram where German's
Barbar is a repeated three-gram, so the record parses correctly and
only the name is coded) and `chargenSession` falls back to
CLASS_CAREERS only when the name is EMPTY, so a non-English install
shows gibberish in the class list; and the port's font maps the
high-bit range to punctuation, so every umlaut renders as `#` or `}`
("St#rke", "W}ste"). Neither can be caught by any pin the suite has,
because the suite has never had non-English data.


| File | Tests | Covers |
|---|---|---|
| intro.test.js | 12 | INTRO2: byte-exact supplied logo and recovered theme, independently measured 20.533333s first closing beat, accelerating title landing and indefinite hold, non-overlapping credit holds, continuous perspective and aspect preservation, fully stationary reduced-motion camera, deterministic data-free terrain, audio-device clock and latency fallback, synchronous gesture unlock, one source across intro/menu, live volume/mute, suspension and idempotent cleanup including late fetch. `tools/introAudioCheck.mjs` independently decodes the score and distinguishes the first beat from its louder answer; `tools/introProbe.mjs` exercises real browser timing, handoff, responsive layouts and failures; `tools/introCapture.mjs` renders a zero-offset review video. |
| action.test.js | 9 | door lifecycle/verbatim constants, move tween + chain gate, activation picking; U6 text actions (the 8600/5400/7700 bases + the verbatim type-12 answers, ShowText firing, ShowTextWithInput skipping the up-front cascade with only a case-insensitive answer match firing ActivateNext, DoorText remapping 7701->7705 + holding the door on first activation + the axisRaw>5 trespass gate); P10 locks (lock value 16 / magic threshold 20, the clamp(5..95) lockpicking chance, all five LookAtInteriorLock text tiers, locked-door refusal firing onLockedDoor while solid, Lock/Unlock/Open/Close delegates through door-riding action nodes incl. only-when-unlocked and StartingLockValue restore) and P10 teleport (resolved next-object warp, null destination refuses, actionless-destination cascade no-op) + the block-instance key namespace (two instances of one RDB position both register and play independently - the 3108-dungeon repeated-block collision fix); AUDIT 23 save-load-11, the ACTION-OBJECT SAVE RECORD run end to end (collectSaveData/restoreSaveData: a Translation-flagged portcullis saved a third of the way up restores a third of the way up with its collider bucket at the restored pose and finishes the remaining 1.8 s, the Move pair is presence-gated so a pre-fix snapshot leaves the live tween alone, and the swing half - state/t/lock/activationCount - still round-trips through the same law) |
| audit28_arrowcounter.test.js | 4 | AUDIT 28 W2a (2026-08-30) - THE ARROW COUNTER (DaggerfallHUD.cs:270-292), a default-ON DFU feature the port never drew: with a bow drawn, the arrow stack count left of the compass, grey for real arrows, the translucent blue for conjured, "0" when the quiver is empty. Three gates in DFU's order - EnableArrowCounter, the weapon DRAWN, a bow (129/130, GROUP checked) in the bow hand, BowLeftHandWithSwitching picking the hand (:275) - the setting read live; the count is GetItem(Arrow, allowQuestItem false, priorityToConjured true) so a conjured stack listed second still counts first and a quest stack never does; placement :281-284 (compass width + label width + 8, centred on the compass's height) in the classic arm after the large-HUD branch returns. All four hosts hand drawHud the drawn state, and the interior frame - which had never handed drawHud a FONT, so no classic-HUD text drew indoors - now carries townTalk's. Campaign: 5 mutants (sheathed ignored, empty says nothing, real before conjured, group unchecked, no 8px gap), 5 killed. |
| audit28_bowdrawback.test.js | 5 | AUDIT 28 W12 (2026-08-30) - BOW DRAWBACK (WeaponManager.cs:341, :353-360). The machine's draw-and-hold half (StrikeUp to frame 3, the 10 s undraw, the StrikeUp -> StrikeDown release) had sat since FX1 with its comment saying it goes live 'the moment the drawback path does': this is that path. Off (shipped): the press shoots at once, as before. On: the press DRAWS (StrikeUp), the machine walks to the hold frame and waits while the button is held; letting go at the hold frame RELEASES (StrikeDown) - before it, nothing, the draw continues (the frame == 3 gate); ActivateCenterObject held at the hold frame UN-draws without an arrow and charges the full cooldown (no new draw until it clears); held past MaxBowHeldDrawnSeconds 10 the machine un-draws by itself. The activate key reaches every rig - a new createWeaponRig dep the three key-owning hosts fill from held(keys, 'ActivateCenterObject') and the dungeon ctx threads from its host's opts. Campaign: 3 mutants (release before drawn, no cancel, looses while held), 3 killed. |
| audit28_flicker.test.js | 6 | AUDIT 28 W2d (2026-08-30) - THE NEAR-DEATH WARNING: HUDFlickerController.cs + HUDFlickerBase/Fast/Slow.cs ported whole into ui/hudFlicker.js, pure with dt and the roll injected. GetPlayerCondition's Dead-then-thresholds (STRICTLY below 0.2 / 0.4); both Init tables verbatim; the fast flicker climbing at 7/s to 0.4, reversing at the bounds (checked BEFORE the step, so the clamp frame does not reverse) and timing out after seven reversals - which can read 8, because Cycle sets IsTimedOut and STILL runs CheckReverseAlphaDirection that frame, verbatim and pinned as such; the slow throb never timing out on its own; RandomlyReverseAlphaDirection's chance climbing 0.008/s from -0.01 and resetting there; the clamp to [0, alphaUpper] NOT alphaLower, so a decreasing step overshoots the lower bound for one frame. NextCycle: the setting and the two fade gates block the write (D4 2026-09-02 fed both from ui/fadeLayer.js - they were answered false while nothing in the port produced a fade, and they are what stops the Normal arm clearing a smashed-to-black screen, since FadeBehaviour targets the very panel this controller writes); Normal clears; Injured runs the fast burst and a fresh HealthLost restarts a timed-out one; Wounded holds the slow throb while the fast burst runs and hands over when it times out; Dead writes nothing. Wired UNDER the bars in both HUD branches as the parent panel's tint, off VitalsChangeDetector's HealthLost the vitals rig just computed (hudVitals.lastHealthLost). Campaign: 6 mutants - 5 killed; `condition !== 'Wounded'` on the gain arm PROVEN EQUIVALENT (the Wounded arm overwrites slow.IsTimedOut from the fast one every frame, in DFU as here). |
| audit28_friendly.test.js | 5 | AUDIT 28 W2b (2026-08-30) - MELEE FRIENDLY PROTECTION (WeaponManager.cs:930-944 + the :1057-1064 fallback), a default-ON law the swing did not have: the bounding-box pass strikes every foe in reach EXCEPT a PlayerAlly (the LIVE team) and a pacified (non-hostile motor) foe - townspeople are the hosts' civilian arm, which already runs only after the pools miss - and only when NOTHING was struck does the vanilla SphereCast take the one thing in front, so a pacified foe can be hit only when it is alone; the port has no ray at that point, so the NEAREST in-reach protected foe stands in (recorded). Before this a pacified foe beside a hostile one took the swing whatever the setting said. Setting read live, LIVE at playerWeapon.js, the three pools pinned to resolve through the one resolveHit. Campaign: 5 mutants (pacified unprotected, no vanilla arm, farthest picked, fallback always, any team) - the farthest-picked mutant SURVIVED a deepEqual over structurally identical fixtures and the pin was rewritten on identity; 5 killed. |
| audit28_handedness.test.js | 3 | AUDIT 28 W13 (2026-08-31) - HANDEDNESS (StartGameBehaviour :269 -> FPSWeapon.FlipHorizontal, :378-388 and :459-464). Setting 1 - DFU's 'left hand weapons' checkbox; 2 and 3 are inside GetInt's 0..3 and do nothing, `== 1` - mirrors the weapon art (the atlas rect from xMax with -width; here u 1 -> 0 through the screen quad's linear sample) and swaps AlignRight for AlignLeft on the three hand-symmetric states, Idle/StrikeDown/StrikeUp; a left or right strike keeps its art and its side, and AlignLeft is never swapped. The module header had recorded the flip as unimplemented 'until a settings surface exists'. Key LIVE, the screen shows the two hands. Campaign: 3 mutants (flips side strikes, no align swap, 2 and 3 flip), 3 killed. |
| audit28_headbob.test.js | 7 | AUDIT 28 W10 (2026-08-30) - HEAD BOBBING: HeadBobber.cs ported whole into player/headBobber.js. Controls/HeadBobbing ships True - DFU's camera bobs and nods as you walk - and the port's never did. The style table (Swimming > Running > Crouching > Horse > Walking) and bobSpeed = WalkStepInterval/2 verbatim; the gates (off, dead, paused, climbing, AIRBORNE - so DFU's landing bounce arms only through the swimming arm, kept verbatim); the timer starting at the crest and walking at velocity * bobSpeed * dt; the cos/|sin| path blended from rest across the first PI of the begin timer; the nod as a per-frame OFFSET on the look (|sin|*nodX pitches down, -sin*nodY yaws - PlayerMouseLook writes absolute angles and Rotate sits on top, so it must not accumulate: removed before re-applied, and switching the setting off leaves the look exactly the mouselook's); the release as a 0.5 s lerp from where the camera IS to rest with the timer unwinding, the reset to PI, the mid-stop restart at PI; the bounce's 0.17 dip over 0.10 s and back with the water speeds, Mathf.Lerp's clamp, the 2*PI wrap. The position rides player.eye as a WORLD offset the motor adds (every camera and every ray reads it, as DFU's read the camera transform), the local right/up taken through HANDEDNESS' right = (cos, 0, -sin); three hosts drive it after the recoil off the motor's moveSpeed (MoveDirection.xz) and axes. Campaign: 6 mutants (nod accumulates, signed rise, no begin blend, no restart at PI, water full speed, airborne bobs), 6 killed. |
| audit28_looksmoothing.test.js | 7 | AUDIT 28 W7 (2026-08-30) - MOUSE LOOK SMOOTHING (PlayerMouseLook.cs:100-105, :154-166; wired at StartGameBehaviour :215). The setting ships 0.5 - DFU's default look IS smoothed - and the port applied every raw delta straight to the camera on the event. player/lookFilter.js keeps the RESIDUAL owed to the camera and pays a frame-rate-scaled fraction each frame: GetFrameRateScaledFractionOfProgression verbatim (identity at 60fps, 2/3 at 30, 1/3 at 120; fraction 1 stays 1), the per-frame factor 1 - G(1 - s) with the Smoothing setter's SmoothingMax 0.9 clamp, and a frame-for-frame equivalence proof against DFU's own lookCurrent/lookTarget lerp on the same deltas; the pitch clamp lands on the TARGET (:142) so the camera never overshoots and the excess is forgotten; an external camera write (a door's facing, a load, a teleport) rides along with no resync because the residual is a delta. All four hosts route both look sites (mouse and touch) through the filter - no raw delta reaches the camera - and tick it on the frame's unscaled dt before the camera is read; the screen's range is DFU's clamp (0..0.9), not the 'ours' 0..1 it had. Campaign: 5 mutants (the fraction formula, no SmoothingMax clamp, target unclamped, the wrong fraction kept, a host never ticks), 5 killed. SELF-AUDIT 3: F-C1 the tick is gated on the host's paused expression (Update :241-244 returns before ApplyLook - the owed look waits under a window); F-C2 a held swing (raw right button on the window, ungated; walk mode; not a bow) is SetFacing(lookCurrent) - LookFilter.settle() drops the owed look, all four hosts pinned on their three-answer shape. +2 mutants killed. |
| audit28_moveaccel.test.js | 5 | AUDIT 28 W8 (2026-08-30) - MOVEMENT ACCELERATION (InputManager.cs:1445-1497, wired at :432), a default-OFF control the port stored and ignored: the hosts produced the movement axes as the bare held-key difference. player/moveAxes.js is one InputManager Update - flags cleared, a force per held action (right, left, forwards, backwards in :1840-1852's order: the axis climbs at moveAccelerationConst 9.8/s toward +/-1, or IS the scale without acceleration), then ApplyFriction (an axis whose impulse was not raised decays at 9.8/s to 0, clamped, or snaps). Pinned: the OFF identity (opposing keys stay the neutral difference - DFU's 'last wins' is keybind-order-dependent); the climb (about 6.1 frames to full at 60fps), the clamps both ways, the decay never past 0; opposing keys under acceleration cancel and hold; a reversal moves TWO steps a frame while the axis is still positive (force AND friction, verbatim arithmetic). All four producers (world, exterior, dungeon, worldModes) hand the motor the axes and advance them only on frames the motor runs (a held overlay is timeScale 0). Campaign: 5 mutants - 4 killed; 'positive friction unclamped' PROVEN EQUIVALENT (the negative-side friction on the next line catches the overshoot in the same Update). |
| audit28_recoil.test.js | 5 | AUDIT 28 W9 (2026-08-30) - CAMERA RECOIL: CameraRecoiler.cs ported whole into player/cameraRecoiler.js. Controls/CameraRecoilStrength ships 3 (High) - DFU's camera reels on a hit by default - and the port's never did. The constants and both formulas verbatim (baseMaxRecoilSeverity 50, the 2% threshold STRICTLY above, timerStart (5 + floor(pct*5))*PI, the setting's fraction table, the health factor clamped to [0.015, 1] - at its floor on every frame but the hit's since HealthLost is per-frame); the gates (Off, paused, at-or-under 2%); a hit starting the sway on a unit axis (rolls stands in for insideUnitCircle.normalized), the timer falling at 2*PI/s, the ROTATE as a PER-FRAME OFFSET on the look (Unity's +x pitches DOWN, so the port subtracts from its +pitch-is-up; +y turns right as +yaw does; PlayerMouseLook overwrites the camera's local euler angles every Update, :257, so it never accumulates - SELF-AUDIT 4's F-D1 corrected a first cut that did, and a pin that asserted it) so the view returns EXACTLY to the mouselook heading when the sway ends, dying out at timer 0 (5*PI = 150 frames at 60fps); a second hit RESTARTS with a new axis and timer; reset stops it. The three player hosts run it after the look on the same paused gate off hudVitals' lastHealthLost/lastHealthLostPercent (VitalsChangeDetector's fields, computed last frame - DFU's two MonoBehaviours have no fixed order either). Campaign: 5 mutants (>= threshold, pitch sign, no floor, round for floor, runs past zero), 5 killed. |
| audit28_settings.test.js | 6 | AUDIT 28 W1 (2026-08-30) - SETTINGS DFU READS THAT THE PORT ANSWERED WITH THE DEFAULT. The sweep: every key tiered `stored` has a DFU consumer; does the port's read it? Four modules quoted the setting in their own header and then hard-coded its default - QuestRumorWeight (TalkManager :1452; no host ever passed the dep), DisableEnemyDeathAlert (EnemyDeath :82), DungeonAmbientLightScale (PlayerAmbientLight :89, the plain-dungeon arm ONLY - castle and special-area unscaled), NightAmbientLightScale (:123, both exterior hosts). Each read now lives at DFU's own site with DFU's getter range (1..100, 0..1), the four keys are LIVE at the module that reads them, and a shape guard holds that a consumer naming a Settings key reads it. Campaign: 5 mutants (rumor default, alert ungated, castle scaled, world night unscaled, tier left stored), 5 killed. |
| audit28_smallerdungeons.test.js | 9 | AUDIT 28 W4 (2026-08-30) - SMALLER DUNGEONS (MapsFile.cs:766-797, :1366-1444; Quest.cs:284; SerializablePlayer.cs:224, :462-472), a default-OFF feature the port carried only the save field for. The five-block plus: a random interior block centred and starting, four random border blocks (name starts with B, case-insensitive) around it, addresses verbatim, DFRandom seeded on the RAW MapId - the pin re-derives the exact draws by hand on the same stream, holds determinism per dungeon, and holds the SOURCE UNTOUCHED (the port's locations are cached objects the exterior shares, so the generator answers a clone; DFU copies a struct). Threshold <= 5 passes through; both verbatim throws (main story; an empty pool - six interior blocks and no border). UseSmallerDungeon's gate order: dungeon, main story, a live quest's FROZEN state through its first SiteLink (Enabled and Disabled both outrank the setting; NotSet falls through), then the setting, read live. Quest.Start stamps the state (frozen across a flip), the save carries it, an old envelope restores NotSet. The entry seam sizes the location BEFORE buildDungeonContext with the bridge's machine. The dungeon save stamps the raw setting (SerializablePlayer :224) and a load under the OTHER setting warps to the start marker with a HUD line - story dungeons and old envelopes never, and the warp sits after the position restore so it overrides it. Campaign: 9 mutants (threshold off by one, fixed seed, centre from the border pool, Disabled ignored, mutates the cache, Start never stamps, old envelope warps, warp always, story warps), 9 killed. SELF-AUDIT 2 (same day): F-B1 the warp now goes through startSpawn({preferEnterMarker:false}) - the entry law's floorLanding over the START marker, DFU's own member at :470 - not the raw marker position; F-B2 the quest world's maps wraps getLocation/getLocationByName through dungeonLocationFor (DFU's law lives INSIDE GetLocation, so markers come from the five-block dungeon; the wrap late-binds the bridge's machine and keeps every other maps method by prototype); F-B3 the enum is DFU's ORDER - NotSet, Disabled, Enabled (Enums.cs:758) - the first cut shipped it backwards and the values are the save format (stamp ? 2 : 1, compare === 2), pinned by value. +4 mutants killed. |
| audit28_swingmode.test.js | 5 | AUDIT 28 W11 (2026-08-30) - WeaponAttackThreshold + WeaponSwingMode (WeaponManager.cs:306-350, :808; StartGameBehaviour :263). THE GATE IS THE SETTING: StartGameBehaviour writes Settings.WeaponAttackThreshold over WeaponManager's 0.05 field default and the shipped ini is 0.005 - the port gated the swing on the field constant and so demanded TEN TIMES the mouse travel DFU does (a 12px drag on a 1200px screen fires in DFU and never did here). The field constant stays as weaponStates.ATTACK_THRESHOLD, what it is; the gesture reads the setting (GetFloat 0.001..1) at the point of use, and the AUDIT 24 trail-vs-sum pins pass the field constant explicitly, since that is what they were calibrated on. WeaponSwingMode 1 is CLICK to attack (the press edge, holding is not more presses), 2 click OR hold (re-attacks as soon as the machine is free), both in a random direction of Random.Range(UpRight, DownRight+1) - six of MouseDirections' eight - with no gesture tracked; a bow ignores the mode (`|| bowEquipped`). Keys LIVE; the screen shows the threshold as a thousandths ratio (a new 'raw' number format - 0.005 must not round to 0.01) and the three modes by name. Campaign: 3 mutants (field default back, hold attacks in mode 1, all eight directions), 3 killed. |
| audit28_togglesneak.test.js | 3 | AUDIT 28 W5 (2026-08-30) - TOGGLE SNEAK (PlayerSpeedChanger.cs:75-78, wired at StartGameBehaviour :277), a default-OFF control the port stored and ignored: with Controls/ToggleSneak the sneak mode is `sneakingMode ^= ActionStarted(Sneak)` - a PRESS flips it, holding is not more presses, release keeps it, a second press ends it - and without the setting the mode is the held key exactly as before. The capture runs every frame in the motor; P15's grounded latch still decides when it takes effect, and running still beats sneaking while it lasts (the toggled mode survives a run and comes back). Read live at the point of use. Campaign: 4 mutants (flips on held, mode ignored, always toggle, no edge), 4 killed. SELF-AUDIT 3 (F-C3): running CLEARS the mode (ApplyInputSpeedAdjustment :121-125) - a run ENDS a toggled sneak; the first pin had asserted it comes back. Corrected, +1 mutant killed. |
| audit28_wagonprompt.test.js | 3 | AUDIT 28 W2c (2026-08-30) - THE EXIT-DOOR WAGON PROMPT (PlayerActivate.cs:649-664 + DungeonWagonAccess_OnButtonClick :1133-1145), a default-ON flow the port lacked and the PRODUCER inventorySession's NT3 note had been waiting for: a dungeon exit with a Small_cart in the pack and DungeonExitWagonPrompt raises TEXT.RSC 38 as a YesNo box and RETURNS - No leaves (:1137), Yes calls AllowDungeonWagonAccess() and opens the inventory (:1141-1142), whose CheckWagonAccess FIRST arm selects Remove and shows the wagon wherever the player stands (the proximity arm still only shows it and keeps OnPush's mode); Escape is DaggerfallMessageBox's allowCancel - CloseWindow with no button - so ServiceFlowWindow grew an onEscape a box may name (without it Escape stays No, the guild offers' shape). The exit itself split out as exitDungeonNow so the prompt's No can take it a frame later; no cart or no setting = immediate as before. Campaign: 4 mutants (prompt keeps Equip, Escape is No, setting ignored, No does nothing), 4 killed. |
| audit39_characters.test.js | 11 | AUDIT 39 (2026-09-01), the characters group - stopDistance switches to ClassicMeleeDistanceVsAI per target (`EnemyMotor.cs:1093-1096`), LiveSpeed re-read per tick instead of frozen at spawn, passive fixed markers spawn with their authored reaction, paperdoll payload's dead imports retired, villager hairstyles read, the editor AO binned |
| audit39_combat.test.js | 7 | AUDIT 39, the combat-formulas group - fpArm's renderer/camera rebound every frame (a dungeon visit no longer freezes the MW arm), the racial-override hit hook fed the LIVE classic minute, the monster reflex gate reads the attacker's own entity, ImprovesTalents' hearing/adrenaline flags read where they are written, lycanthrope combat-voice suppression live |
| audit39_dungeonshared.test.js | 11 | AUDIT 39, the dungeon-shared group - the dungeon exit RETURNS the death presenter, avoid-death hook and infection host it borrowed, the pause window's dead Pack/Chronicle buttons act, the save-restore foe cull honors dropCandidate, the dungeon death sinks from the live capsule, interior NPC rigs ground on the live foot, infection videos claim the frame generation, townTalk's greeting takes the overlay slot |
| audit39_entity_magic.test.js | 9 | AUDIT 39, the entity-magic group - maxMagicka's live getter installed on every load path, the Oghma's 30-point pool reaches the level-up UI, bootstrapRegionPower runs after the faction store attaches, biography effects before the starting kit (DFU's creation order), HealthLeech's Used/Strikes doors mounted with real stamps, MinimumCastingCost ported, the Mace's drain takes DrainEffect's clamp, the lycan move-sound timer re-arms on morph |
| audit39_formats.test.js | 8 | AUDIT 39, the formats group - the WEAPON CIF loop takes DFU's 64-record bound instead of hanging on TotalSize 0, spectral emission lerps alpha, BssFile loads the truncated tail, MAGIC.DEF keeps the post-NUL tail, textRsc's step-back tests tokens, clipSweepTimes takes the array its callers pass, the spell-stance turn groups compose, flattenNif carries the six dropped classes, NiSourceTexture/NiBoolData read by the reference's law, PRT_Hair survives a worn part's slot string |
| audit39_guildstravel.test.js | 14 | AUDIT 39, the guilds-travel group - the TG/DB initiation quest end GRANTS membership (`Guild.IsMember` law), all court exits FillVitalSigns, banishment sets its SeverePunishmentFlags bit, guild service and travel gold gates count letters of credit, ship travel routes from the boarding position (`GetPlayerTravelPosition`), the knightly free-room perk reaches both fare doors |
| audit39_itemstrade.test.js | 13 | AUDIT 39, the items-trade group - staging an item MOVES it out of the pack (the repeat-pay duplication is dead), every mint carries `value = basePrice` (loot sells for its worth, guild potions cost money), the shop's MagicItems arm takes the rarity gate and the stock roll, IsItemStackable's potion/book arms live, the Identify SPELL path skips the paid-service gold gate, PAINT.DAT loads and paintings describe themselves |
| audit39_motor.test.js | 16 | AUDIT 39, the player-motor group - CancelMovement on the levitate/swim mode edges (Levitate saves the fall it was cast to save), crouch refused while mounted, the mode returns zero stale momentum, the horse's 1.75 jump multiplier and the cart's jump cancel, ToggleRun/AutoRun live, CameraRecoiler.reset wired to load/relocation/court |
| audit39_render.test.js | 13 | AUDIT 39, the render group - one fog per sprite (the RT and the composite split the factor), the sprite RT fogs by ITS camera not the world's, MaterialReader's 157-record auto-emissive table ported (lit torches glow), renderer.stats sees every pass, releaseTexture bumps _texGen, the sub-mesh cache keyed to survive streaming, star layer 0 lives, sunOcclusion's CPU twin takes the horizon term, the overworld pass marks its foreign seam + PERF-ON: F50's draw-site sweep MADE GENERATIVE. It enumerated `Elements|Arrays|ArraysInstanced`, so `drawElementsInstanced` - the text run's one draw - matched nothing, and the pin failed reading an OVER-count for the one reason it was never meant to fire on. A rule enforced by an enumeration is a rule enforced by memory. Every `gl.draw*` is a draw now, with WebGL2's one non-drawing `gl.draw*` (`drawBuffers`) the single named exclusion, so a future draw entry point is covered unasked. |
| audit39_savesettings.test.js | 8 | AUDIT 39, the save-settings group - the classic import reads EnchantmentTypes.None as -1 and converts potions/recipes to potionRecipeKey plus the five AssignCharacter fields, the faction save carries ally/enemy/ruler columns, snapshotPlayer takes travelMap/escortingFaces/smallerDungeonsState, transportMode rides the envelope, Reset publishes to live consumers |
| audit39_terrainlayout.test.js | 9 | AUDIT 39, the terrain-layout group - exterior ambient applies the weather sunlight scale DFU's twice, the storm lightning flash is isEnhanced-gated, skyOffsetForWeather's dead season parameter retired, terrainGenClient copies the worker reply by manifest, the windmill subrecord append stops corrupting the shared block's building bound, archive-216 treasure flats leave the enemy-marker array, streamed pixels clamp to the world map |
| audit39_uicore.test.js | 15 | AUDIT 39, the ui-core-hud group - pixelDial's commit releases the _open singleton, the item rail pages by the scrollbar thumb, the dead touch attack(dx,dy,held) hook removed (the tap is the strike), the list picker's selected row drops its shadow, text metrics take the ASCII fold, and the classic HUD laws run under the DEFAULT skin: the vitals detector (CameraRecoiler lives), Detect compass markers, escort faces, the nameplate collider arm, the docked HUD shrinks the viewport, InteractionModeIcon none, whole-pixel vitals fills |
| audit39_uiwindows.test.js | 9 | AUDIT 39, the ui-windows group - the enhanced spellbook/char-sheet doors answer the host's input/click/wheel contract (no more keypress TypeError on the default skin), the classic inventory grows DFU's twelve accessory buttons (equipped jewellery reachable again), the char sheet shows GetGoldAmount, censor welds honor PlayerNudity, the enhanced overworld map calls GuildManager.FastTravel, settingsWindow keeps onPickMorrowind, ArrowUp with no selection stays home FD1 (2026-09-11) RETIRED F146 (onPickMorrowind on the SettingsWindow) with the window; the Morrowind attach door is the Mods page's card. |
| audit39_worldlegaltalk.test.js | 15 | AUDIT 39, the world-legal-talk group - the crime region is read at the MOMENT of the crime (a getter, not the boot value), both hosts supply spawnCityGuards, the quest machine's six region/vampire faction seams mount, quest legal repute reads and writes ONE region, GetFactionName's HolyOrder arm reads factionId, currentWeatherKey answers the live weather, talkMcp carries its five missing reads, common rumors macro-expand, the regional where-is walks the region's map table, residences enter the building directory, and a directory row's position is BLOCK ORIGIN + the subrecord's block-local Position - two blocks a cell apart keep their own coordinates instead of stacking on block (0,0), so the compass hint whereIsAnswer speaks points east rather than southwest |
| audit39_worldmodes.test.js | 19 | AUDIT 39, the worldmodes group - a window in the OUTER host's slot pauses the modal frame, the aborted dungeon transition unwinds mode/collider/context, detected theft summons the watch, castleNPCsSpokenTo clears on entry, the scene cache keeps an action door's pose and lock, interior movers and the weapon swing freeze under a window, interior arrows resolve impacts on player and foes and the player's own arrows land above ground (`playerArrowHitFoe`), the static-NPC door runs StartNewConversation, the interior frame passes cursorActive, the deferred building entry catches + AUDIT 58 REVIEW (2026-09-03): the interior arrow's TARGET LIST was `interiorFoes?.foes` alone, so a shaft loosed at a watchman spawnCityGuardsInside had stood in the room met nothing and died on geometry - arrowFlight's `if (foeImpact && foeTargets)` walk is a shaft's only foe contact - after the loose had spent the Arrow and tallied Archery, while the same host's MELEE ray hit that watchman. DFU makes no pool distinction (DoCollision takes whatever DaggerfallEntityBehaviour the struck collider carries), and both sibling hosts already fed both of theirs. It reads the join now, and the player-arrow damage door takes the same `_encounter` split this host's sinks take, so a killed watchman runs the crime and the corpse in the pool that owns it. 2 mutants, 2 dead | AUDIT 62 F20: R16's struck foe carries a 1.6 centreOffset and the splash is asserted at feet + centreOffset - hitTransform.position is the sprite CENTRE, not the base. + FOE1 (2026-09-15, Mac relaying players: "during online play, certain enemies cant be damaged"): THE OPTS A CALL REALLY PASSES. HT1 appended `// HT1: the torch keys` to the end of the physical line that already carried `onFoeHit: (hit) => host.onFoeHit?.(hit),` in the buildDungeonContext opts, and the property went into the comment with it. `opts.onFoeHit?.()` is the ONLY way a joiner's blow on a layout foe reaches the room's host - damageFoe's non-authority arm sends and RETURNS, applying nothing locally - so every layout foe in every online dungeon absorbed every blow from everyone but the authority, in silence, for eight slices. It reads as "certain" enemies because foes past the layout (quest foes, summons, rest encounters) take the local path and died normally beside them. The pin STRIPS THE COMMENTS from the whole call and then asserts the six online seams survive (onFoeHit, onActions, peers, selfId, onLootClaimed, foes) plus the host seam and world.js's wire, because a text match over the raw line is exactly what failed to catch this - the characters were right there. 3 mutations (the original bug recreated verbatim, a sibling property swallowed the same way, the wire end removed), 3 dead. |
| audit39_worldstate.test.js | 15 | AUDIT 39, the world-state group - paralysis gates the two above-ground hosts' movement and weapons, the enhanced HUD draws without hudArt (its no-ARENA2 law), a teleport/quickload sweeps the live foe/guard/missile/arrow pools before restoring the save's (`CleanupUntrackedObjects`), fast travel refuses with enemies nearby, the exterior touch tap defers to a readied spell |
| a10_world_misc.test.js | 26 | ROAD A10 (2026-09-01) - cross-host Recall anchors carry {host, location, position} through the one load door, the ship board/disembark rides sceneCache, and DungeonLightHandler's UnscaledBlockRange XZ rule composes with the 16-slot GPU cap. + CLOSEOUT: PlayerGPS FREEZES its map pixel underground (StreamingWorld.cs:297-301, whose own comment gives the reason - a dungeon can extend beyond its map pixel). The port converted the player's DUNGEON-LOCAL feet through the streamer's exterior origin, and RDB block origins are signed, so one block west or south of the start slid the pixel by one - which is exactly the read IsSameInterior's dungeon arm compares against the anchor's streamer pixel, so Recall in the room you are standing in took the CROSS plan and tore the dungeon down. Driven over the real StreamingWorldState: the slide is reproduced pixel by pixel, then teleportPlan is shown answering 'cross' on the slid read and 'same-interior' on the frozen one |
| a12_lefthand.test.js | 14 | ROAD A12 - the left hand: WeaponManager ToggleHand/SwitchHand/usingRightHand, the H release-edge (ActionComplete), the mirrored FPS draw, the shield rule, and the per-hand swing laws |
| a6_motor_residue.test.js | 27 | ROAD A6 - the -0.28 doorway head-dip (HeadDipHandling), PlayerMoveScanner's FindStep/FindHeadHit probes with their classic consumers, controllerSwimHeight 0.30 with DoSinking/DoUnsinking, FreezeMotor's physics settle and DaggerfallAction.Teleport's full-rotation copy |
| a8_combos.test.js | 14 | ROAD A8 - key combos through ControlsConfigManager's duplicate phases: combo storage, the modifier walk, the controls window display. ROAD-G G3 corrected the one pin here that read the port's ORDERLESS answer - `held({Shift, Ctrl, K}, 'Jump')` was asserted false whichever way the two modifiers went down, and DFU's answer depends on the HISTORY, not the Set: `modifierHeldFirstDict` is a latch a later Ctrl cannot lower and a held Ctrl cannot raise (:1695-1708). ROAD-GR drives that pair - and the R9 inventory/jump pin beside it - as ORDERED FRAMES, since the latch is stored state and a bare Set cannot say which frame came first; the ring itself is pinned in `g3_heldorder.test.js` |
| a8_pointer.test.js | 8 | ROAD A8 - pointer parity: Mouse0 activate/cast per interaction mode with Mouse2 + E preserved, routed through the AUDIT-39r mouse-button keys |
| a8_shortcuts.test.js | 13 | ROAD A8 - the DaggerfallShortcut/HotkeySequence binding table and the window consumers that consult GetBinding |
| advancementui.test.js | 18 | ROAD A11 - the mastery law (a PRIMARY skill landing on exactly 100 raises TEXT.RSC 4020 + ArenaFanfareLevelUp), skillsRecentlyRaised's uint[2] word/bit split with the sheet highlight and the non-levelling-close clear, and the char sheet's own statsRollout leveling arm + AUDIT 63 F35 (2026-09-08): the highlight pin named the wrong constant. ShowSkillsDialog overrides the message box's highlight before showing it - SetHighlightColor(DaggerfallUnityStatIncreasedTextColor) (DaggerfallCharacterSheetWindow.cs:321), Color32(178,207,255,255) at DaggerfallUI.cs:66 - so a recently-raised row in THIS dialog is pale blue, not DaggerfallHighlightTextColor's orange (:54). Both constants are pinned, and asserted different |
| bootorder.test.js | 1 | BOOT-TDZ2 (2026-09-18, Mac twice over: "boot failed: can't access lexical declaration 'yn' before initialization", then the same sentence with 'xn') - THE BOOT WALK'S OWN ORDER, GATED. `bootWorld` runs top to bottom and partway down awaits its own first pixel build; everything that build reaches runs while every `const` and `let` below it is still in its temporal dead zone, and `?.` is no shield (`a?.b` evaluates `a` and throws exactly as `a.b` would - it softens null and undefined, which an uninitialised binding is not). TO1 broke it twice in one slice: first the mod itself, whose B3 hook read `travelOptions` three and a half thousand lines above its `const`; then, with that hoisted, the same line's own `px === playerTravelPixel().x`, which reads `walkMode`, `player` and `cam`. Both killed the boot of every character whose first pixel carries a location - every ordinary save - while a character in open wilderness booted fine, which is why no pin and no browser probe saw either. This walks the boot's statements up to and including the first build, follows every call into the functions bootWorld declares, and names every binding they can reach that the boot walk has not declared yet: eight stand on an allow-list, each reachable only down a branch a first build cannot take and each older than this arc, and a ninth fails here rather than in a player's browser. The allow-list is held honest from both sides - a name that stops being reachable is a stale reason and fails too. |
| bulletinboard.test.js | 12 | ROAD A9 - ActivateBulletinBoard verbatim: the reach gate, the location-name head row, the mill's sign face through GetNewsOrRumorsForBulletinBoard, and an empty mill still opening the box |
| gamepad.test.js | 10 | GP1 (2026-09-11). THE GAMEPAD LAYER, InputManager.cs's joystick law. EXECUTES: the synthetic axis keys from 5000 (two per axis, even positive, odd negative, sixteen and no more, the names round-tripping, a key polling its axis by sign); the browser's standard pad in Unity's frame (sticks on 1/2 and 4/5 with Y flipped up-positive, triggers on 3 and 9/10, the d-pad on 6/7, the asset's 0.19 dead zone a CLIP, the face, bumpers, back/start and stick clicks as JoystickButton0-9); FindInputAxisActions (null in the radial dead zone, ±dist over the threshold capped at 1 on BOTH axes, the four actions, backwards clearing autorun, the inversions, MaximizeJoystickMovement); the camera stick, PlayerMouseLook's 120 degrees a second at 1.0 with the mouse setting playing no part, the 0.5 smoothing floor, GetUIScrollMovement, the cursor step; the store - the defaults, the single-bind setters that clear the action's old home, the autofill that fills only a missing action THROUGH THE SAME STEAL (a moved stick loses its axis to a missing default: DFU's quirk, pinned as the law), the KeyBindData_v1 round trip, an older file's absent blocks, an unknown name dropped not thrown, a full reset bringing the defaults home; the poller against a fake pad and a captured dispatch - buttons and bound axis keys as synthetic keys by Unity name, the stick pressing the bound move codes and handing its throw to axes(), the UI buttons as Mouse0/1/2, Back as Escape only under a window, the camera stick's look in the hook's units, a swing riding the camera stick while Y is down and the release frame looking again, the pad the live device until the mouse moves, everything released when the setting turns off or the pad goes; the look filter's controller floor. Text pins: all four hosts attach the pad on the touch layer's own hooks object and tick it before the paused gate, the three combat hosts fall through to its stick, the five Controls keys live and read by the law with their shipped defaults, the flag narrowed (GP2: to the cursor; GP3 retired it), the tab's window. GP3: THE CONTROLLER CURSOR against a fake canvas and an event factory - born at the canvas centre when no mouse was seen and at the mouse's last point after one, stepped by the movement stick at 900 px/s times JoystickCursorSensitivity with up as up and a pointermove per step, clamped to the canvas rect, the three click actions as pointerdown/pointerup at its point with the DOM button each is, the OS cursor hidden meanwhile, all of it only under a window, a real mouse move ending it with a held click released, the cursor's own synthetic moves ignored. QS3 (+1): the tick writes the pad's FAMILY off `pad.id` beside `setControllerLook`, and clears it when the pad goes. |
| joystickwindow.test.js | 4 | GP2 (2026-09-11). THE JOYSTICK CONTROLS WINDOW against DaggerfallJoystickControlsWindow.cs. The layout: the mouse window's panel, the eight rows on its row geometry at DFU's anchors, each invert box twenty below its axis row, the four float sliders (JOY_SLIDERS - one home per name, AUDIT 24) with DFU's ranges, the strings. The staging EXECUTES: ResetUnsavedSettings reads the store and the settings, SaveSettings writes the settings and inversions, rebinds ONLY what changed (the store's axis set counted), saves and re-reads. The window EXECUTES: OnPush into the controls and OnPop back; the axis capture binding the AXIS of an axis key (either half) and reverting on a button; the UI capture taking a keyboard key, refusing an axis key of a BOUND axis and taking a free trigger's, elongated past ten; duplicates across axes and keys in one list reddening and gating CONTINUE and Escape behind the multipleAssignments box that any click or key closes; Enable Controller written at once and re-read every tick; an invert box and a slider read back at OnPop; CONTINUE as CancelWindow; a draw against a recording renderer. The grid: the JOYSTICK tab pushes the window on the grid's staging, input/hover/wheel/release/tick reach it, its close reads back, Y to defaults re-reads the staging off the reset store, the grid's close SAVES the joystick settings after the keybind save (text-pinned in order), the poller edges all thirty-two axis keys and holds the move codes back under a window, the note is gone. |
| mousecontrols.test.js | 21 | ROAD-G G6 - THE MOUSE / ADVANCED CONTROLS WINDOW, against DaggerfallUnityMouseControlsWindow.cs, HorizontalSlider.cs and DaggerfallUI.AddSlider. The geometry is LITERALS derived from DFU's own alignment arithmetic rather than transcribed: the 318x170 panel really is Center/Middle over 320x200, CONTINUE really is Right/Bottom, and the keybind row's button really is Right/Middle inside its 85x15 panel. The six keybind rows are pinned by what the CLASSIC GRID does not offer - the two windows together cover the whole 44-row Actions enum exactly once - so moving a row into either window fails here. HorizontalSlider is pinned as a component: DisplayUnits 20, totalUnits = (max-min)+20, the float mode's x10-and-round with C#'s "n1" indicator, a click paging by TWENTY (two whole points of sensitivity), the 10px thumb floor, the drag's TRUNCATION, and Mathf.RoundToInt's banker's tie - Math.round in its place is a killed mutant. The smoothing slider's six factors and GetMouseLookSmoothingStrength's EXACT-match-or-zero (a stored 0.45 reads "None", not nearest). THE ORDERING is pinned from both ends, because it is the whole reason the window works: its own CONTINUE writes NOTHING, and the KEYBIND SAVE is what pays all ten settings out, through the OnSavedKeyBinds event this slice gave systems/inputActions.js - so dropping the raise, disposing before the save, or hard-wiring the call each go red. The threshold field carries DFU's TryParse quirk in both directions: an untouched box leaves WeaponAttackThreshold exactly as it was (TextBox.Text is empty; DefaultText is display only), and a typed value is written through Mathf.Clamp. The ADVANCED tab opens ONE cached instance over the grid on the GRID'S OWN staged dicts - a popup with its own dicts is a killed mutant - and the JOYSTICK tab's note is pinned unmoved beside it, because that half of the Ledger row is still live. Also here: the sensitivity range is DFU's 0.1..16.0 end to end (window, consumer clamp and settings row), and HorizontalSlider.cs has ONE home - nativeTalk's topic thumb delegates and the second copy is pinned GONE, with SetScrollIndex's clamp taken from verticalScrollBar, which is where DFU itself took it from. ROAD-GR G6 then pinned the POINTER PATH, which the lane had left open end to end. The thumb drag is held by its DIRECTION (rightward raises the index, leftward lowers it) and by the edge that ENDS it: `release()` is HorizontalSlider's else arm (:148-154), and because the four hosts deliver that edge as `overlay.release?.()` on the GRID, the pin runs it both through ControlsWindow's forwarder and through the real townTalk host - a grid with no release member let one press on a thumb glue that slider to the pointer for the rest of the session and then persist the runaway value. The wheel arm is reachable and directional now (MouseScrollUp/Down :180-190 - one unit a notch, and only on the slider under the pointer). The checkbox click target is DFU's Checkbox rect - art + gap + LABEL (Checkbox.cs:103-105, the Size BaseScreenComponent hit-tests) - pinned from three sides: the label toggles, one pixel past it does not, and the pre-draw fallback is the art alone. Re-opening the ADVANCED tab runs OnPush -> OnReturn -> CheckDuplicates (:146-155), pinned in BOTH directions - a clash made on the grid colours in the popup, and a cleared one stops. Also dead: a right click reaching a checkbox, a slider or the field (OnRightMouseClick is registered on the keybind buttons alone, :203), a remove prompt raised on an UNBOUND row (ControlsConfigManager.cs:290-292), a re-push that leaves `done` standing, an identity `splitCamel`, the panel outline drawn in the text colour instead of Outline.cs:29-31's white, and the OnSavedKeyBinds raise moved inside the write's try - the port raises after a FAILED write where DFU does not, and that departure is now stated rather than denied. 47 mutations, 47 dead. |
| prisonrelease.test.js | 16 | ROAD A3 - the prison sequence: serving time passes classic days, release runs RaiseReputationForDoingSentence + FillVitalSigns, the player repositions at the location entrance, and preventEnemySpawns holds the catch-up spawn loop across the skip. Since the ship landing (2026-09-03) the release no longer hand-rolls the landing: it hands _teleportToPixel the reposition method (RandomStartMarker) and the core runs PositionPlayerToLocation's outer overload once the pixel is built, the one seam the ship's boarding takes too + ROAD-G G2 (review): ClearEnemies is pinned on the fixed-city host too - it sweeps its watch AND its encounter pool, and was reducible to the watch line alone with the suite green. JAIL1 (2026-09-15, a player through Mac: "twice once I served my sentence the game loaded me outside of a room and falling into the void!"): you can be ARRESTED INDOORS - the indoor watch has no court flow and asks the world host for one through `onGuardHit`, which is the shape a theft arrest takes anyway - so the sentence is served with the mode still 'interior', and the release teleported to the town's start marker with the building still standing. Pinned: `forceExitToExterior` runs BEFORE the teleport, the HasLocation guard still runs first, and the whole CLASS is walked by execution (every `_teleportToPixel` caller reachable from indoors leaves first) rather than trusting the one line. Two mutants killed - the exit removed, and the exit moved after the teleport. |
| qx1_exterior_host.test.js | 22 | QX1 / TP2 (2026-09-03) - THE FIXED-CITY HOST TAKES THE TWO LAWS IT WAS MISSING, and nothing here is retyped: every expression this file runs is LIFTED out of `scenes/exterior.js` and mounted on stubs, so a revert runs inside the assertions. QX1 - PX3 said "this test host mounts no quest bridge, so the pause window's Quests tab says so", and EIGHT surfaces in that one file had each separately recorded the same absence as a decision. One missing construction: `createQuestBridge` is built here over the route's ONE loaded city, and its world adapter is RUN - `currentLocation` IS `dfLocation`, the region/location indices, the location type, GetRaceOfCurrentRegion as RegionRaces[i]+1, the quest clock's pixel, the climate and the live weather key all answer the fixed city where the streaming host asks a pixel. The pause rail's walk is run against a machine (one row per quest that has WRITTEN a log entry, newest-first messages, the SHORTEST live clock - a finished or disabled resource is not a timer), and it has ONE home that both this host's pause and the mode machine's interior pause read, where world.js keeps two copies. The cold bridge answers the EMPTY hook object, so a sheet opened during chargen withholds the LOGBOOK button rather than opening it onto a log nothing can fill. The other seven surfaces are pinned by name: TickRest's per-hour tick, a quest letter's display name, record 22's macro context, the automap's residence plates, the bridge riding into the mode machine (mountScene over this city), QuestMachine.Update behind the pause gate, OnInitWorld and OnStartGame. And the JOURNAL DOORS U43 left hanging - `preloadQuestJournalArt` had been imported by that file with no caller since, because there was no machine to fill a book from: one builder behind ui/chronicleDoor.js answering null without a bridge or without art, and four doors onto it (L, N, the sheet's LOGBOOK, the pause window's Chronicle) plus the interior arm's `makeJournal`, with the classic window never constructed beside the door. TP2 - "Recall pends here" was true of ONE arm of Teleport.cs and false as a refusal of the spell, the same shape A10 fixed in the dungeon context. Set-anchor runs (the natives are stated in the STREAMING host's frame - the location's own world rect - and the landing is its exact inverse, so a recall lands where the anchor was set); the 4001 refusal and record 4000's two-arm box; the same-interior MOVE with nothing torn down; the cross-context arm INSIDE the pixel, exit-then-landing with CacheScene(Interior.name); the building arm's restoreInterior and DFU's reposition line when the door is gone; the dungeon arm's mount with `local` as the whole landing and PlayerTeleportedIntoDungeon raised. The NARROWED flag is pinned as narrowed: a jump to an anchor on ANOTHER map pixel refuses BY NAME, tears nothing down and KEEPS the anchor so the world host can still use it. 12 mutants, 12 dead - the PX3 payment reverted, tickQuests back to null, the null macro context, the bridge dropped from the mode bag, the rail taking the LONGEST clock, the adapter forgetting its location index, the anchor dropping its height, the cross-location refusal deleted, the mode exit deleted, the whole TP2 block reverted to the pending line, the journal maker's refusal removed, and L/N unanswered again + THE WAVE-F REVIEW PASS widened the lift and paid what it found. The slice now starts at `_questStore` itself and runs the REAL factionRep chain, because the first cut stubbed the store away and could not tell the raw FACTION.TXT dictionary from the player's persistent CLONE of it - which was the defect underneath it: `getFactionData` read the file while every rep WRITE went to the clone, so `change repute` and `when repute` were two different Maps. The Person chain's faction-store family is mounted and run (findFactionsOfType, findFactionByTypeAndRegion, the region People/Court/Faction/VampireClan reads off a Province search over this route's one region, and the %vam pair whose NULL - never '' - is what prints C#'s own literal), with the cold-dictionary arm pinned as the charter's refusal rather than a throw. B2's PcAt is pinned on BOTH mounted arms, not just its outside-null one, with %cbd regenerating the name from the building's own seed beside it. The CreateFoe seams are wired and driven - the mint, the inside placement arm reaching worldModes, the outdoor arm answering false WITHOUT reaching it (this route mints no exterior foe pool, the one recorded departure), RaiseOnEncounterEvent and Place's hot-place mount. The bridge asks IsPlayerInTown(TRUE, TRUE) - GivePc.cs:84's gate, run over the real PlayerGPS predicate through this host's own strict closure - while CanRest's flagless second arm keeps its own seam; MakeEnemiesHostile walks the UNNARROWED database and only questFoeInstances keeps the narrowed walk. The journal door's order is the law: the art is warmed BEFORE anything asks whether it is warm (the classic arm of the chronicle door IS the art gate, so the old order made L / N / the pause Chronicle answer null for ever), plus the boot warm that wins the first press. The read pins carry their arguments - a live weather box, legalRepOf answering 100+region so the wrong region cannot pass, the legal WRITE asserted beside the read, an arg-echoing isHouseOwned - and AUDIT 28 F-B2's fourth copy of the smaller-dungeon law is pinned at last, both getters through `dungeonLocationFor` with the LATE-BOUND machine and everything else delegating to the real MapsFile. On the TP2 side: record 4001 verbatim in DFU's own ClickAnywhereToClose box rather than a HUD paraphrase, the 4000 box's two arms RUN rather than enumerated (the A/T swap used to pass), the INTERIM guard split into an x-only and a y-only anchor so each half of the pixel test decides alone, the cross-arm shape pinned (the cacheScene three-way from inside a dungeon, the mode gate outside, the re-entrancy guard under a double cast), and the FAILED dungeon mount finally repositioning - a cast from inside a shop had torn the room down and left the player in the building shell, where DFU's own 'all else fails' arm teleports to the map pixel's origin. 32 further mutants, 32 dead + **ROAD-G G2 (2026-09-04): THE FIXED-CITY HOST'S THREE SEAMS.** CreateFoe's OUTDOOR arm was this route's one recorded departure - it answered false without even reaching the mode machine, because the host mounted the WATCH and nothing else, so an `create foe` wave aimed at the street pended for ever. The host mounts the encounter pool now (the same `createExteriorFoes` factory the other two exterior hosts take, over its own collider, ticker and rig) and the arm is PlaceFoeExteriorLocation (CreateFoe.cs:245-248) - PlaceFoeFreely's DEFAULT 5/20 ring, because TryPlacement (:203-206) picks that arm exactly when IsPlayerInLocationRect and this route stands in its ONE city's rect for its whole life, so the wilderness 8/25 arm has no reachable branch here at all. Pinned by RUNNING the shipped seam over the real placement law with a stubbed world: 200 placements inside the 5/20 ring and outside the FOV cone ON BOTH SIDES - the band is `fovDegrees + Range(0,4)`, so [75, 79), and a radians-into-degrees FOV puts every foe dead ahead while a cone anchored anywhere but on the player puts them at 103 - the walker on the probed floor and the flier lifted 1.5 (FinalizeFoe :341-359), LookAt the player (:328), the Foe resource's own gender over the pool's coin, the QuestResourceBehaviour bound at the stand, the occupancy sphere reading the host's WHOLE street database (:317-321 tests ANY collider), a floorless spot refusing with `false` (TryPlacement's own failed-placement shape), and the fly camera refusing for want of a capsule. The find-place seam's recorded absence NARROWED to its one true clause: this route mounts no travel map, so `gotoPlace`/`canFindPlace` - both DfTravelMapWindow members - stay unset, while the gate C# asks FIRST (:450, the place is the city you are already standing in) is answered outright from `dfLocation`; the arm is RUN through a real QuestJournalWindow, the city under foot refused and a place elsewhere offered. 5 mutants, 5 dead + **THE REVIEW PASS PAID FOUR THINGS THIS ROW HAD CLAIMED.** (1) THE CAST ENGINE WAS DEAF. This host owns its own cast engine - and worldModes takes THAT instance indoors, so the hole covered `?exterior`'s shops too - and it passed neither of EntityEffectManager's ready-spell events, which are the ONLY route into the machine's CastSpellDo / CastEffectDo latches (hostMagic.js:76-77 raises them; machine.js:799/:805 fan them out; C# subscribes in the action's constructor). Every `cast X spell do` on this route could never latch. The pair the other two engine-owning hosts wire is wired here and RUN into a stub machine, cold bridge included, with CastSpellDo's two world reads beside it (getClassicSpellEffects and the byte-folded spellHasMatchForClassicEffect - absent, the action self-completes at PARSE). (2) THE OCCUPANCY TERM WAS UNFALSIFIABLE: under the comment "the real bite: a body ON the chosen spot refuses it" stood `assert.equal(typeof dense.tryPlaceFoe(handle), 'boolean')`, and tryPlaceFoe returns a boolean on every path, so deleting the occupancy term from questFoeHost.js outright left this file green. It is a REFUSAL now - bodies on a 1.4 grid over the whole 5/20 band, 60 attempts, false every time and nothing stood. (3) THE CONE AND THE CAST ORIGIN, two terms the pin's own header named and did not hold: the bearing bound was one-sided, and the stub world was a flat analytic plane no origin height could miss. The cone is closed at 79 now, and a ledge 3.5 below the feet - 4.4 below the CONTROLLER CENTRE, past maxFloorDistance 4 - refuses on the shipped origin and places on a feet origin. (4) THE FRAME SEAMS, which no pin held: the pool's tick under the overlay gate, its draw on the flats' axis, the ONE senses context both pools share, the unnarrowed active-enemy database the senses walk (EnemySenses.cs:741-749) and the enemy arrow's walking-player target are windowed at the source here; the corpse-key router, the three-pool swing, ClearEnemies and the quest-foe click ray went to the host-enumerating pins that already owned those laws. 19 further mutants, 19 dead |
| roada_activate_gate.test.js | 11 | ROAD-Ar - the activate gate made whole: touchSpell passed at all four hosts, PlayerActivate's clickDelay ported with the window-pop caller, the hudBlocked input, and the CRITICAL overlay gate - a click on an open window activates nothing behind it |
| roadb_castle.test.js | 19 | ROAD-B B4 - isPlayerInsideCastle live in both flavors, the Castle Daggerfall magic-doors hack (loadIDs 29331574/29331622, every term of the conjunction refusing alone), IsPlayerInsideTavern/Residence latches |
| roadb_door_bash.test.js | 9 | ROAD-B B2 - the exterior static-door bash arms with the castle-hostility tail (DaggerfallActionDoor.AttemptBash), byPlayer gates, SPECIAL doors refused. AUDIT 63 F41: the exterior arm's source pin follows the seam onto `exteriorOpenSpellFor` |
| roadb_exterior_water.test.js | 23 | ROAD-B B3 - the exterior submersion model: deep-wat…232152 tokens truncated… while a comment explained why it was wrong; it reads code now (`codeOf`), and a commented-out teardown correctly counts for nothing. (2) the factory-homes map walked nine named directories one level deep, so a declared binding whose factory lived elsewhere got `homes.get()` undefined and the MINTS expiry check was SILENTLY SKIPPED behind `if (home && ...)`; it walks all of src/ at any depth now. The fourth test is the lesson as a rule: THE THREE ANSWERS ARE EXCLUSIVE - a hand-off that is also ended by hand is a double free, and the pin fails on the exact line that was wrong. |
| hard2_activationrace.test.js | 5 | HARD2 - THE ACTIVATION RACE, ONE HOME (2026-09-14). The second slice of `01-Overview/Hardening.md`, and the first that MOVES code. DFU fires one ray and the nearest thing it strikes is the hit; deciding which - the body against the pile, the torch against both and the door, and the two rivals MC-2 split - is pure arithmetic that was written out by hand in BOTH exterior hosts, character for character. AUDIT 66 F7 is what that cost: the torch's term was added to one arm of it and raced against the corpse and the pile alone. The law is `player/activationRace.js` now and the hosts keep their ARMS, which differ. EXECUTES the law directly: the body and the pile by distance with the tie to the body, an empty ray deciding nothing, the two rivals split (the person arm never measuring against the persons, the foe arm measuring against the townsfolk too), the torch beaten by a nearer door / pile / body and winning on a tie, and a winner out of REACH still handed over because refusing out loud is its arm's job (MC-2). By source: both hosts import the law, feed it every family the ray can strike, read all four answers off it, and carry none of the arithmetic - plus a derived gate against a THIRD copy, which looks for the race's own signature (a `Math.min` composing two or more picks' distances) rather than the single-pick rival the merged-list hosts legitimately use. The extraction's proof: 20,000 input combinations of the old inline arithmetic against the new law, 0 differing, and AUDIT 63's body-and-pile cases - which used to LIFT the line out of the host with `new Function` - now call the law with their assertions unchanged. **AUDIT-HARD added the DIFFERENTIAL.** The extraction's proof was 20,000 combinations run in a scratchpad and quoted in a commit message - a proof nobody can re-run, in a port whose whole doctrine is that the evidence is checkable. The OLD arithmetic now lives in this file, lifted character for character out of world.js at d784ecd~1, and the two laws are made to agree over all 32,805 combinations of the inputs that can reach them (the count is asserted, so a narrowed domain cannot pass by covering less). Mutation-verified against the exact AUDIT 66 F7 shape: drop `doorDistance` from the torch's comparison and it goes red. |
| hard3_types.test.js | 4 | HARD3 - TYPES AT THE SEAMS THAT CRASH (2026-09-14). The third slice of `01-Overview/Hardening.md`, and the one that slice itself rated lowest: walking AUDIT 66's twelve findings against a type checker catches approximately none of them, because ordering, omission, placement, reachability and unit convention are not shapes. So it is NOT a repo-wide `strict` conversion and this file exists partly to stop it becoming one. `tsconfig.json` sets `checkJs: false`; a seam file opts in with `// @ts-check` on its first line, and everything else is parsed for inference and never reported. THE THREE SEAMS ARE DERIVED, not listed - every file in `render/`, every file in `net/`, and every `systems/` module that knows SAVE_VERSION (a module that knows the envelope's version reads or writes the envelope) - so a new file at a seam turns this red until somebody decides about it, and each derivation is itself floor-checked so a refactor cannot quietly empty one. NO ESCAPE HATCHES: `@ts-ignore`, `@ts-expect-error` and `@ts-nocheck` are banned outright across `src/`, with no allow-list, because the only way to green must be to write the shape - twice during this slice a red line meant the TYPE was wrong (a Color32's view is not always 8-bit; a SaveTree record's `parsedData` is a union keyed by `recordType`) and a hatch would have buried both. The fourth test holds `render/contract.js` to being types-only (`export {}`, no value exported) and checks its `BillboardBatch` against what `createBillboardBatch` actually mints, field by field, so the contract cannot drift from the factory. WHAT THE SLICE FOUND: `classicSave.js`'s no-Character arm returned four of the five fields its own `@returns` promises, sending `goldPieces: undefined` into the save envelope's purse (unreachable from the one production caller, which throws first; reachable from a direct caller, and a pin is one); `peerBodies` destructured two constructor parameters its JSDoc never mentioned; `remotePlayers`' `bodyHeight` default took no arguments while every call passes a peer id; `skyRenderer` named a `SkyFile` type no import resolved; `saveSlots` ran one `parse()` for two different shapes, the slot card and the save envelope; and `activationRace.js` - HARD2's own file, one day old - documented five `@param`s for a function that takes one. Not one of them is arithmetic. Every one is a seam saying less than it should. |
| hard4_registry.test.js | 4 | HARD4 - THE VENDORED-WORK REGISTRY (2026-09-15). The fourth slice of `01-Overview/Hardening.md`, and the one that points the program's principle at PROVENANCE rather than at code. Fourteen directories under `vendor/` carry other people's work, and each one's record lived in its own README and nowhere else. The gate derives the row list of `01-Overview/Mod-Registry.md` from the FILESYSTEM (a new vendored work cannot arrive without a row, and a row cannot outlive its directory), cross-checks every version and author cell against the vendored `.dfmod.json` where one ships (seven do), requires every row's bible page to exist and every vendor key the player can switch on in `systems/modSettings.js` to be registered - so a mod cannot become player-visible without its provenance written down. WHAT READING THE FOURTEEN TOGETHER FOUND: SIX carry an unfilled `[Mac: paste the text of the permission, or the link to it, here.]`, each directly under a sentence saying permission WAS granted. One README reads as settled; six is the shape of a record nothing checks. It is a RECORD problem and not a licence one - Mac's word, with a date, settles whether the port may carry the files - so those rows read RECORD OPEN and the gate holds the mark and the placeholder in step IN BOTH DIRECTIONS: drop the mark while the README still prompts and it is red, fill a README and leave the mark and it is red too. All five failure modes were verified by breaking the tree and restoring it. Also surfaced, as a row rather than a defect: PCAAO's port is built from the shipped 1.44 bundle decompiled, while the last single-file source in its repository is 1.40. |
| ww2_idlebob.test.js | 2 | WW2 (Mac, 2026-09-14: "the bob movement plays even when idle") - THE ONE MOTION BAG. EXECUTES motionBagOf from a player at rest, one walking, a bare one and the real motor; by source the five host sites read the one bag with none written out longhand, the mod's idle gate unchanged, the record. (ww1's Bob pin gained its idle half - the slight stride inside the 0.1 bound, none with BobWhileIdle off - and its per-file host grep became a per-site count.) |
| wind4_windweather.test.js | 4 | WIND4 (2026-09-15, Mac: "1. The wind wisps are far too many... 2. Clouds dont follow on the world timer with the direction of the wind 3. Grass doesnt get darker at night") - A CONSTANT, A SIGN AND TWO MISSING TERMS. The wisp curve EXECUTED at its ends and its middle (650 at a gale, 52 in a calm, the clamps and the smoothstep unchanged) and held to the measure the complaint was about - wisps per 1000 m3 of the box, under one at a gale where the old maximum was over three; the sandstorm's own 7000 left alone. The drift's SIGN by source in both reads of the cloud field, with a modelled arithmetic pin (labelled one - a fragment shader cannot be called from node) that a cloud at the origin is drawn exactly `drift` metres downwind, plus the seam that feeds it: dt in GAME minutes off the world clock, the live wind model's vector. The grass's scaled sun and moon term in the shader text, the host handing all five light terms, the `?? 1` / `?? 0` fallbacks that keep a hostless draw the OLD look rather than a black field, and GR1's law - the lab carries the same two stages verbatim and sets the new uniforms itself. Live proof is tools/wind4ShaderProbe.mjs, 10/10 in a real WebGL2 context: both programs link, every new grass uniform survives the link (an optimised-away uniform reads null), both cloud marches compile, and every use of the drift in the shared field is a subtraction. |
| ht2_packlight.test.js | 6 | HT2 (2026-09-14, Mac: "So you cant equip the torch in your offhand, you can only drop it on the ground") - THE PACK'S ACT ON A LIGHT SOURCE. EXECUTES the root cause: every one of the four light sources answers `EQUIP_SLOTS.None` and `equipItem` null, which is the "cannot be worn." the enhanced card showed. Then the cure, executed: `localPrimaryAct` answers Light for an unlit source, Douse for the one the entity holds BY REFERENCE (a second identical torch still reads Light), Wear for a sword and Take off once it is worn - with `useItem(item, null, { entity })` performing the light and the douse through the one law, no collection (AUDIT 22 F6). `itemLine.lit` carries the same reference compare (the fact the classic list paints gold). By source: the view performs the act it was handed, label and all, the card reads Lit for a light source instead of Worn, and the classic window's own arm still stands beside it. Live proof is tools/ht2TorchProbe.mjs, 11/11: the card on a torch reads `Light | Drop | Use` with no Wear, the press lights THAT torch, one row says `lit`, the card comes back offering Douse over `Lit: yes`, and a sword still reads Wear. HT2-AUDIT and MODS-ON (Mac: "audit this to ensure youre not just guessing and this actually works", then "Yes all mods should be on by default") added the sixth: the two switches a lit torch was INVISIBLE without are ON by the port's decision, pinned FROM BOTH ENDS - what the port answers (`Modules.Sprite` true, `PlayerTorchFromItems` true through the getter AND the merged view) and what the vendored sources still say (`Sprite = False` in the shipped modsettings.json, "False" in the generated ini table), because a departure nobody can see the far side of is just a port that got it wrong. The `PORT_DEFAULTS` layer is asserted ONE key wide with a neighbouring key still reading the ini, so a third departure cannot arrive unnoticed. The other half of that audit is tools/ht1HandProbe.mjs: one entity, lit through the pack's card, handed to the real component on a real Renderer over real WebGL2 - all eight vendored sprites fetched, decoded and uploaded with no console line, a 90x205 texture, a free hand, and `draw()` true with the sprite module on, the rect settling at 88,186 144x328 on a 640x400 canvas. |
| ht3_sprite_upright.test.js | 3 | HT3 (2026-09-15, Mac: "The player held torch mod shows the sprite upside down when held") - TWO RIGHT ANSWERS, AND THE DOOR TOOK THE WRONG ONE. `toColor32` is a FLIP: right for a Unity texture (Unity stores bottom-up, so flipped its row 0 is the picture's top), wrong for a decoded PNG whose row 0 already is. Which one a sprite wants depends on where it is DRAWN - a world billboard samples v with 0 at the bottom, a SCREEN quad does not, because drawScreenQuad hands the rect's top the source rect's v0 and nothing flips at upload. EXECUTED: both orders over a five-row raster (so the pin cannot pass on a two-row accident), the same SHAPE from each (TEX1's law untouched), and toScreenOrder refusing a raster whose bytes do not match its size. By source: the two screen doors keep their rows, the widget's BUNDLE arm still flips, the three world doors are untouched, and neither screen door imports the flip-only conversion. |
| tex1_texturedoors.test.js | 5 | TEX1 (2026-09-14, Mac's live crash - the CLASS behind it) - ONE SHAPE FOR EVERY TEXTURE DOOR. EXECUTES the crash's real path: the handheld-torches component on the real Renderer over the audit39 gl stub, its door's answer reaching texImage2D bottom-up; and the regression, which is the crash itself - a door answering the OLD `{ data }` shape must NOT reject the promise nothing awaits (assert.doesNotReject), uploading nothing, drawing nothing, and warning ONE line that carries the upload path's own sentence and the `ht:` key. By source: no module under src/ imports toColor32Order (the class closed by construction), each of the five doors converting with toColor32 by name, the seasons re-wrap gone from both hosts, both torch contracts documenting `colors`, and both torch loads guarded (the dropped record failing empty, which build no-ops on); the records, including WW3's correction. |
| ww3_widgettexture.test.js | 4 | WW3 (Mac, 2026-09-14, the live crash: `can't access property "buffer"` out of asBytes, from uploadTexture, from the weaponRig chunk) - THE TEXTURE DOOR'S SHAPE. EXECUTES the whole path the crash took: a loose PNG registered through the real door, decoded through the real decodePng (createImageBitmap and OffscreenCanvas stubbed), converted by the real door and uploaded by the real Renderer.uploadTexture on the audit39 gl stub - the answer is `{ width, height, colors }` with no `data`, the bytes reaching texImage2D are the picture bottom-up, and the key memoizes. The OLD shape through the same call is pinned to throw, with the caller, the missing key, what it got and `toColor32` named in the message; the same guard on the emission upload, and color32Bytes executed over null/undefined/{}/a width-height pair. By source: both door arms convert with toColor32 and the module holds no toColor32Order, the site warns instead of swallowing, and color32Order.js still carries the sentence that predicted the fault. |
| wind3_windworld.test.js | 9 | WIND3 (Mac, 2026-09-14: wisps that show the wind, a quiet wind, the trees moving with it) - THE WIND SEEN AND HEARD, OFF ONE MAPPING. EXECUTES windDrive over no deck, the sunny row, the thunder row, the controller's gust and the legacy stack, the clamped and the bad frame; floraSwayOf's three answers; the wisps' count law and the renderer on a stub WebGL (two stages, nothing without a deck, the floor in a calm, the whole field in a gale, the travel summed and wrapped); the loop's gain ceiling over a sweep, the clip swap, the driver's slew, floor stop and modal stop over a fake engine; the five clip names against DFU's dungeon list; the three rows, the derived defaults and lane answers, each switch's skin/pref/door; by source the BB_VS lean, the renderer's uploads (setFlatWind executed), both hosts' wiring with no copy of the mapping, and the records. (Re-aimed: gr3's grass-wind line, wind's gust line, labGrass's two slider pins and AUDIT 49 F4, the seam counts in glstate/farring/perf2, FT0's id list, FT2's and FT8's counts.) |
| enhancedsounds.test.js | 3 | ES1 + MAC-O6 (2026-09-16, Mac: "lump this in as a new enhanced toggle. Enhanced Sounds, add the wind noise to it" / "looting gold/items makes no sound"): the `enhanced-sounds` row where the wind row stood (enhanced, on, the player's own online, the shelf deriving the default); `enhancedSoundsOn` EXECUTES over skin and pref and `windSoundOn` rides it with its own kill door; by source the enhanced inventory's two transfer cues (take and store, off `plan.sound`) sit behind the switch and the classic window's cue is unchanged. |
| ccrep.test.js | 3 | CC-REP + CC-GRID + CC-STEP (2026-09-16, Ember on the Discord via Mac: the class builder's missing reputation setting, the list that needs scrolling, the small + and -): `repStep` EXECUTES (one integer step, clamped to the bar's -10..+10, negative zero normalised, an unknown group refused, the same reach as a bar click); the flow EXECUTES the window (ReputationButton opens it, steps move the ledger, the exit gate holds until the negated sum is zero and shows TEXT.RSC 303, the reopened window's stale zero is DFU's own quirk); by source the view draws the window and presses the flow's doors alone, the builder is a two-column grid from 900px, the steppers carry a minus sign and a plus at the button's size. |
| dglnx_skincarry.test.js | 3 | DG-LNX + SKIN-CARRY (2026-09-16, two Discord reports via Mac: the arena2 folder pick failing on Linux where a zip works; the classic toggle booting enhanced): a throwing localStorage EXECUTES the refused-store path (setPref false, setUiSkin null, the in-memory choice standing, the warning said); switchSkin carrying a refused choice on the URL and deleting the override otherwise, by source; the data gate's folder arm guarded like the zip arm with progress and the empty-pick and throw messages naming the zip and the drop, by source. |
| weather2a_snowground.test.js | 7 | WEATHER2a (Mac, 2026-09-14: "it can rain when theres snow on the ground") - NO RAIN OVER SNOW, ENHANCED LANE. EXECUTES groundIsSnowy over every climate and both seasons with the terrain archive keyed on it; overGround's four arms and the law off; the three writers (the boot drain, the respawn roll, the travel arrival) over a winter woodland, a jungle storm, a desert and a summer, the raw word kept, a raw change under one worn word no change, a word taken whole; the switch over skin, pref and seam; the wind model's violence word against the same roll; by source the hosts' minute and violence plumbing, the sim's one write, and the records. (SAV3's import pin names the classic lane; WIND1's tick pin re-aimed.) |
| weather2c_cloudcells.test.js | 7 | WEATHER2c (Mac, 2026-09-14: "a thunderhead in the distance with the weather happening elsewhere") - CLOUD TYPES BY PLACE. EXECUTES cellOf over every weather (the profile, the row's cover and grey, the rim), slabOf's union, packCells (three arrays, the tier's and the arrays' caps, the metre rim, the caller's arrays), parseCloudCellDoor's forms and refusals; by text the field's arrays, resolveAt and the density on the resolved terms, both marches resolving before and at every step under the union slab, the light march and the grey, AUDIT 47's own law over the two templates, the class's setState/upload/recenter and the controller's two paths; the records. (The tiers' literal pin, the recenter's sample pin and the setState pin re-aimed.) |
| weather2b_weatherfield.test.js | 8 | WEATHER2b (Mac, 2026-09-14: "weather can be traveled out of and into") - THE WEATHER FIELD. EXECUTES the coordinate seams (natives to field metres, the pixel under a place, the fixed location's frame), the cells' table and the seeded candidate (the same again, another day/word another, jittered inside its lattice cell, the coin near its share) and the day's drift; fieldAt inside a cell and in the clear on a woodland rain day, the drift carrying a cell, nearest-first within range, a mountain thunder day, a sunny desert, both zones' cells from a border; the sim's seam (nothing off the lane; a drain's change no crossing, a live change a crossing, an arrival a jump, the travel arrival and the respawn sampling the destination with a throwing roll); the switch and the forced-online row; the wind's short-lead front against the day roll's; by source the controller's ease on the front's own lead and the arrive hook, both hosts' sampling, stamps, arrivals and cells; the records. (The ease pins in clockArc and wind, AUDIT 57's stamp pin and WEATHER2a's arrivals pin re-aimed.) |
| weather2d_sandstorm.test.js | 7 | WEATHER2d (Mac, 2026-09-14: "a new sand storm weather event for desert regions") - THE SANDSTORM. EXECUTES the eighth word appended with DFU's seven intact, its fog row (and the heavy fog under a mod's five), sun scale, sand precipitation and default sky offset; the sky row, the cloud profile on the ground, the tint through cellOf/packCells and the union slab, the wind's violence through a front, the grass dim; the front's sand kind and peak, a boot into a sandstorm and its taper, the ear; the field's seats (the desert tables' land under cloudy or thunder, nowhere else), a cloudy desert day scanned for a wall and the clear, a sunny desert and a cloudy woodland with none, the sim's word through the field and the ground law; the sand renderer in its look on a stub WebGL (instances, count, uniforms, wrap, nothing at nothing); by source both hosts' sand branch and the rain program never built for it; the records. (WEATHER_TYPES, LAB_DIM, VC3's base, CELL_WORDS, cellOf, the field's globals and the march's tint, the packing, the wisps' alpha, the precipitation gate in weatherfront/weathersim, and the seam counts re-aimed.) |
| waternpc.test.js | 4 | WATER-NPC (Mac, 2026-09-15, from live play: "NPCs arent water aware and will walk into it") - THE TOWNSFOLK NAVGRID AND THE WATER THE PASS DRAWS. The classic lane is held to DFU's `CityNavigation.TileTypes` exactly (records 0/5/6/20/21/30/31 weight 0, and the twelve the enum never learned still weight 7 there, so the verbatim switch cannot drift under the departure). The enhanced family is held to its DERIVATION - every record with any water corner in `world/waterCorners.js`'s own table, recomputed here from the table rather than listed, so a hand-written roster fails and the one water table stays the one water table. DFU's own contradiction is pinned as the reason: `PlayerMotor.OnShallowWaterTile` (:551-563) wades the player through 8, 23, 33-36 and 49 while `GetTileWeight` walks its townsfolk over them dry-shod. And both exterior hosts are swept for the SAME switch (`waterSwitchOn()`), because a departure gated in one host and not the other is the four-hosts trap wearing a new coat. |
| guard1_watch_despawn.test.js | 7 | GUARD1 (2026-09-15, a player report relayed by Mac: "after a while of chasing me around the villages they disappeared. And then i was free to go on a killing spree") - THE CITY WATCH'S DESPAWN LAW, and the first pins on this pool that need no ARENA2. `EnemyEntity.Update` (:184-191) despawns the watch on FOUR terms and `scenes/cityGuards.js` carried three; the missing one is `!IsTransformedLycanthrope()`, and it composes with `LycanthropyEffect.SuppressCrime` (which this port DOES carry) into a trap - a transformed player can never hold a crime, so the three-clause test was true every frame and the whole watch was deleted for the duration. Held here: the three clauses that were there (a live crime keeps them, clearing it walks them away with their batches freed and their records pruned), the fourth that was not, that the term is the TRANSFORMATION and not the lycanthropy, that an ordinary mortal is untouched by it, that a fresh crime after a walk-away still reaches the spawn chain (the report's other half), the cap on BOTH sides of DFU's `<= maxActiveGuardSpawns`, and `crimeCommitted` as a NUMBER from one enum - `systems/talk.js` wrote the string `'Pickpocketing'` because the court/talk cycle put both the setter and the enum out of its reach. Driven against the live pool through its own public `guards` array (roadg_pools' technique) with a spawn chain that throws, so every gate is observable with no game data - which is the point: `cityguards.test.js` is ARENA2-gated end to end and CI had never run one line of the watch. |
| macn_bugs.test.js | 19 | MAC-N (2026-09-16, Mac: "1. Weapon and Armorsmiths dont want to pay for loot / 2. Classic inventory issues in shops. Item inventory is very large, scrollbar not working correctly, submenus are completely unclickable / 3. Chat UI not visable with classic in online mode") - THREE ROOTS, NONE WHERE THE REPORT POINTS, pinned at the PRODUCERS and the POPULATIONS. **N1**: `setItemFields` (SetItem's name + value, the one export that replaced six private copies) fills and never overwrites; `equipmentItems`' seven records all carry a name and `itemBaseValue`; a smith's Sell window over a REAL corpse totals a finite price and offers gold, and the pre-fix shape driven through the same window totals NaN and offers 0 (the discriminating half); no `mintCondition({ group: 'Armor'` stands in `src/` without `setItemFields` in front of it; the bare recovered-arrow literal is gone from all seven sites and `bowDamageArrow()` stacks with a loot quiver. **N2**: the trade window's four inherited tab rects; the initial page per mode (MagicItems for Identify, :253); the pack AND the basket filtered per page with the basket first, on every page equal to `filterByTab`; a tab click switching, resetting the local scroll and clicking; the four hotkeys in DFU's add order ahead of the action panel and the exit; the wheel one row a notch on either list and its rail, not on an arrow, not off the lists, not under a box, silent; the thumb drag against `dragScrollIndex` with the clamp, the latch dropping on the button up and on `release()`, waiting on a hover with no event; the pack's own latch and its right-button refusal; the selected tab's INVE01I0 cutout at the tab's rect over the base through a spy renderer; every host's `hover(vx, vy, e)` and `release()` seam. **N3**: `publishBootParams` writes the decided search with the bare-value spelling and the kept hash, does not rewrite an equal URL, warns and survives a refusing history, and the string it writes reads ONLINE to the lane and ENHANCED to `uiSkin` over a stored Classic; main.js publishes before both world boots and clears the decided keys before the menu; `BOOT_DOOR_KEYS` is derived from main.js's own `params.set`/`delete` calls. Campaign: 14 mutants, 14 killed. NOT SEEN ON A GPU. |
| menu1_enhanced_chunk.test.js | 9 | MENU1 (2026-09-15, Mac relaying a player: "sometimes you're unable to open the enhanced menus. For example a player might open the radial and select the spellbook, but it will fail to open") - THE DEPLOY IS THE ROOT CAUSE AND THE SILENCE IS THE BUG. Every enhanced menu builds to its own content-hashed LAZY CHUNK, fetched the first time a player opens that door, and the deploy DELETES what it replaces - measured live, the previous build's `assets/main-EDqp0hIk.js` answered 404 within the hour while a chunk whose content had not changed kept its hash and survived. A tab open across a deploy therefore asks for old chunk URLs, and the ones that 404 are exactly the menus that deploy touched; the dial is in the main bundle, so the rose still opens and the door behind it does not. That is the whole "random". Six of the seven doors caught the rejection, wrote `console.warn` and closed in silence; `charSheetDoor.js` had no catch at all. Held here against the one home (`ui/enhancedChunk.js`, driven with an injected failing loader - no browser needed): the retry that mounts on the second try, the exactly-twice bound, that a failure SPEAKS in the door's own host and does NOT close the door behind the player's back, that the Reload button is offered for a stale chunk and withheld for a module that threw (a reload does not fix a throw), that the reload is offered and never taken, that a door torn down mid-flight mounts nothing and says nothing, the three engines' wordings for a fetch failure read as one, and THE LAW: no `ui/*Door.js` fetches a lazy chunk outside the one home, and none is left catching a mount failure by hand. Three mutants, three killed. |
| soc1_hub.test.js | 18 | SOC1 - THE SOCIAL HUB (2026-09-16, Mac: "A social button next to the chat UI ... friend other users, see if they are online/last online + be able to invite friends or other individuals to the new 4 person party system"). WHERE THE STATE LIVES: every player online holds one socket in the world channel (ROSTER-G's one room everyone is in), so that room's Durable Object is THE HUB and keeps accounts, friendships, requests, presence and last-seen, and parties in its storage; presence rooms are untouched, and a party member's name is green because the hub told MY client which peer ids are my party's, never because a self-declared claim rode a cell's hello. TWO IDENTITIES: a peer id is a tab's (TABS1) and a friend is a person, so the hub's hello carries an ACCOUNT id and its secret beside the peer's - the same id law, both guarded (the first hello mints the secret, a wrong one is admitted to the chat and told 'account taken' with no account on the socket). The wire: `{t:'social', k}` acts naming exactly what SOCIAL_ACTS says each kind needs (an account, a peer, either, a party, nothing), `{t:'party', p}` the party pose projected by validPartyPose (the map pixel clamped, six vitals bounded and rounded, the place a filtered label, the portrait by validLook's bounds; refused whole), and the hub's six frames projected at the client's door by validSocialFrame (a bad row refuses the frame whole, a list is cut at its bound, a leader who is no member is no party, a note is a known code). The hub: friend request by peer or account with the inbox and outbox both told and a note to the receiver, accept both ways, decline and cancel quiet, remove mutual, a request back is a yes; FRIENDS_MAX and PENDING_MAX either side in words; presence to friends alone with the peer ids the tabs stand as, last-seen stamped when the LAST tab goes; a party made by the first invite with the maker in the seat, the invite to the invited alone with its expiry, four seats and no fifth, a lapsed invite refused and shed, a no told to the asker; leave passes the seat to the longest-standing, the last out dissolves, kick is the leader's and the kicked is told, a yes to another party is a no to the old; the party pose kept on the attachment and fanned to the party's other members alone (never back, never to a stranger) and carried in the next view; a seat kept PARTY_OFFLINE_MS for a member that dropped and lapsed on the next party event with the rest told; SOCIAL_HZ_MAX a socket (dropped, struck, closed past DROP_STRIKES_MAX), SOCIAL_ROOM_HZ_MAX the room ('busy', nobody struck), PARTY_HZ_MAX the poses, junk outside the hub; a hibernation between acts loses nothing; the drain sweeps parties and never accounts. Every frame the hub sends is checked through the client's own door. RELAY_VERSION world78. Each pin names its mutants AUDIT SOC (2026-09-16): re-pinned under the audit's rules - a pending row carries no presence (A6), a request or an invite BY ACCOUNT to a stranger is 'meet them first' (the tests ask by peer, as the game does), a re-request or a re-invite inside SOCIAL_REPEAT_MS is 'already asked' with nothing reaching the target (A1/A8), the picture names my own tabs (C20), the widest picture read in the runtime's batches (the fake's 128-key wall), the drain's sweep pinned on its own body; RELAY_VERSION world79 |
| soc2_session.test.js | 8 | SOC2 - THE CLIENT'S HALF (2026-09-16). The account's pair (net/social.js accountId/accountSecret) minted once in the APP'S storage - the profile's, not the tab's, because a friend is a person; net/online.js never touches the browser-wide storage (TABS1's pin stands). The hello carries the account when the session holds both halves and NOTHING otherwise (a build before SOC1 sends the hello it always sent, key for key), and world.js hands the pair to the hub tab's link alone, after the join and before the socket can open. sendSocial gated at home at the hub's rate with the token spent only on an act that left, refused without an account or a kind the wire has no law for; sendParty projected first, no sooner than PARTY_SEND_MS, only when CHANGED, forced on request, whole again after a reopened socket. The frames in reach onSocial through the wire's door (a shaped one dropped whole) and onParty never carries my own account's pose back; a handler's throw is contained. SocialState (pure): a state replaces everything, presence keeps a friend's row and the seat's, a party view rides over and keeps the poses already held, an invite lands and is spent by the seat, notes become words ('You lead the party now' when it is me), an error is kept for the panel, the version moves on every change and only then; actionsFor a peer in the world (a stranger friended and invited, a friend invited but not friended again, my party's member neither, a full party invites nobody, myself nothing); lastOnlineText in a row's units. Each pin names its mutants |
| soc3_socialpanel.test.js | 28 | SOC3 - THE SOCIAL SURFACE (2026-09-16, Mac: "A social button next to the chat UI, that when tapped opens the new friends list + party interface. Players should now be able to friend other users, see if they are online/last online + be able to invite friends or other individuals to the new 4 person party system. Party system: Upon joining a party, the players name who are in a party together should turn green"). Driven headless over chat1's fake document and window - copied into this file rather than shared, because a fake DOM two slices tune for each other stops being either one's - with a REAL net/social.js SocialState fed real hub frames, so nothing here mocks the thing under test. THE BUTTON stands in BOTH chat states with one badge law (requests to me plus live invitations, which an unread LINE is not), is no chat tab, is drawn on a desktop too because Enter opens the chat and until SOC5 nothing opens this, swallows its press and passes its release, and is not built at all for a host that asked for none. THE PANEL is a pointer surface with the chat's own three doors: the host refuses it under a window, the cursor is freed inside the opening gesture and taken back inside the closing one, Escape closes it and never reaches the host's pause door behind it, a covered frame takes the panel AND the toast out of the page, and destroy takes the one capture listener and both roots. THE FRIENDS list is ONLINE FIRST then by name the way net/roster.js sorts, each row saying when they were last seen on the RELAY's clock (a minute of WORLD5 offset is a minute of "ago"), a seated friend green and everyone else blue; empty, it says where to go. Requests come first and send by ACCOUNT - a friend is a person, not a tab - and Remove ARMS on one click, sends on the second and disarms itself, with no window.confirm anywhere near it (pinned over the source with its comments stripped, for the fifth time in this port: the sentence explaining the refusal names the thing refused). Invite is live only when the friend has a tab in the world and a seat is free, and a refused one carries "offline", "in your party" or "the party is full" as its title, because a control that quietly vanishes teaches nothing. THE PARTY tab draws the seats out of PARTY_MAX, marks the leader, says each member's last pose as "Daggerfall - 50/60 HP" and nothing at all for a member who has sent none, offers Kick to the LEADER alone and Leave to everyone; an invitation is a row with a countdown that RUNS on the same node, answered by PARTY id, and a lapsed one is gone because time passing moves no version and the panel raises its own. THE TOAST works with the panel SHUT, counts down there, and goes away on the answer, on the seat being taken and at expiry; a rate-gated answer leaves it up with its note. A send that returns false keeps its button exactly as it was and says "try again", which clears itself; the hub's own refusal stands under the header and a fresh state clears it. THE CHAT'S TWO SEAMS: nameColor paints the author of a line and the roster row and is asked AGAIN every frame - the SAME span turns green when a party forms with nothing said in the chat - while a stranger is left alone; a roster row is a DOOR whose menu is the host's answers (actionsFor), my own name is no door, a disabled action sends nothing and carries its reason, an act closes the menu, the menu survives five frames, and a host with no picture yet leaves every row a plain name. THE HOST by source: the panel is made in socialStart AFTER the picture, the chat's three seams are lazy closures because the chat panel is built first, hudCtx.openSocial is SOC5's door, and the render rides chatFrame after SOC2's pose line under the chat's own covering rule. Campaign: 7 mutants walked, 7 killed - Remove on one click, Kick for a non-leader, the colour pass dropped from the frame, the closed-state button dropped, a lapsed invitation left standing, my own roster row made a door, the friend order's online clause dropped. NOT SEEN ON A GPU AUDIT SOC (2026-09-16, +11): one Escape per press - the panel and the chat take `above` and yield to the surface over them while the chat keeps a half-typed line (C2/C14); the toast on its own pointer-transparent strip (C3); the try-again note expires (C4); the Party badge follows a lapsed invite with no version move (C5); 'Last online N min ago' TICKS on the same node with 30 quiet frames writing nothing (B8); 44px targets under the touch skin with desktop sizes pinned unchanged, and the panel below the chat box under 840 wide (C8/C13); a disabled action's reason written beside the label (C11); a pending request draws no presence dot (A6); a roster row with nothing behind it is no door (C20); the Social button sized as the Chat button, the panel a labelled dialog with real tabs (C21/C24); the host's counted pointer surfaces and the second door gone (B6, B14) FONT1 (2026-09-16, +1): the panel and its toast in the pixel stack with the five-face in the sheet, sizes retuned by the measured x1.29 width ratio, the 44px targets still holding |
| soc4_partyhud.test.js | 11 | SOC4 - THE PARTY HUD AND THE GREEN NAMES (2026-09-16, Mac: "Party system: Upon joining a party, the players name who are in a party together should turn green. Theyre character portrait + health/stamins/magicia stats displayed on a new party UI element"). The panel (`ui/partyPanel.js`) is driven over this file's own fake document - chat1's shape, grown a canvas with a 2D context and a WRITE COUNTER on every node, because the whole of this panel's per-frame budget is the promise that a frame where nothing changed writes nothing, and a pin that greps for a class name cannot tell a bar that moves from one built and never written again. Pinned: one card per OTHER member in the hub's SEAT ORDER and never my own (driven with me sat in the MIDDLE seat, so a panel that drew `party.members` would put my own card between them - my three bars are the HUD's and my face is on my paper doll); the leader's mark on the leader and not on the first seat, in words as well as in a glyph; the name's green is `net/social.js` PARTY_GREEN_CSS and there is no second green; the three bars and their digits from the pose's own field pairs (`h/hm`, `f/fm`, `m/mm`) in the HUD's order, each with its own colour; the place line with `dungeon` and `inside` from the wire's `in`, and `the wilderness` for a nameless exterior; dashes and empty bars before a member's first pose, never `0 / 0`, which reads as dying; an away seat greyed whole with the last-online words on the relay's clock; hidden out of a party, alone in one, and under a window - with the change that arrived WHILE covered drawn the frame the window closes. THE REPAINT IS A WRITE, NOT A REBUILD, and it is counted: sixty quiet frames write no property, make no element and re-parent nothing; a pose moves one card through the same nodes and does not touch the other seat. THE PORTRAIT IS THE GAME'S OWN ART and is proved twice - through an injected loader seam (two seats wearing one face load it once, a face that changes loads once more, a face that comes back loads not at all, the plate stands until the art lands and after a failure that is warned once and never retried), and through `createFaceLoader` over a CIF SYNTHESIZED BYTE BY BYTE: a real IMG-record header, real indexed pixels, the real `CifRciFile` walk to the SECOND record and the real `bitmapToColor32`, so "the same CIF the paper doll reads" is a fact and not a comment. The green names ride `net/remotePlayers.js drawNames`, driven over PERF-ON's stub renderer and font: my party's tab green, a FRIEND who is not in my party white (a friend is a list, a party is a formation), and the DEFAULT path - no colour function at all, which is every caller written before the party existed - white byte for byte. The host pinned by source: the panel made ONCE in `socialStart` over the escort faces' own fetch door and palette, drawn from `chatFrame` after the pose goes out under the chat's own `covered` word, and the name pass asking the picture rather than deciding what green means. Each pin names its mutants AUDIT SOC (2026-09-16, +2): the HUD 92px down, clear of the FPS counter's measured 76-tall band (C6), bottom-right at 180 wide on a phone under 560 (C7); an away seat's 'last online' ticks - 60 quiet frames write nothing, one away seat writes exactly once a minute, nothing under a window (B8/C22); the bar rows a labelled group (C21) FONT1 (2026-09-16, +1): the party HUD in the pixel stack, the name at 13px (the pin's literal moved with the reason); NAME1: drawNames takes `blocked` BEHIND `colorOf` so SOC4's law holds as written, and the host's two faces both ask the picture for the colour |
| soc5_interact.test.js | 17 | SOC5 - F ON A BODY (2026-09-16, Mac: "Players should be able to interact with others in the world upon encountering them by pressing F on their body, which should show options to add as a friend or invite to a party"). THE ACTION: 'SocialInteract' appended past DFU's forty-four - the port's own, a Ledger A row (SOCIAL), because Daggerfall Unity has no other players to stand in front of - APPENDED and never inserted, because ui/controlsWindow.js indexes ACTIONS by NUMBER against fixed pixel anchors on CNFG00I0.IMG and a name spliced into the middle would silently re-label thirty-eight buttons; parseable, defaulted to KeyF (the key Mac named, and one SetupDefaults leaves free), and - the case that decides whether a live save survives the slice - autofilled into a bindings file written BEFORE it through loadOrCreateBindings' own resetDefaults(store, true), with no reset, nothing else disturbed, and a key the player had already spent on F left alone (the action simply waits, unbound and rebindable). THE ROUTE: ui/input.js routeAction hands the action to `ctx.socialInteract` and passes THE DOOR'S OWN ANSWER back, so an offline page's false falls through the host's ladder instead of eating the key, and a host without the arm consumes nothing. THE REACH: player/socialPick.js pickPeerInFront, pure, driven with plain vectors and the REAL cylinder (scenes/townTalk.js rayPersonDistance, handed in the way the host hands it in, never copied - one ray-vs-person law, and a pin holds this module to not restating its radius or height) - the nearest in front wins, BEHIND is not in the race at all (the cylinder answers Infinity for t <= 0, so it does not merely lose), past the reach is NOBODY rather than the nearest of the far ones, a tie keeps the order the room was handed in, and SOCIAL_REACH is the game's own MobileNpcActivationDistance so "close enough to talk to" and "close enough to friend" cannot disagree on one street corner. THE MENU: ui/socialMenu.js over a fake document and a two-phase fake window (chat1.test.js's shape, copied rather than shared) - the rows in order with Remove friend only for a friend and only when the picture holds their ACCOUNT ('friend.remove' is an account act; a peer the hub could not resolve would be a button that lies), every act checked against net/wire.js SOCIAL_ACTS for the field its kind requires, a dark button DISABLED AND SAYING WHY in actionsFor's own words on both the row and the title, a disabled click sending nothing however it arrived, the card DOWN before the host is handed the act (the send can answer false, and a card left standing takes the next press as a second act), the pointer freed on open and taken back on close with a re-show over a second peer asking once, Escape closing and STOPPED so the host's pause door never sees it, a press inside the card swallowed and a release never, the host's `covered` frame taking it away with a window, a host that will not have it opening nothing and touching no pointer, and destroy taking the one listener. THE PANE: a third group, because GRID_ACTIONS is still DFU's Actions[2..40) and ADVANCED_ROWS is still mouseControlsWindow's six, and the coverage rule (every bindable action exactly one row) is what makes PORT_ROWS compulsory rather than tidy - with the classic window's own geometry driven to show it cannot help: its lowest rows sit at y=181, a button is 7 tall and the tabs start at y=190. THE WIRING, by source in scenes/world.js: the door on hudCtx, written below the peersNear it measures, the camera forward composed exactly as the activation site composes it, one call to the cylinder in the whole host, the menu built inside socialStart beside the picture so it exists exactly when an account does, its acts leaving through the LIVE session rather than a captured link, and a word on the world tab either way - the sentence when it went, 'Try again in a moment' when the gate refused. And the one key the slice had to move: Handheld Torches shipped its light toggle on F (HT4's gate holds that no vendored mod ships a key the port has spent, and online forces every vendored mod on), so the toggle's shipped default is O now - free in DFU's defaults and in every vendored mod - and its pins say so. Each pin names its mutants AUDIT SOC (2026-09-16, +7): the F-menu's sheet is `dfpeer*`, disjoint from the panel's classes (C1); the menu is the TOPMOST surface - it ignores no Escape and stops the one it takes (C2/C14); Remove off the card (C15); a disabled row's contrast 4.90:1 by the sRGB arithmetic, the sheet into a head-less document (C12/C25); the phone's F is the host's door handed to ui/touch.js (C9); the port's own actions YIELD their key in the classic windows and never in the enhanced pane (D3); a saved 'F' light toggle migrated to O (D4); the two sentences of player/socialPick.js driven (D14); the host's door ABOVE the exterior gate (B4/D1), its closures the counted surfaces' (B6), 'try again' vs 'not connected' (B17), the menu's roles and focus (C21) FONT1 (2026-09-16, +1): the F-menu's sheet in the pixel stack with the five-face |
| soc6_partymap.test.js | 18 | SOC6 - THE PARTY ON THE WORLD MAP (2026-09-16, Mac: "Party members should be able to be seen on the world map, regardless of their location"). ONE seam and two drawings of it: `party: () => [{acct, name, px, py, in, loc, online, leader}]` in the travel map's one dep bag, read on each window's own refresh and never snapshot at open (driven: the dep re-read on the poll, rate-limited between polls, a member who joined and one who left while the map was up, and an absent dep drawing nothing at all - the ordinary case for a solo or offline host). ui/partyMapMarks.js as the one reading of that shape: a row off the bay's edge, a non-finite pixel, a poseless seat and a non-list answer are all DROPPED rather than clamped to 0,0 (a friend reported in the Iliac Sea off Northmoor), the far corner is IN, offline is a mark and not an absence, a nameless seat still gets one, and the repaint signature moves for a pixel, a floor, a name or a presence and not for a pose that said the same thing. The enhanced map (MAP1, 2026-09-18: the held parchment, the party as INK driven through a recording 2D context): one ring per member at the pixel's centre (x + 0.5, y + 0.5) placed through the sheet's own toPaper and told from the player's mark by colour, the name under its ring riding a pan and a zoom and neither drawn while the mark is off the sheet, offline grey in ring, name and hover words, all three `in` cases marking the SAME place pixel with "(dungeon)" / "(inside)" / nothing beside the name, the hover line "Name - place (dungeon)" beating the town under the same cursor, a moved member repainting the sheet while `_marksDirty`, the marks and the chains are left alone, and a legend that opens with the first member and closes with the last. The classic region page: the dot over the town the member stands in and on a dungeon's own pixel, green derived from net/social.js's #73ff73 with a distinct grey for offline, both containment laws held - Wayrest's pixel refused on Daggerfall's sheet, and the two offsets that WRAP onto the wrong row (one pixel west of the origin, one past the far edge) refused although both land inside the buffer - and the timed poll rebuilding only on a change, never below the rate, never with no page up, and never frozen by a popup. The host: ONE construction seam with U61's `woods,` and `getPlayerPixel: playerTravelOrigin` intact, `party: () => partyMarkers()` beside them, and the poseless-seat filter driven against a real SocialState (the seat taken, `p` still null, the unfiltered read throwing). 29 mutants driven, 29 dead AUDIT SOC (2026-09-16, +4): members on one pixel stack in seat order and the hover names them all (D2); the legend a flex child of the foot row (C10/D5, on the held map); no label node, no title, no clamp - the name placed by the same transform as its ring (C16/C17, re-read for ink); `loc` in the repaint signature, one offline grey, the ring's radius said right (D6/D7/D9); a pin that passed with its code deleted replaced by the driven ones (D12) |
| auditsoc.test.js | 15 | AUDIT SOC (2026-09-16, Mac: "Can we do an audit of everything just merged. Just want it to be perfection"). THE FOUR LENSES' PINS over the merged social arc, driven over the fake Durable Object with the runtime's walls (128 keys a batch, 2 KiB an attachment, a paged list) and over a fake socket. THE HUB: a request-and-cancel loop and an invite-and-decline loop reach their target ONCE per SOCIAL_REPEAT_MS, the stamps per target and per ACCOUNT (a reconnect resets nothing, a refused act stamps nothing) - A1/A8; a storage that throws is contained in every social arm and closes no socket - A2; the room marks itself a hub and arms the sweep on the first account hello, and the sweep forgets an account nobody's list names after ACCOUNT_IDLE_MS with its secret, SWEEP_PAGE a firing with the cursor in storage, never a listed, a recent or an online one - A3; a party of one with no live invite goes with its maker's last tab, one with an invite out waits it out, and the sweep deletes a party whose every seat has lapsed and keeps one with a seat held - A4; a picture for an account with no socket is not composed, the records an awake object read are kept, a swept party is a kept miss - A5/A9; a replaced socket leaves its account - A7; ACCOUNT_TABS_MAX is the hub's bound and keeps the NEWEST tabs, and the newest tab speaks for the seat - A10/B9; 'no account' is refused under the room's budget - A11; the widest attachment measured under 2 KiB. THE LINK: the picture, the lines and the poses gated coming in at the wire's own rates, per room - B3; a frame past INBOUND_FRAME_MAX (derived: a memory plus a full roster of hellos) dropped unparsed - B20; an act projected by the wire's own law before it leaves - B11; `clockRead` on the channel's welcome and the host reading the hub link's clock - B7. THE PICTURE: my own tabs are me, never a stranger to friend, and the colour answers are kept per version and dropped on every change - C20/C23. THE HOST by source: fatigue in the sheet's digits and the pose reading the entity with the pixel once, the accountless page told once, the picture expecting the account it sent, the `above` closures and the touch hook - B5/B18/B10/B19/C2/C9. Each pin names its mutants (tools/mutants/auditsoc.json: 55 over the hub, the link, the picture, the host and the surfaces - 54 dead, 1 equivalent as recorded) |
| name1_bubbles.test.js | 20 | NAME1 + BUBBLE1 (2026-09-16, Mac: "Player names clip and cut off the top of the sprite head and additionally grow in size the further away + are able to be seen through walls"; "I want to introduce chat bubbles above the player when they chat"). THE NAMES: the head top projected exactly (the old +0.25 world lift was half the clip) with the clearance NAME_GAP_PX in SCREEN pixels, so the label's bottom edge sits above the head at every depth (pinned at z=-4 and z=-28 against real perspective/lookAt); `projectToScreen` hands back `depth` and `nameScaleFor` is the perspective law - scale 1 at REF 18, the near clamp at 12, the far at 32.7, half at double, monotone over 0.5..60, MAX for a point on the lens - so a far name is the SMALL one; sight is ONE ray on a REAL `player/collider.js` Collider with a real wall mesh (blocked through it, seen past its edge, this side, over it, with no collider; the skin proved both ways; the ray counted as the last cull - an out-of-range peer costs none); the DOM face `ui/nameLayer.js` in the pixel stack, its sheet once, party green through SOC4's own seam (`cssRgba(PARTY_GREEN) === PARTY_GREEN_CSS`), moved not rebuilt (a write counter: a moved name is two writes and no node; a quiet frame none), covered, destroyed. THE BUBBLES: over the right peer, replaced by the newer line, faded then gone at BUBBLE_MS, refused for another tab, a system line, my own line, no id, no text - each individually and through a real ChatLog - the cap BUBBLE_MAX held with the oldest evicted, and the sanitizer never re-run (no wire import, no innerHTML). The host by source: one face per frame, the live collider handed in. tools/mutants/name1.json: 39, 38 dead, 1 equivalent as recorded AUDIT NAME (2026-09-17, +11): the dungeon overlay arm runs the pass before it returns (a covered frame hides the root, costs no ray, still pumps the log); the collider's buckets keep an AABB and a slab test rejects a ray before any walk (30 off-line buckets cost 0 cell lookups; a fan of rays agrees with brute-force Moller-Trumbore), and a per-peer sight cache with hysteresis (18 rays for 60 frames of 3 peers; 0 nodes built over 10 flickering frames; the exact hold boundary); the size law at two heights and two FOVs with the HUD scale outside the legible band (16.0 / 10.7 / 9.0 / 32.0 px); z-index strictly below 4; a five-minute-old line never bubbles and a future stamp cannot hold one; the skin gate; the watermark keyed to the log; zero-alpha neither shown nor counted; whitespace no bubble; a NaN point draws nothing; one bone; the bitmap gap scaled at hudScale 1/2/4; the host's composition driven end to end (four culls, one ray each for the two peers that earn one, one face a frame). tools/mutants/name1.json: 71, 68 dead, 3 equivalent as recorded |


## PY1 - THE PARITY PINS WERE BLIND (2026-08-28)

**Fourteen pins across eight files were reporting green by never
running.** They are not remembered-value pins - they REGENERATE a port
table from Daggerfall Unity's own C# and compare it cell for cell:
ENEMY_BASICS off `EnemyBasics.cs`, LOOT_MATRICES off `LootTables.cs`,
the ingredient ITEM_GROUPS and artifact rows off `ItemEnums.cs`,
MAGIC_ONLY_KEYS off the effect classes, the race templates,
PlayerActivate's ladder, the two enemy-motor waves,
`DaggerfallInterior`, and MacroHelper's whole %-table. They are the
ONLY pins that can catch the port drifting from a DFU nobody re-read.

Every one resolved the checkout as a hardcoded `'../tools/parity/dfu/'`
and skipped when it was absent - which is every environment where the
reference tree lives somewhere else, including the one this project is
developed in (the clone sits outside the repo). The suite printed
`# skipped 14`, which at a glance reads exactly like `# pass 14`.

`tools/parity/prepare.sh` has honoured `DFU_PATH` since AUDIT 18 ("Point
it at a checkout you already have"); the tests simply never learned the
convention. `test/dfuRoot.mjs` is the one home now - env var first, the
in-tree sparse clone second, prepare.sh's own precedence - and with no
checkout at all the pins still skip, correctly: Port-Doctrine keeps DFU
an EXTERNAL reference, never vendored, so CI's shape is unchanged.

**All fourteen pass against the live checkout**, so the tables had not
drifted - the pins were right and blind, not wrong. Run them with:

```sh
DFU_PATH=/path/to/daggerfall-unity npm test
```

THE LESSON, and it is why this got its own slice: a skip is not a pass,
and a suite that cannot tell you which it printed is lying by omission.
The campaign taught the second half - the first version of the
env-var pin GREPPED the helper for `process.env.DFU_PATH`, and the
mutant that disabled the arm with `false ?` left the matched text
intact and survived. The pin spawns a child with the variable set and
asks where the root actually landed. **A pin that greps for a mechanism
does not test the mechanism.**

Pins: 3 in `test/parityroot.test.js` (a SOURCE SWEEP so no future test
re-hardcodes the path - the shape R6's bare-`this` sweep takes - plus
the behavioural precedence pin and the still-skips-cleanly arm).
Campaign: 5 mutants, 5 killed.

## EF1 - A COVERAGE CLAIM MUST BE MEASURED FROM BOTH SIDES (2026-08-28)

PY1's sibling, and the same failure one level up: PY1 was a pin that
never ran, this is a measurement that never asked the second question.

The effect library's `out.skipped++` had carried "the library grows one
family at a time" since the port's first magic slice, and it was picked
as the next slice on the strength of that one sentence. Three
measurements later there was no gap - and a defect the gap question
could not see.

**The first measurement used a proxy.** `effectActiveIdentity` was
asked which families it could name and answered "65 reached, 17
skipped". It is the ACTIVE-ENTRY classifier - an instant family handled
inline has no active identity by design - so Open, SpellAbsorption,
CreateItem and fourteen others were counted absent while sitting in the
file. A grep found all 17 in the port. The number was never reported as
a finding, because a number that measures the wrong thing is not a
weaker finding than the right one, it is not a finding.

**The second used the right instrument against the wrong corpus.** The
real counter, driven per key - but over a key set built by grepping
`MakeClassicKey(g, s)` for literals: 82 of DFU's 91.
`ElementalResistance.cs:194` and `PacifyEffect.cs:111` build theirs
from a loop variable, and nine real families are invisible to that
grep. Zero gaps against an incomplete corpus is not zero gaps, and the
mutation campaign now carries an arm that removes a single loop-keyed
variant (8,4) so a corpus that quietly loses them again fails.

**The third asked the other direction, and that is where the bug was.**
Sweeping all 65,536 classic keys through the real dispatcher, the port
answered 346 where DFU defines 91. The 255 extras were group 43,
subgroups 0-254: the Teleport arm tested `e.type === 43` and never its
subgroup, where DFU keys the class `MakeClassicKey(43, 255)`. Its own
comment read "(43,255)" the whole time.

THE LESSON: **"nothing is missing" and "nothing extra is answered" are
two claims, and a coverage pin that only makes the first will pass
forever on code that fails the second.** It was true on the broken
code. Whenever a pin asserts that some set is covered, ask what it says
about the things OUTSIDE the set - usually the answer is nothing, and
usually that is where the defect is.

The control arm is the other half of the discipline and costs four
lines: keys outside the classic space must REACH the counter. A
coverage pin whose measurement cannot report a miss proves nothing, and
the campaign's second mutant (delete `out.skipped++`) is exactly the
vacuous-measurement case it exists to catch.

Pins: 4 in `test/effectcoverage.test.js`. Two are UNGATED and catch the
defect in CI where no checkout exists; two REGENERATE DFU's key set
through `test/dfuRoot.mjs` (PY1's one home), so they run wherever the
tree is - and were verified against the live checkout, not left to
print a skip. Campaign: 5 mutants, 5 killed.
