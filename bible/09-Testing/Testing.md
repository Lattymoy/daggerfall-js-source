# Testing

Runner: `node --test` (bare - a trailing `test/` path breaks discovery on
Node 22). Suite: 2073 tests across 230 files.

| File | Tests | Covers |
|---|---|---|
| action.test.js | 6 | door lifecycle/verbatim constants, move tween + chain gate, activation picking; U6 text actions (the 8600/5400/7700 bases + the verbatim type-12 answers, ShowText firing, ShowTextWithInput skipping the up-front cascade with only a case-insensitive answer match firing ActivateNext, DoorText remapping 7701->7705 + holding the door on first activation + the axisRaw>5 trespass gate); P10 locks (lock value 16 / magic threshold 20, the clamp(5..95) lockpicking chance, all five LookAtInteriorLock text tiers, locked-door refusal firing onLockedDoor while solid, Lock/Unlock/Open/Close delegates through door-riding action nodes incl. only-when-unlocked and StartingLockValue restore) and P10 teleport (resolved next-object warp, null destination refuses, actionless-destination cascade no-op) + the block-instance key namespace (two instances of one RDB position both register and play independently - the 3108-dungeon repeated-block collision fix) |
| factionrep.test.js | 19 | S25's faction reputation store against DFU's PersistentFactionData. The propagation law needs CONTROLLED trees, so most pins run on synthetic dicts - and the first test asserts a synthetic record carries exactly the field set the real reader mints, so a hand-built fixture cannot describe a shape nothing in the game produces (it caught this file doing exactly that: the zeroing pin hand-built DFU's regionData[].legalRep, which the port never mints, and passed against an implementation reading the same wrong field - a live probe found the pair, and the pin now lets court.js's changeLegalRep MINT the shape). Pinned: the clamp bounds against DFU's LITERALS rather than the port's constants (asserting MIN_POWER let a mutation of that constant move the expectation with it - vacuous, and a mutation run caught it); halving that truncates TOWARD ZERO, the C# int-division quirk every negative half-delta rides on; allies +half and enemies -half, flat, never fanning out again; the Dark Brotherhood treated as its own root although it has a parent; the knightly-order branch; the God test reading the ROOT's type and not the origin's; questors and root parents taking full where siblings take half; and that zeroing RESTORES FACTION.TXT values rather than blanking them. Last, that propagation TERMINATES on the real 366-faction corpus - which is a fact about the DATA, since the walk re-enters itself and only ends because Generic Temple's type is 9 rather than God and 844's ggroup is 17 rather than 9 - asserted BOUNDED so a mutation fails instead of hanging the suite; AUDIT 21 added two: F10 the Generic Temple hop, which was pinned `!== 0` and so left BOTH its amount and its propagate flag free - PersistentFactionData.cs:434-435 passes `amount, true`, and on the real file 450's children include the questor 240, so replacing the call with `(GENERIC_TEMPLE, 1, false)` collapsed five factions and 70 reputation points to one faction and 1 point undetected; F12 the law that DFU has NO visited-set - allies and enemies are paid BEFORE the hierarchy walk, so a faction that is both an ally and a sibling takes the change TWICE (from Molag Bal, Azura reaches +100 and Mephala reaches exactly 0), which is load-bearing on the real Daedra tree at 32 origin/faction pairs and which an 'obvious' de-duplication guard would have broken with every test still passing |
| gamedate.test.js | 9 | S28's calendar against DaggerfallDateTime.cs. The port had a minute counter and no DATE, which is why the guild 28-day rank gate could not tick; the pins are the arithmetic that closes that. SetClassicGameStartTime is pinned against DFU's own docstring - 13:30 on the 4th of Morning Star, 3E405 - as the zero-based field set AND through the 1-based readers, and the class's field defaults are pinned as DIFFERENT from it, because both exist in DFU and collapsing them would lose the distinction between a fresh DaggerfallDateTime and a new game. DayOfYear is swept over all 360 (month, day) pairs to prove it is 1-based, gapless and bijective, so a lost +1 fails rather than shifting quietly. GetSeasonValue keeps DFU's own admitted approximation - clean month boundaries, Evening Star wrapping to Winter - pinned as a twelve-entry table, and the day-name cycle is pinned at the 30-day month's wrap where a naive week count drifts. Last, the reason it landed: daySinceZero over the port's elapsed-minute clock moves exactly one per game day, crosses DAYS_BETWEEN_RANK_CHANGES at 28 and not at 27, and survives a year boundary |
| guilds.test.js | 31 | AUDIT 20 added five: the membership slot is the GUILD GROUP (all eight temples share one, so joining Mara's replaces Arkay's - keyed by name the port held all eight at once), HasJoined taking a GROUP the way DFU asks it, the per-RANK promotion message (five of six guilds announce the benefit the rank unlocked; the port returned one flat record), guildOfFaction resolving temples and orders after G2 shipped them, and a temple faction resolving to HolyOrder through its CHILD because every divine's own ggroup is None. G1's guild foundation against DFU's Guild.cs. Rank is RECOMPUTED from reputation and skills rather than stored, so most pins are table-walk boundaries: the three rank tables deepEqual'd against DFU's literals; a negative reputation expelling before a skill is read; `return --r` giving -1 rather than 0 when row 0 fails; and the `else if` that stops a HIGH skill also counting LOW, pinned by the case where one maxed skill would otherwise satisfy "two skills, one high" alone. The 28-day gate is pinned on days-since-zero so it survives a year boundary, with day 27 moving nothing and day 28 promoting. EXPULSION REMOVES THE MEMBERSHIP, pinned on the KEY rather than the rank - the first cut left removal to the caller, which let the port hold a keyed membership at rank -1, a state DFU never has; a mutation run found it. On the real FACTION.TXT: all four guild faction ids resolve to the right factions, and THE MERCHANTS (510) really do ship ggroup 11 (FightersGuild), which is why DFU hardcodes 510 to None and why every shop would otherwise answer as a Fighters Guild hall + AUDIT 21: two laws the port had no counterpart for. THE THIEVES GUILD AND THE DARK BROTHERHOOD NEVER EXPEL - both override CalculateNewRank with AllowGuildExpulsion, clamping a negative rank to 0, and the port's single shared calculateNewRank DELETED the membership of either at negative reputation, unrecoverably since both are initiation-only. Pinned on both sides: those two clamp to 0, the other four still expel, and the end-to-end updateRank path keeps the record. AND THE JOIN GATES IN DFU'S ORDER - JoinButton_OnMouseClick asks the guild manager for the guild ALREADY IN THAT GROUP SLOT and stops dead if IsMember, so a rank-7 member is offered nothing and a Patriarch of Mara cannot trade her temple for rank 0 in Arkay's; the port ran only the eligibility check, and joinGuild ASSIGNS into the slot. The gate is keyed by GROUP, not guild name - keying it by name passes the same-guild case and still lets the temples trade + guildOfFaction resolving by GUILD GROUP as GetGuild(factionId) does, not by matching a guild's own faction id - 68 more of the corpus's factions now answer, and the pin holds the REAL split rather than pretending everything resolves: 307 factions carry a group, 161 are in one of the six groups CreateGuildObj implements, 98 of those resolve, and the other 63 are sub-factions like 'Apothecaries of Arkay' or 'The Royal Guard' that carry a variant group but are not one of the eight divines or ten orders - Temple.GetDivine THROWS for those and DFU catches it, so null is DFU's answer too. Oblivion's 209 Daedric factions must likewise stay null, because CreateGuildObj's switch ends `default: return null`; AUDIT 21 added seven, six of them the guilds lane's second half: F4 IsSatisfyQuestReqByLevel (the two-guild flag - Mages and the knightly orders - plus questRankFor, the three lines in DaggerfallGuildServicePopupWindow that read it, so a level-12 rank-2 mage draws the level-12 quest pool and a fighter does not); F7 getTitle, which corrected a DOC LIE - the header claimed the player-name fallback was 'DFU's own non-member return' when Temple, KnightlyOrder and DarkBrotherhood each override GetTitle and return a localized "nonMember" string, so the port was shipping a WRONG KNOWN value for half the guilds while claiming to withhold only the unknown one (getTitle answered null there, and the four gendered rank slots were recorded as structure); F8 the initiation quest ids O0A0AL00 and L0A01L00, the ONLY way into the two invitation-only guilds - INVITATION_ONLY had named the refusal and the port had an exit with no entrance; F9 vampMemberships, the second parallel membership book DFU swaps in on vampirism (a plain object still means the mortal book, so no call site broke) and ClearMembershipData clearing BOTH; F10 GetGuildFactionId derived from GUILDS so it cannot drift, including the two deliberate zeros for the variant groups; and F12/F14/F15 the four fixed guilds pinned to LITERALS - which skills, which text records, which promotion maps, which guildGroup - after the lane proved eight separate id-table mutations survived because the pins read their expected value back out of the table under test (M10 was the sharpest: it changed the Mages Guild's default promotion record 5236 and the pin still passed). Two equivalent mutants are recorded in-file so nobody re-hunts them: questRankFor's `>` vs `>=` (Math.max either way) and deleting guildFactionIdOfGroup's variant branch (the derived map misses those groups and falls to the same 0, exactly as DFU's switch falls through to `default:`); U23 met F7 from the other side: widening the DFU sparse clone to Assets/Localization recovered the strings the structure was waiting for, so the same test now reads a title instead of a withheld null - members read RankTitles[rank] in all six guilds, the three overriding guilds' non-members read the 'non-member' string rather than the player's name, and all four gender swaps are pinned in BOTH halves (the rank that swaps AND the string it swaps to). The swap is pinned PER RANK, not per guild: a female at rank 7 of the Dark Brotherhood reads 'Assassin', so moving a swap to a neighbouring rank fails. Five mutations were run against it - the swap made inert, the non-member marker ignored, the marker leaked to members, the rank index off by one, and the female string set equal to the male one - and all five died. |
| guildserviceactions.test.js | 16 | U24's three performable guild services, from DaggerfallGuildServiceTraining/Donation/CureDisease. Training's cooldown is pinned as a DIFFERENCE of 720 classic minutes at both boundaries (719 too soon, 720 clear), and the too-skilled gate as STRICTLY greater - a player sitting exactly on the 50 cap may still train, which an off-by-one would silently take away. `Random.Range(10, 20 + 1)` is pinned at BOTH ends to hold its inclusive upper bound, and the three-hour cost is pinned as the pair DFU writes: 180 minutes of clock and DefaultFatigueLoss x 180 of fatigue, charged in one go because DFU's own per-minute drain fires once across a jump. The donation chance is the pin worth having: `(2 * amount / max(rep,1)) + 1` with C# integer division, pinned at a value where truncating and rounding differ, and reading the ABSOLUTE reputation - a temple that hates you is exactly as hard to impress as one that loves you. The change is pinned NON-propagating against a two-node tree, because a flat one-faction fixture cannot tell propagation apart and a mutation run proved it: the pin passed with propagate:true until the parent was added. Curing is 250 per disease through quality and bargaining with the NEUTRAL regional adjustment (DFU calls CalculateCost with two arguments), only Arkay discounts it and only for members, three holidays cure free even with an empty purse, and the haggle message is pinned as three SHIFT bands at their exact boundaries with an odd cost to expose the truncation |
| guildserviceflow.test.js | 15 | U23's law half - StaticNPCClick's routing and the guild popup's OnPush. The routing pins are the branch table itself plus DFU's TWO bug-numbered escapes, each named in its own source: t=1238 sends a holy-order NPC standing in a non-divine building to TALK rather than to a service menu it has no guild for, and t=2037 does the same for the Thieves Guild spymaster in a faction-less building - with the third TalkToStaticNPC argument pinned as surviving the routing, since it is the only thing that makes that talk different from an ordinary one. Merchants are pinned as routing by BUILDING type and not by the NPC. Setup's member flag is pinned FORCED for the Dark Brotherhood and for GeneralPopulace - which is the Thieves Guild's group, the reason that arm looks mismatched and is not. The two refusals are pinned as DIFFERENT: a non-member is told to join in a plain string, a member of too low a rank gets TEXT.RSC 3100, and the same temple service clears at the rank its column names. OnPush is pinned in DFU's own order - the heal lands BEFORE its box, the recharge AFTER, damaged attributes ASK before curing and only the Yes branch cures, a positive stat mod is not damage - and the rank step is pinned carrying UpdateRank's own text id rather than a recomputed one |
| guildserviceflows.test.js | 17 | U24's list picker and the three message-box chains. DaggerfallListPickerWindow's geometry is pinned against its literals - PICK00I0 at (60,36), the list at (26,27,138,72), the 9x9 paging buttons, rowsDisplayed 9 and rowSpacing 1 - plus the SELECTED row colour, which is DaggerfallUI's 162,36,12 dark red and not the brighter yellow a reader would guess. Paging is pinned moving a WHOLE PAGE and clamping to Count - RowsDisplayed at both ends, and a list click is pinned resolving through the SCROLL INDEX rather than the visible row. The chains are pinned in DFU's own order: training checks gold BEFORE the picker opens (so a poor player never sees the list) and checks the skill cap BEFORE taking payment (so a too-skilled pick costs nothing); the donation field opens pre-filled on "1000", refuses everything but digits, caps at 8 characters, and does NOTHING AT ALL on an unparsable entry, which is what int.TryParse's missing else means; the free-holiday cure fires on OPEN, before any question and with no gold at all. The picker's result box is pinned present after a pick - the live probe found the chain closing on itself and throwing that box away |
| guildservices.test.js | 12 | G3's guild services against Services.cs and the Benefits regions. The service NPC table is pinned as DFU's SWITCH rather than its enum names, by the three entries where the two DISAGREE - MG_BuySpells maps to BuySpellsMages, KO_Smith to ReceiveArmor, KO_Seneschal to ReceiveHouse - which are exactly what deriving from the `PREFIX_ServiceName` suffix gets wrong. Pinned that -1 in the temple service table PASSES the rank gate rather than meaning "never" (CanAccessService tests `<= rank`; the service is simply not OFFERED, since there is no NPC - the gate and the offer are different things, and G2's comment had it backwards). The money formulas are pinned on their TRUNCATED values - a rank-3 reward on 1000 gold is 1296 not 1300, a rank-9 repair 97 not 100 - because the C# `/10` truncates before the multiply and a cleaner formula passes at rank 0 and drifts everywhere else. Plus: training caps at 50 and costs the 100/400 split TIMES LEVEL with no rank discount; a knightly order trains NOTHING; a temple trains NON-MEMBERS where a guild does not; the Mages Guild's free recharge is gated on the Sorcerer's NoRegenSpellPoints flag; a knight rooms free at rank 4 or at any rank in their own region; and only Arkay discounts curing. The temple training-vs-rank split is pinned as MEASURED - five differ, Kynareth/Mara/Zenithar are identical - after a blanket "they always differ" pin failed on three of eight |
| guildservicewindow.test.js | 8 | U23's window geometry and hit routing against DaggerfallGuildServicePopupWindow's #region UI Rects. The panel position is the pin that matters: DFU declares `Position = new Vector2(0, 50)` and then sets BOTH alignments to Center/Middle, which makes BaseScreenComponent ignore it entirely - so the panel sits at (95, 75) and the test asserts it is NOT 50, which is what a reader porting the declared line would have written. The four button rects are deepEqual'd against the literals and their 9px stacking checked. The rest is the window's own machine: each rect routes to its own handler; a MEMBER's join rect is DEAD rather than merely unpainted, since Setup only ADDS that button for a non-member; an OnPush box stack eats clicks until it clears, one per box, and a YesNo box refuses a stray click; the No branch does not run onYes; and a join offering a YesNo closes the popup with the box, because JoinButton_OnMouseClick calls CloseWindow before showing it |
| guildvariants.test.js | 17 | G2's variant-keyed guilds (Temple's eight divines, KnightlyOrder's ten orders) and the join decision. The variant tables ARE faction ids, so most pins run on the real FACTION.TXT: all eight divines are type God with EXACTLY ONE child - their templar order - and all ten orders are ggroup 9. GetDivine is pinned taking either the temple hall's faction or the TEMPLAR ORDER's (resolving through the parent, so the Order of Arkay answers Arkay), and GetOrder is pinned NOT walking a parent, by asking it for an order's parent and requiring null. Per-divine data is pinned as per-divine (different skills, different welcome records) against the orders' one shared list, and the verbatim oddities are pinned rather than tidied: Arkay's blessing id really is 0, Julianos alone welcomes on 6610 and alone sells magic items, and Zenithar's faction is named "Zen". A temple and an order are run through calculateNewRank unchanged, proving the variant record is the shape the G1 rank law already consumes. Last, the join decision: bad-rep and low-skill route to DIFFERENT TEXT.RSC records (a player refused for the wrong reason has been told to fix the wrong thing), and the Thieves Guild and Dark Brotherhood refuse the question outright - both throw NotImplementedException in DFU because they are joined by INVITATION; AUDIT 21 added five, all pin-quality, all against mutations the lane proved survived: F11 all 112 TEMPLE_DATA values (8 rows x 14 columns - four had been pinned to a literal, one column range-checked, and the other 107 asserted only to be OF TYPE NUMBER, so scrambling Mara's library/healing/buyPotions left the suite green); F12 WHICH guild skills, for all eight temples and the shared knightly list, after they had been checked for validity and never for content - guild skills are the rank law's only skill input, so a wrong entry silently caps every member a rank low; F13 the entire Temple promotion mechanism, which had NO pin at all and survived five independent mutations - TEMPLE_PROMOTION's eleven ids, templePromotionId at each selecting rank, and the templeOf -> promotionForRank -> promotionTextId wiring, including Arkay rank 4 where makePotions and soulGems are BOTH 4 and DFU's declaration order decides (reordering TEMPLE_SERVICE_ORDER inverts that one answer and nothing else would notice); F14 TEMPLE_TEXT, KNIGHTLY_TEXT and the knightly promotion map as literals; F15 guildGroup by value on every temple and order plus a corpus check that each record's group is the one the shipped FACTION.TXT carries, since membershipKey IS that value and the pre-existing 'count is 2' pins were satisfied by any group at all. The expected literals were GENERATED from Temple.cs and the subclass files - decoding RankData's positional constructor through its own field assignments, note makeItems -> this.makeMagic - then diffed against the port mechanically, not re-transcribed from what the port already said. U23 added the rank-title tables themselves: ten DISTINCT strings apiece (a duplicate is how a mis-transcribed slot shows up), the male form sitting in the ungendered table where DFU swaps it, and femaleRankTitles keyed by EXACTLY the ranks femaleTitleRanks names - a slot declared but not filled falls silently through to the male string in getTitle, which is the failure this pins against - plus the identity check that all eight temples and all ten orders share ONE table object apiece rather than per-variant copies that could drift |
| holidays.test.js | 6 | S29's FormulaHelper.GetHolidayId. Both tables are pinned at 53 rows with the loop bound covering all of them, which makes the enum's 54th member Old_Life_Festival unreachable - swept over every day of every region to prove it, with the last row Saturalia proved reachable in the same sweep so the pin cannot pass by the loop simply stopping early. The `dayOfYear <= 355` gate is pinned by sweeping days 356-360 across all 62 regions and requiring None, and by showing the tables end exactly at 355. The 0xFF/regionIndex+1 rule is pinned on Scour_Day, which one region celebrates and its neighbours do not. Last, a structural pin: every reachable id equals its table row plus one, checked row by row, so a shifted table renames every festival and fails instead of drifting |
| iteminfo.test.js | 11 | U25's ItemHelper.GetItemInfo. The thirteen record ids are deepEqual'd, and the branches that pick between them are pinned where they are easy to miss: armor and weapons each have a with- and without-material record, an ARTIFACT never shows its material, a helm and a shield never show one under classic's own setting default, and an arrow has a record with no condition line at all. The condition ladder is pinned band by band at every threshold boundary (`while (pct > threshold[i]) i++`, so 40 is Worn and 41 is Used), including the fall-out case where an item above its maximum prints a raw number. The two computed macros are pinned as DFU writes them - the C# "+0;-0;0" format really does put a plus on every real armour value - and the material name is pinned reading TWO differently-keyed enums, armor's bit-packed against the weapon's plain 0..9. Last, a corpus sweep: nine items through the real TEXT.RSC, asserting NO MACRO SURVIVES to the screen. AUDIT 22 added two: all EIGHT variants of TEXT.RSC 3100 read distinctly through the new variant reader while a single-variant record is untouched and the pick stays in range at both ends (the port drew variant 0 forever where DFU picks at random); and a producer sweep for the three item flags - artifact, azurasStar, oghmaInfinium - that these branches read and NOTHING in src/ writes, pinned BOTH ways so the day one gains a producer its author is sent to the flag |
| mysticism.test.js | 13 | S26's Mysticism, the effect library's one entirely empty school. Pinned first that NOT ONE of the ten supports magnitude - the structural reason the school needed its own module rather than another branch of applySpell's magnitude ladder - and all ten classic keys against the same DFU extraction the coverage measurement uses. The Open/Lock ASYMMETRY is the centre: Open yields a lock only to a caster whose LEVEL REACHES it (equal is enough, and a door it cannot beat stays shut), while Lock has no level test at all and simply imposes the caster's own level - a level-9 caster cannot deepen a level-3 lock, and an already-locked door is refused rather than re-locked. An item cast and the Skeleton's Key skip Open's chance roll entirely, and the Key ignores the level rule too. Dispel is pinned rolling PER TARGET rather than once for the group, and as DESTROYING rather than killing (DFU: no loot, and it can break quests). Soul Trap fills AZURA'S STAR before any ordinary gem wherever it sits in the pack, and azurasStarOnly refuses to fall back. Silence blocks only a cast that COSTS spell points, so an item cast fires through it. S27 added three HOST SWEEPS: silence gating BOTH the ready and the cast (each clearing the readied spell - the gates now read in hostMagic.js, the S30 engine), the four-host sweep - originally pinning casting as DUNGEON-ONLY so a host growing a cast path would fail it and send its author to the gate, which S30 DID, so the sweep is INVERTED: every host now must mount the ONE engine - and that Open/Lock are still unwired, which fails the moment either ActionSystem owner calls the door payload without the record being updated. These hosts have no execution coverage in node, so the seam is read rather than run |
| talktopics.test.js | 7 | T3c building names + Where-is: GenerateBuildingName verbatim (the seeded B-then-A draw order reproduced by hand, seed-stable, Bank/GuildHall/House forms, the macro carriers), the 8-way compass bands over (east,north), the NPC-stable reaction tier + the 30-record answersToDirections shape (knows-Commoners tier 0 = 7261), the named-building pool merge over the REAL Daggerfall city (named>20, taverns>3, classic two-part tavern names through the directory); the 17c audit pin - the pool merge BOUNDED by the subrecord count (header garbage past it never draws); T3d - the answer invariant under a pure frame translation (the streaming host's location-frame doctrine); T3e - the knowledge roll (rollToBeat 15 for Commoners, MakeBuildingKey incl. the 1<<24 sentinel, hand-reproduced seeded rolls, seed-stable, BOTH table halves reachable and whereIsAnswer drawing the matching half); T3f - the tone tiers (the three verbatim mod tables, the neutral wrapper identity, the Dice100 skill roll separating pass/fail bands, the Merchants sgroup fold, the per-session reaction cache beating a fresh skill roll, the first-use-only tally) |
| court.test.js | 14 | G2 arrest + court verbatim: reputationLossPerCrime charging LegalRep per region + the surrender gates (SetHealth(1) BEFORE any refusal, < -20 involuntary refusal, the DFRandom coin, dead never surrender), the penalty math (base +- perRep*rep clamp [min,max] /40, the coin split 40 gold / 3 days, the gold cap converting to days) + the guilty plea halving-and-deducting, the not-guilty pleas (Etiquette/Streetwise + Personality chance clamp 5..95, free vs guilty with the fine roll x2/half) + THE NEVER-CHARGED VERDICT QUIRK (DeductGoldAmount lives only in the guilty PLEA - a failed defense adjusts but never charges the fine, preserved 1:1), sentence rep raise = half loss - 1 + AUDIT 21: the sentence refunding BOTH channels (the port credited only legal while the crime debited both, making the faction side a ratchet - Murder cost the People 10 and refunded 0 where DFU refunds 4); ClampLegalReputations and NormalizeReputations, which had no port counterpart at all, so twelve High Treasons reached -900 and nothing ever decayed - pinned with DFU's asymmetry intact, the legal side nudged directly and the faction side going through ChangeReputation so it propagates; the SHORT-CIRCUITED second court roll, since C#'s `&&` draws it only when the first fails and drawing both burned a number from the generator on every court appearance (an audit18 pin had encoded the old consumption and is corrected here); and HandleCourtLogic's opening guard, without which a court with no crime indexed the penalty tables at -1, produced a NaN fine, and charged a point of legal reputation for serving it; AUDIT 21 added three and rewrote one: F9 all 76 penalty-table cells as literals transcribed from DaggerfallCourtWindow.cs:60-63 and PlayerEntity.cs:2284 - 56 of them had been unreachable by any pin, and setting every unexercised entry to 0x63 left ten test files green; F11 the four constants that survived their own mutations (both Math.min(75) thresholds, exercised at legalRep -200 where the cap actually bites; the 5..95 chance clamp at BOTH ends; the `roll > 75` fine-halving arm; and the state-2 release credit); F7 the Execution arm, which DFU cascades three ways on both plea paths and the port tested only `=== 0` - unreachable, since startCourt cannot mint a 1, but an arm that is absent and an arm that is WRONG read the same from the call site. The 'released || prison' assertion was replaced: resolveGuiltyVerdict has no third return, so it was a tautology, and deleting its raiseRepForSentence killed nothing. Chasing that revealed a twin - pleaGuilty's zero-day arm is three IDENTICAL lines, a mutation aimed at one landed on the other, and neither was pinned. Both are now |
| cityguards.test.js | 5 | G1 the city watch (SpawnCityGuards verbatim over real CLASS18.CFG): the constants (146/max 5/77.5/105.469), immediate spawn converting the in-range wandering guard FIRST + disabling the source NPC (out-of-range ignored), the behind-player civilian 1/4 conversion, the 2-5 ring fallback when nobody converts, the over-max gate refusing further spawns; the 17c audit pin - the verbatim seen-by-guard MASS conversion (once any guard sees, every REMAINING pool NPC converts, gate-independent; the pre-seer stays); G3 corpse loot - KILLED guards are loot targets (walk-aways vanish with their items), takeLoot transfers once into the player entity and empties the corpse; G4 murder/assault - a civilian in weapon reach dies to ONE hit (disable + Murder + the response), out-of-reach and walled strikes are no-crimes, a wandering guard NPC converts on the spot (Assault) with the swing carried onto the fresh foe, and killing the watch through the real death path is Murder |
| shopstock.test.js | 5 | E1 the shop foundation: the 288-row baked template table with spot pins (Ruby/Horse/Book), the group enum mapping (Armor 102-112, Dagger first weapon), ItemBuilder's material value laws (weapon/plate basePrice*3*mult, chain x2, leather flat); StockShopShelf verbatim (all-templates at always-pass rolls, none at always-fail, the rarity<=quality gate, general-store horse+cart + the NO-dice book ladder both branches (q7 -> 4, q20 step-down -> 5), the gender clothing swap, the verbatim pair tables, every item carrying a value); CalculateCost verbatim (the 2x@q10 anchor, +-percent in C# toward-zero truncation, the value>=1 clamp, the regional adjustment FIRST, the lazy sticky 750..1250 region band); E2 - CalculateTradePrice verbatim fixed-point (the hand-reproduced 144/256 anchor, sell < buy, Mercantile lowering buys and raising sells, quality raising prices); E3 - storeBuysItemType verbatim (Bookseller Books-only, WeaponSmith Armor+Weapons, PawnShop 10 / Alchemist 9, the gate refusing off-list groups + Currency + non-shops) |
| nativetrade.test.js | 3 | U8c the native trade window: the composed DFU geometry (inventory lists 163/261,48,59x152, the SHOP00I0 cost strip, the INVE08I0 action panel, 38px slots x4), the click trade machine over fake hooks (remote click buys / local click sells with the item crossing lists, outside-rect not consumed, exit + digits + Esc); HOTFIX (Mac's catch) - icon draws V-FLIP the record texture (getColor32 stores bottom-up GL rows, drawScreenQuad samples v0 at the top - the shelf books rendered upside down; the flipped source rect pinned); AUDIT 17d - the ItemListScroller law VERBATIM (itemListPanelRect 9,0,50,152: the 50x38 buttons at x=9, the LEFT 9px rail = up arrow 0,0,9,16 / down arrow 0,136,9,16 / the bar between; rail clicks scroll and never trade), icons ScaleToFit MaxAutoScale 1 - never upscaled - centred in the BUTTON at x9, the V-flipped record source rect pinned, stack counts the only cell text (FONT0004; no names - the info seam pends) |
| startinggear.test.js | 6 | S3d AssignStartingGear (ItemHelper.cs:1277-1364) verbatim: the class weapon + material tables pinned WHOLE in career order (a spot check would survive shuffling a class's weapon) with the Mage's STEEL shortsword and the Monk's staff read back; a male Warrior beginning dressed in the men's Short_shirt/Casual_pants with a RandomClothingDye and a random variant, both EQUIPPED to Chest/Legs clothes, carrying an unworn Spellbook and an iron Broadsword with 100 gold; women taking their OWN clothing templates (206/190, never the men's); the ARCHER alone adding a steel Battle Axe + 24 iron arrows, proven archer-only against the Ranger who carries a battle axe as its class weapon but no bow or arrows; custom classes getting an iron Longsword instead of the table weapon; PlayerTorchFromItems ported but defaulted OFF (a DFU setting, not classic) and giving 5 torches + 2 candles when on; and every minted item carrying a value and a name (the 17e F2 root fix - a valueless item priced at 1 in the shop) |
| races.test.js | 2 | S3c/U9 the race table: all EIGHT RaceTemplates pinned against the DFU literals (background/male+female bodies/head CIFs/morphology per race) with the 1-BASED Races enum called out - the art tables are GENERATED from the scheme precisely because that off-by-one makes hand-typed tables drift; the camel-split display names (Dark Elf), 10 face records per CIF; raceArt picking the gendered set and falling back rather than throwing on an unknown race; and body MORPHOLOGY driving the armor archive (ApplyArmorSettings - Argonian 0 / Elf 1 / Human 2 / Khajiit 3, with Khajiit exactly one past Human) where the paperdoll used to hardcode Human |
| audit17e.test.js | 13 | AUDIT 17e Wave 0 ship-blockers, each pin mutation-proven: the shop round-trip invariant (buy never undercuts sell, both sides resolving value the same way and multiplying by the stack - the old asymmetry was an unbounded gold loop); the OnExitLocationRect crime clear (first-sync/wilderness/same-key are not exits, town->wilderness and town->town are); save/load rebuilding the equip table from the restored items and re-deriving armor from it (fresh load, same-session load dropping a stale bonus, duplicate slot claims); IsEnchanted DERIVED from the enchantment array (the dead `enchanted` flag means nothing, the FP magic anim set promotes, all three stacking clauses bite on real shapes); an unequip returning the armor it granted; and the MODAL CONTRACT - worldModes.frame() carries a value from every exit (a bare return read as "not handled" and made the hosts draw the town over the dungeon); WAVE 1 - the paperdoll click mode (PaperDoll_OnMouseClick unequips in EQUIP/Select, REMOVE is inert), the clothing armor branch (leather boots 3*5 on Feet for both genders' 147..149 / 186..188 windows, Sandals outside it granting 0 as DFU has it), arrows valued at basePrice with no material multiplier, delayScrollUp's late snap (in-range and partly-filled columns untouched, past-the-end snaps), the INVENTORY icon law (player texture not world sprite, the Katana +1 bump, arrows/ingredients/MiscItems keeping the world sprite, variants advancing the record, and the templates now carrying the FULL DFU row), refreshPaperDoll coalescing instead of dropping, and the worn-weapon bind living in the RIG so all four hosts inherit it; WAVE 2 - the Where-is category list (CheckBuildingTypeInSkipList dropping Palaces/Furniture stores/houses/ships, DFU's enum ORDER, the three corrected captions), the three GL leaks each having an owner (the paperdoll's per-refresh composite, emptied loot piles freeing at WINDOW CLOSE as DFU does - not on the empty transition, which would leave an invisible-but-activatable ghost on refill - and the dungeon's per-foe batches), and dropped piles carrying STABLE ids that survive a cleanup splice while following the floating origin by REBUILDING their batches (the centers are baked into a STATIC_DRAW buffer and cannot be shifted in place) |
| audit17f.test.js | 10 | AUDIT 17f - the parity pass over the audit's OWN changes (17e Waves 0-3, S3c/U9, S3d), each pin mutation-proven: SetRace reaching the INVENTORY LIST and not just the paperdoll (the template archive is the morphology-0 base, so every list drew Argonian clothing for every player - Breton/Khajiit/Argonian rows, armor's per-gender base, and the two windows agreeing for the same identity), the archive-0 sentinel surviving the offset so GetItemImage's world-texture fallback still fires; ONE DFU MEMBER, ONE EXPORT - paperdollArt's rival `armorArchive` (same name, RACE where the other took a MORPHOLOGY) and its third copy of the SetVariant clamps now wrapping the single home, with the transform pseudo-races falling back to Human; AssignStartingGear's three drifted details - the Spellbook heading the bag (AddPosition.Back makes collection order draw order), the pants variant rolling over the TEMPLATE count (women's Casual pants has FIVE where the port hardcoded four), and item names coming from ItemTemplate.name; createChargenWindow firing onDone EXACTLY once through a live flow walk (every key after done re-ran applyCharacter and re-rolled the kit); and finishChargen filling the starting spellbook when handed a table while leaving an existing book alone without one - the town hosts called it with no table at all, so a Mage created in a town began with an empty book; and ONE GOLD MINT - Currency.Gold_pieces (276) carrying the classic pile icon, the template weight and one spelling of the name where three producers had hand-mint stacks with no template index at all, plus the additive-field upgrade that stops a pre-17f save growing a second gold stack goldAmount would never see |
| chargenart.test.js | 7 | U10 - the classic chargen screens, each pin mutation-proven: the eleven window rects pinned WHOLE against the DFU literals (a spot check lets one drifted rect through, and a rect is what drifts - the 17d lesson); the three DaggerfallUI colours these windows use, including the ALTERNATE shadow the rollouts take where every other native window takes the default; the province picker's verbatim law (TAMRIEL2's palette INDEX at the click point IS the race id, because GetRaceDictionary keys on ID) with index 0 rejected and a row-wrapping x rejected - a flat buffer would happily read the next row; GetSkillName's DISPLAY strings replacing the raw enum keys the port had been printing ("Short Blade" not "ShortBlade", the one hyphenated Hand-to-Hand, single words untouched); ONE DFU MEMBER, ONE EXPORT for the seven derived stats - scattered across charsheet/spellcast/rest/chargen with ToHitModifier and HitPointsModifier missing entirely - with the old import sites proven to resolve to the SAME function; the derived block following the flow's LIVE stats through a spend; and clicks landing on DFU's own buttons - face PREVIOUS/NEXT/OK, the stats reroll/OK/stat buttons/spinner halves, the three skill groups' rows, and the 3px BETWEEN rows staying dead (the button is 7 tall on a 10px pitch) |
| messagebox.test.js | 6 | U11 - DaggerfallMessageBox's parchment frame, each pin mutation-proven: the frame constants (SPOP.RCI's 22x22 slice, which is WHY the box rounds to 22; margins 10, minBoxWidth 132, the 44 floor, 32x16 buttons, spacing 32, text gap 4, the 11px single-button nudge); MessageBoxButtons values pinned because AddButton indexes BUTTONS.RCI BY THE ENUM VALUE, so a drifted number draws the wrong button; UpdatePanelSizes rounding UP to the slice and flooring at 132, checked by hand arithmetic on a uniform-glyph font (one more glyph rounds up a whole slice); the `finalSize.y - buttonPanel.Size.y > 0` gate that grows the label block ONCE no matter how many buttons follow, and the verbatim single-button HACK that drops one button 11px LOWER than a two-button strip; a wide strip driving the width past the text; and clicks landing only on the button rects - the 32px gap BETWEEN them, the row under them and the frame itself all dead |
| audit17g.test.js | 6 | AUDIT 17g - the deep parity pass over U10 and U11, each pin mutation-proven: a box keeping each row's OWN alignment (MultiFormatTextLabel centres only the rows a JustifyCenter closed, and the port centred everything - 80 of TEXT.RSC's 676 multi-row records draw wrong that way, 53 entirely left and 27 mixed), with a crafted record proving records really do mix the two; chargenHit returning null without art on EVERY branch including `class`, which alone derefed the art and threw - the one state that needs it was the one that crashed; sizingRows fixing an input box at its widest so the parchment does not grow a slice mid-word, with the un-optioned call proven to still grow; the class list scrolling MINIMALLY (ListBox only pulls the window up when the selection falls above it and pushes it down when it falls below - the port recomputed a CENTRED window at draw time, so the list jumped on every arrow), walked through the window's edges and clamped at the end; and BOTH confirm paths opening the race description box - a keyboard confirm used to walk straight past what a click always showed - with an art-less flow proven never to trap |
| biography.test.js | 21 | S3e - the chargen BIOGRAPHY, each pin mutation-proven. THE PARSER (BiogFile.cs:92-125) on crafted files in the shipping shape: question lines, answers and their effect lists; C#'s Split('.', 2) keeping the REMAINDER where JavaScript's limit DISCARDS it (a question reading "Mr. Smith?" would lose its tail); and the '#' backstory-id extension not desyncing the walk. TWO of DFU's parser rules (the second-line '.'-at-2 arm and the answer loop's is-a-LETTER arm) are DEFENSIVE and UNREACHABLE in the shipping data - every question is preceded by a blank line and the effect loop stops there - so they are ported but deliberately NOT pinned: a test would have to hand-build a file no producer mints, and the corpus gate is the real proof. THE EFFECT GRAMMAR (ApplyPlayerEffect :248-445): skills; gold with both quirks - "GP + 250" being ONE argument and the player never carrying negative gold; social reputation ACCUMULATING while the six single-field mods ASSIGN (the second RP wins); faction rep queued for the faction slice; items with the weapon-to-armor material map; the text commands; the three DFU has never implemented staying unimplemented; and the bare '&' hitting the invalid-command branch. THE FLOW: an answer index past the question being INERT, effects tagged with their question number, the last answer leaving the screen, and a missing question set SKIPPING it rather than trapping. Plus the ten answer-button rects in their two columns. U13: DigestRepChanges totalling the social groups only (the rf arm is DEFENSIVE and redundant - "rf42".split('r')[1] never parses - so it carries no pin); the backstory expanding %qN from the player's OWN answers, with %qNa being the question's SECOND token and a question with no token expanding to NOTHING rather than leaking the macro; the five reflex rows filling their 66x45 panel exactly with VeryHigh on top; the flow ending on reflexes, clamped at both ends, the pick riding the result; and the reputation box being modal - any key closes it AND ends the screen. CORPUS GATE (ARENA2_PATH): all 18 shipping files parsing to twelve full questions that fit the ten buttons, and every one of their ~2200 effects being a command we implement - bar exactly the twelve bare '&' lines |
| audit17h.test.js | 4 | AUDIT 17h - the save gap S3e/U13 made load-bearing, each pin mutation-proven: a save carrying all eleven SOCIAL-GROUP REPUTATIONS and the reaction modifiers (SerializablePlayer.cs:152-162 writes them out field by field; nothing persisted them, so a quicksave/load reset the player's standing with every group to ZERO - and getReactionToPlayer reads them on every greeting, which the pin checks by comparing the greeting either side of a round trip); the six BIOGRAPHY modifiers (:136-141, :305-310) with the reaction one proven to reach the greeting through the load; the queued faction deltas and the composed backstory riding along AND the SNAPSHOT detaching from the live entity, because the quicksave write happens after snapshotPlayer returns; and a PRE-17h save leaving the entity's own reputation state alone rather than nulling it - the additive-field shape DFU's serializer gives missing members |
| chargenseam.test.js | 2 | AUDIT 17i - THE ONE CONSTRUCTION SEAM, the guard rail against a bug shape that recurred three times (a host building a ChargenFlow by hand and missing a dependency it had grown - the starting spellbook and the starting kit in 17f, the biography in 17h). The seam handing back a flow with EVERY dependency ATTACHED, not merely returned beside it (18 careers, a non-empty SPELLS.STD map so a town-created Mage has a spellbook, 18 biography sets reachable through flow.biogFor); and a SOURCE SWEEP over src/scenes asserting that no host constructs a ChargenFlow of its own - the rule enforced rather than remembered, because a host that news one up gets whatever dependencies existed the day it was written and silently misses every one added since |
| chargenpointer.test.js | 4 | U14 - the MENU BACKDROP and the POINTER path, each pin mutation-proven: the backdrop being OPAQUE black (DaggerfallBaseWindow.cs:40 paints the parent panel black - chargen is a MENU and the world is never behind it; a translucent value is the bug, not a style); the gender BUTTON setting the gender AND closing its box, because CreateCharGenderSelect's handlers both end in CloseWindow and the classic box has no OK for the port's extra confirm to use; EVERY chargen screen answering a click at EVERY one of its own controls - not merely one of them, since a screen whose OK works while its controls are dead is still keyboard-only in practice, which three of them were; and the race screen returning null rather than throwing when its picker art is absent |
| equipmechanics.test.js | 5 | U8f/U8g the equip mechanics + the live paperdoll: the EquipItem swap machine over string-group bag items (Either right-then-left, 2H clearing both hands, shield bumping a held 2H, the 2H-replace rule, arrows/wands refused, slot swap, SplitStack) + FilterLocalItems hiding worn items; the GetItemImage forPaperDoll laws (armor archive 249+Human 2, the SetVariant material-family clamps incl. chain greaves 6, plate-iron dye 15 vs leather Unchanged 18, the Either-worn-RIGHT record+1, clothing archive+morphology with Blue default, jewellery only past slot 11, books none); the COMPOSITE doll over real art (plate cuirass + longsword composed 110x184, the layout carrying exactly both slots, GetEquipIndex walking every panel pixel finds exactly the two worn layers, unequip + recompose empties it); U8h - the armor value table verbatim (the 100-per-part baseline, leather chest 85 / steel greaves legs 55 via material*5, the DAEDRIC buckler material-BLIND at 5, the tower shield covering Head, swaps + unequips restoring, weapons/clothing never touching it, the (100-av)/5 display law, the verbatim armourLabelPos) + the starting seed (the interim dagger IN the bag worn, idempotent, probe bags untouched) and the worn-weapon binding shape (slots[RightHand] ?? null -> bare hands take the unarmed path) |
| droppedloot.test.js | 5 | U8e dropped loot: the verbatim treasure flat (archive 216, the 20-entry randomTreasureIconIndices, upload + batch at the drop position, the corpse-shape activation box) + EMPTIED piles vanishing from batches/targets (SerializableLootContainer's removal law) + empty drops minting nothing; the inventory window's remote side - Remove-mode local clicks feed droppedItems, remote clicks transfer back to the player (both Equip and Remove - RemoteItemListScroller verbatim), closing with session drops fires onDrop (the OnPop world mint) and loot-target opens default to REMOVE (the anti-accidental-equip law) with pile clicks transferring in. P2 (items-2): the pixel sweep - collectPixel kills every pile stamped with the torn-down pixel with its batch freed, pixel-less (dungeon) piles exempt; the world-save halves round-trip in NATIVES under a moved origin + y shift, empties skipped, the SAVED record re-minted under a POISONED roll seam (the reroll mutant survived a fixed pick - the seam moves before restore so a reroll is distinguishable); and the world-host wiring sweep (collect at teardown, pixel stamps at both drop sites, both envelope halves) |
| nativeinventory.test.js | 9 | U8d the native inventory window: the verbatim DaggerfallInventoryWindow rects (four tabs, the six 31x14 action buttons at x226, lists/exit), the tab+mode click machine, the AddLocalItem four-way filter law verbatim (weapon/armor not enchanted; magic = enchanted or Spellbook 132; ingredients = templates 0..77 exactly per ItemTemplates.txt; the rest to clothing & misc), and the SHARED ItemListScroller rail law (up/down/page kinds at the verbatim rects, clamped paging, slot mapping). U25 added: an EQUIP click on a LIGHT SOURCE uses rather than equips (DFU routes it that way, and it is how a torch is lit in play); the gold button dropping into the remote pile while REFUSING 0 and refusing more than is carried rather than clamping; and THE ONE CONSTRUCTION SEAM swept over all FOUR sites the exterior hosts build this window from, since a hook added to three of them is exactly the failure that rule exists for. U26 added two more, and turned one around: the four-hosts pin USED to say the dungeon still opened the keyed window, went red the moment U26 swapped it, and now reads the other way - the keyed window is DELETED, the dungeon builds the native one in exactly ONE place (so its loot targets and its F6 press cannot drift apart), and worldModes opens none. The second is the routing: a NATIVE overlay gets the raw key code where a keyed one still gets the action map, with typedChar pinned reading both hosts' vocabularies. U26's second sweep reads the four things the dungeon swap needed and the two hosts that must stay in step: droppedLoot mounted, drawn, activatable, released on close and FREED IN destroy() (that last clause is exact, because a pin that only saw the array being emptied survived deleting the batch destroy); both dungeon hosts routing the droppedLoot: prefix; takeLoot OPENING the window rather than vacuuming the pile; and the native overlay drawing against the REAL canvas, since applying the letterbox twice left the dimmed world showing in the bars - AUDIT 19 F2 for the seventh time |
| nativetalk.test.js | 2 | U8b the native talk window: DaggerfallTalkWindow rect spot pins (whereIs/topicList/conversation/goodbye/tone radios), the click state machine over fake hooks (Where is -> categories -> buildings -> the answer appended to the conversation, tone radios, outside-rect not consumed, goodbye + onClose), the keyboard accelerators mirroring the session keys (W/digits/T/Esc); AUDIT 17d ListBox pins - topic rows 7px (FONT0003 fixedHeight, RowSpacing 0), conversation lines 11px (RowSpacing 4), DaggerfallQuestionTextColor (0.698,0.812,1) in the player-says panel (123,8,124,38), DaggerfallAnswerTextColor (227,223,0), the NPC name centred in its 197-wide panel |
| nativepanel.test.js | 2 | U8a the native 320x200 panel foundation: integer-scale centered metrics (1400x900 -> s4 letterbox 60/50, sub-native floors at 1), the screen->virtual point mapping round trip + outside-null, the DFU default label color (243,239,44); INFO00I0.IMG loading 320x200 through the real IMG reader + palette into the upload seam |
| townpopulation.test.js | 5 | T1 towns (CityNavigation/MobilePersonMotor/PopulationManager verbatim): tile weights (water 0/stone 4/dirt 6/grass 12/road 15/default 7) + the automap carve with the per-block row flip matching the rendered tilemap + occupancy flags + the spawn probe; the person motor (road-following seek via the 80% downgrade-leave, the 1.6-unit march, the politeness idle record 5 / guard 15); the pool laws (clamp(blocks/16,1,4)*24 sizing, the verbatim race/gender texture tables + guard 399, one spawn per 10Hz tick, the anti-skate first-move hide, the night recycle); the 2026-08-17 audit pins (single tick per frame with the timer RESET, the rect+62.5 spawn range gate, RandomiseNPC re-rolling at every spawn with the 1/32 guard branch, the self-target place() + nav rederive, the N/S/E-only best scan - DFU's Range(0,3) quirk) |
| animalambience.test.js | 2 | A4 the shared PlayRandomlyIfPlayerNear pass (dungeon + both exteriors): the verbatim AddAnimalAudioSource record table (0/1 horse, 3/4 cow, 5/6 pig, 7/8 cat, 9/10 dog, 2 silent) + the 768-unit distance + rand()<=100; the 16Hz cadence with the range gate BEFORE the roll (classic-stream sequence preservation) and the over-100 silent roll |
| ambient.test.js | 6 | A3 scene ambience (AmbientEffectsPlayer verbatim): the 14 dungeon one-shot clip ids 63-76 in enum order + storm 348-350 + sunny 437/438 + the rain 389/crickets 6 loops + water 439/114, the SERIALIZED scene wait windows (Dungeon 5/28, WeatherAmbientEffects 5/25 - overriding the script's 4/35), the WeatherManager preset mapping (rain/thunder divert, snow/fog/overcast fold to day/night), one-shots firing after the Next(min,max) wait somewhere-around (distance 10..20, min 13/max 104) with doNotPlayInCastle silent, the EXCLUSIVE ambient channel skipping while a clip plays, preset switches stopping loops (rain->storm restarts the rain loop, clearNight crickets, dispose stops), storm shots on the horizon ring (|xz| 0.94 / +0.34, min 3000), and the classic-cadence water (rand<50 WaterGentle AT the surface beside the player min 8; submerged bubbles sharing - and here losing - the channel; dry blocks silent) |
| anims.test.js | 2 | Directional attacks: verbatim DFU direction->strike mapping pins; delta-clip well-formedness (start/end 0, sorted keys); sampler continuity + keyframe hit + null past dur |
| pointerlock.test.js | 3 | requestLook never throws - swallows a rejecting requestPointerLock promise (the sh/< crash + lock:N frozen-yaw bug), tolerates the void-returning API, survives a synchronous throw |
| playerdeath.test.js | 10 | D1 - THE DEATH SEQUENCE (PlayerDeath.cs), the timed law between health 0 and the title menu. The constants verbatim (fallSpeed 2.5, TimeBeforeReset 3, FadeDuration 2, classicPlayerDeathSound = WoodElfMalePain1 405 for EVERY race and gender - the race/gender Pain3 table is DFU's CombatVoices option), and targetCameraHeight = height - height*1.25, a quarter of the capsule BELOW the feet, which is why the view sinks through the floor. The sink falls at 2.5/s and RESTS at the target; the fade ramps over two seconds and clamps; the death sound plays exactly once, on death; the run ends ONCE and STRICTLY after 3s (C# compares with >), and a spent sequence neither re-fires nor sinks further. The screen drives it and ENTER skips to the end rather than reloading the same scene (DFU's death lands on the title menu, not a restart). Plus the wiring laws: ANIM0012.VID is in the ingest diet on both the desktop and lean sets now that something plays it (AUDIT 19's one-VID-at-a-time rule), the shared seam plays the video BEFORE returning to the bare URL, and all four gameplay hosts route through that one seam. AUDIT D added the TICK pin over all three overlay slots: raising the screen and ending the run are not enough, the sequence needs a CLOCK, and worldModes' interior arm drew its overlay without ever ticking it - so dying inside a building froze the sequence and the run never ended |
| player.test.js | 13 | verbatim speeds/constants (audit 16e F1: the walk DRAG term 0.5*(100-max(30,SPD)) with its SPD-30 floor and SPD-100 zero pinned, the UNDRAGGED run base, the crouch-base run), collider ground/slide/step, motor gravity/jump, strafe = true camera-right, wall-ladder regression (step-up needs a clear raised path); P11 swim/levitate (GetSwimSpeed base*(skill/200)+base/4, levitate 4.0 look-directed with prior fall velocity dying, swim zeroing the look's vertical + float-up + the can't-surface clamp, waterWalking normal speed, the .7071 diagonal limit landing on the grounded path, the per-frame jumped flag); P12 crouch (controllerCrouchHeight 0.9 + the 0.8 eye, toggle edge, crouchSpeed for crouched walks and the crouch-base run for crouched RUNS (GetRunSpeed's branch, audit 16e F1), crawling under a 1.2 ceiling the standing capsule cannot enter, the CanStand probe refusing to stand under it and allowing it back in the open) and P12 breath (MaxBreath = LiveEndurance/2 int-div fortify-aware, the (END>>3)+4 short threshold + HUDBreathBar colors/width pinned, WaterBreathing (30,255) in the buff kinds both subType spellings, currentBreath riding the save envelope); the FIXED physics timestep (08-17 live hotfix: update() accumulates render dt and steps at 1/60 with the 0.25 jank clamp - a 10 fps jump reaches the SAME apex as 60 fps; raw-dt integration stole g*dt from the takeoff and collapsed phone jumps to ~0.1); P18 the timed height transition (the crouch flips at the END of the shared 0.10s camTimer - standing speed/capsule/full jump all window, the eye sinking first - the stand at the START with only the eye lagging on the SAME clock, a switched action inheriting it) + the Argonian swim-fatigue race gate short-circuiting BEFORE the Dice100 roll (sequence-pinned with a counting rolls(), the tally running for both races) |
| mapReveal.test.js | 6 | T4 - the 35% map-reveal + %hnr/%ra: the hint fork (reveal AT or under 0.35 - DFU tests `>` for the direction arm - never while inside, 7332/7333 ids pinned), the PlayerGPS discovery store (whole record, once, per location; the save-envelope round trip incl. the pre-T4 empty restore and the no-alias copy), the honorific/race display-name tables vs the Internal_Strings literals (two-word elves), the townTalk source sweep (the LAZY %hnt gate - a refusal never rolls or marks - the reveal-arm-only discoverBuilding, the entity-fed macros), and the ARENA2 gate grounding the record ids (all 7 reveal variants name %loc on the map, all 9 direction variants carry %di) |
| breath.test.js | 4 | P18 - systems/breath.js: the DeepBreath fold (identity for every guild but Kynareth, the (10+rank)/10 trunc ladder pinned whole at duration 25, the double-vs-float32 edge rank 4 x 45 = 62 per the Ledger A Mono row), the dive-from-empty fill through the fold, the 19th-classic-update drain cadence, the Argonian coin refund (race-gated, roll-injectable, refund-before-drowned pinned both ways), surfacing + WaterBreathing zeroing the counter |
| motorStairs.test.js | 11 | P14/P15 movement parity, NUMERIC traces (restores the reverted b9e9aa6 harness): a classic 0.3-riser staircase summits grounded, riser 0.45 climbs while a 78-deg ramp blocks and a 60-deg climbs (slopeLimit 70 is THE block criterion - a lone tall box crests via its ~45-deg edge contact, as Unity), a flat wall never ladders (the P9 facade pin), a 2.2 stairwell ceiling CAPS the lift ladder (stands tread 2, never wedges the head into the plane) while a 2.05 ceiling (no legal stand) refuses cleanly, jump apex = (4.5 x (1 + Jumping x 0.5/100))^2 / 2g (skill 30 -> +15%, not the one-frame 0.069 kill), the 0.1 s grounded-time gate blocks the first-frame re-jump, crouched jump x0.8 + moving-jump forward boost 0.05 with airborne momentum FROZEN through reversed input (airControl false), and a landed fall reports its distance (8-drop) while slowfall falls at -105*dt with the fall start re-anchored (lands billing ~0); P15 sneak - applied speed = walk/2 - 1/39.5 with IsMovingLessThanHalfSpeed TRUE while moving (the P13 stealth payoff - the subtracted classic unit lands it under the half line), plain walking not qualifying, running beating the held sneak, and the grounded-only latch (a sneaking jump stays sneaking through a mid-air release until the landing frame re-latches) + THE JUMP DESCENT PIN the apex-only tests never had: on a MESH floor the falling frames ride the arc like the rising ones and no frame drops more than 0.1 - the ground snap (STEP_OFFSET 0.5 > discrete apex 0.469) is withheld while `jumping`, which is what made every jump teleport its whole descent in one 33ms step since launch |
| arrowflight.test.js | 4 | C13 host arrows (exterior/interior hosts, the no-targets subset of the S5 missile): constants single-sourced from spellcast.js + the oriented arrowMatrix law (nose-down r12 = -1), flight at MISSILE_SPEED with the swept wall kill and the 8s lifespan retire, the terrain landing via the collider heightAt floor + the late-resolving function-collider form + the all-dead list sweep |
| useitem.test.js | 15 | U25's UseItem ladder. The predicates are pinned on GROUP AND TEMPLATE together, because several of them are one index away from something else in the same group - IsLightSource spans TWO groups (the Holy candle is in ReligiousItems, not beside the torch), and a glass JAR is not a potion where a glass BOTTLE is. NextVariant is pinned as the twenty-four garments DFU names by hand, with the four templates literally called "unchangeable" proved absent, and the cycle walked to its wrap against the TEMPLATE's own variant column. The oil arm is pinned refusing a partial top-up and splitting a stack. THE DRUG BUG is pinned verbatim with its arithmetic shown: DFU's `+ 66` on a template of 78..81 lands on 144..147, outside the Poisons enum its own comment says it should hit, so GetClassicPoisonEffectKey formats a key nothing is registered under - the drug is eaten and NOTHING is inflicted. The pin fails if a later reader "fixes" the constant to 58, and the poison guard that makes it nothing-rather-than-undefined is pinned beside it. AUDIT 22 added three and turned two around: a QUEST item now runs the whole ladder (DFU returns in exactly one case - the quest system watching a non-parchment, non-clothing item - and falls through otherwise, which is why a quest torch lights); the ENCHANTMENT payload is a rider on the arm's own result rather than a kind that replaced it, pinned on an enchanted torch that must BOTH light and fire it; RemoveOne takes THIS record rather than the first sharing its template, pinned on two potions that differ only by recipe; and the oil arm searches the LOCAL pack, pinned with the oil on a loot pile and the lantern in the bag |
| weaponrig.test.js | 5 | C9 the FP-weapon host rig (the non-dungeon hosts' surface, mirroring the audited dungeon laws): classic starts sheathed with DrawWeapon 78 only on unsheathing a REAL weapon (sheathing back silent), sheathed = no attack processing while an unsheathed right-drag past the threshold enters a Strike state with the pitch-matched swing sound on the entering edge and reaches its hit frame (paralysis holding the machine eventless), the zero-arrow bow auto-sheathe with the classic line from the draw path, and the WeaponEnvDamage ray - a door in reach BASHES and consumes the swing, other action objects Receive(Attack) and the swing continues, a wall in front occludes; C10 clickAttack carries the sheathed gate (the inline touch tap had bypassed it) |
| talksession.test.js | 4 | T3b mobile talk: the verbatim greeting thresholds (0/10/30 -> 7207/7208/7209, below 0 -> 7206) with the -20 refusal EDGE (< -20 refuses via 7205; exactly -20 still talks), the macro set (%pcf first token, %oth drawn only when the greeting carries it, oath = TEXT.RSC 201+FactionRace - the DFU oath fix, Nord 201/Redguard 203/Breton 204), the activation ray (cylinder hit at the controller radius 0.45/height 1.8, misses beyond radius/behind/above), greedy word-wrap; NextInteractionMode's verbatim Steal>Grab>Info>Talk wrap (the touch cycle button) |
| faction.test.js | 3 | T3a talk foundation: the real FACTION.TXT parse (exactly 366 factions, the Daggerfall province + its People-of child via relinkChildren, ruler power bonus 20..70, flat round-trip), the verbatim reaction sum (rep + biography + reactionMods[sgroup] + sGroupReputations[sgroup], out-of-range sgroup guarded) + the PlayerActivate distances (6.4/3.2), pickpocket (chance clamp 5..95 + the 5/level enemy modifier, the three outcomes: gold 1-6 onto the Currency stack / found-nothing on Dice100(33) / failure landing CrimeCommitted=Pickpocketing with guards FLAGGED) |
| freeaction.test.js | 3 | S22 FreeAction (26,255): the key both subType spellings + the Restoration (20,8) cost + AddState stacking; the AssignBundle hard-immune DROP (incoming Paralyze sheds silently - no stack, no chance roll, no message, throwing-roll proven); the READ-TIME fold (casting over a live paralysis frees NOW without curing - the bundle ticks underneath and RESUMES when FreeAction expires first) |
| concealment.test.js | 4 | S21 the concealment family (ConcealmentEffect/DaggerfallEntity/EnemySenses verbatim): the classic keys land as kinds - Invisibility (13,0/13,1), Shadow (24,0/24,1), Chameleon true (23,1) - with the Illusion duration costs pinned (40/120, 60/140, 20/80, 40/120, 40/120; skill 24); IsInvisible/IsBlending/IsAShade FOLD normal + true powers; a cast lands the buff and says the start line ONCE on new incumbency (You are invisible./blending./a shade.) while a re-cast STACKS rounds silently (ConcealmentEffect.AddState); and the P13 gate consumes the folded flags end to end - invisible always blocks with NO roll (the 13 seers exempt), shade rides the 4% branch, blending true the 8%, unconcealed rolls nothing, and expiry re-opens the gate |
| save.test.js | 4 | the snapshot/restore round-trip (entity fields, deep-copied stats/items/actives, spells re-resolved by index, extras + the S12 world/locationKey riding the envelope, the version gate refusing, storage round-trip + corrupt-JSON null), F12 piercing overlays while F9 stays gated, and the Q4-v quest slot riding the extras verbatim (opaque like world, null when the host passed none). AUDIT 22 F7/F8 added the two fields this arc introduced and did not persist: the training clock (the 12-hour gate is a DIFFERENCE against it, so a save that dropped it gave free unlimited training across a reload) and the LIT LIGHT SOURCE - pinned on the reference IDENTITY, because UseItem douses by `LightSource == item` and a restored copy that merely looked like the torch would have left it permanently unquenchable |
| sky.test.js | 3 | SKY reader pins, panorama mirror law, night mapping |
| smoke.test.js | 1 | runner sanity |
| clock.test.js | 4 | hour gates, LightCurve pins, sun sweep, flicker determinism |
| questmachine.test.js | 7 | Q2 - THE QUEST MACHINE: a crafted quest runs END TO END headlessly (the headless startup arms a clock and logs a step, world time drives the clock to zero which starts its same-named task, say's IMMEDIATE popup raises questBreak mid-task with the rest of the task running next tick, end quest takes the two-tick grace, the machine tombstones and expires the tombstone after one in-game week 604800s); the WhenTask when/and/or-not law with DFU's short-circuits and the primary always-on trigger un-triggering its task; PickOneOf's seeded draw; UnsetTask's permanent drop (start can never re-trigger); the GlobalVarLink task reading/writing the machine's 64-global store (Quests-GlobalVars id 0 LiftedCurse); and THE COVERAGE + OWNERSHIP PIN - the 61-action registry resolves exactly 7231 of 7235 corpus action lines (99.94%; the 4 pending are DFU's own 'Action not found. Ignoring' floor, permanent by parity), exact against the pinned vendor commit, PLUS a per-action resolved tally deepEqualed against counts derived independently by running the C# patterns in DFU's RegisterActionTemplates order - a future slice must move resolved UP and pending DOWN, and a pattern hijack shifts a bucket and fails |
| questmacros.test.js | 28 | Q4-i - THE MACRO ENGINE (QuestMacroHelper + QuestMCP + the resource ExpandMacro overrides). THE CORPUS GATE: all 3,817 messages of the 265 vendored quests expand HEADLESS - exactly 3 throw, the %di NRE trio (LastPlaceReferenced.Scope read before the null check, DFU's own crash surface, pinned by quest+message id), and C#'s GetValue error shapes land exactly ([undefined] 15 = the corpus's 14 %G3 + 1 %G1 which really render that way in DFU; [srcDataUnknown] 69; [nullMCP] >1000 = the headless charter). Plus: the ordered macro alternation with the UNDERSCORE TRUNCATION quirk (_one_day_ -> _one_, misses its resource), the Person macros (display name, dialog-place town/region, the literal BLANK, the FLATS.CFG caption seam with C#'s dead individual-flat arm noted, the questor faction swap), the Foe INVERSION (_sym_ = TYPE name, =sym_ = display name), the Clock ceiling-days, the %-macros over the seams (%pcn/%pcf/%ra/%pg3, %pct falling through the null-MCP arm to the PLAYER's name, the pronoun family off lastResourceReferenced with %G capitalizing and the Male default), UID-seeded %n/%fn/%mn determinism with the +3457 male offset, %qdt/%dat, %god's temple and random-divine arms, the %rn/%rt/%t Province walk, TEXT.RSC %oth/%jok, the %vam error literal, %kno's The-trim, %di's three arms incl. the NRE quirk, revealDialogLinks feeding addDialog on NameMacro1 only, expandQuestString and getMessageResources; the AUDIT IX boundary pins: the Item macro arms (template name, the gold STACK COUNT, a wrong-type token staying raw), the DFU message DEFAULTS (bare reads expand and reveal nothing; an explicit variant answers 0), the exact Place answers (killing MACRO_TYPES enum drift), the questor faction swap, direct-unit determinism (Raithi/Baos-i/Cauvin at uid 4242; %nrn's Individual-child and srand(rulerNameSeed&0xffff) arms), %vcn's success path over an arg-exact clan seam, %oth's questor and clicked arms, the %reg idRegion override, typed dialog reveals with isSpecial FALSE, and %qdt through the matched-entry arm |
| questoffers.test.js | 35 | Q4-ii - THE OFFER FLOW (DaggerfallQuestOfferWindow + the quest popup's offer half + the guild popup's Quests service, headless). The questor click: IsNPCDataEqual's four-field STRUCT identity (null = the zero struct, copy equality), SetLastNPCClicked's ungated sweep, the incomplete-quest questor scan stamping the mcp on the ACTIVE quest and skipping completed ones, the machine's own click beating the deps seam; CreateMessagePrompt's YesNo shape with the variant draw riding quest.rolls and the missing-message null; the SOCIAL door: the ctor's questor-pool removal (castle-gated, and running BEFORE the active-questor bail), the QuestorOffer prompt with NO external MCP (C#'s own TODO), Yes starting the quest immediately behind the AcceptQuest popup, No running the three TalkManager scrubs in C#'s order then RefuseQuest with exitOnClose false, the TEXT.RSC 600 fail flavour silenced inside castles, the missing-message quest LEAK, and the one-time flag suppressing re-offers end to end; the GUILD door: the member's guild landing as quest.externalMCP, the orders homing reputation on the BUILDING faction, the Temple deity fold (Arkay admits A rows plus the tplMemb Member fold; an unknown deity throws Enum.Parse's error), IsSatisfyQuestReqByLevel's rank override, and questPool.Clear() after ONE offer; the quest picker: the gettingQuests wait-box literals, header-only (partialParse) labels with the localization override, the full parse on pick throwing out uncaught, and C#'s RemoveAt-BY-ORIGINAL-INDEX BUG replayed exactly (the shifted list prunes the WRONG row; past-the-end throws the ArgumentOutOfRangeException mirror); the Quests service destination flip; ExpandLetterSignoff (the LAST non-empty line only under the 'Letter: ' prefix with the trailing space, ungated NameMacro1 reveals, location macros swallowing their WHOLE word into '...', context-macro error shapes baked in, the last-to-first one-line break proven by a trap resource) |
| questremainder.test.js | 16 | Q3-iv - THE REMAINDER SWEEP, closing the corpus at 7231/7235 (the 4 left are DFU's own 'Action not found. Ignoring' floor). WhenPcEntersExits's POLLED exterior-type trigger (enters fires on the standing state - seeded at create - exits on the observed rect transition; 'anywhere' is the p2=-1 wildcard; a non-p1=2 type THROWS at parse, headless too), WhenNpcIsAvailable's click PULSE over the machine's new faction-listener and active-person surfaces (identity click-memory, the active-Person lockout, the listener slot claimed), WhenReputeWith's LIVE rep bar (inclusive >=, the unknown-faction-false law - no record answers false even at a bar of 0, never 0>=0), the non-individual parse throws for both, MakePcDiseased/CurePcDisease over the S18 seams (the Quests-Diseases ids, the corpus 'saying' tail DROPPED verbatim - C#'s pattern has no such group - and the case-insensitive re-test landing 'cure Vampirism' on the vampirism arm through the third alternate), CastSpellDo's readied-spell gate (every real effect must match, type -1 skipped, fire-once-and-complete; the unresolved-spell TEMPLATE-SetComplete quirk leaves the minted action idling forever), and the HEADLESS charter for all six; the AUDIT VIII boundary pins: the create seed needs rect AND loaded (no phantom exit on the first poll), an ENTERS trigger never fires on an exit transition, 'exits anywhere' on the p2=-1 wildcard, the wrong-faction click stays unavailable, the always-on rep bar UN-triggering when the bar drops, and activeFactionPersons filtering on faction AND person-ness |
| questfoes.test.js | 23 | Q3-iii - FOE SPAWNING. CreateFoe's tick-driven spawn law over the new deps.world foe seam (createFoeGameObjects/tryPlaceFoe as GameObjectHelper.CreateFoeGameObjects and TryPlacement+PlaceFoeFreely): all four parse forms with minutes*60/indefinitely=-1/send-without-count=infinite and the msg option riding C#'s Substring(match.Length) slice, the first-update random BACKDATE within one interval on the injectable rolls, the wave lifecycle (one placement attempt per tick, a failed tryPlaceFoe retried without losing the handle, the msg popup oncePerQuest on the FIRST placed foe only, spawnCounter incrementing only after the whole wave deploys), a failed Dice100 chance roll CONSUMING the whole interval, the hidden-foe block spending the interval too, the send-variant placement gate on isPlayerInLocationRect, InitialiseOnSet restarting the cycle on the task-rearm edge (clear observed by an update, then start), creation-failure and missing-Foe error-termination, and the HEADLESS idle; CastSpellOnFoe's queue (Quests-Spells classic id, the custom-key form, NO duplicate refusal unlike the item queue, and the C# QUIRK where a table miss completes the TEMPLATE so the minted action still queues classic 0); SetFoeName's world half (monster names through classic srand with the Ledger-A seed, typeName from the grounded 62-entry enemyNames list, the 55%-MALE humanoid gender roll at the exact 0.55 boundary) with the namePending headless charter; kill/deathTrigger and the cloned item queue keeping the originals; the VERIFY boundary pins: the Dice100 equality law (a roll landing exactly ON the chance fails; Range(0,100) tops at 99 so 100% never fails), indefinitely running FOREVER past the counter, a SHORT mint wave error-terminating like a null one, msg-once holding ACROSS actions through oneTimeDisplayedMessages, the spawn-count clamp at 8, the 128-BIT class routing (Imp id 1 stays a monster at a female-side roll), the 0.55 gender boundary strict-<, and exact-name determinism through both srand chains (Chird-e / Baaliblex) |
| questpersons.test.js | 34 | Q3-ii - THE PERSON TRANCHE. Person.cs's Setup*NPC chain over a crafted world seam with a persistent-faction-store mock: the identification ladder (named individual reading the faction record's NAME/race with NO generated home; a Daedra binding like an individual; a named non-individual THROWING), the group Questor arm off the machine's last-clicked NPC (nameSeed/gender carried, AssignGender skipped, the audit-III addResource auto-track LIVE in quest.questors, the home configured from the player location), the no-click virtual-questor fallthrough to the regional People, the career table mapping (Chemist->Merchants 510, Spellcaster->Mages 40, Cleric->Generic Temple 450, Noble->Nobles 242), the factionType arms (Temple drawing a random temple EXCLUDING 450; Court reading the region court), home generation through the faction row's building type (Shopkeeper p2=9 -> getKeyForValue -> generalstore, locally scoped by the 50/50 roll), the HUD-face quirk (the PARSED face is IGNORED - AssignHUDFace always rolls Range(0,10)) with the gender roll on the injectable rolls, ChangeReputeWith through the PROPAGATING overload (missing person completes silently), ReputeExceedsDo's threshold gate, CreateNpc's placeAtHome (sitelink + marker assignment; a questor refuses), and the HEADLESS npcPending charter - plus the AUDIT VI mutation pins: the exact zero-faction record bound whole on a failed lookup (with the id!==0 display-name gate live), the WHOLE 22-row career table against the C# switch (including careerID 4 falling to the regional People - the switch's own gap), every factionType arm with the p3=-1 Range(0,4) quirk EXERCISED, the Local_4.10k 10000->0 fold, missing-row warn-and-zero fallthroughs, faction-512 forced Female, the gender/face/nameSeed roll bounds on the injectable surface, the Temple pool at a high roll, the home-scope forcing law (unloaded or buildingless towns never roll local), residence rows vs the house wildcard, Table.getKeyForValue's first-row law, the signed ChangeReputeWith amount, the absent-getReputation 0 default, and the three CreateNpc refusal guards surviving the tick; round 2 added the P0-quest player-vampire-clan arm, the Generic split and home-scope roll at the exact 0.5 boundary (both strict <, proven by the one-town dart board's remote-site throw); and the AUDIT VI parity pins: C#'s DEAD AssignHomeTown arm (an atHome individual gets NO home - the field the arm reads is assigned after it runs), the exact all-zeros default(FactionData) with race 0 = Nord kept, the race fold (only FactionRaces 0..7 map - Skakmat 11 reads regional), Person.Tick's auto-home hot-place (dormant until the player enters, permanent once placed), and ChangeReputeWith's allowRearm=false (a task rearm never repeats the move) |
| questplaces.test.js | 35 | Q3-i - THE PLACE TRANCHE. Place.cs's site-resolution laws over a CRAFTED world seam speaking the port's own MapsFile/BlocksFile shapes: local collection (the tavern walk with the wildcard sets, the Dark Brotherhood faction-108 and markerless-building exclusions, the generated building name; the house form drawing "The %s Residence" and the owned-house exclusion falling through to the throw; the magery guildhall matching ONLY faction 40), remote dungeon selection (type filter, the machine-wide assigned-dungeon exclusion via getAllActiveQuestSites, RDB marker enumeration with the CLASSIC marker identity blockPosition+objectPosition and per-block dungeonX), fixed sites (p1 direct = Town, p1-1 + hasDungeon = Dungeon, p2 0xfa01 masking to magicNumberIndex 1), the HEADLESS pend (no world seam = sitePending, the corpus charter), the SiteLink reserve + the selected-marker reuse law pinning person/item/foe to ONE marker, assignQuestResource's item-marker preference and the machine cull on movement (marker index 1 by id), isPlayerHere's building-key/rect/mapId gates, PcAt's set/clear toggle with its once-saying and the "any house" placesTable form, RevealLocation's discover + the grounded readMap note, DroppedItemAtPlace's actionWatching/allowDrop/drop flow, TeleportPc carrying the marker through the seam with CreateNpcAt the documented no-op, and the clock travel arm through the port's REAL TravelTimeCalculator (the one-day floor, the 2.5x return trip, the _2place_ one-way, and DFU's parse-order sensitivity - a clock sees only EARLIER places) |
| questscene.test.js | 39 | Q4-iii - THE SCENE MOUNT (QuestResourceBehaviour.cs whole + GameObjectHelper's AddQuestResourceObjects walk + PlaceFoeFreely's raycast ring, headless over stub hosts). The behaviour law: the injured check firing BEFORE death with its held-tick RETURN, DeathTrigger's one-shot zeroing behind the isFoeDead latch, the hidden-foe self-destroy raising the uncoupling destroy event, restrain latching restraintApplied even with no motor seam, castSpellQueue parking on a missing effect manager and on EXACT zero health (negative health still casts) then jumping the position to the END, addItemQueue landing clones on the corpse container over the live entity, doClick's item transfer+hide and the individual broadcast's ASSIGN quirk (a matchless individual overwrites the direct click's true; completed quests still click), the person hide/deactivate + reload recouple, the base Tick's SetActive(!IsHidden) wire with destroy-unsubscribe, cacheTarget + the v1 save shape (restraintApplied excluded); the mount walk: person/foe/item stood off marker targets with two-way coupling, the foe injury REARM at placement, the flat-pick law (individuals ALWAYS flat1, questors wear the clicked billboard indices, the gender split), the classic marker position (dungeonX/Z*RDB_SIDE + flat), the IsAlreadyInjected snapshot guard, enableNPCs/enableFoes gates with C#'s DEAD enableItems, killCount<spawnCount and load-in-progress parking foes only, the dangling quest/Place throws verbatim; the individual trio (isIndividualNPC off the faction type, isIndividualQuestNPCAtSiteLink's away-from-home walk with the log-and-continue, setupIndividualStaticNPC disabling the home copy or ALWAYS attaching the bootstrap behaviour); the CreateFoe wave invalidations sweeping live AND scheduled quests; the Place hot-place/hot-remove tail (mount when here, destroy when not, the missing-seam return skipping BOTH); PlaceFoeFreely's ring: the FOV+jitter side coin, the open-area distance roll, the wall-hit cos_normal separation and slack gate, the floor probe + 0.65 overlap veto, and the 8/25 wilderness constants |
| questsave.test.js | 29 | Q4-iv - JOURNAL + SAVE (the QuestMachine->Quest->Message/Resource/Task/Action v1 envelope + PlayerNotebook.cs whole). THE CORPUS ROUND-TRIP GATE: all 265 vendored quests save -> restore -> save to a FIXED POINT; a started quest's live state (log steps, clock) round-trips and the restored twin TICKS IN LOCKSTEP with the original; the uid allocator advances past restored uids. The failure laws: the removed-mod catch (an unknown action type drops that quest with the verbatim warn + HUD line and its SiteLinks scrub), the duplicate-UID Dictionary.Add throw (first wins), the Person faction-id deserialize throw. The inner shapes: Message.getSaveData reconstructing SOURCE lines (<ce> re-prefix, variant re-split, the unexpected-formatting throw), C#'s rumorsMessageID hole (in the struct, never saved, never restored - loads reset it to -1), the task restore ORDER law (IsTriggered lands while globalVarLink is still -1, so a load never re-writes the global store), the journal's ACTIVE-page walk (insertion order, completed quests skip), EndQuest's notebook filing resolved to Message objects. The notebook: the 70-column wrap breaking at the LAST space with C#'s leading-space prefix, the noteHeader/finishQuestHeader literals with completed/ended and the 'Quest' fallback, moveNote's destIdx-- law, the 50-slot message ring (rotated reads, Clear() sparing it, LOST on save - C#'s holes), the finished-quest overflow quirk (the headerless refile + the unguarded empty entry), and the D:/Q:/A: line-encoding round-trip with the double-newline law |
| answerpipeline.test.js | 27 | TK-iii - THE ANSWER PIPELINE (TalkManager.cs's question/answer half). GetClassicQuestionIndex's mapping with the 2 that "gives no bonus or penalty" as its default. THE REGIONAL LOCATION-KEY BIT FIELD decoded exactly: the classic key is three flag bytes (temples 0, stores 1, guilds 2) and C#'s `(byte)(flags << n) >> 7` truncates BEFORE the shift, so it reads bit (7-n) - every store, guild and divine pinned to its own bit with the complement proving it reads no other, all ten knightly-order indexes sharing one bit, 0x1c pinned as the HOLE in that run, the byte lanes proven not to bleed, and the out-of-range/unmapped/unknown-divine zeros. GetQuestionText's record ladder with the key-subject state each arm leaves (incl. the regional arm lower-casing its leading "Any" mid-sentence and Thing's literal 'Not implemented'). The PC opening (a greeting on the first question, a follow-up after; the NPC addressed by name only at reaction > 0 - the <= boundary pinned). organizationInfoRecord's >7 SKIP that DFU's own comment calls error-prone. GetAnswerText's dispatch with its question counter and opening-text reset, and the Work arm's four records where only a real offer picks a questor. GetAnswerWhereAmI's FOUR arms (discovered building, the building-list fallback, dungeon-with-its-own-region, outdoors) plus C#'s unreachable inside-but-nowhere resolvingError. The tell-me-about arm's table halves and its ONE-ANSWER GATE, with THE KNOWS-BUT-SILENT ARM proven (a known topic still refuses once the gate closed - knowledge alone does not win) and the three reopeners (spymaster, debug flag, same building). GetAnswerWhereIs's halves, its same-building answers with the nameless-building resolvingError, the fact that directions NEVER spend the topic counter, and its ASYMMETRIC gate (it honours NPCsKnowEverything but not isSpyMaster, where tell-me-about honours both). The knowledge roll's whole ladder - the region refusal, the always-knows arms, the organization-membership fix, the quest-person BUILDING dependency (an NPC who cannot place the building cannot place the person, and the building item is resolved and stamped on the way through), and the seeded 1..20 proven stable per (NPC, topic) with the modifier's direction verified against the table over 300 seeds. The hint fork (above 0.35 directions, below map, the boundary belonging to the MAP arm, indoors always directions) and the mark flag left down by both arms. GetKeySubjectPersonHint's questor key, site-details name, the "Residence"-and-empty fallbacks to the person's own home, and the key-subject backup/restore. GetDialogHint/2 with the spymaster INVERSION, the empty and null rumors fallbacks, and NPCsKnowEverything deliberately NOT applying. The token-array answer CLONING before expansion so altering macros re-evaluate each ask. THE COMPASS MARK stamping ReceivedDirectionalHints without ever downgrading a map mark, and markKeySubjectLocationOnMap's key-0 default-struct guard marking nothing. The regional walk's count-then-pick with its no-carrier and headless nulls |
| topictree.test.js | 30 | TK-ii - THE TOPIC TREE (TalkManager.cs's topic-list core). The ListItem/QuestResourceInfo shapes deepEqualed against the C# field defaults, the twelve QuestionTypes, the 34-entry infoFactionIDs, the 30-row FactionsAndBuildings + its matching localized caption list + the ten KnightlyOrderRegions, the FULL 17-entry skip predicate (AllValid and Special1-4 included, which the T3c list omitted), BuildingTypeToGroupString's thirteen captions with the empty-string default, RMBLayout.IsResidence (House1-4 only), and a CROSS-MODULE pin that actions.js's QUEST_INFO_RESOURCE_TYPE copy still agrees with the tree's (a drift would silently mis-route every dialog link). The quest pipeline: AddQuestTopicWithInfoAndRumors(Quest) filing the RumorsDuringQuest 1007 message to the mill then a topic per resource, THE DISCARDED FIRST ADD (an empty resourceName on a NEW questID leaves NO entry - the bag is created before the bail and written only at the tail), THE STICKY TELL-ME-ABOUT FLAG (only ever set - a re-add with null answers keeps a previous true - while hasEntryInWhereIs is rewritten both ways), the Location arm undiscovering only symbol-matching residences, DialogLink's three deduped link lists with NotSet still hiding and an unknown type returning BEFORE the hide, its tell-me-about-ONLY rebuild, AddDialog's un-hide + per-type instant flags with the null-name skip that still rebuilds, and the remove sweep. The misnamed person lookups (every throw carrying the copy-pasted 'GetBuildingKeyForPersonResource()' literal, the null-symbol arm's wrong 'not of type Person' words included), the questor buildingKey with its home-NAME fallback reading C#'s default-struct zero on a miss, and GetBuildingTypeForBuildingKey's zero/duplicate throws. IsBuildingQuestResource's flags with the >= hint comparison and the map/key double gate. The assemblers: the two fixed heads + quest topics + 34 indexed org rows, the caption NULL test (an EMPTY buildingName is taken as-is, never falling to the location name), the three tell-me-about gates incl. the same-person partner; the location list's enum-ordered type groups each with a Previous List head pointing at the parent, the quest General section's same-map/keyed/residence gates, and THE SHARED GROUP VARIABLE proven by a palace joining an existing General group rather than minting a second; the regional group's three arms (orders gated on region, stores by TYPE, temples by FACTION) with local buildings removing their rows and an unloaded location keeping everything; the person list's questor-mapID and assigned-place gates; the always-empty Thing list; and the instant pass rebuilding only flagged lists then refreshing the window. The knowledge gates (the recursive reset walking item groups, the region gates with the -1 home-index bypass, the same-building compare with the palace's compare-by-NAME arm and the placeless-individual false). The conversation-save halves round-tripping as a fixed point with the orphan sweep, the three-type relink to LIVE objects, and the TellMeAbout restore tail |
| rumormill.test.js | 28 | TK-i - THE RUMOR MILL (TalkManager.cs's rumor family + the RUMOR.DAT reader). The reader's exact record layout over a synthetic fixture (the 9-byte CString cursor advance proven by a second record aligning past NUL-trailing garbage); TokensToString's separator law (' ' default, '' when addSpaceAtTokenEnd false, an empty token APPENDS the separator); ImportClassicRumor's three skip gates (quest flag 4, sign flag 1, NPC-specific npcID) with the carried fields and THE BULLETIN HOLE (imports never set textID, so a board can never show one); AddNonQuestRumor freezing ONE random variant at add with the 43140 TTL LITERAL (a hair under 30 days, not 43200) and the stamped textID; GetFlagsForNewRumor's EXACTLY-seven sign types (10/18/7/4/28/27/26 -> 1, else 8); THE REFRESH CULL verbatim (timeLimit < now with NO type filter - the quest entries' unset 0 dies on the first sweep, TalkManager.cs:2742's own hand); GetValidRumors' filters (the region gate, the sign/spoken flags&1 split both directions, the 75% faction-flag suppression rolling ONLY when a flagged faction rides the entry, quest entries bypassing EVERY common filter, the bulletin textID gate against the exact allowed-id list); the region ladder (entry -> faction1 -> faction2 -> current, DFU's dropped classic random) probed per arm and threaded into the bulletin macro context; the bulletin face answering the FIRST valid CommonRumor as TOKENS; GetNewsOrRumors' one-answer gate with the spymaster + know-everything bypasses, the common arm stringing WITHOUT the separator, the quest arm expanding through the quest seam at a rolled variant, the 1457 out-of-news face, and the resolvingError quirk ('...never mind...' spends the answer); WeightedRandomRumor's running choice (rolls at 0 keep the first, top rolls take the last, the range walk 1/51/52 pinning the default quest weight 50); the six quest seams with their throw LITERALS, unexpanded-variant capture, the empty-list overload adding nothing, AddOrReplaceQuestProgressRumor swapping ONLY the first match's variants (fields kept), the type+id remove sweeps, the questor-post slot (variant 0 unexpanded, last write wins); and the SaveDataConversation halves round-tripping as a detached fixed point |
| questbridge.test.js | 25 | Q4-v - THE HOST BRIDGE (scenes/questBridge.js + the gameDate header strings, the machine live over host seams). The NPC-data law: GetPositionHash's x ^ y<<2 ^ z>>2 with the shifts binding tighter (int32 wrap and the arithmetic negative shift pinned), and StaticNPC.cs:218's famous PRECEDENCE QUIRK - nameSeed = position ^ (buildingKey + locationIndex), the + tighter than the ^, proven against the wrong-precedence value; gender = flags & 32 exactly (bit 32 decides, neighbours ignored); the billboard indices riding for the Q4-iii questor flat-pick. QuestMachine.Update's pacing verbatim: 10 Hz of REAL time with the timer reset to ZERO on fire - the excess is DROPPED, pinned by a fresh sub-interval not firing. The offer surface: IsSatisfyQuestReqByLevel's base-false with EXACTLY the MagesGuild + KnightlyOrder overrides, membership-presence as isMember, deity/rank/reputation pass-through. The window faces: tokensToRows (text appends, every formatting token breaks, a bare break is one empty row) and offerBoxes (silent 'close'/null and popupless answers show NOTHING, 'fail' boxes the TEXT.RSC record, 'offer' carries YesNo with the answer steps recursing through respond). THE END-TO-END DOOR: a guild offer over fs-backed data seams - list table -> pool -> the QuestorOffer prompt -> respond(true) -> the AcceptQuest popup with the quest LIVE in the machine - plus the vendored pack answering the questData contract from disk. clickNpc stamping the derived record as LastNPCClicked; ClearState (QuestMachine.cs:533-546) wiping quests/siteLinks/questsToInvoke/lastNPCClicked, standing between restore and the duplicate-UID throw; the whole envelope round-tripping into a fresh bridge as a FIXED POINT (notebook + oneTimeQuestsAccepted riding), restore(null) a no-op; the tombstone filing wired into the notebook and onQuestStarted fanning to the one-time recording AND the ctx listener. The date headers: GetSuffix's 1/21-2/22-3/23 law (11 takes 'th' - C# has no teens rule), DateTimeString with the day UNPADDED and the {5:00} spec DROPPED on the month-name string (System.String is not IFormattable), MidDateTimeString with the day PADDED ({3:00} lands on the int) |
| questitems.test.js | 33 | Q2b-ii - THE ITEM TRANCHE. THE MINT (Item.cs's create ladder verbatim): named/class/template/random arms resolving through Quests-Items and the enum arrays to DFU templates (letter -> Parchment 279, class 17 subclass 13 -> Saint's Hair 54, random weapon at 0.5 -> Claymore 122), clothing drawing subclass THEN dye in that roll order, MAGIC.DEF-less magic items and artifacts pending LOUDLY instead of throwing (Chrysamere = artifact 14), every minted item quest-linked (uid + symbol clone); GOLD - the range form's Range(low, high+1) with the floor of 1, and the no-range CLASSIC REWARD FORMULA hand-computed through C#'s integer division at every step: the level path (level 10 + PriceAdjustment 400 = 735), the playerMod clamp at 10 (level 30 = 750), the guild path with the faction's power and FightersGuild's fixed-point AlterReward ((358*349)>>8 = 488), and the NonMemberGuild passing the identity AlterReward; an unknown item name throwing verbatim. MakePermanent syncing the virtual flag, the instance unlink and the held-copy sweep; dispose sweeping only the still-quest-linked item at tombstone (the permanent one stays). THE SEVEN ACTIONS: GivePc's bare offer (QuestComplete + success + PERMANENT + released + offered), "nothing" without loot, and the notify form's town+daylight gates then the 40..500-tick delay landing the item at the FRONT on the 41st tick; GetItem's gold arm (addGold + the grounded "You receive %s gold pieces." HUD line) vs the front-of-inventory arm, both releasing first and plain release NOT making permanent; TakeItem; HaveItem never completing and re-starting while carried; TotingItemAndClickedNpc needing click AND carry, consuming the click and releasing; GiveItem queueing on the Foe with the player copy leaving, and a not-in-scene Person target keeping it trying verbatim. THE QUEST LISTS: the vendored tables filing 188 quests with counts derived independently from the raw rows (the dash-commented Oblivion block never parses - DFU's own Table law), the guild pool law whole (membership, minReq rank-vs-rep at the 10 boundary, the HolyOrder membership fold, oneTime spent after noteQuestStarted, adult behind PlayerNudity), the social law's level-or-rep gate and N/M/F gender rows, SelectQuest's seeded pick loading through the machine's parse glue, GetQuest preferring the source seam, a listed quest with no source throwing, and the machine's onQuestStarted event reaching the one-time recording |
| questactions.test.js | 42 | Q2b-i - THE STATE TRANCHE's law pins over crafted quests on the live machine with capture-everything hooks, plus the Q2b-VERIFY block: the adversarial-review fixes (endQuest reputation through the PROPAGATING overload with factionId-0 silence, addQuestTopics between start and the live table, the faulting quest tombstoning AFTER the surviving quests update, addResource's incoming-questor auto-track) and the mutation-campaign survivors as pins (the enum LITERALS, the allowRearm-false clear law, WhenTask's one-eval guard, the secondary-cannot-STOP law, DailyFrom's inclusive minutes-bearing lower bound, Prompt's static-message-name form, the DialogLink/AddDialog triple forms in the C# call order, fresh-resource cold state, KilledFoe's saying popping ONCE, the destroyed-AND-behaviour hide gate). The CLICK LAW whole: SetPlayerClicked starts ClickedNpc's task with its say and PostTick rearms the click unconditionally (a click lives ONE tick); N0B00Y16's first-come-first-serve click ownership (the ScheduleClickRearm after the owning task hides the click from a later task in the same tick); the muted/destroyed Person refusing the click, with MuteNpc's rearm UNMUTING; ClickedNpc's gold gate (enough gold deducts and fires, short gold starts the otherwise-task and refuses). Foe tracking: KilledFoe at the kill count with its saying + the clamp-to-1, InjuredFoe on the flag, RestrainFoe's flag, RemoveFoe hiding - and a MISSING foe on remove ERROR-TERMINATING the quest. Person flags: hide/restore flipping isHidden with a missing person keeping the action trying; DestroyNpc with the Tick destroyed->hidden law GATED on a scene behaviour. The questor registry laws incl. C#'s DropQuestor-leaves-IsQuestor-raised quirk and the unnamed-symbol throws. DailyFrom's ALWAYS-ON inclusive window stopping its task outside hours (18:00 exactly still fires). LevelCompleted >= through the player seam. Prompt's yes/no fork with a missing message showing nothing but completing. PlaySound: vengence -> id 386 off the vendored Quests-Sounds, the interval gate, only a REAL play re-stamping (a busy source burns timesPlayed - verbatim), count 0 = forever / count 1 = one, an unknown sound name pending the line, never SetComplete. PlayVideo's ANIM%04d.VID law + five digits pending. AddFace/DropFace reaching the HUD hook with the IMMEDIATE saying. DialogLink's solo-by-symbol-name / pair-by-display-name calls with the C# EMPTY-namePlace quirk pinned; AddDialog's per-resource calls with the literal false. RumorMill passing a null message through. LegalRepute's signed amounts. ItemUsedDo watching until use then saying/starting/unwatching. StartQuest formatting S0000500 and scheduling through the data seam. The tombstone talk half posting by outcome + both scrubs, and EndQuest's own scrub |
| quest.test.js | 11 | Q1 - THE QUEST SCRIPT PARSER over the vendored DFU quest pack (+ the Q2b-VERIFY mutation pins: parseInt's int32 rails at exactly +/-2^31, and the Clock flag&16 / flag&1-hack travel arms discriminating on every operand - bit 4 among others, the nonzero-time defeat, no-range no-hack, bit 1 is not bit 0) (vendor/dfu-quests, MIT - the Ledger sourcing decision's route (a)). The corpus gate runs EVERYWHERE - the pack is committed, so no ARENA2 and no network: all 265 quest sources parse. M0B00Y16 pinned deep: 20 messages with fixed-type ids from Quests-StaticMessages, 2 rumour variants on <--->, centring tokens, the 11-resource roster (Foe Giant -> MobileTypes 16 via Quests-Foes, Place dungeon p1/p2/p3 via Quests-Places, gold's isGold), 27 tasks with 1 NextUID-named headless (triggered) and 5 variables, action lines queued in pendingActionLines for Q2's registry. Plus the Symbol wrapper law, the Table reader (GetInt -1, no-schema throw), parser statics (whitespace SplitLine, SplitField arity throw, ParseInt/CustomParseInt), Clock time math + seeded range draws, and the DFU failure modes verbatim (no name/no QRC/second headless block/tables-not-loaded) |
| restwindow.test.js | 3 | U7 the rest session machine: the sub-tick law (audit 16f: waitPerHour / minutesPerTick VERBATIM - the divisor is the constant 10, so a rested hour passes in 0.45 real seconds, loiter 0.75; the first cut divided by the 6 ticks-per-hour and rested ~1.7x slow), the 0-hour quirk (DFU tests hoursRemaining < 1 only AFTER an hour - resting 0 rests ONE full hour, vitals ticked), completions (timed wakes on TEXT 353 after its vitals ticks, full rest ends healed on 350 - instantly when starting already healed with NO time passed, loiter ends on 349 with vitals NEVER ticking - a throwing tickVitals proves it) and the 3-hour classic loiter cap constant, interrupts (enemies break at the hour boundary on 354 BEFORE vitals - a throwing tickVitals proves the order - with the hour's time still passed; death mid-rest ends at once with no text - the death screen owns the flow), and endEarly's per-mode finish texts |
| rest.test.js | 2 | S20 rest/exhaustion: HealingRateModifier floor(END/10)-5 (classic's negative-mod bug deliberately not recreated, following DFU), the health rate's Medical+60 base with the RapidHealing 100 upgrades (Always; InLight only when day AND outside; InDarkness otherwise) and its floor of 1, the fatigue rate maxFatigue/8 in stored x64 units, the spell-point rate zeroed by the NoRegenSpellPoints ability bit (the low-byte cast pinned), and the exhaustion outcome - a safe collapse rests one hour (all three rates + the 1071 text), enemies nearby die on 1072, water dies on the drowning line |
| riding.test.js | 2 | Platform riding (groundKey identity - the mover wins over the static floor within skin; the frame delta carries the capsule with ZERO lateral ejection; stepping off returns 'dungeon') + ClickToAttack (the DFU Range(UpRight, DownRight+1) six-direction table at roll boundaries, busy-machine refusal) |
| audio.test.js | 6 | A1 pure parts: pcm8->float32 verbatim, SoundClips indices vs SoundClips.cs, GetSwingSound pitch table + PlayHitSound roll families (boundaries MEASURED), the restored enemy sound columns (rat/imp rows, 61-entry count); A2 ambient data verbatim (IsTorchFlat 210/{0,1,6,16..20} + 5m/0.7 + Burning 420, the 201 animal record->clip pairs with gap records silent + 19.2m + the rand()<=100 classic cadence) and the action Play sound seam (soundIndex > 0 fires from effect origin / mover matrix, 0 stays silent) |
| datasource.test.js | 2 | ARENA2 key rule: uppercase basename across path styles (unix/windows/bare); readZip mobile ingest on a crafted archive - store + deflate-raw byte-exact round-trips, the arena2/ prefix filter dropping siblings, directory entries skipped, flat archives ingesting whole (real-zip witness: both DaggerfallGameFiles.zip and a self-zipped arena2 round-tripped ART_PAL.COL byte-identical in the arc probe) |
| books.test.js | 11 | B1 - THE BOOK READER. BookFile.cs over synthetic BOK bytes: the 64/64/8-byte C-string header, naughty tag, page offsets, truncation throws; THE CLASSIC PRICE LAW (DFRandom seeded with the file's first four bytes - the title! - rolling 300..800 inclusive, non-BOK names keeping the header price); the TextFile token reader (printable runs, NewLine, centre-closes-the-row, Position/FontPrefix argument bytes, the peek surviving a truncated tail, EndOfPage); the id/filename laws over the VENDORED DFU mapping (baked booksData pinned equal to vendor/dfu-books/books.txt verbatim - 92 classic ids, trailing comma tolerated as Unity does; BOK%05d of the LOW byte, the 10000->5 Ark'ay alias, unmapped ids null); the reader window's verbatim scroll law (24-step, up clamps at 0, down rests where the next step would pass -maxHeight, page-turn sound on the 159px centre-page crossing, OpenBook on open, ButtonClick exit); the UseItem book arm's new shape (item + ruined-book failText, no longer pending); the bookseller shelf minting mapped ids; the ARENA2-gated whole-corpus sweep (every classic book parses and tokenizes); and AUDIT B's three lanes: the LAYOUT LAW pinned to ConvertTokensToString (NewLine-only breaks, sticky justify BOTH ways, pages concatenating, PositionPrefix dropped), the 64-byte field widths, the 0x7f printable bound, an empty book scrolling nowhere, a corrupt page offset reading empty rather than throwing, and the hand-off RUNNING the inventory close law so a session drop still mints its pile |
| bsa.test.js | 6 | BSA containers, closure invariant, FOO quirk |
| palette.test.js | 10 | 8 palettes, MAP.PAL x4, embedded reads |
| textrsc.test.js | 3 | the verbatim record table on crafted bytes (headerLength/6 - 1 count, id->offset walk, raw bytes INCLUSIVE of the 0xFE terminator, missing ids null), plainText flattening (NewLine, SubrecordSeparator variants, the two one-operand prefixes consuming without leaking printable operands, justify/control drops)  ; U11 - JustifyLeft (0xFC) and JustifyCenter (0xFD) each BREAK THE LINE (MultiFormatTextLabel.cs:333-345 calls NewLine for all three), where the port dropped them as "every other control byte" and fused every centred record into one run-on line; plus linesById returning rows WITH their per-row alignment |
| texture.test.js | 6 | 472-archive corpus, per-codec checksums |
| imgcif.test.js | 8 | IMG table, palettized files, CIF/RCI/weapons |
| arch3d.test.js | 5 | 10251-mesh corpus, UV rules, patch table, model 456 |
| blocks.test.js | 4 | 1295-block corpus, resource closure, FixRdbData |
| dungeon.test.js | 14 | dfRandom LCG, texture tables, RDB matrix order, action records, overlap removal, Privateer's Hold, 187-RDB closure, full 4232-dungeon sweep, R6 light collection pins, per-light flicker bounds, R7 water corpus (32/187) + Maorn pins, the P10 teleporter-resolution corpus gate (84 teleports, 82 resolve, N/W0000003 verbatim-dead like DFU's null NextObject) |
| monstercareer.test.js | 1 | ENEMY{nnn}.CFG careers from a crafted MONSTER.BSA through the shared BsaFile+ClassFile readers: fields real on the entity, session cache proven (throwing re-fetch), missing-index null path |
| actionchain.test.js | 9 | the Play cascade through EVERY flag (relays: Teleport/Activate/text/quest/Unknowns), chained doors keyed act:<position> running their OWN verb (OpenDoor unlock+open, CloseDoor close+relock-to-starting, Lock 16-if-unlocked, Unlock 0), special doors (verbatim DaggerfallActionDoorSpecial swing, C# `OpenDoor || CloseDoor && !door` precedence pinned), ExecuteActionOnToggle on the player toggle through the Door trigger gate (Direct-gated records stay silent), None-flag chained doors cascade without swinging, AttemptBash verbatim (open bash-closes, lock >= 20 magically held, d100 under 20 - lock bursts + clears) |
| activate.test.js | 1 | activationTargets single source: effect objects target on their precomputed aabb (crash regression - scenes read o.cpu on cpu-less traps), relays excluded, cpu path transformed through the matrix, door reach default |
| fpsweapon.test.js | 7 | the TRUE classic FP weapon (pivot 2026-08-17): WeaponBasics animation tables verbatim (records/frames per state incl. the bow draw-hold-loose), ItemHelper template-index -> WeaponType (enchanted promotion), WEAPON*.CIF corpus pins on real ARENA2 (frame counts/dims/offsets), the metal dye band, the 320x200 alignment math, the dye-parity pins (Steel skips as native, Silver DYES through the aliased 18 - DyeColors Silver = Unchanged = 18 routes into the Silver table), ToggleSheath verbatim (starts sheathed, the draw sound only on unsheathing a real weapon) |
| playerweapon.test.js | 3 | verbatim reach 2.25+0.25 + view/LOS hit rule + the swing-mod table, drag-gesture -> mapped strike on the shared machine (threshold + release reset), hit-frame kill through the full chain (deterministic rolls) + reach/dead gating |
| weaponPose.test.js | 2 | sampleClip UNITS regression (audit 2026-08-16): FP pose frames 2-3 sample the clip (phase * dur mapping pinned against a direct seconds sample), every enemy 1H clip samples at frame 3 (short durs died under phase-as-seconds) |
| formulas.test.js | 6 | FormulaHelper verbatim: Dice100/DamageModifier/H2H (sheet rule)/weapon min-max tables/material mods/body parts, career attack-modifier bits (bonus + phobia ADDITIVE) + enemy-type bonus, to-hit chain (dodging/4 bug preserved, the flat -50 + the +40 monster adjustment, weapon material x10 on the weapon branch, roll ordering, 3..97 clamp), damage paths (str-after-skeletal ordering, silver x2, material gate), the monster multi-attack loop (basics damage spans 1-3, the DFRandom reflex gate 50-10*(reflexes-2), per-hit enemy-type bonus, class enemies staying on the H2H path), the classic 0.25/MeleeDistance/35.156deg hit gate |
| spellcost.test.js | 2 | the verbatim component math (offset + A*starting + B*trunc(inc/per), averaged magnitude, per-effect skill scaling gold*(110-skill)/400, the zero-component fudge 60/100/160), target multipliers on the SUMS (x1.5/x2.5 pinned, the table's x2.0) + the castCostFloor 5 at mastery; the 2026-08-16d byte-cast pin (a real subType -1 Slowfall hits its table row, not the fudge) + the S19 Paralyze (28/100 duration+chance) and S19c cure rows (8/100 disease+poison, 20/140 paralyzation) pinned |
| spellcast.test.js | 5 | verbatim saving throw (tolerance precedence Resistant>Immune, immunity->0, DF-Chronicles proration edges at exact rolls, 5..95 clamp + MagicResist), magnitude roll (base+plus x floor(level/per), per-0 guard) + damage-family gate + full resolve skipping non-damage, the CastSpell 45.454546 cooldown firing through the sink with origin + the verbatim 1000 reset (fires at activations 1/23/45), TARGET_TYPES verbatim + applySpell (the one door) vs a fire-immune foe (nothing lands) and a soft target, skipped-family count, the ranges-II pure targeting (touch reach + nearest-live + LOS gate + corpse skip, the 4.0 sweep in/out/dead) |
| magicitems.test.js | 3 | MAGIC.DEF verbatim 62-byte records (index = stream position, -1 unfilled enchantment slots, artifact types read), CreateRegularMagicItem routing (regular-only filter, group-byte tables, the arrow re-roll, magic name swap + condition = uses, -1 slots filtered), the MI loot halving loop firing with the registry set and skipping without |
| magicka.test.js | 3 | SPELLS.STD verbatim 89-byte walk on crafted records (subType ALWAYS read - the dead 0xFF sbyte branch documented; invalid all -1 record gated), the 0x1C00>>8 multiplier decode table + SpellPoints floor, DrainMagicka max(1, flat?mag:axis) + 0-floor through the action system |
| advancement.test.js | 3 | the 35-entry advancement-multiplier table + uses-needed/level formulas, the raise flow (360-min gate, reflexes bit math 0x10000-((r-2)<<13) incl VeryHigh 1.25x, uses reset, the 95+/mastery cap), the level-up sum shape (primaries + majors - min + max minor) + headless leveling applying HP and the 4..6 pool |
| arrows.test.js | 2 | combat bows. C-slice REWROTE the first pin from the retired 'ranged foes fire from sight' claim into DoRangedAttack's band law: inside the strict 6..51.2m band an archer fires on the 1/32 classic-update roll within the yaw gate with the melee timer UNTOUCHED and the swing marked RANGED; the roll is the cadence (forced high never fires), facing away never fires, the 4m dead zone and the 60m outside are silent, and at 2m the archer swings MELEE (marked, timer reset) - the fallback. Plus the weapon-skill bow->Archery mapping and BowDamage's recoverable arrows stacking on the target |
| conditiondamage.test.js | 4 | C-slice (combat-1) - DamageEquipment/ApplyConditionDamageThroughPhysicalHit/LowerCondition/ItemBreaks: the (10*damage+50)/100 int-division amount with the 20% floor roll PER ITEM and the weapon-null/damage-0 gates (hand-to-hand and whiffs degrade nothing); the struck side routing to a SHIELD covering the struck part else the part's own armor slot, an empty slot degrading nothing; a break clamping at 0, speaking the classic line (plural for Boots), unequipping and RESTORING the armor table; and CalculateAttackDamage running the whole law at its TAIL - a landed weapon hit degrades both sides through the real chain |
| travel.test.js | 4 | F-slice - TravelTimeCalculator verbatim: the exact fixed-point chain on synthetic climate grids (woodlands 104/121 per move, mountain via CLIMATE_INDICES[3]=1 -> 220 -> 135, ocean 51-ship/255-shipless, horse 128 and cart 192 through 102*transport>>8, the inn skipping camping's 300/256 so beds are FASTER, reckless exactly the >>1, the longest-axis stepper walking furthest-axis moves only); the trip cost at its boundaries (the always-one-stay +5, DFU's negative-nights guard, the 25-per-started-24-ocean-pixels ship rental, ownership and the free-rooms perk zeroing their halves); the arrival clamps at their edges (3:20+230=7:10, the 31-hour next-day form, sun-averse to dusk with the minute kept, reckless never clamping); and the real-map Daggerfall-to-Wayrest run - ~650 pixels over CLIMATE.PAK with mode ordering, the exact halving, and slowest/fastest day bands |
| travelmap.test.js | 4 | F-slice - the WINDOW and the host: the classic prefix-match typeahead (way finds Wayrest and Waycrest Manor, never Gothway), Enter picking with the popup defaults (cautious/inns/no-ship) and Escape backing out one level at a time; the toggles changing the computed numbers both ways with Enter handing the host the whole choice; the gold gate refusing WITHOUT closing while ship costs flow through; and the world-host sweep - V opens the map, performFastTravel's order (deduct, teardown, streamer re-init, build, spawn, cautious restores honoring NoRegenSpellPoints, the one-clock advance, the clamps off the landed clock), the whole directory feeding the search |
| worldsave.test.js | 2 | P-slice - the above-ground quicksave: the envelope round-tripping the world half (pixel, NATIVE coordinates, the compensation-free height, the readied index, the location key) and the streamer's localFromWorld as the EXACT worldCoords inverse - pinned through a RECENTERED state, because the zero-compensation fixture let the dropped-compensation mutant live; and the world-host sweep (F9/F11 on the classic bindings, natives not locals in the save, the height shedding the vertical compensation, the load teleporting through the travel core to the exact spot, the encounter-anchor reset as LoadInProgress parity, the cross-side restore-and-say arm) |
| littlelaws.test.js | 4 | L-slice - four small ledger laws: canHoldAmount's GP-unit integer arithmetic (the 0.0025-kg gold piece = 1 unit, integer division, the weightless pass, the hasNoEncumbrance zero through the named effectiveUnitWeightInKg); the window gate end to end (refuse-with-the-box at zero fit, the split-take of exactly what fits with the bag merging and the pile keeping the rest, the no-entity open gate); the material-ineffective line for the PLAYER only with foes failing silently; and checkForLevelUp raising one step while behind and staying quiet at par. The entity-9 re-offer/convergence arms live in charsheet.test.js and advancement.test.js, whose jump-law pins were rewritten to the one-step law |
| magicfidelity.test.js | 5 | L2-slice - the magic-fidelity laws: the buff family's landing gates (the OnCast chance rolled for Silence alone, the no-magnitude full save for external casts, the stack-before-the-failed-chance quirk, chance-free concealments); the Magic save element for the magic-only families (a fire spell's concealment saved by MAGIC immunity, blind to FIRE immunity); the manager-level paralysis gate (career Immune, the Resistant-first precedence quirk, the player racial bit with the Low/CriticalWeakness override, the FreeAction flag, the silent assign drop); Transfer-onto-Drain incumbency (a drain claims an incoming transfer - one entry, summed, caster healed - and never the reverse); and the trap/enemy CastSpell wiring sweep (free CasterOnly ready, loud casterless-AoC no-op, ByTouch retarget, the enemy at-caster AoE with the caster excluded, the engine's free-ready state). The aim-directed touch cast rides spellcast.test.js's rewritten ranges pin |
| c2combat.test.js | 5 | C2-slice - the combat audio and fine-grain laws: the voice tables (the 1..5 spawn race roll, male attack = the race's first pain clip with pain one above it, the male HighElf-to-WoodElf swap, the female shared-clip forks, heavyDamage at a quarter of max health); the host gates (class-only voices, the 20/40 rolls, ONE cached voice race per foe, the CityWatch forced-male arm, the bow-less player grunt reading the PLAYER's race); the backstab roll drawing ONLY behind the level>1 gate with the landed backstab speaking; the enemy melee DFRandom byte drawing on every idle classic tick BEFORE the band and the timer gate; and the wiring sweep - whiff/failed-roll miss sounds, the ArrowShoot loose, the arrow-vs-player Dodging tally, the player-side poison hook through resolveHit in all three pools and the player arrow |
| ch3.test.js | 3 | CH3-slice - enemy FALL DAMAGE and the EQUIP DELAY consumer: the fall tracker (grounded refreshes the anchor, a past-threshold landing reports LastGroundedY minus y exactly once, a 4-unit hop stays silent, the double-math trunc exactly as the C# cast); both pools billing the landing through their damage doors with the FallDamage clip; and the swap pause - both halves bill (the leaver at the one unequip door, the arriver in equipItem), the += accumulation across a swap, the verbatim table quirk (a shield's ARMOR group index reads the WEAPON delay table), non-hand pieces bill nothing, and the rig blocks the attack while draining at the classic 980 units/second |
| x3casting.test.js | 3 | X3-slice - exterior enemy casting: the ONE cast executor's gates (silence blocks with nothing spent, the free rider bypasses both gates, the cost priced off the PLAYER entity and floored at 0); the three release arms (CasterOnly to SELF with the caster wrapper + the element sound + the sprite one-shot, the at-caster AoC at the mid-capsule with the caster excluded and no flight, the missile loosed from feet+1.2 carrying the caster level); and the wiring sweep (the pool assigns the S16 lists and mints the shared decision driver at spawn, releases through the one executor, the world hands the SPELLS.STD map and the engine's fire seam, and hostMagic flies enemy missiles - the player-hunting impact at the CASTER level with the transfer heal-back sinks) |
| sl2rewind.test.js | 3 | SL2-slice - applyWorld REWIND semantics (save-load-2): the backward resurrect arm (save-alive + live-dead -> un-kill, the corpse flat spliced from BOTH owner lists and GL-freed by its foe key, health SET from the save before the reconciliation); the pile flat FOLLOWING the restored items both directions (emptied-in-save tears down per RemoveLootContainer-on-restore, refilled-by-rewind re-mints at the build-time size with the pile's own record, gated on p.half so a never-mounted pile stays out); and spawnCorpse keying its batch to the foe with the mid-warm race guard re-checking dead AFTER the texture await |
| ch4senses.test.js | 4 | CH4-slice - the senses verify pass: the stopped 22.5 look gate vs the moving 5.625 (a 15deg-off player at melee STANDS, 30deg turns exactly one 20deg classic step); the hearing ray's CENTER origin captured off the mock (feet + h/2, never the eye) with the outside-radius no-cast; the cadence split (detection resolves on the very first 1/60 step with ZERO classic ticks; exactly one illusion die per classic tick; a failed 8% see-through blocks the ladder until the next re-roll); and the snapshot halves (hostile/encountered/magicka collected, restores gated on presence for pre-CH4 saves) |
| climbing.test.js | 5 | M3-slice - CLIMBING (ClimbingMotor's classic path): CalculateClimbingChance verbatim (the lerp pair, the Khajiit +30, the 5..95 clamp with an inflated skill reading 95, the pre-clamp effect doubling) + GetClimbingSpeed; the start machine (no checks during the 14-unit countdown, the failed GROUND start re-checking every step with the timer NOT reset - one tally per step, the verbatim quirk - and the failed mid-air grasp resetting to zero); the continue/slip cadences (slip on a failed 15-unit check, regain on the faster 5-unit cadence, standing still clearing the slip with no roll); the abort ladder (forward released, wall lost, slipped-to-ground, the 0.12 drift) + the underwater forgiveness starting a climb on a hopeless roll; and the LIVE climb - a real Collider floor+wall, the motor rises past 0.5 with the skill tallied and the mode line spoken once, and the release fall bills from the RELEASE height |
| uicancel.test.js | 3 | U-cancel (ui-chargen-4) - the race screen's back CANCELS the wizard (RaceSelectWindow_OnClose :299-302): the flow's cancelled flag set by the FIRST screen's back only (the gender walk-back lands on race un-cancelled; the race description box's No just closes the box); createChargenWindow firing onCancel exactly once on the modal-contract latch with onDone untouched and further input dead; and the host sweep - the dungeon's tickOverlay reload arm (the raw-flow path) plus both exterior hosts' onCancel reloads |
| wagon.test.js | 4 | W-slice - the WAGON: the button ladder (no Small Cart says noWagon, a dungeon away from the exit says exitTooFar with no toggle, else ShowWagon swaps the remote to the cart's own list and back); CheckWagonAccess (a no-loot dungeon open near the exit lands ON the wagon in Remove mode, a loot open keeps the pile with the toggle now armed, no cart never grants); the 750kg gates (400 books split-take exactly 375 into an empty cart with the bag keeping 25, the dead-full refusal box, the drop-gold clamp - 100000 offered into 10kg of headroom lands exactly 4000 with the wagonFullGold box); and the save/wiring sweep (both envelope halves, all the hosts' wagonItems hooks, the exit-door distance measure) |
| settings.test.js | 10 | SETT-slice - the settings store: the BAKE against the vendored defaults.ini bytes (13 sections, 171 keys, a stale bake fails); the typed getters' DFU-verbatim FAILURE MODES (an unparseable bool reads False not the True default, an unparseable clamped int reads MIN, both clamps); overrides as a DELTA with setting-back-to-default dropping the override and reset restoring the shipped values; the LIVE tier proven by DRIVING each consumer (the voice gate follows CombatVoices, the loiter cap and the refusal line that quotes it follow LoiterLimitInHours, the kit seam follows PlayerTorchFromItems, sensitivity 1.0 reproduces the port's own feel constant and invert flips the pitch term); and the tier map's honesty - every key tiered, every live key's named consumer really importing the store (this caught two lies in the map as it was written). AUDIT 24 added four: the tier map agreeing BOTH ways (every settings read under src/, re-derived by walking the tree at test time, must be tiered live - re-introducing the ShowOptionsAtStart mistier fails it by name); the launcher taking POINTER input with every control inside a 393px phone canvas and a tap selecting/changing/launching; the mouse-look settings reaching ALL FOUR hosts with no raw constant left; and the ShowOptionsAtStart lock-out warning naming the way back. MENU added: Video/FieldOfView as DFU law (GetInt 60..120 clamped at both ends, the shipped 65 reading as 65 degrees) reaching ALL FIVE projection hosts with no private Math.PI/3 left - including the SKY's own fov argument in two of them, which if left behind would tear the horizon off the world |
| settingsText.test.js | 3 | DFU's OWN settings words - vendor/dfu-settings/GameSettings.txt baked into settingsText.js: the bake against the vendored bytes (labels AND the keyInfo tooltips split out, with DFU's comment and schema lines skipped rather than swallowed); DFU's words present for the five page titles, the section headings and every setting this port made LIVE, with the raw ini identifier never the thing a player reads (DFU names it "Game Console", not LypyL_GameConsole); and the key convention - the ini key with a lowered first letter - resolving ~64 of our 171 keys, which is the honest measure of how many settings DFU's own UI exposes at all |
| settingsUI.test.js | 10 | MENU-slice - the settings SCREEN, whose laws are the ones nine design critiques established by measurement against this codebase rather than by taste: the category map TOTAL and DISJOINT over all 171 keys (a key with no home would vanish from the screen, and the 21/16/5/66/37/19/7 shape is pinned so a re-bake that renames a key fails the build); no player ever reading a raw ini identifier (no camelCase, no underscore, ASCII only - the font has no glyph below 33) and no copy anywhere calling the port's own gaps broken or unsupported; the widget law TOTAL with an unavailable key resolving to 'blocked' and NOTHING else, carrying a readout and an instead-sentence so it never prints its stored value - EnhancedCombatAI holds DFU's True while this port runs the classic path, so 'On' would be a lie; the RANGE-EQUALS-CLAMP law, every slider's travel re-derived from its consumer's own getInt/getFloat bounds at test time (this caught a real parity bug: LoiterLimitInHours had been given an invented 1..24 against DFU's AddSlider 3..12) and stepping never escaping either end; tier as a GROUP - every group with members on screen even when folded, its heading count COMPUTED not typed, the header sentence built from the same tally, and no unavailable row offering an operable control; the TOUCH-REACHABILITY law across eight viewports x two text scales (every target inside the canvas, PLAY present and launching exactly once everywhere, every rect's centre hit-testing back to its own id); the screen depending on no ARENA2 art and no 320x200 window, so it draws before any data path exists; and the 44px FINGER law on a touch device - the browser probe found controls at 36px because the pill was inset inside a tap-sized row, so drawn size and target size are two rects now and this holds them apart. Carried in from the launcher this screen replaced, whose merge audit caught a step read off the CURRENT value: a number row shows the value IN EFFECT (a store holding 2 for SoundVolume is a bus running 1, so the row says 100%, not 200%), stepping cannot escape either end, and the same walk driven END TO END through the screen's own key input - mute the volume and one press back up is a step, not the whole scale |
| guildreveal.test.js | 4 | G8-slice - the guild promotion map reveals: DiscoverRandomLocation's candidate filter (the baked Discovered flag AND the runtime store both exclude - a second pick cannot land the same location), the uniform injectable pick, the picked-clean null, and the {buildings, locations} envelope round trip; the ThievesGuild rank-6/8 gate (reveal wins 5228/5229, a dry region falls to the plain 5235, ranks 2/4 never call the reveal - spied); the DarkBrotherhood's three-promotions-three-reveals with the even-rank default message; and the host-to-law threading sweep (onPushEffects hands the seam, the interior host threads it, the world host builds candidates off the map TABLE) |
| travelvisibility.test.js | 2 | TV-slice - the travel map's dungeon visibility: the typeahead hides an undiscovered dungeon (baked flag false, store empty) while the baked town is findable from the first key, and discoverLocation makes the barrow findable; the source sweep (the index rows read mapId + the BAKED flag off the map table, the filter is checkLocationDiscovered exactly with no type arm, the world's pixel-crossing writer discovers entered locations) |
| teleport.test.js | 3 | TP-slice - the Teleport (Recall) effect: the prompt marker rises on CasterOnly arrivals only (TargetFlags_Self holds against a ranged arrival, nothing assigns, the rest of the bundle still heals); the anchor's envelope round trip as a copy with pre-TP saves restoring null; and the seam sweep (the engine routes every player arrival through onTeleport, both prompt arms + Escape, the 4001 refusal, the anchor consumed on arrival per both DFU arms, forceExitToExterior landing mode+collider, and the standalone hosts' loud interim lines) |
| enemydoors.test.js | 4 | C-slice (characters-3) - enemies opening doors: raycastHit attributing the nearest hit to its BUCKET with an opened door (bucket removed) unblocking the ray and the plain raycast contract intact; the senses recording the sight ray's blocking door and a clear line recording nothing; every OpenDoors gate (2m foe-to-center, IsLocked, IsOpen, CanOpenDoors, no-door) refusing before the toggle; and the dungeon host arm sweeping DOOR_VERB_FLAGS-only behind canOpenDoors off the senses key into the ActionSystem's own door path |
| encounters.test.js | 5 | E-slice - RandomEncounters + IntermittentEnemySpawn verbatim: the 45 baked tables against the C# rows (Crypt's open and close, Slaughterfish leading the deep, class ids in the cemetery); ChooseRandomEnemy's pick (towns by day and unknown climates spawn NOTHING, guildhall/default building tables, underwater forcing 19) and the level band at its edges (the [0,5] floor, the [14,19] ceiling, the 96+ arms both sides of level 5); the 144-minute cadence pinned against the %6 mutant at minute 72, the alert-gated dungeon roll, and the intermittent decision end to end (dungeon rest at 8 units, town night at 10, the midday town null, the wilderness day window); the alert laws (raise stamps, exactly-8h holds by the strict >, past-8h clears); and the dungeon host sweep (the rest catch-up loop breaking on the first spawn, sight raise, kill clear, the closed routed leg, the frame decay, the spawner minting through the load chain) |
| exteriorfoes.test.js | 3 | X-slice - the exterior mobile-foe mount: the pool laws (the relevance cull running AFTER fresh senses so a newborn's Infinity placeholder never culls it - the live probe caught the inversion - the sight alert raise and the targeting-kill clear above ground, the explicit no-crime sweep, the loud archery residue) and the world-host wiring (the bounded per-minute catch-up loop with the location-rect and climate feeds, fast travel's PreventEnemySpawns anchor reset, magic.foes() seeing both pools with the sinks routing by pool, the watch -> encounters -> civilians melee order, the floating-origin follow) X2-slice: the archery pins joined - bow foes arm by the dungeon's ranged-flags law, the shoot frame fires the host's arrow seam, the loose rides the C13 flight with the enemy meta + ArrowShoot, and the impact runs the shared damage member (Dodging tally, poison, the recoverable arrow); the arrowflight home pins the mid-capsule hunt law itself |
| footsteps.test.js | 4 | FS-slice - PlayerFootsteps verbatim: the sound-set decision (Outside by day, Snow in winter unless IsSnowFreeClimate - the desert never snows - Wood in buildings, Stone in dungeons, DFU's override write order for exterior water/paths and the dungeon Submerged/Shallow arms, the stateless pick landing back on stone out of the water for free); the stride at its boundaries (2.5 units walking AND running, alternating clips at 0.7 with the half-speed halving, stillness accumulating nothing); the ground-loss laws (airborne silence + reset, ONE immediate landing step, the first boot landing swallowed, levitation silencing and resetting); and the four-host sweep (every host drives the machine off its live motor into playOneShot, the modal host splitting wood/stone by mode with the block-water arms, the exteriors feeding season + the raw CLIMATE.PAK index) |
| pacification.test.js | 4 | C-slice (characters-2) - the language table (six stealth classes Streetwise, other classes Etiquette per DFU's BCHG, 24 monster careers to nine tongues, vampires/liches Etiquette, beasts nothing); the chance arms AT THEIR BOUNDARIES (social skill/10 + personality/5 vs a tongue's full skill + personality/10, C# int divisions pinned at 59, the sheathed +10 / drawn -25 swing making a polite approach impossible at average stats with a drawn blade); the host sweep (the first-encounter edge, the sheathed feed, stand-down, tally 3/1 split, damage re-hostiling); and the motor edge firing ONCE with a pacified foe neither pursuing nor even TURNING - the decision tick itself is gated |
| chargen.test.js | 4 | verbatim stat rolls (base + 0..10 inclusive, pool 6..14), skill tiers (28/18/13 + 0..3, defaults 3..6), HP (25 + hpPerLevel; per-level floor 1 + endurance mod), the interim lowest-first pool policy + full create (totals pinned), skillValue dual shape driving formulas (weapon skill hits where HandToHand misses on the same roll), the verbatim starting-spell sets (Mage/Sorcerer shared, Bard single, >6 none, resolution skipping missing records loudly) |
| charsheet.test.js | 3 | raiseSkills with a sink sets ready+pending WITHOUT applying (headless path unchanged), applyLevelUp (HP roll + endurance mod pinned, hand distribution, flags cleared, idempotent), the level-up screen sharing the verbatim clamps (pre-level floor, pool-0 block, confirm gate) with the hand-built stats landing |
| wizardorder.test.js | 6 | U15 - THE CLASSIC WIZARD ORDER and the RANDOM NAME button. The order pinned verbatim against WizardStages (DaggerfallStartNewGameWizard.cs:63-79 - race, gender, class, biography, name, face, stats, skills, reflexes) rather than the port's invented one, because the order is not cosmetic: the face picker draws RACE-and-GENDER face art, so a face screen that runs before the race is chosen paints the wrong faces, and CreateCharNameSelect.cs:112-119 DISABLES the random button when there is no race template, so a name screen that precedes the race can never be classic. The name screen owning a random button that mints a NAMEGEN name for the CHOSEN race and gender - seeded so the button and a direct getNameBank/fullName call agree exactly, since a button that merely returns SOME non-empty string passes while wired to the wrong bank. And the Argonian quirk carried verbatim: NameHelper has no Argonian bank, so DFU falls through to Imperial - NameHelper.cs:50 says so in as many words ("Imperial names appear where one would expect Argonian names") |
| audit17j.test.js | 13 | AUDIT 17j - the parity pass over U14 + U15, read against DFU's wizard HANDLER TABLE rather than the port's STATES order. The name screen RESEEDING DFRandom on every entry (ShowRandomButton, CreateCharNameSelect.cs:123-126) - without it the random button handed every character of a given race and gender the same name on every boot, DFRandom being a global whose last srand before chargen is the dungeon locationId; pinned by varying the injected engine draw AND by holding it fixed, so the reseed is proven to be the only source of variation. The class screen cancelling to RACE, skipping gender (ClassSelectWindow_OnClose:353-370). The name screen having a cancel at all, landing on the biography and DISCARDING the answers so far, because answerBiography appends and re-answering would otherwise double-apply every effect. The name box emptying when race or gender changed (SetRaceTemplate/SetGender:142-161) and NOT emptying on the first entry, which is what DFU's non-null guard buys. The 31-character cap, TextBox's untouched default. worldModes reaching dungeonContext's overlayClick, by a source sweep that must match the WHOLE condition - the first draft matched only half of it and passed while the mode test was mutated away. And the stats and skills screens RESTORING on re-entry instead of rerolling (SetAddBonusStatsWindow's DFClass check; SetCharacterDocument's isRestored arm), with the screens' own Reroll button still forcing and a class change still rolling fresh |
| summary.test.js | 11 | U16 - WizardStages.Summary, the wizard's closing screen and the last stage the port was missing. The summary sitting BETWEEN the reflexes and done, since ReflexSelectWindow_OnClose goes to SetSummaryWindow and it is SummaryWindow_OnClose that calls StartNewGame. OK gated on ALL FOUR pools (the stat pool and all three skill pools, pinned one at a time), because the screen lets you take points back DOWN off any of them, and an unspent point pops TEXT.RSC 14 as a ClickAnywhereToClose box that must NOT also confirm on the way out. Entering the screen ZEROING all four pools and putting the stat selection back on the first row, both because SetCharacterSheet re-runs the rollouts' setters on every push (StatsRollout.cs:190 calls SelectStat(0)). RESTART returning to the RACE screen with the document intact - a SOFT restart, so the F7 rule still holds - and, the pin that matters most, RESTART being unable to double-apply the biography: the reset lives on _enterBiography, which every arrival goes through, where 17j F3 had put it on the name screen's cancel alone. The name box, the face picker and the reflex picker all live here, the last at the summary's own origin rather than the reflex screen's, because ReflexPicker is POSITIONED by its host rather than rewritten per screen. The two rollouts having INDEPENDENT selections, or a click on a skill row would move the stat spinner. And a source sweep keeping the seven DERIVED labels out of the shared stats block - they belong to CreateCharAddBonusStats' own panel, and sharing them drew them across the summary's skill panels, which only the screenshot caught |
| classpicker.test.js | 10 | U17 - the class picker's real selection model and the three skill spinners, both ListBox/rollout behaviour the port had collapsed. A SINGLE click only SELECTING a row (ListBox.MouseClick raises OnSelectItem and nothing else) and a DOUBLE click PICKING it (MouseDoubleClick -> UseSelectedItem -> OnItemPicked), which is the bug Mac hit: the port picked on a single click and then wanted a keyboard confirm the picker has no button for, so on a phone the class screen was a dead end. The 300ms window pinned at its boundary and on TIME ALONE - the second click need not be the same row, because MouseClick has already moved the selection by the time MouseDoubleClick reads it - plus the clock RESETTING on a fire, or three evenly spaced clicks would pick twice. The class DESCRIPTION box on TEXT.RSC 2100 + index with Yes leaving and No returning to the list, Return going through the same door as the double click, and a missing description letting the pick simply stand rather than stranding the player. And SkillsRollout's THREE spinners: one selected skill per group, each spinner spending its OWN group's pool rather than the cursor's, the selections surviving a pointer jump into another group, and all three resetting when the screen is re-entered |
| audit17k.test.js | 5 | AUDIT 17k - the fist crash + the U16-U18 parity pass, each pin mutation-proven. THE FIST RULE enforced, not remembered (the 17i sweep shape): no file in src/scenes may dereference playerWeapon.weapon without ?., because bare hands are a NULL weapon since U8h and the DEFAULT state - the dungeon host's raw deref crashed every bare-handed strike frame; the sweep fails on the exact line. The bare-handed path driven functionally: unsheathing fists plays NO draw sound (WeaponManager: not a real weapon), the tap swings, sixty machine steps run, and the damage call resolves through CalculateHandToHandAttackDamage on the formulas fixtures' own entity shape. CORPUS: WEAPON10.CIF - the fist art, now the default draw - carries every record MELEE_ANIMS indexes with at least its NumFrames frames, and weaponTypeForItem(null) IS Melee. F1: constellationPalette writes (0,0,blue) into slots 192/160/128 on answered runs and RESTORES the file's own captured colours on a pristine draw - DFU re-reads the IMG per window construction, the port keeps one palette, and without the restore a re-entered questions screen re-uploaded the last run's glow under the 'pristine' key. F3: the scroll-down clamp pinned AT its boundary - a 7-row label one pixel past the text-window edge scrolls once and is then BLOCKED exactly on the edge (strict >, the >= mutation the U18 pin survived) |
| classquestions.test.js | 31 | U18 - the class-questions path, each pin mutation-proven where a pin can fail. The FALL.EXE 0x0059820C answer table deepEqual against the DFU literal WHOLE (any drifted byte fails) plus every question's three answers steering three DIFFERENT paths; GetQuestions' ten-UNIQUE pick with the linear probe pinned under a stuck PRNG (index 0 forever without it); DisplayQuestion dropping empty rows and finding the a)/b)/c) boundaries (Contains, last match wins); the gender accept arm landing on the METHOD screen through key AND click; the method screen's no-cancel-arm law (ChooseClassGen_OnClose has no cancelled branch - Escape goes to the LIST); the missing-data fall-to-the-list never-trap; ten answers walking the weights to [7,1,2] and resolving the class on a crafted CLASSES.DAT, the pick standing without a description source; the description box's Yes-adopts/No-drops-to-the-list arms with the modal gate; re-entry constructing FRESH questions (SetClassQuestionsWindow has no null guard); the constellation blues 8 + 24-per-answer; the scroll clamp with the parchment frame wrapping over all 16; the method rects' clicks (and the panel outside them dead); the scroll-margin/answer-row click law with NO x test; the header nibble law (slots past 3 mask); the results walk from offset 18 at four slots per class with the no-match -1. THE F2 AUDIT added the pins its lanes found missing: the weight-index -> constellation mapping (correct as shipped, and a swap would simply have lit the wrong constellation invisibly), the ordering law in BOTH directions (weight and count commit at the answer, the display waits), every exit arm dropping an in-flight animation (only re-entry was covered), a hidden tab NOT killing a healthy animation (the watchdog spends TICKED seconds, because rAF freezes while the wall clock runs on), four SOURCE pins for the draw-half laws the headless suite cannot execute (the chart's blues come from qPaintBlues and are never re-derived from the answer count; the replay re-arm; the started-player duration floor; the per-frame upload gate), and THE REAL SEAM under ARENA2: preloadChargenArt through drawChargenNative, driving the actual CEL load, player, upload gate, chart repaint and release - before it, every F2 pin drove the INJECTED animation and the whole chargenArt half could have been dead code with the suite green. CORPUS (ARENA2): all forty TEXT.RSC 9000 questions parse numbered-prefix-free with ordered answer rows; CLASSES.DAT is 216 bytes and EVERY triple summing to ten resolves to a real class - all-warrior the Knight, all-rogue the Thief, all-mage the Mage; both SCRL GFX files decode 8 full 320x80 frames. And the GFX RLE proven byte-exact on a crafted file (runs + raws + uncompressed rows, the extension gate) |
| customclass.test.js | 28 | U20a - the custom-class builder, each pin mutation-provable. THE LAWS: the difficulty tally (+1 per HP above the default, -2 per HP BELOW) and its multiplier pinned at both ends of the legal band (exactly 0.3 at -12, exactly 3.0 at +40); the dagger's Y with C#'s (int) TRUNCATION and both visual clamps; the picker's alphabetical list of UNASSIGNED skills with a replaced skill returning to it; the built career carrying a fresh DFCareer's all-zero flags plus the x0.50 spell multiplier - decoded through the port's OWN spellPointMultiplier, not just asserted as a literal (TEST THE SHAPE THE PRODUCER MINTS); GetClassAffinityIndex's strictly-greater tie rule and its zero-overlap default; and the custom spell rule at every arm, including the negative that a magic MINOR does NOT qualify. THE REPUTATION WINDOW: RoundNearestBarHeight's quirk (a remainder of exactly 4 rounds UP, 1..3 DOWN, capped at 50), all four column thresholds, the sign rule, the middle-line ZERO (repVal is computed on every click even when no bar moves) with its never-negative-zero guard, and the negated-sum ledger. THE FLOW: the Custom row's null career (the raw index read THREW - the live probe caught it) and its CLICKABILITY (the list hit bounded on careers.length, so the row was keyboard-only - the U14 shape); the ListBox CLAMP that replaced the port's wrap; the reroll memo keyed on the CAREER, proven by a custom class whose affinity index collides with a previously rolled one; a fresh builder's DFU defaults; the HP and freeEdit clamps with the pool's conservation invariant; the four exit gates in DFU's ORDER on their own records; the affinity + reputation handoff; the isCustom quirk pinned BOTH ways (set at the row pick, never cleared - so a cancelled builder and a later standard class both still read custom); the sub-windows' modality; and the typed-CAPITALS seam, swept in source so the exterior hosts' key router cannot drop the event again |
| uisounds.test.js | 3 | THE UI ONE-SHOTS - the windows were entirely silent (zero playOneShot calls in all of src/ui) while DFU's click, roll and clink. Pins the DFU-verbatim SoundClips values (Ignite 16, LevelUp 96, DiceRoll 300, ButtonClick 360, GoldPieces 361, PageTurn 362, ParchmentScratching 363, SelectClassDrums 374, OpenBook 384); messageBoxHit playing ButtonClick on a button hit and staying silent on a miss (DaggerfallMessageBox.ButtonClickHandler :487, the one funnel every popup click routes through); and the chargen stats roll riding DiceRoll on the entry roll and the Reroll button while the memoized restore stays silent (StatsRollout.Reroll :180). Spies on the audio singleton - headless _ensureCtx disables itself without a window, so the real path is a no-op in node |
| villagerdesigns.test.js | 10 | The VILLAGER DESIGNS - the 25 wandering-townsperson variations as data for the neutral-body rig. Fully redesigned rather than sprite-traced (Mac's call, per tools/neutral's README), but the ROSTER is not ours to choose: PERSON_TEXTURES is 3 races x 2 genders x 4 archives plus the guard, and an archive with no design spawns a blank townsperson - so the drift guard demands exactly one design per spawnable archive, no duplicates, and race/gender labels that match the table they came from. Every zone's material must be defined (an undefined one falls through to SKIN at build time - a villager wearing their own body colour), every span must be a named RAMPS entry rather than a hand-rolled pair, and designOpts must reverse each span, because the rig indexes ramps by lighting intensity and step 0 is the SHADOW. The palette pins run on the real ART_PAL and caught three of my own bad choices: block 13 is a CLIFF not a ramp (130->100 then a drop to 40), block 11 RESTARTS mid-block, and block 1 is monotone in brightness while alternating gold with neutral grey - so a garment cut from it shades in speckles. That last one needed an OSCILLATION measure, since total hue spread fails to separate it (madder legitimately shifts more), calibrated on the real palette at good <= 17 vs the mixed block's 34. HANGING CLOTH is pinned separately, because it is not a zone: a gown expressed as a cloth zone spanning [body, legL, legR] cannot work - displace pushes a group's faces out from THAT GROUP's centre, so it thickens each leg about its own axis and renders a tunic plus shorts. The render showed it; no numeric check would have. So a skirt, robe or surcoat is a `drape: {name, mat}` naming a pieces/draped.js garment, and the pins demand the name is in DRAPED_NAMES, the material is one of the design's own, designDrape resolves it dark-to-light, and no zone ever spans the torso and the legs again. The editor drives its hairstyle, hair-colour and skin-tone controls off these fields too, so each is pinned against what can actually be built - that is what caught the city watch, whose race 'Guard' is not playable and had no tone, leaving them wearing the previously selected villager's skin. Last, A HOOD LEAVES THE FACE OPEN: gated on height alone it painted the whole skull, so displace grew a depth gate (zLo/zHi, unbounded by default) and the hood became two zones - temples-back plus crown-above-the-brow. The pin measures the BUILD, not the literals: it asks whether any forward, below-brow head face got recoloured, and skips the guard, whose closed helm covers the face on purpose |
| absorption.test.js | 9 | S24 - spell absorption, each pin mutation-proven. spellAbsorptionFlags had ZERO consumers: the SORCERER ships absorb = Always alongside NoRegenSpellPoints and only the PENALTY was live, so the class paid its whole cost and got nothing back. The gates in DFU's own order: DESTRUCTION ONLY (a Restoration heal passes straight through an Always absorber - DFU's comment says absorption is tested against every incoming effect, so a benign self-heal would otherwise be swallowed); the target must have ROOM, pinned at the boundary in both directions (exactly enough room absorbs, one point short refuses, a FULL pool absorbs nothing at all - the throttle on the trait); the cost carrying the target multiplier and DFU's floor of 5, itself the guard against an absorb draining the pool it is meant to fill. The three career branches with darkness as inside-OR-night and light as outside-AND-day, each pinned on all three contexts, plus a career-less entity never throwing. The effect being SKIPPED rather than reduced - no damage reaches the hurt sink at all - and the points credited to the pool with the HUD line; the negative that a non-absorbing target takes the spell normally; the casterless bundle that is never absorbed (:505); and the SELF-CAST CAP, which applies only when caster === target and not to another entity's spell. And the Sorcerer end to end on the real CLASS03.CFG - no damage from an absorbed bolt, spell points instead |
| equiprestrictions.test.js | 7 | S23 - the career equip restrictions, each pin mutation-proven. DFU refuses the equip in the inventory WINDOW (DaggerfallInventoryWindow :1343-1381), not in ItemEquipTable, so starting gear equips straight past it - a restricted class really can START in gear it could never put back on, ported at the same seam. The four tests: forbidden ARMOR TYPE off the material's high byte (the port's ARMOR_MATERIAL values ARE DFU's raw nativeMaterialValue, so `>> 8` carries over), forbidden SHIELDS indexed from the Buckler template with a shield never routing into the armor-type branch, forbidden WEAPONRY by proficiency and by the FLAT material index weapons carry, and the armor MATERIAL test gated on PLATE ONLY - pinned on Chain2, whose 0x0103 shares its low byte with Elven and which a career forbidding elven must still be able to wear. GetWeaponSkillUsed mapped across the port's own WEAPON_SKILL rather than minting a second weapon table, with DFU's Skills.None = -1 default ported bug-for-bug: -1 masks against every bit, so an unnamed weapon-group item reads as forbidden to any restricted career. And the CLASSIC corpus, which is the point of the slice - the Mage refused plate, chain, tower shield, axe, LONG BLADE and bow while keeping short blades and a staff (the live probe reached for a Longsword as the 'allowed' weapon and was rightly refused); the Monk refused all three armors and all four shields; the Knight refusing LEATHER; the Barbarian's daedric blade; and the Warrior, the one class with nothing forbidden |
| audit17n.test.js | 8 | AUDIT 17n - the parity pass over U20b, each pin mutation-proven. F1: the enemy-type attack modifier was broken in TWO independent places and so had never applied to anybody. DFU reads attacker.Career.<group>AttackModifier for every attacker (FormulaHelper.cs:993-1030); the port flattened the byte onto the entity and only the FOE builder set it, so a player - who carries `career` and no flat field - scored 0 on every swing. And the WEAPON branch of calculateAttackDamage called weaponAttackDamage WITHOUT the targetGroup it had just resolved, leaving that function reading `target.group`, a field nothing in the codebase mints - so even a correctly-flagged attacker got nothing through the path players actually use. Pinned on both entity shapes (a career-only attacker and a flat-byte foe), on an attacker with neither, through real damage via calculateAttackDamage, and against the real CLASS11.CFG - the classic ASSASSIN ships 0x04, a Humanoid bonus it had never received, so this predates U20b and is not a regression from it. F2: the difficulty table's 50 keys proven distinct AND proven not to collide - a concatenated key that shadowed one of the eight bare-priced primaries would silently misprice a pick - plus every label non-empty with an unknown key answering '' rather than undefined (THE NATIVE-WINDOW RULE: a row that cannot name itself must not draw). F3: parseCareerData leaving every numeric field finite and non-negative under the maximal fourteen-pick set with both packed bitfields spelled out, and a career's flags surviving the save round trip - the shape AUDIT 17h caught dropping player reputation. And the CORPUS-PATH RULE swept rather than remembered: no test may name /dfdata/ or /arena2/ in a string, because the original data is freeware but NOT redistributable and CI has none. That sweep exists because 17n's OWN Assassin pin shipped with the path baked in - it passed locally, where the data happens to sit, and took the deploy down |
| specialadvantages.test.js | 27 | U20b - the special advantages/disadvantages window, each pin mutation-proven. THE LISTS: DFU's eleven and eleven in picker order, every key carrying display text, and each primary's own secondary list - the tolerance QUARTET sharing the seven effects, regeneration alone carrying the fourth While Immersed row, and the four primaries that open no second window at all. THE PRICES: the table's 50 entries (not the 53 an earlier reading recorded - counted off the C# literal, no key repeats), its extremes at both ends, the bonus-to-hit row's DAEDRA asymmetry, and the eight primaries whose secondary does not change the cost - Immunity is 10 whatever it is immunity TO. THE ADD RULES: the exact-duplicate test; the incompatible pairs matching only on an EQUAL secondary, which is what lets a character be immune to fire and critically weak to frost; cannotAdd reading BOTH windows' lists, the only reason an advantage can clash with a disadvantage; the three ONLY-ONE limits refusing before the secondary window opens while an unlimited primary repeats freely; and the add button doing nothing at seven. THE POINT: the two adjust terms reaching the dagger, the advancement MULTIPLIER and the 306 gate - advantages alone can push a legal-HP class out of a band HP alone cannot leave (pinned at exactly 40 and at 41). PARSECAREERDATA decoded through the port's OWN consumers rather than re-asserted literals: the tolerance quartet's four separate EffectFlags bytes, the ability bits through hasSpecialAbility, Increased Magery REPLACING the spell-point multiplier at every rung without disturbing the ability, darkness and light bits packed beside it, proficiencies split across the bitfield's forbidden and expert halves, and bonus/phobia through careerAttackModifier. Plus the label block's +6 tandem squish against its +8 step |
| audit17m.test.js | 6 | AUDIT 17m - the class LIST's selection vs the DOCUMENT's class index, each pin mutation-proven. DFU carries TWO members and the port had collapsed them into one: characterDocument.classIndex (written at the wizard's :343 questions arm, :364 list pick and :382 custom AFFINITY) and CreateCharClassSelect's own listBox.SelectedIndex, which the wizard never writes and which SURVIVES a revisit because SetClassSelectWindow (:158-167) REUSES the window. With one field, customExit's affinity write also moved the picker, so backing out of a finished custom class re-opened the list on a STANDARD class and confirming there ran _acceptStandardClass and threw the built career away - pinned end to end (build -> the bio-method cancel arm -> the Custom row still selected -> Return re-enters the BUILDER with the career intact). Plus the two writes that must stay separate: a list pick copies the row onto the document and ONLY on accept (browsing moves nothing), and the questions path writes the document while the picker keeps its own row. THE KEYBOARD: the builder has no stat control at all - DFU's steps are UpDownSpinner *Button* handlers, mouse only, while the name TextBox holds focus unfiltered - so the port's live 'plus' arm against its DEAD 'minus' one (the overlay table matches '-' as a character first) let a keyboard spend from the freeEdit pool and never refund it; '-' types into the class name as DFU's TextBox does and the pool moves by CLICK. And the RULE swept in source, not remembered: customExit may not write the picker, and the list art may not draw the document index |
| biomethod.test.js | 6 | U19 - the biography-method screen, each pin mutation-provable. Every class-accept arm landing on the METHOD screen (SetChooseBioWindow :344/:365/:401) and a missing question set SKIPPING the stage rather than trapping; the questions arm entering the biography over a fresh reset (stale answers gone); the GENERATE arm's pick law at both PRNG extremes - rolls 0 chooses answer a everywhere, rolls .999 the LAST answer via floor(r * Count), every effect of a multi-effect answer landing through the same tagEffect, DigestRepChanges running, and the artless no-box path going straight to the name screen; generating twice unable to double-apply (the fresh-BiogFile reset rides EVERY arm); the three cancel arms verbatim (Escape -> the class list per the OnClose cancel arm, the name screen's cancel -> HERE per :483-493 not the questions the port had collapsed onto, the biography's own cancel -> here per :477-480); and the pointer path - both DFU button rects hit art-free, the panel outside them dead, the ClickAnywhereToClose reputation box eating the buttons and closing to the name screen |
| chargenui.test.js | 3 | the four verbatim pool clamps (stat max 100/pool 0/floor-at-rolled with points returning; skill pool-only up/floor-at-rolled), the flow end to end (name/gender/class/stats/skills, pool-0 confirm gates, stat-total conservation pinned, group pools landing on their skills), reroll replacing the working set |
| effects.test.js | 16 | HealHealth (10,8) instant + the verbatim duration arithmetic (base + mod x floor(level/per) with the multiplier CLAMPED AT 1, per-0 guard), the S15 GetMagnitude save gate (CasterOnly skips the save entirely; a RANGED heal on a magic-immune target saves to nothing), continuous (1,0) firing its INITIAL round at cast with saves rolled FRESH every round in the verbatim magnitude-then-save order, the DFU expiry shape (a 0-round entry survives its final pass and Ends the next), immune targets joining at zero damage, empty lists safe, the S8 buff kinds (slowfall/waterWalking/chameleonNormal keys, incumbent rounds STACKING on re-cast, expiry clearing the query); the 2026-08-16d subType byte-cast parity pin (real records read 0xFF as -1 - buffKind/isRegenerate accept both spellings); S19 Paralyze (0,255) - both key spellings, ChanceValue base+plus*floor(level/per), CasterOnly skipping the save, a chance fail consuming ONE roll and reaching no save, the ranged full-save immunity vs a failed save landing, the AddState-first quirk (a failed-chance re-cast still stacks rounds and rolls no save), expiry restoring the query; S19c the Cure family (3,0..2) - chance-gated IMMEDIATE bundle removal (a cure lifts the disease statMods NOW while a drain survives, Cure Poison strips poisons only, Cure Paralyzation lifts the paralysis instantly), chance fails counted with the disease intact, a ranged cure refused on a full save; S13 the SpellPoints/magicka family - DamageSpellPoints (4,2) instant saving-throw scaled DecreaseMagicka, ContinuousDamageSpellPoints (1,2) draining from the cast round with per-round saves; S15 the fatigue stat + families - HealFatigue (10,9) x64 (the parity FIX of S13's HealSpellPoints key: DFU Heal-SpellPoints is potion-only with no classic key), MaxFatigue (Str+End) x64 fortify-aware, DamageFatigue (4,1) + ContinuousDamageFatigue (1,1) x64 through the sink; S14 FortifyAttribute (type 9, subType=stat) - a live additive stat mod via liveStat, like-kind (stat + settings) stacking rounds, different-settings instances coexisting with mods summing, expiry restoring the base; S15 DrainAttribute (7,s) permanent-until-healed (incumbent by stat, magnitude ADDING per re-cast, the never-below-1 clamp vs the permanent value, ticks neither count nor act), HealAttribute (10,s) healing drain damage only (partial, overheal clamped at base + Ending the drain for next-pass removal, undrained no-op), the Transfer family (11,s attribute steal healing the caster's drained stat by the pre-clamp roll + separate incumbency from Drain; 11,8 TransferHealth hurt/heal both directions with the caster REQUIRED; 11,9 TransferFatigue x64 both ways), Regenerate (18,255) healing per round from the cast round with settings-keyed incumbent stacking, and fortified/drained stats moving combat statsToHit through the formulas |
| effectactions.test.js | 4 | Hurt21 every-20th gate + verbatim exclusive Range * level (min<=max guard), Hurt22-25 flat/axis every activation + Poison as DFU's own empty delegate, chain cascade lever->spike (ActivateNext-first order, activationCount), the verbatim Receive trigger gate (Collision03 blocks Direct/passes WalkInto, chains always valid, MultiTrigger's exact trio, undefined flags never fire) |
| enemyattack.test.js | 3 | verbatim reset-timer arithmetic (Range/level/reflex terms, /980, 0-floor), the floored-speed >>3 roll gate, strike gating (range+sight+22.5deg yaw) + hit event at HIT_FRAME_MELEE |
| enemyentity.test.js | 3 | ClassFile verbatim 74-byte parse (incl the (a<<16)|(c<<8)|b shuffle) on a crafted record, class entity rules (level, HP roll bounds, skills clamp, career Speed, city-watch +3..6), monster rules (predefined level, inclusive HP range, armor*5) |
| flc.test.js | 12 | F1 - THE FLIC READER (FlcFile.cs), the port's ELEVENTH format reader: Daggerfall's .CEL animations are Autodesk FLICs, a different format from the .VID movies, which is why the U22 video reader could not stand in for the chargen constellations. No ARENA2 in CI, so the decoders are pinned over SYNTHETIC FLIC bytes built to the spec (the book reader's method): the 128-byte header with its SIGNED magic and the 1/1000s frame delay, NumOfFrames+1 frame slots (the extra is the RING frame that loops back), the .FLC/.CEL extension gate; COLOR_256 including the count-byte-0-means-256 quirk and the transparency switch blanking only a matching entry; BYTE_RUN's runs and literals; DELTA_FLC's paired-pixel packets; a PSTAMP thumbnail skipped whole with the chunk after it still decoding; and a bad frame type refusing to play. THE BOTTOM-UP QUIRK is pinned both ways - FLIC row 0 lands in the buffer's LAST row (Unity's texture origin, not the format's) and getFrame() flips it to the top-down shape every other reader here produces. Plus the ARENA2-gated sweep: every .CEL decodes and every frame including the ring frame buffers. AUDIT F added: DELTA_FLC advancing x by PAIRS proven with TWO packets on one line (the single-packet pin could not see the advance at all), an out-of-range pixel write THROWING rather than vanishing (C#'s Color32[] indexer throws where a JS typed array drops the write - the same emulation the VID reader carries), and AUDIT F-P2, which the PLAYER pin caught: C#'s FrameHeader[] is a VALUE-TYPE array, so a slot the frame walk never filled is a DEFAULT struct that decodes as an empty frame and STILL ADVANCES CurrentFrame - the port's defensive refusal instead stalled the counter, so a non-looping player could never reach NumOfFrames and its OnAnimEnd never fired (an animation gating input would have hung for ever). Plus the PLAYER itself (FLCPlayer.cs): Update's order verbatim - the end check FIRST, then the frame clock, then display-the-current-buffer-and-only-then-decode-the-next, so the displayed frame is always the previously decoded one and the ring frame is never shown; the clock gating the first frame; OnAnimEnd raising exactly once; a stopped player displaying nothing; and Loop = true never ending, with start() rewinding. The F2 audit added the ONE-TICK-AT-A-TIME pin: the old 20-tick loop proved only "it ends eventually" and hid both halves of Update's order, so the end check running FIRST (the ring frame never shown, nothing displayed on the ending tick) and display-then-decode are now stepped and asserted per tick |
| fnt.test.js | 2 | verbatim FNT layout on crafted bytes (header, 240-entry table, the L/R half SWAP with MSB-first expansion - x0 from the odd byte, x15 from the even, bounds null), the white atlas (256x240, pixel spot-checks) + measure (space rule, 1px classic spacing) + row-1 cell UVs |
| hudtext.test.js | 2 | the popup queue (4-line cap dropping oldest, per-line 2s life with independent lifetimes across a late add, expiry, empty ticks safe) | (AUDIT 18: the popup now draws from the TOP as DFU does)
| hud.test.js | 3 | compass scroll verbatim (trunc(258 x heading), wrap both directions, the 64 window never exceeds the 322 strip), bottom-anchored bar fill (v-window + clamps + max-0 guard) + integer scale flooring at 1, indexed->RGBA with the classic index-0 transparency |
| inventory.test.js | 5 | the verbatim stackable rule (equipped/enchanted never), AddItem merge-vs-append incl material split, CalculateWeightForMaterial verbatim (quarter-kg quantized, banker half-to-even: iron and daedric daggers BOTH 0.5kg) + leather formula + transferAll, the house-container predicate (418xx group, the 13-index list, shop-shelf 5 excluded, record = id%100) , removeOne (stack decrement / singleton splice / absent false - the per-loose Arrow consume) |
| inputmap.test.js | 3 | the binding tables (overlay chars/controls/null, F5/F6/Backspace/KeyC/none), routeKey precedence (overlay wins so Backspace edits not opens, toggles fire, the cast dir threads, unconsumed falls through), the one-shot click-cast latch (unarmed nothing, fires once, double-arm single fire) |
| inventoryui.test.js | 6 | the spellbook (the interim known-list from the file excluding caster-only/non-damage, an entity's own book preferred, ready callback, null-map empty). S30 added the MANAGEMENT laws run against the real window: delete arms the YesNo and No/Escape close the BOX not the book while Yes removes the SELECTED spell in place with the selection clamping, the curse tags (vampire/lycanthrope, PlayerEntity.cs:41-42) refuse BEFORE the prompt with no YesNo and nothing deleted, u/j swap bounds-guarded with the cursor FOLLOWING the moved spell, sort confirms then goes alpha first and point-cost when the sequence did not change (SortSpellsConfirm's SequenceEqual arm), and every mutation lands on entity.spells ITSELF - the window list IS the book by reference, which is the persistence story (save.js:79 maps that array to SPELLS.STD indexes in order), so the pin fails if the window ever starts copying. U26 removed this file's inventory test along with the keyed window it opened: the arrow exclusion it pinned was never the window's - it lives in systems/equip.js and is pinned in audit18_ui_native F13 against the native window - and cursor wrapping belonged to the list widget the native scroller replaced |
| loot.test.js | 3 | 22 matrix rows exact (key C pinned) + brace-bounded group lists (no cross-enum bleed), gold x level + the WP halving loop at exact roll consumption + the '-'-key empty path, level-split ingredients (C1 scales/C3 flat), arrows stack/material, book template+variant, gender clothing, unknown-key -> '-' |
| enemyequipment.test.js | 4 | verbatim material walk ([64,128,...] modifier table, level clamps), armor materials (70/90 split, plate+weapon-material), variant-0 loadout + the init-100/subtract/class-60-clamp armor pass, variant-2 + monster keep-better + city-watch itemLevel-1 + weapon-vs-weaponless averages; G3 - equipmentItems mirrors DFU's Items.AddItem (weapon + shield armor item, no marker duplicate) |
| poisons.test.js | 7 | S19b poisons: the enum + timing tables pinned (weapon poisons 0-7 vs drugs 8-11, Arsenic 10/10/20-1000, Moonseed instant), Start rolling both Range(min,max+1) spans with an incumbent same-poison no-stack that still consumes the discarded instance's rolls, the Waiting countdown ticking its FIRST active minute at 0, minute catch-up running straight to Complete with negative statMods persisting until healed (heal-all cures on the NEXT CompletePoison round - never directly), drugs pushing POSITIVE mods stripped ONCE at Complete (Sursum's +str crash pinned) with Somnalius/Quaesto Vil fatigue x64 vs Aegrotat's RAW magicka, InflictPoison (career Poison-immunity veto, the full-save resist, the classic level-1 immunity, bypassResistance skipping the save), the ItemHelper weapon-poison roll (level gate, Orc/Centaur/OrcSergeant + class eligibility, Assassin 60%, Range(128,136) weapon poisons only) and the formulas seam inflicting ONCE on a damaging hit then clearing the blade (a miss keeps it), plus cure + the save round-trip carrying the entry |
| diseases.test.js | 13 | S18 diseases: the 17-row DiseaseData table byte-pinned (multiplier columns, damage spans, only Caliron's Curse/Blood Rot/Wizard Fever finite at 3-18 days) + the 0xff/0xfe markers and Diseases ids, startDisease gates (player-only, level >= 2, AddState's no-double-catch, the finite Range(min, max+1) day roll), InflictDisease (level-1 classic immunity consuming NO rolls, the full-save resist, uniform list pick), the daily tick (infection day = silent incubation, one damage roll per elapsed day through the multiplier matrix with statMods accumulating negative, daysPast catch-up rolling each day but alerting once per round, HEA hurting while FAT/SPL drain RAW units - DecreaseFatigue WITHOUT the x64, bug-for-bug), a finite disease's final day landing damage then ending with mods lifting when the same round's tick removes the entry, Heal{Attribute} healing disease statMods clamped at 0 WITHOUT ending the disease (it drains again next day), the OnMonsterHit rider table (rat 5%/bat 2% listB, zombie 2%/mummy 5% listC, nymph/lamia fatigue damage x2 x64, the vampire 0.6/2.0 float branch with vampirism ROUTED, werewolf consuming its roll, spider paralysis FLAGGED to S19), the multi-attack loop firing onMonsterHit per LANDED hit at the FormulaHelper.cs:662 site, and the save round-trip detaching statMods both directions; the S19 spider/scorpion rider (free-cast Spider Touch 66 through the castParalyze closure with no rolls, scorpion sharing the row, the incumbent-paralysis veto) |
| mobileunit.test.js | 10 | C11 classic sprite mobiles (DaggerfallMobileUnit/EnemyBasics verbatim): the record layout tables (Move 0-4/Attack 5-9/Hurt 10-14/Idle 15-19, back diagonals mirrored, speeds 6/10/4/4) + the quirk sets (rat idle inversion, ghost/wraith patterns, slaughterfish bounce) + GetStateAnims branch order; the signed-angle orientation law (front 0/back 4/symmetric flanks, yaw-relative); the attack chance ladder + the -1 damage marker (skipped, one hitFrame) + iterator-exhaust revert to idle; state transitions (hasIdle=false frame carry across idle<->move, hurt one-shot, the bounce reverse at n-2); the Giant Scorpion flip INVERSION + the orientation-switch frame RESCALE (frame*newN/oldN - the Ancient Lich 8-vs-4-frame overflow); the regenerated EnemyBasics anim fields (rat/imp sequences verbatim, all 42 monsters carry frames, >=5 with chance variants); AUDIT 08-17: the LEADING -1 (Frost Daedra's 50% [-1,4,5,0] variant - hitFrame flags at attack start, display advances to the second entry, exactly one damage moment) + the DFU priority (the attack EDGE overrides hurt, knockback-hurt never interrupts PrimaryAttack) + the FlyAnimSpeed 10 clock on flyers' move/idle (walkers hold at 6/4); C14 the Spell state (HasSpellAnimation -> records 20-24 for the Orc Shaman alone, everyone else casts over the primary records with NO ghost/wraith special, the shaman one-shot [0,0,1,2,3,3,3] playout to idle, and the interrupt laws: attack cuts a cast, hurt cuts a cast, a cast never cuts an attack); C16 the -1 frames ARE the damage moments (the Frost Daedra BASE swing lands TWO); C17 humanoids (FemaleThiefIdleAnims record-11 diagonals verbatim + the 483-female route, the ranged one-shot on records 20-24 with the -1 as shootFrame NOT hitFrame - one loose per draw - and the class extraction pins: mage 486/485, the shared archer sequence, thief 483, the 475 cast-scale class, corpse 380) |
| enemyspells.test.js | 5 | S16 enemy spellcasting: the verbatim EnemyEntity spell lists (13 monster casters spot+count pinned, EnemyClassSpells order, element cast sounds 352/353/350/351/349), SetEnemySpells (MaxMagicka 10xlevel+100, the six magic skills pinned at 80 over the flat fill via skillOverrides, missing SPELLS.STD records skipped loudly), the SetEnemyCareer assignment tail (monster lists, class level/3 buckets capped at 6, non-casters empty), EffectsAlreadyOnTarget (all-live vetoes, instants never count, per-stat identity), and the classic AI decisions (DoTouchSpell resetting the SHARED melee timer, the ranged 1/40 classic-update roll inside the 6..51.2 band, bow precedence, magicka-0 refusal, CasterOnly counting as touch) |
| enemymotor.test.js | 13 | C15 knockback (the WeaponManager formula exact + the 15-classic floor, monster/class weights 40/240/350, the motor shove along the attack ray INSTEAD of pursuit with the grounded y-drop (SimpleMove), hurtKnock riding the 5-classic threshold, the 5-per-classic decay to 0 with pursuit resuming after, and a knocked FLYER taking the full 3D ray AND falling - flyerFalls), C12 behaviour motors (flying 3D face-pursuit + gravity-free hover + MeleeDistance stop at striking height, aquatic WaterMove - the 2.5 surface margin cap, beached and waterless-block freezes with NO gravity, horizontal close - and the paralysis triad: flyers FALL, swimmers freeze, walkers stop pursuing but keep gravity), P17 fixed stepping (a 10fps foe pursues to the BIT-IDENTICAL spot/yaw as a 60fps foe - same 1/60 decomposition - and a 10s hitch integrates at most the 0.25 clamp), verbatim classic AI constants (sight/hearing/FOV/melee/turn/update-rate/system-divisor/walk-base), 11.25deg turn clamp, LOS wall + FOV gates, pursue-and-stop at MeleeDistance on the classic cadence; P13 stealth (the 2*((dist*Stealth)>>10) chance pins, the classic spawn/despawn bands with the 384-unit y hysteresis + the 1094 cap, the illusion gate - sees-through exemption, invisible always blocked, blending 8%/shade 4% with NO roll for unconcealed targets - and the flow: a quiet unseen player inside 25 units stays HIDDEN (hearing gates on prior detection), a failed stealth roll detects + sets hasEncounteredPlayer, earshot then holds it, the once-per-classic-minute cache, the odd-minute sneak skip and fast-move auto-detect both consuming NO rolls, ONE Stealth tally per minute across all foes, and chameleon blocking a plain-sight detection until the see-through roll pierces) |
| enterexit.test.js | 6 | verbatim landing offsets, door transforms, landing selection, dungeon exit, ladder climb, FixStanding floor snap |
| interior.test.js | 8 | ModelDoor extraction, static doors, interior layout, 6832-interior corpus, R8 light offsets + MAGEAA00 pins |
| people.test.js | 2 | AddPeople position/data verbatim, 14174-people corpus (6724/6832 interiors, archives 176-184) |
| poses.test.js | 1 | Static pose table (POSES): melee1H well-formed, sagittal angles within joint ranges |
| weapons.test.js | 2 | Verbatim DFU weapon data pins (enums, damage table, material mods/dyes, ApplyWeaponMaterial math incl. half-to-even weight round); Longsword piece well-formed on the armR fist |
| names.test.js | 5 | Region->bank verbatim, deterministic name composition on DFRandom, Redguard female stream parity, 76-NPC exterior corpus (SENT7 lamp quirk); S17 MonsterName (the engine-PRNG bank pick vs the forced bankType on one DFRandom stream, Monster2-female D set, both Monster3 branches, seed determinism) |
| enemies.test.js | 6 | 45x20 encounter tables, classic pick replay, fixed/passive/gender/water rules (gender FIXED-only: random flags = slot), the LIVE playerLevel banding (level 18 vs 1 differ on the same stream), Privateer's Hold pin (42 enemies / 25 fixed) |
| rig.test.js | 2 | Vendored Rewrite rig byte-parity vs canonical (16 bare-humanoid hashes), 710-face shape |
| rigmath.test.js | 5 | Two-bone IK invariants: exact bone lengths, reach/clamp, pole side, degenerate-target guard |
| paperdollart.test.js | 3 | Variant/cloak record resolution, material addressing (bases+morphology+clamps), plate-vs-leather band pins on 251 |
| piece.test.js | 2 | Sprite-shell geometry round-trip (synthetic + real plate-cuirass 1:1 witness + dye resolve). DAGGER_SPEC profile/pin cases removed with the trace rig. |
| neutral.test.js | 2 | Neutral paperdoll (buildNeutralBody): well-formed mesh (quad/normal/rgb shape, unit normals, on-ramp colours, feet-grounded ~7.5-head height), deterministic + snapped (blocky) shading |
| clothSim.test.js | 4 | Draped-garment cloth sim (verlet): stable (no NaN/explosion over 400 steps) for skirt/robe/cape; no clipping - every free particle stays outside the measured body collider after 500 steps under gravity + wind |
| relief.test.js | 4 | Relief identity witness + field arithmetic pins (synthetic + whole classic body), offsets pin (222,41 / 237,44), back shell mirrors z on the shared silhouette |
| charsprite.test.js | 1 | shared pixelize pass: projection-exact ph/pw at CHAR_PIXEL, hand-computed center/half-extents |
| backstab.test.js | 3 | verbatim 8-orientation back-facing wheel (records 3/4, Unity half-to-even at 112.5/157.5, sign-symmetric), backstab channels (chance on chanceToHitMod, x3 post-calc roll), FP pose composition (base+clip, couplings zeroed) |
| charmesh.test.js | 2 | Face packing (fan order, color/normal interleave), bare-humanoid pack + bounds |
| orcbody.test.js | 14 | The rig's BUILD girth spec + the orc line. The load-bearing pin is the INERTNESS of opts.build: no opts / {} / BUILD_IDENTITY all return a byte-identical face list, because raceCharacter, engineRig and villagerRender call the rig with no build and the villager payload is packed as a DELTA against that exact list - a silent shift would corrupt 25 villagers and every race body at once. Plus: a partial build keeps the unnamed keys at identity, every design changes geometry WITHOUT changing the face count (the delta packing depends on it), out-of-band and junk values clamp to 0.6..1.6 or fall back to identity rather than tear a seam, every shipped build value sits inside that band, the four designs' level/damage/team are pinned against their ENEMY_BASICS rows (7/12/21/24) so a table regen cannot leave a silhouette tuned to a stat the enemy lost, the melee three escalate monotonically while the shaman (chanceForAttack2 20, the caster) sits deliberately off that curve with the longest tusks, orcOpts resolves every named material dark->light out of ART_PAL with no zone referencing an undefined one, and the tusk/brow pieces are finite, quad, head-tagged and shaded - a bigger tusk grows LONGER from the same socket rather than walking down the chin, and both track the jaw they anchor to |
| undeadbody.test.js | 20 | The undead line (Zombie, Mummy) on the mechanism the orc line proved. The load-bearing pin is that every BUILD key a design declares is one the rig ACTUALLY HAS: BUILD_IDENTITY is torso/shoulder/arm/hand/neck/skull/jaw/leg, the rig spreads a spec over its own keys, and an invented one is dropped in silence. `gut: 1.24` on the zombie would have shipped a corpse with a flat stomach and nothing anywhere to say why - it is a displace zone now, and the swell is asserted rather than assumed. Also: zone materials are declared, and both ride the base face list so they can ship as deltas. THE SKELETON is the third design and the first that needed GEOMETRY: ribs are separate bones with gaps between them and no girth multiplier will ever put a hole in a closed loft, so its silhouette comes from a piece (pieces/skeletonBones.js) while the body is scaled away under it. Pinned: only it carries bones, its torso and neck are below 0.7 or it is a man in a rib costume, the cage is six hoops and a sternum rather than a solid barrel, the pelvis is two blades with a gap, and every bone is tagged 'body' so it rides the torso through a swing instead of hanging in the air. THE LICH is the composition test: every enemy before it carried ONE kind of thing (a tusk, a cage, a body) and a lich is a skeleton IN ROBES, needing the piece table and the drape path to share a figure when they had never been asked to. Pinned: it carries both, its robe material is declared, and the drape survives undeadOpts — without which the viewer never sees it. The bones check that read "only the skeleton" failed correctly when the lich arrived and now says what is actually true: bones go to the bone-bodied, and nothing with flesh on it gets a ribcage — and that check now states the RULE rather than listing its members, having broken twice (once for the lich, once for the ancient lich) while only keeping a register. THE VAMPIRES are the first designs here that keep their body, so they say everything with COLOUR: pinned that the pallor reads at least 1.8x the coat, or the whole figure is a silhouette and the design has said nothing, and that their build stays inside 0.85-1.2 because a body they kept is a body. THE DRAPE FIT: a garment is carved ONCE per name and shared by every design that wears it, which is what keeps the payload small — but the body under it is resized by its build spec and nothing was telling the cloth, so every drape sat at scale 1.000 whoever wore it and the wearer came through. Pinned: the fit exceeds the wearer's torso, armour under a garment pushes it further out (two designs of equal girth and unequal plate must not get the same fit), and the scale is GIRTH ONLY - the check reads the actual scale.set call and fails if Y is ever touched, because every exported constant on this rig is a height and scaling a garment vertically drops its hem through the floor on a giant. SPECTRAL (Ghost, Wraith) is the first design here that is not a SHAPE: every enemy before them said what it was with geometry and colour, and a ghost has nothing to add to the figure and everything to take away - what makes it one is that you see through it, which is a MATERIAL property the character path had never been asked for. Pinned: opacity stays between 0.15 and 0.7 (below is a smear, above is not see-through), a wraith is fainter AND thinner than a ghost or the two are the same enemy in different blue, and - the one that bites - the transparency does not OUTLIVE the ghost: the body material is shared by every design, so setting it for a ghost and forgetting to clear it makes the next orc you pick a see-through orc. |
| humanclasses.test.js | 10 | The human class enemies - Mage, Sorcerer, Battlemage, Bard, Thief - collectively the commonest thing in a dungeon (mapChance 3,3,2,2,2) and the last large group with no rig. They are just PEOPLE: no geometry, no new mechanism, a human build barely touched and everything said with clothing. The load-bearing pin is that they SCALE: every other design carries a level and a damage band out of ENEMY_BASICS and these carry NEITHER, because Daggerfall builds them to whoever walks in - a made-up number would be a lie about the game's own rules, so `null` is the assertion and the viewer prints "scales". Also pinned: the BATTLEMAGE's robe clears his plate, and a mage in the same body without plate gets a TIGHTER fit or the clearance term is not reading the armour at all. He was built after the drape-fit change, which is the only honest way to check it holds for a design it was not written against. THE FILE NOW CARRIES NINETEEN, every human the player meets plus the six that only come by quest, and the added pins are about them being NINETEEN PEOPLE rather than one person nineteen times: no two designs may share a silhouette-plus-materials-plus-drape signature (which caught Assassin and Nightblade shipping as literally the same figure under two names), the torso spread must exceed 0.25 or the cloth is doing all the work, and plate must broaden its wearer more than robes do or the armour is paint. The "is it a person" band was widened from 0.9-1.15 to 0.85-1.32 with cause: it had been drawn around the first five designs, which were all scholars, and a man in full plate IS broader than one in robes. |
| atronachs.test.js | 6 | Fire, Ice, Iron and Flesh - the only group in ENEMY_BASICS whose members are STATISTICALLY IDENTICAL (all four level 16, 5-15 damage, 25-130 health, armour 6). The game tells them apart by element and by nothing else, so this is a design problem with the numbers taken away and the pins are all about difference: no two may share a silhouette, iron outweighs flesh which outweighs flame, and THREE RENDER MODES must survive across four designs - fire ADDS light to what is behind it (additive), ice subtracts itself from it (transparent, and denser than a ghost or it IS a blue ghost), iron and flesh do neither. Plus the same trap the ghost opened: one material is shared by every design in the editor, so the additive blend must not OUTLIVE the fire or the next orc glows. |
| roster.test.js | 6 | THE ROSTER, ENFORCED. Every enemy in ENEMY_BASICS has a voxel design, with one deliberate exception, and this exists so that "which enemies are built" is a question the SUITE answers rather than one somebody has to count - adding an enemy to the game without a body is a failure now rather than a thing nobody notices. The exception is Horse_Invalid: no level, no damage band, no health, affinity None. It is a hole in Daggerfall's own table rather than a creature, and building a body for it would be inventing an enemy the game does not have - so a second test asserts it STILL has no stats, because the moment it does the exclusion stops being honest. Also pinned: no enemy is built twice by two lines, and every design names a real MobileType. Plus the DRAPE SELECTION, which is the third time "a design wears what its design says and nothing else" has had to be enforced somewhere: first the loose armour pieces that stayed on when a villager was picked, then the drape FIT that sized a garment for the wrong body, and now the selection itself. applyVillagerDrape(null) means "no design is selected, leave the control where the user put it" - right for the bare rig, where cycling garments is the point, and absurd for an enemy. Handing it null for every design WITHOUT a drape meant a bat wore whatever happened to be showing: cycle to a skirt, pick an animal, and the animal is in a skirt. The design is passed now so it can say NONE, and a second test asserts the nine animals ask for none - if somebody ever gives a slaughterfish a gown they have to mean it. |
| beasts.test.js | 17 | Giant Rat, Grizzly Bear, Sabertooth Tiger - the first enemies with NO HUMAN IN THEM. Every other design is a person underneath (scaled, stripped, robed, or with a horse hung off him); these collapse the rig's WHOLE body, all six groups, using the mechanism the centaur needed for two, and what the player sees is entirely the piece. Pinned: every group is collapsed or a human arm hangs inside a bear; each animal stands ON the ground rather than through it or above it; one builder produces three animals that are provably not each other (the rat lower, the bear broader, the cat longer) or the parameters are decoration; each has its own pelt because three brown animals is one animal; and - the one that bit - THE PIECE MUST EXIST. Adding 'beast' to PIECE_KINDS without building a table for the line left all three rendering as NOTHING, because the rig under them is collapsed and there was not even a man left to see. Piece tables are keyed by line now. THE WEREBEASTS then split this file in two: a Daggerfall werewolf is NOT a wolf on four legs, it is a man with the wrong head, so it collapses ONE group of six where a full beast collapses all of them. The "every group is collapsed" pin failed correctly when they arrived and now states the rule instead of the roster - collapse exactly what the piece replaces. Plus a CONTRAST pin, the second time contrast has bitten: the wereboar's bristle resolved to a mean of 58 against a background of about 20 and read NARROWER than a werewolf despite being measurably wider, because its edges went into the dark. THE CONTRAST FLOOR THEN MOVED FROM 70 TO 95, because 70 was set by the thing that FAILED rather than by the thing that has to pass: a spider at mean 100 cleared it and was still invisible, being a small body on thin legs with a fraction of a wereboar's area to be seen with. Area and brightness trade off, and the raised floor immediately caught the rat and the grizzly too — two animals shipped a commit earlier that nobody had looked at. ARACHNIDS are the third body plan in this file and get their own builder: the quadruped's assumes a BACKBONE with a leg near each corner, and a spider's legs radiate from one joint in a ring with a body of two lumps rather than a tube. The test of whether a mechanism is right is what happens to the thing that does not fit it — the beasts stretched from a rat to a bear on numbers alone, an arachnid does not stretch at all, and that is the mechanism telling the truth about its edges rather than being forced past them. WINGS are the last body plan: `behaviour: Flying` is the one category in ENEMY_BASICS that is neither foot nor fin, and a wing is a MEMBRANE ON FINGERS - what the eye reads is the sheet between the bones, so it is panels rather than boxes. Pinned: FOLD moves the wing BEHIND the animal rather than shrinking it (a folded bat is narrower AND deeper, and the face count must not change, or it is two pieces pretending to be one), a GIANT bat is sized like a giant one (the first cut used a real bat's proportions and made a flat strip at ankle height that the viewer's own UI sat on), and the same piece serves an imp - a small man who happens to have a pair - at a fold nothing like a bat's. The collapse check has now been rewritten THREE times for the same reason: each version described the kinds of design it knew, and the next design was a kind I had not thought of. It states the rule now - a group is collapsed exactly when something replaces it - and the imp, which collapses nothing, is the case that finally forced it - then the LAMIA became the fifth counterexample, replacing her LEGS with a fish tail, and the rule generalised properly at last: the expectation is whatever a design says it replaces. WATER is the last plan the builders could not say. A quadruped tapers nose to rump and stands on four legs; a fish tapers the same way and stands on NOTHING, which is not a leg length of zero because that leaves a barrel on the floor. One builder gives a whole slaughterfish and a lamia's tail - its `from` is why, since a tail starting at a waist is the same geometry as a fish starting at a head. Pinned: the tail meets the waist (at 0.72 it hung below the collapsed pelvis with daylight between, two animals in a stack), a lamia's SKIN is not her scales, and the dreugh's borrowed claws come without the arachnid's eight legs - legPairs 0, after it shipped standing on its own legs with a spider's underneath. The shared-pelt pin also learned to distinguish a whole fish from a tail: the same scales on two bodies is correct, two brown quadrupeds is one animal twice. |
| beastattack.test.js | 9 | ATTACKS FOR BODIES WITH NO ARMS. enemyAttack.js is a verbatim DFU port and its TIMING is right for everything - a bear and a knight attack on the same clock - but its own comment says it plays "the authored 1H strike clips", which are ARM animations. Nine enemies collapse their arm groups to a POINT: the timer fired, the hit landed, the damage applied, and the bear did not move. For these the motion belongs to the whole animal, because there are no joints to turn. Pinned: every armless design says HOW it reaches you and a design WITH arms does not get one (the lamia has a fish tail and a woman's arms, so she swings); the motion peaks within 0.08 of the frame the game's own primaryAttackAnimFrames marks with -1, so the timing is read rather than invented; a slaughterfish bites TWICE because it carries two -1 frames and nothing else does; the five kinds are provably different curves and a sting goes BACKWARD as it strikes, because the tail does the reaching; and every clip returns to rest - the gather held at full through the recovery and left a lunging animal 0.08 behind where it started, which after three attacks is standing somewhere else. THE STING then came OUT of the body: welded into buildArachnid, the only way to animate it was to move the whole scorpion, so a sting looked like the animal rocking backwards and the one part of it anybody watches did nothing. It ships as five POSES from coiled to thrown - cheaper than a skeleton for one appendage, and how the sprites this port replaces did it - and the attack clock picks one. Pinned: coiled stands UP over the back and thrown comes DOWN and forward, buildArachnid must NOT build it again (if it does the animation dies silently, because the body would still move and nobody would notice the tail had stopped), and only the scorpion has one. |
| daedra.test.js | 6 | Frost, Fire, Daedroth, Lord and Seducer - all minMetalToHit 5, which is what marks them a class in the game's own terms. THE POINT OF THE FILE IS HOW LITTLE IT COST: a Daedroth is the werewolf's design exactly (collapse the skull, beast head over it), a Fire Daedra is the fire atronach's additive blend, a Frost Daedra is the ice atronach's transparency at a higher density because a daedra is a THING and not a substance. The only new geometry across five enemies is a pair of horns. Pinned: each borrowed mechanism is actually borrowed rather than reinvented, a lizard has no ears, the lord is the broadest and the seducer the slightest, and - the one worth keeping - A GARMENT THAT SWALLOWS ITS WEARER IS THE WRONG GARMENT. The lord's Formal Cloak scaled to 1.49 on his build and hid the plate, the gold band and the horns; the drape fit was doing exactly what it should and the garment was simply wrong for him. Anything that wears one must be under a 1.1 shoulder or it vanishes inside it. |
| drapeclip.test.js | 3 | NO DRAPE CLIPS THROUGH A BODY PART. A garment is an ellipse of rx by rz at each row; a body point is inside it when (x/rx)^2 + (z/rz)^2 < 1. This walks every drape-wearing design, every row of its garment and every body vertex near that row's height. THE FIT USED TO BE GUESSED from torso and shoulder, and that guess cannot see the thing that decides the answer - ARMS HANG OUTSIDE THE TORSO. When the check was first written it found 669 clipping rows and every single one was an arm: a lich's stuck 111% beyond its own robe, because the guess cut that robe at 0.657 where the body needed 1.404. The fit is MEASURED now, at build time where the body and the garment grid are both in hand, and per ROW rather than as one number - a single scale is set by the arms and then applied to the hem and collar too, which turns a robe into a cone. The viewer still applies the widest row uniformly because every garment is VERLET CLOTH and its constraints hold the rest shape, so genuinely widening one means rebuilding it; the per-row data ships and waits for that. |
| paperdoll.test.js | 3 | 27-slot EquipSlots verbatim, 288-template DB (index resolution, Short Shirt pin), BlitItems order |
| dyes.test.js | 3 | Clothing dye range shifts (Blue identity, Red 0xEF), 11 metal tables extraction-pinned, band-only swap |
| equip.test.js | 5 | Slot-rule extraction pins (94 cases), hands verbatim, paired first-open, 2H right-hand rule, all-wearables corpus |
| maps.test.js | 9 | 62 regions, converters, climate, city + Privateer's |
| vid.test.js | 28 | U22 THE VID READER, VidFile.cs ported WHOLE - the tenth format and a true 1:1, unlike the song reader beside it. Corpus-swept over all 17 retail VIDs with TWO exactness gates: decoded video blocks == the header FrameCount, and the final stream position == the file size (98,343 to 19,685,242 bytes) - a one-byte desync anywhere could not land on the last byte. Per file: header fields, a per-type block histogram, chained FNV-1a over every frame buffer AND every audio buffer, total audio bytes, and the 740-sample delay floor engaging on exactly the 8 files whose shortest audio block is under 740 bytes. FOUR DFU QUIRKS PRESERVED as real behaviour rather than smoothed: a mid-stream palette desyncs the stream (streamPosition += 768 is clobbered by the tail assignment), frameDelay is recomputed from audioBuffer for EVERY block so a video block inherits the last audio delay and one before any audio block throws, a swallowed exception does NOT advance the stream so a bad block parks the reader forever, and Audio_StartFrame consumes its data before rejecting a bad PlaybackRate. The null-block 'not counted' quirk is pinned on real data (currentBlock == blocks - nulls, with 83 nulls in DAG2). C#'s IndexOutOfRangeException on Color32[] is emulated with an explicit bounds throw, because a JS typed array drops out-of-range writes SILENTLY and would have completed the block. Plus the player: the frame gate, the audio clip built verbatim (srcLength+2, pos=1, (b-128)/128) scheduled on the WebAudio clock, and the release-then-upload that stops a texture leaking per frame + THE SPLASH LETTERBOX, caught by the EYEBALL and not by a pin: the first live shot of ANIM0001 had a sky-blue border, the renderer's Iliac Bay clear showing around the native panel - the THIRD time that has bitten (U21 blacked it for the menu, U21b for chargen). Pinned synthetically so it runs without ARENA2: the FIRST quad of the frame loop is the solid backdrop, covering the whole real canvas rather than the 320x200 panel, in the SHARED MENU_BACKDROP constant rather than a third hand-written black. Mutation-proven both ways - dropping the backdrop, and sizing it to the native panel + AUDIT 19: the ERROR BOUNDARY playVideo never had - the reader throws OUTSIDE ReadBlock's try by design, so a truncated video killed the rAF chain and left the promise permanently pending, which main.js awaits, so it was a permanent black screen and the file's own NEVER TRAPS header was false; pinned on both the reader half and the draw half. THE SPLASH WAS UNCONDITIONALLY SILENT and the source blamed the wrong thing: the player resolves its AudioContext once at construction and nothing had booted an engine by splash time, so there was no context to resolve - the gesture rule gates whether a context RUNS, not whether one EXISTS. The context is now built eagerly and suspended, and is no longer gated on DAGGER.SND loading, which music and video do not read. Measured live: 70 audio clips scheduled where it scheduled zero. Plus the frame pin that compared the buffer WITH ITSELF - it reached through .buffer, discarding offset and length, so uploading a ONE-PIXEL view passed; the renderer had the same blind spot, which is why neither caught it + THE SUSPENDED-CLOCK PIN: a context that exists but is not running (autoplay policy, repeat visit) has currentTime pinned at 0, and pacing on it froze the splash at its first frame - solid black for ANIM0001; a non-running context is treated as absent (wall clock, silent, no clips), a stub with no state field still counts as running, and the boot-order pin now demands ensureAudio UN-awaited with the context minted in its synchronous prefix, so the splash no longer parks on black behind the DAGGER.SND + MIDI.BSA loads |
| music.test.js | 48 | A5 MUSIC, and the pins are split by what can be PROVEN. The PORT half is SongManager.cs: the playlists whole rather than sampled (a sampled table lets a drifted row through - the 17d lesson), DFU's DUPLICATE entries preserved because a duplicate doubles that song's odds (FM_DNGN1 and 15FM twice in the FM dungeon list, FSNEAK2 twice in FM sneaking), and SelectCurrentSong arm by arm - taverns index gameDays DIRECTLY so they walk in sequence day to day, dungeons seed DFRandom with unknown2 XOR (region<<8) with the region masked to a byte as DFU's cast does, everything else seeds on gameDays so a song is stable all day, and A LIST OF ONE returns index 0 WITHOUT consuming the generator (the length>1 guard - if it seeded, entering a temple would shift every later roll in the session). Corpus-gated: every name in every playlist resolves to a real MIDI.BSA record and the picks DECODE, so a typo cannot become a song that silently never plays. The SCHEDULER half is pure: the lookahead window is HALF-OPEN (at 480 PPQN a closed one retriggers every beat), the window provably exceeds the wake interval or notes fall through the gap, and channel state FOLDS FORWARD so tick-0 program/volume survives into second three instead of the opening playing on the wrong instrument and silently 'fixing itself'. The SYNTH half is OURS and is pinned for STRUCTURE, not taste - there is no source to be right against, and a pin asserting 'program 48 is a sawtooth' would assert my own choices back at me (the vacuous-pin shape two audits have caught here). So: all 128 GM programs resolve into their GM family with finite ordered envelopes and a non-zero attack (a zero ramp clicks), out-of-range programs and notes CLAMP rather than throw because the reader hands through whatever the bytes said, no percussion key is ever silent, and no conversion can put a NaN into the audio graph. A440 at note 69 is the one piece of real law and is exact. Mutation-proven eight ways + A5b FM AND THE OUTDOOR/TAVERN ARMS: the voice bank is now two-operator FM - the OPL synthesis Daggerfall was scored for, which is why the archive carries an F*/FM* arrangement of nearly every song - and its ratios are OURS, pinned for structure like the subtractive specs were (no OPL bank exists to read: it lived in HMI's driver, ARENA2 has no .AD/.BNK/.OPL and the song headers carry a device map and zeros where patches would be, checked byte by byte). Every program resolves to a finite voice with a POSITIVE carrier ratio (0 Hz is silence) and a NON-ZERO index decay (exponentialRamp needs it). AssignPlaylist's outdoor arms verbatim - night overrides weather entirely, Fog folds in with Overcast and Thunder with Rain, and an unrecognised weather falls to Sunny by DFU's OWN default arm rather than going silent. THE CORPUS PIN EARNED ITS KEEP: it caught a NIGHT_SONGS_FM list I had extrapolated from the GM shape - DFU's FM night array has SIX entries to the GM list's seven and there is no 10FM record in MIDI.BSA at all, which is exactly the asymmetry DFU's own :818 note describes. Plus the host sweep - all four hosts wired, taverns taking the DIRECT gameDays arm + AUDIT 19: the SIXTEEN playlists the first pass never ported and the fourteen AssignPlaylist arms that went with them - castle, court, shop, Mages Guild, plain interior, fighter trainers, palace and the FM tavern, every one resolving to a real MIDI.BSA record, so 'not ported' meant music the player could never hear rather than a missing asset. The whole switch is pinned arm by arm, including the two that LOOK like the day/night gate and are not - a dungeon exterior and a graveyard take NIGHT songs at any hour, pinned at noon - and the arrested check that precedes the switch entirely. The building fold too: eleven mercantile types collapsing to one Shop, a GuildHall that is the Mages Guild only when its faction says so, a Temple that is FighterTrainers for the Fighters Guild and otherwise takes its god's alignment, and DFU's own `default` so an unmapped building plays the interior list rather than nothing. Mutation-proven four ways - reading the clock for a graveyard, moving the arrested check, making every GuildHall the Mages Guild, dropping one type from the shop fold + AUDIT 19 THE 1:1 PASS - SongManager's ENGINE, not just its tables. The port had the playlists and none of the loop: hosts called playFrom at moments they chose, where DFU rebuilds a CONTEXT every frame and reacts to the difference (UpdateSong :198-231). Three behaviours are unreachable without it and are now pinned - a new DAY or a new LOCATION re-picks even when the playlist is IDENTICAL (a playlist-only comparison cannot express it, the lists being the same object), locationIndex is in the context at all, and a finished song re-evaluates the context before the next is chosen. PlayCurrentSong's guard is pinned BOTH ways: it must not restart the song already playing, and it MUST replay one that ended - DFU's guard reads Song == currentSong AND IsPlaying, and the second half is what makes a finished song eligible again. Plus UpdatePlayerMusicEnvironment's whole switch with HomePoor sitting among the DUNGEON exteriors, the rect test that precedes it, the castle arm, the temple/god faction tables index-for-index, PlayNext/PlayPrevious wrapping at both ends, and StopPlaying latching. THE TWO INPUTS EARLIER PASSES CALLED UNAVAILABLE ARE BOTH REAL, pinned on the archive: the dungeon seed (unknown2 XOR region) over 4,232 dungeons giving 3,769 distinct keys and a near-uniform spread across the 15-song list, and the building faction id over 634 temples resolving to all three alignments plus the Fighters Guild, with 119 of 237 guild halls the Mages Guild + CONTINUOUS CONTROLS: pitch bend and CC7 volume used to be read once at NOTE START, so the 15,017 controller events the archive puts INSIDE a sounding note were all inaudible until the next note began. Bends are scheduled on the sounding voices' detune params at their own tick, and channel volume moved to a PER-CHANNEL gain node - which also stops it fighting the per-note envelope, as modulating the voice's own gain would have. And the ARRESTED flag, whose single consumer is the music: PlayerEntity.CourtWindow sets it and OnPop clears it, and AssignPlaylist checks it FIRST, so without it CourtSongs was unreachable + AUDIT 21 pinned what a mutation campaign found LOOSE in the 1:1 engine: the two guild faction ids (FactionFile.cs:89-90 - a wrong id silently routes the Mages Guild and the fighter trainers to the plain interior), DFRegion's LOCATION_TYPES enum WHOLE (the environment switch is written by NAME, so a drifted value moves a location into another arm and nothing else notices), the manager actually ROUTING the tavern arm (pinned by walking the list in sequence twice around, which no other arm does), playPrevious/playNext at the boundary rather than near it (testing only from index 0 left the comparison free to be `< 1`), and playSong initialising TRUE + AUDIT 21 F1: the director was fed AFTER the modal early return in both outdoor hosts. worldModes.frame() returns TRUE for interior and dungeon and musicContext() returns null ONLY for exterior - the SAME predicate - so the context was fed exclusively on frames where the overlay was guaranteed null, and the whole interior/dungeon music path was dead code. A source ORDER pin, stated as the weak instrument it is; the real proof is tools/musicHostProbe.mjs, which walks a browser into a real building and reads the resolved environment on both sides (outdoors GDAY___D.HMI/city -> inside 23.HMI/interior, and with the ordering reverted the inside reads city and the song never changes) |
| hmi.test.js | 9 | MIDI.BSA, the ELEVENTH format reader and the first with NO DFU SOURCE AT ALL - DFU never reads this archive (DaggerfallSongPlayer loads 133 pre-converted .mid files plus an SF2 from Unity Resources), so every claim is established from the shipped bytes and corpus-gated over the whole retail archive. The slice was commissioned as XMI and the bytes refused: there is no FORM/XDIR/CAT/ XMID chunk anywhere, only 131 "HMI-MIDISONG061595" songs holding 1286 "HMI-MIDITRACK" tracks, and HMI's deltas are ordinary MIDI VLQs where XMI's are interval counts. TWO INVARIANTS, and AUDIT 19 corrected how strong the first one is. The byte-exact gate - all 1286 tracks consuming to FF 2F 00 exactly at the next track's offset - was described as proving the 0xFE event sizes, and it does not: 0x14 = 1 or 2 lands every track byte-exactly too, while blowing D1.HMI from 26,926 to 2,123,950 ticks. It DOES determine 0x15 = 7 (size 6 makes 130 of 131 songs throw) and the self-checking 0x10. What discriminates the rest is a SECOND invariant now pinned: the tracks of a song END TOGETHER - 123 of 131 songs have every track finishing on the same tick with the decoded sizes, and 4 of 131 with a wrong 0x14. 0x12/0x13 appear once each in the whole archive and are determined by NEITHER check; they are recorded as an assumption rather than dressed up as proven. The reader also ENFORCES the gate now instead of computing it and discarding it, and an unknown status byte throws rather than decoding as a pitch bend on a phantom channel. The track table (u32@0xE4 count, u32@0xE8 offsets) is pinned against the scanned signatures at 0 mismatches. Plus the merged tick-ordered event stream with note-offs sorting before note-ons at equal ticks EXCEPT a zero-duration note's own; and the corpus quirks preserved rather than smoothed - SUNNYDAY track 5's real 1,192,560-tick delta, TAVERN track 22 the only track whose header count disagrees with its stream and the only holder of FE 12/13, FOLK3's two FE 14 and no FE 15. Nothing decodes to garbage silently: unknown FE subtypes, misaligned data bytes, over-long VLQs and missing end-of-track all THROW with song name and offset |
| snd.test.js | 3 | 459 sounds, byte-exact header, zero-length record 5 |
| climate.test.js | 5 | applyClimate verbatim rules, texture-info classification, exterior-window table, nature/ground archives, 8820-combo corpus sweep (735 pairs, 0 missing) |
| spectral.test.js | 2 | SetSpectral constants + gray remap (eyes 14->247, 96-index), the V^1.9 emission lerp (red eyes, body toward black) on a hand-built albedo |
| streaming.test.js | 7 | world-coord conversions, nearest-first 7x7, crossing offsets + column swaps, two-pixel jumps, vertical recenter, 30-crossing floating-origin invariant, 2000-step invariant fuzz |
| terrain.test.js | 14 | WOODS.WLD reader + corpus pins, perlin, cubic, sampler pins, umRandom (float + int), marching-squares lookup, blend flattening, Daggerfall-on-terrain integration, 15251-location corpus sweep, terrain key, nature scatter rules + integration |
| weather.test.js | 4 | verbatim fog tables, mapping, offsets, scales, fog math, lightning strobe |
| window.test.js | 2 | MaterialReader style constants, real glass-texel mask pins |
| world.test.js | 13 | mat4, meshReader, rmbLayout, location grid, flats, nature-quirk pin, city lights, R9 tilemap conversion + grid pins |
| audit18.test.js | 19 | AUDIT 18 pins, each mutation-proven: F1 GetBaseDamageMin/Max resolve the TEMPLATE (every WEAPONS entry finite and table-exact at both roll ends, the baked-field path unreachable, chooseEnemyWeapon's average over templates, and every weapon assignStartingGear mints carrying a templateIndex across all 18 classes); F2 the ingest diet vs live readers - a RULE that re-derives the fetchBytes/getBytes name list from src/ on every run and asserts KEEP() accepts each on BOTH diets (lean and desktop), names the three it starved (CLASSES.DAT, FACTION.TXT, all 18 BIOG**T0.TXT), holds the template-built CLASS**.CFG range, and still refuses the bulk the diet exists to refuse; F4 Vector3.Normalize multiplies by the reciprocal (ARCH3D record 1162's 4-point plane, expected UVs taken from DFU's OWN Mono-compiled output, not the port's); F5 the overlay clock - a structural rule that EVERY host branch returning on uiOverlayActive must call tickOverlay first (both dungeon-mounting hosts), plus a behavioural hours-advance; F6 the audio bootstrap - all three hosts boot through audio.ensure, no caller may go back to audio.init, and ensure() loads DAGGER.SND exactly once however many hosts call it; F7 LINK INTEGRITY - every module under src/ is imported, so a dangling import fails the suite instead of the deploy (AUDIT 18's eleven-domain merge produced exactly one, and it survived all 904 tests because the four scene hosts have no execution coverage - only vite build caught it); F8 THE PLAYER WORLD CLOCK - one classic minute is one magic round at 12x real time, fatigue drains ONCE per minute-change (a ten-minute catch-up costs one minute, not ten), the Default/Running bands with Athleticism truncated AFTER the multiply, and the RULE that every host ticks it while the per-minute laws live in exactly one module; F9 the CROSS-HOST agreement on that clock - found by re-running the mutation audit, which caught F8 running the tick unconditionally in the two exterior hosts while the interior host gated it on !overlayHeld. DFU freezes game time under a paused window (PauseGame sets timeScale 0; WorldTime.Update advances by deltaTime * TimeScale), so the gate is right and the ungated hosts aged diseases while the game was paused. The pin asserts the hosts AGREE, because F8's per-host grep passed on all three while they differed; AUDIT 21 F2 THERE IS ONE WORLD CLOCK - F8 extracted the per-minute LAW and left the ACCUMULATOR with each carrier, so world/exterior/worldModes each built a createPlayerTicker counting from zero and dungeonContext kept a fourth, and crossing a host rewound time (diseases are DAY-driven, so daysPast went negative and `daysOfSymptomsLeft -= daysPast` ADDED days - a finite disease got longer every time you opened a door). Two pins: a behavioural one that ticks three game-days through one carrier and reads them back off another, and a structural one that fails any classicMinutes-ish binding under src/ whose initialiser does not read worldMinutes() - the first draft matched only `let classicMinutes =` and a mutation to `const classicMinutesRef = { value: 0 }` restored the whole defect while the pin still passed |
| audit18_formats_world.test.js | 13 | AUDIT 18 formats/world, mutation-proven: GetRulerTitle's full 12-case switch (rows 11 Lord / 12 Lady were missing) + %rt through generateBuildingName; city lanterns adding the billboard height at DFU scale, not twice-scaled (MAGEAA00 and SENT7 - the only block in BLOCKS.BSA with an archive-210 exterior subrecord flat); random-marker ground tiles decoding to grass record 2, not texture record 8 (252 real WALLAA03 markers, count asserted so the loop cannot pass vacuously); fixed-length ReadCString keeping embedded NULs and decoding UTF-8 (three anchors a first-NUL scan structurally cannot produce); the sky clearColor/fillColor split pinned at BOTH ends on SKY16 frames 31 and 0 (either alone passes both ways); the night-sky right-edge seam, with its 132-differing-row precondition asserted first; SPELLS.STD keeping the FIRST duplicate index; and the KRAVE01.HS2 Order-of-the-Raven override over the real corpus |
| audit18_world_dungeon.test.js | 10 | AUDIT 18 dungeon actions, mutation-proven: action doors running Play's real delegate table (Move-family flags never moved - the headline, same shape as 17n); a door's ShowText/Teleport/SetGlobalVar record firing on both the chain and the toggle path; PlayerActivate's SECOND, separate Receive(Direct), without which three corpus doors were permanently un-openable; ExecuteActionOnToggle's IsPlaying gate and activation counter; special (secret) doors refusing player open and bash; interior action-door audio (the onDoorState/onDoorBash host gap); and AddLight's per-record RANGE switch |
| audit18_combat.test.js | 12 | AUDIT 18 combat, mutation-proven: GetBonusOrPenaltyByEnemyType taking the TARGET ENTITY so both discriminants and the player-as-target arm live again; GetAttackModifier decoding its two bits EXCLUSIVELY, not additively; CalculateRacialModifiers ported at last (a Redguard was 4 to-hit and 4 damage short, forever); hand-to-hand reading LiveStrength; the attack skill resolved by template, not display name (enchanted weapons fell back to Hand-to-Hand); the Skeletal Warrior edged halving un-inverted; the enemy armour loop stopping one slot short of Feet; the bow strike's 7 frames; and the unarmed StrikeLeft animation |
| audit18_systems_items.test.js | 10 | AUDIT 18 items, mutation-proven: looted gold minting a Currency stack that actually joins the player's gold (it carried no templateIndex and was permanently unspendable); hasNoEncumbrance honoured (a bought horse added 800kg); isStackable keyed on the template isIngredient bit rather than a hard-coded group list (Gems, MetalIngredients, Oil); ApplyWeaponMaterial's female archive-1 rule reaching the paperdoll; RandomizeArmorVariant and clothing variants/dyes on shop shelves and in loot; book variants over TotalVariants; and shop/loot rows carrying a name so the inventory window stops labelling them by group |
| audit18_player.test.js | 4 | AUDIT 18 player, mutation-proven: the crouch key edge surviving a render frame shorter than the fixed step (it was swallowed ~50% of presses at 120Hz); a BLOCKED stand-up retrying for PlayerHeightChanger's timed window instead of being dropped; IsMovingLessThanHalfSpeed carrying DFU's grounded requirement and a fresh speed; and CorpseActivationDistance's 150*GlobalScale reach |
| audit18_systems_chargen.test.js | 12 | AUDIT 18 chargen, mutation-proven: startingLevelUpSkillSum anchored LAST, after the biography effects, so a biography skill bonus stops counting as post-creation progress (it level-jumped the player on the first raise); the composed backstory actually assigned and surviving the save round trip; the biography's Arrow arm rolling a 1..20 stack at forced iron material; CreateRandomBook's variant; all eight races' resistance/immunity/low-tolerance/critical-weakness/specialAbilities flag sets deep-equalled against the RaceTemplate literals; the %qNb macro suffix; and Athleticism's +10% jump |
| audit18_systems_magic.test.js | 10 | AUDIT 18 magic, mutation-proven: EFFECT_COST_TABLE carrying DFU's whole effect library (91 classic keys, each with its own MagicSkill) so the 26-of-58 mispriced spells price correctly; liveStat clamped to 0..MaxStatValue as GetLiveStatValue does; savingThrow applying the racial tolerance flags AND the biography resist mods that had been minted, saved and read by nobody; a 0-hour timed/loiter rest ending on the first frame with no world time passing; and an instant effect being live for one magic round so EffectsAlreadyOnTarget can veto a recast |
| audit18_systems_social.test.js | 7 | AUDIT 18 social, mutation-proven: %oth and %cn expanded in the Where-is answer; the court verdict/acquittal/banishment boxes expanding their macros instead of printing %dip/%pcn/%cn raw; the save carrying crimeCommitted, the per-region legalRep and haveShownSurrenderDialogue; numQuestionsAsked reset per conversation so every talk after the first stops opening on the follow-up line; the NPC's own name on the plate and in %n; and the guilty-plea-without-prison branch not showing the sentence record; AUDIT 21 added F8: TIME PASSES IN CUSTODY. advanceDays defaulted to a no-op AND both exterior hosts constructed the flow without it, so a thirty-day sentence advanced the clock by zero - not inert once F2 made DAYS drive diseases. The fix is not host wiring: F2 gave the port one world clock, so the flow moves time itself. The separate four-hour bump was missing entirely - ReleaseFromPrison (:482-491) opens with RaiseTime(240*60) on EVERY release, reached from state 100, which the guilty plea, the state-2 verdict, banishment and the acquittal all funnel through. Pinned at 240 as a LITERAL, because asserting against RELEASE_MINUTES would ask the mutated constant what it expects; and DFU's ORDER (credit at state 3, days elapsed at :475) is pinned by sampling the reputation FROM INSIDE the skip, since with nothing decaying it today the two orders read the same from outside |
| audit18_ui_native.test.js | 17 | AUDIT 18 native UI, mutation-proven against the DFU window sources: the space glyph's width and measureText's trailing glyph spacing (every string the port draws or measures); the HUD vitals stride, border margin in screen pixels, compass inset and breath-bar anchor; the popup drawing from the top; the talk window's conversation anchor, 5-pixel arrow scroll, 6x6 tone marker, listbox origin and wrap; the trade scroll clamp; the character sheet answering clicks at all; the dungeon inventory equipping armor and clothing; the cloak interior entering the click mask; and QuickLoad on F11 |
| audit18_ui_chargen.test.js | 19 | AUDIT 18 chargen UI, mutation-proven against DaggerfallStartNewGameWizard and the CreateChar* windows: skillsNeedReroll as the flag it is; the face picker reset on every push with a cancel that writes nothing; the stats/skills OK buttons popping TEXT.RSC 14 instead of swallowing the click; advPickerItemCount 12 read at last with SmallFont loaded; the summary's own name box and reflex picker reverted by its cancel; a fresh career per QUESTIONS trip but not per LIST trip; SAVE ROLL and LOAD ROLL, which were absent from the stats screen entirely; and the removal of an invented reroll control the port had grown |
| audit18_enemies.test.js | 12 | AUDIT 18 enemies, mutation-proven: the Orc Warlord's PrimaryAttackAnimFrames3 (DFU's source has a missing comma, so `4 -1` is a constant expression evaluating to 3; the port's extractor emitted null and Math.min(null,n-1) is 0), plus the extractor itself; the 20 deg/classic-update turn rate; the five 7-row classic spawn/despawn bands indexed by ClassicSpawnDistanceType; melee and touch reach without the rate-of-approach term that is identically zero on DFU's classic path; enemy spell decisions gated on DetectedTarget and TakeAction's CanAct so foes stop casting at a chameleoned player; the Spell state stepped as a plain frame run; and ResetMeleeTimer's integer roll |
| audit18_hosts_outer.test.js | 38 | AUDIT 18 outer hosts, mutation-proven by reverting each host individually: the motor finally given the LIVE entity through a shared motorStats (getters, so a fortify or drain reads per-step as PlayerSpeedChanger does) - a rolled Speed had changed nothing; the levitate/waterWalking/slowFall flags recomputed by the EFFECT each frame in every non-dungeon host (they were written only in dungeon mode and never cleared, stranding a levitating player in the no-gravity branch forever); an open dungeon window actually holding the world in worldModes; platform riding extracted so both dungeon-mounting hosts get the mover delta; AdjustFallStart on the floating-origin recenter (crossing the threshold mid-air billed ~503m); the night-sky star pass and the weather-dependent NITE/day choice; DFU's gray-fog rule; blockIndex stamped so IsBadInteriorModel can fire; and the standing host rule pinned as a rule. Plus the F2 audit's diet/manifest COUPLING pin: MANIFEST_V was pinned as a floor (>= 3), which passes for ever once bumped, so the next slice to widen KEEP would have shipped a diet no existing install re-ingests - the two are now coupled by checksum, and touching the diet fails here until the version is bumped and the pair re-pinned |
| hostmagic.test.js | 11 | S30 - the ONE cast engine's laws run behaviorally with stub deps and rolls: () => 0.99 (ENGINE-PRNG - the saving throws are injected, not Math.random). Ready: silence refuses and clears, the cost gate speaks the classic refusal, a ranged spell arms the click latch, and a CasterOnly spell casts INSTANTLY at ready with no latch. Cast: ByTouch aborts BEFORE spending on a whiff and lands through the LOS raycast in reach, AreaAroundCaster sweeps every live foe in the radius, silence at CAST clears the readied spell spending nothing. Missiles: fire and fly at MISSILE_SPEED, seek the foe's mid-capsule, retire and PRUNE (every allocation has an owner), a wall retires and an AreaAtRange wall hit EXPLODES at the impact point, the lifespan retires a flier that hits nothing. The click seam: interceptAttack consumes the armed click and firePending casts exactly once. The magic-5 end-to-end: an Always-absorber's self-cast refund never exceeds what it cost (magicka lands back exactly where it started). And the save seam: readiedIndex round-trips through setReadiedByIndex |
| hostmagic_wiring.test.js | 5 | S30 - the engine's HOST WIRING, source pins because the hosts need a browser (the four-mounts sweep itself lives in mysticism.test.js S27, inverted): an armed cast eats the attack click in EVERY host's input path (drag/press/touch in the exterior pages, the interior route wrap, the dungeon's playerAttackInput); every host frame drives firePending + update and draws the batches; the exterior pages cast through MODE FACADES (the collider follows the door via modes.interiorCollider, guards are targets only outside, absorbCtx's darkness/light law follows the mode) with worldModes exposing the interior collider; spell damage reaches guards through cityGuards' ONE damage door (corpse + Murder ride the same path as melee); and SPELLS.STD rides into the interior arm while the dungeon save seam writes the readied index and restores it through the engine |
| audit18_hosts_dungeon.test.js | 28 | AUDIT 18 dungeon host, mutation-proven: enemies can fire bows at all (rangedAttack was minted from an equipped weapon that never existed); equipment-using MONSTERS get SetEnemyEquipment; the attack skill read by template not display name, at the five skill-ADVANCEMENT sites too (an enchanted longsword trained Hand-to-Hand for the whole game); swingWeaponFatigueLoss and the CriticalStrike/Backstabbing tallies, which ran nowhere; targetGroup threaded so DFU's player-is-humanoid branch fires; the arrow path's four dropped arguments; the zero-damage swing taking WeaponManager's swing/parry pair rather than its WALL-hit pair; guard weapon poison reaching InflictPoison; loot rolled on the PLAYER's gender as LootTables does; enemy cast cost priced off the player's skills; and Athleticism's fatigue multiplier |
| audit18_bible_docs.test.js | 17 | AUDIT 18 doc-truth, pinned as RULES so the bible cannot drift silently again: every open-flags citation in Home.md resolves to the exact line it quotes AND the list matches the FLAGGED/INTERIM sites in src/ BOTH ways (regenerated mechanically - AUDIT 21 found the row quoting a hard count of 109 that had rotted to 115, so it names no number now: the BOTH-ways rule below pins the list and src/ to EXACT agreement, which is the real claim); every src/ path the bible names in backticks exists; no doc claims a Ledger row that is absent; arc status lines match what shipped; and the specific claims AUDIT 18 falsified stay falsified - the Athleticism inert-list entry, the Systems-Arc spell-cost row, the Audio.md queue closure, and Port-Ledger C's breath/crouch exemption over live code; AUDIT 21 added the one that makes this file's oldest claim true: Testing.md has said the open-flags list is "regenerated mechanically" for three audits and NO SUCH TOOL EXISTED, so the list rotted on every host edit and three audits hand-patched line numbers - each patch a chance to point a citation at the wrong line. tools/regenOpenFlags.mjs writes the list from the FLAGGED/INTERIM sites in src/; the new pin runs it with --check and fails when the checked-in list is not what the tool would produce |
| startwindow.test.js | 19 | U21 THE MAIN MENU (DaggerfallStartWindow.cs): the three button rects to the pixel (Load 72,45,147,15 / New Game 72,99,147,15 / Exit 125,145,41,15) and inside the virtual screen; every button answering a click at all four corners and its centre while leaking on none of its four edges, with the gaps between them dead; a press recording the action and a miss recording none (but still consumed, so it can never reach a pointer lock); clicks on the letterbox bars hitting nothing; PICK03I0 decoding 320x200 on its OWN palette with the shared ART_PAL provably untouched (the U18/17k palettized law); and the boot seam - the bare URL reaches the menu, ?shot bypasses it BEFORE it (or every probe in tools/ blocks), and LOAD goes through the host's own quickLoad rather than a second loader. + U21b THE CHARGEN PARENT PANEL: drawMenuBackdrop SUBTRACTS the host's letterbox offset (the dungeon host draws overlays with a virtual canvas and an offset already set, so a quad at (0,0) landed down-right by the margin and left the top and left strips showing the 60% dim over the pale sky - measured live at 1400x900, the left strip was (57,75,97) before and (0,0,0) after), that it is OPAQUE black not a dim, that a no-offset host is unchanged, that an offset-less renderer does not throw, and the renderer's screenOffset getter + U21c THE TITLE SCREEN (our own branding, Ledger A - DFU does not draw TITL00I0 either, it replaced classic's title with its own): a MISSING logo means no title screen at all and runTitle returns immediately without touching the renderer, so an absent asset costs a splash and never a boot; logoRect centres the banner, preserves aspect against BOTH axes (a wide logo is width-bound, a tall one height-bound), caps it at 86% of the canvas, and returns null for a degenerate 0-dimension image rather than dividing by zero; and the backdrop is OPAQUE black drawn before the logo, so the renderer's pale Iliac Bay clear never shows through + U21c THE LOGO IS NOT CLASSIC ART (after Mac sent the artwork): every screen quad in this port takes a 1-BIT cutout - discard a<0.5 then force alpha 1 - because classic art IS one (palette index 0 transparent, every other index fully opaque), and the logo has anti-aliased serifs and a soft shadow that the threshold would jag and silhouette; the title screen opts INTO alpha blending and into a LINEAR/CLAMP upload (NEAREST aliases a high-resolution banner drawn at a non-integer scale), with BOTH proven opt-in so no game art path changed - the backdrop takes no texture blend, the shader keeps the discard as its ELSE, and the upload defaults stay NEAREST/REPEAT. Mutation-proven four ways: dropping either option at its call site, and ignoring either inside the renderer + U21d THE FALLBACK IS CLASSIC'S OWN TITLE, not a placeholder: with no public/logo.png the screen draws TITL00I0.IMG out of the user's ARENA2 at runtime, so the port has a real title screen with nothing to ship - pinned that loadTitleArt asks for TITL00I0 only when ours is missing and NEVER fetches it when ours is present, that a missing IMG too resolves null rather than throwing, that the native arm returns nativeMetrics rather than a logoRect, and that classic art does NOT take the logo's blend opt-in - it is a 1-bit cutout like every other classic screen and blending it would let its index-0 pixels ghost instead of dropping out |
| audit21_music.test.js | 11 | AUDIT 21's MUSIC-ENGINE lane. Its headline was not a wrong constant: createMusicDirector - the single seam through which every host feeds SongManager - had ZERO behavioural coverage, and three mutations that each silence a large part of the game left the WHOLE SUITE green. Reversing the spread so `base` wins kills all interior and dungeon music (the AUDIT 21 F1 defect re-introduced by a different edit than the one F1's pin watches, since that pin checks statement ORDER in the host files); `songEnded: false` kills DFU's whole !IsPlaying arm; `play: () => {}` means no music plays anywhere. The sinks are injectable now - nothing in the hosts passes them - so this file drives the real director, and one pin patches the module singleton to prove the DEFAULT sink is not a no-op, which the injected harness cannot see. F3 CASTLE MUSIC was unreachable: both call sites hardcoded insideDungeonCastle false behind a flag claiming "no castle-block detection yet", while rdbLayout had computed castleBlock verbatim on every block all along and five real castle blocks were already pinned - and the same hardcode left ambientEffects' doNotPlayInCastle READ by the code and WRITTEN by nobody. F4 an unresolved temple forced Interior where SongManager.cs:494-518 has NO else and the environment HOLDS - the flag blamed a missing temple-faction table that was in the same file, hiding the real divergence behind a retired excuse; two existing pins encoded the old behaviour and are corrected in place with the reason recorded. F11 the clock started at 0 where DaggerfallDateTime's classicGameStartTime is 523530 - DAY 363 - and gameDays is both the DFRandom seed for every non-dungeon playlist and the tavern index, so every song on a given in-game date differed from DFU's. F5, F6 and F7 are pin-quality: locationChanged, the unseeded Mages Guild arm and AUDIT 19 F5's own `>>> 0` fix were each one edit from silent loss. The last test is a structural RULE for the host half, written as a field-set comparison rather than a substring match, because a rule that greps for "factionId" passes on `factionId: 0` - which is the mutation that has to be caught. 15 mutations, 15 killed |
| audit21_hosts.test.js | 10 | AUDIT 21's HOSTS lane. The six scene hosts have ZERO node execution coverage, which is this project's most repeated bug shape - a feature lands in one host and not its siblings and every test stays green - so these are RULES over the host sources plus behavioural pins on the shared machinery underneath. F3 LEVELLING OUTSIDE A DUNGEON SILENTLY AUTO-SPENT YOUR ATTRIBUTE POINTS: all three above-ground carriers built a player ticker with no onLevelUp, so advancement.js took its headless arm and dumped every point into your LOWEST stats with no message and no choice - the same XP built a different character depending on where you were standing. Fixing it needed a second half nobody had noticed: townTalk understood Escape and Enter/E and worldModes passed the raw key CODE, so even once the screen opened it could not be driven (a LevelUpScreen wants up/down and plus/minus). Both route through overlayAction now, the same map routeKey feeds the dungeon host. The behavioural pin's FIRST draft was vacuous - its fixture never levelled at all, so both arms trivially matched - which is the exact shape this audit kept finding elsewhere; it now asserts a real level-up fires and that the headless arm visibly spends where the hooked arm leaves the stats alone. F4 the 3D audio LISTENER was moved by world, exterior and dungeonContext and by nothing in worldModes, so in INTERIOR mode it stayed parked at the town's world position while interiorContext played door sounds at interior-LOCAL coordinates against a finite maxDistance - out of range entirely; the rule also pins that the call sits BEFORE the mode branch, since moving it inside the dungeon arm restores the defect for interiors. 9 mutations, 9 killed. NOT filed, and recorded in Ledger B instead: the outdoor rain loop following you indoors is DFU (WeatherManager.Update returns early when inside, so Presets stays Rain); F6 DYING OUTSIDE A DUNGEON DID NOTHING AT ALL - a guard beat you to 0 HP in the street and the game carried on, you kept walking and the guards kept hitting a corpse; four paths wrote player health and exactly ONE (the dungeon's) checked, so the check moved to the write. One damage door in playerEntity.js, a presenter registered by whichever host is live (the same shape F2 gave the world clock), and the door fires on the TRANSITION rather than the state - firing whenever health reads zero re-presented the screen once per effect round, which the dungeon had been hiding behind an instanceof guard in its presenter. F8 swinging a weapon INSIDE A BUILDING cost no fatigue and trained no skill: WeaponManager drains the swing whatever it hits, the dungeon and the city guards both do it, and the interior rig did neither - its bow arm tallied one hand-picked skill and drained nothing, disagreeing with the dungeon's own bow arm, which takes tallySwingSkills because a bow always reaches DFU's tally arm. 14 more mutations, 14 killed. Two of the survivors in the first pass were artefacts of the MUTATION HARNESS, not weak pins - it replaced only the first occurrence, so removing one of three setDeathPresenter calls looked survivable; it replaces all occurrences now; F7 NO HUD ABOVE GROUND - ?world and ?exterior drew no status bar at all (no health, no fatigue, no magicka, no compass) while guards, falls, per-minute fatigue drain and disease attribute drain all applied, and inside a BUILDING it vanished too because drawHud lived inside dungeonContext.drawFoes which the interior arm never calls. ui/hud.js was already host-agnostic, so this is the art loaded once per host and drawn last. The rule's other half is tools/hudProbe.mjs, which boots both exterior hosts for real and counts saturated pixels where the bars belong - calibrated BY MUTATION (1600/1799 with the draw, 438 without, floor 1000), because the grass and plaster behind the bars are themselves colourful and a bare 'is there any colour' test does not discriminate. Two earlier drafts of that probe are recorded in it as warnings: one called readPixels on a context re-got from the canvas, which reads a swapped buffer and reported 0% while the screenshot plainly showed the bars; the other compared PNG byte sizes, which cannot tell a textured sky from a status bar. The F7 mutation also exposed a flaw in every source rule in this file - they grepped RAW text, so removing the drawHud CALL left the word in the comment above it and the rule passed; all of them read comment-stripped source now. 28 mutations across the four hosts campaigns, 28 killed |
| audit23.test.js | 6 | AUDIT 23 Wave 1 (save integrity), each pin mutation-proven: C1 the ship-blocker - a save restored before anything attached the faction store dropped every earned reputation, so restore now STASHES the columns on the entity, a store-less re-save carries the stash forward instead of nulling, and attachFactionRep replays the stash onto the fresh store before draining the biography's pending deltas; guild memberships restore UNCONDITIONALLY (GuildManager.RestoreMembershipData clears before applying, so a pre-guild save wipes a later-joined book on backward load); lastSkillCheckTime rides the envelope (SerializablePlayer.cs:124/:293) and chargen anchors it at CLASSIC_GAME_START_TIME (PlayerEntity.cs:881 anchors at NOW); the sticky region price band (SerializablePlayer.cs:168) rides the envelope as a copy, not an alias; dropped piles round-trip the dungeon world snapshot with their SAVED icon (LootContainerData_v1 - a restore never rerolls, an absent list clears, batches mount and free); both dungeon-mounting hosts pass the player.spawn quickload applier (worldModes wrote raw pos, keeping velY/falling across a load) and collectWorld/applyWorld carry the piles |
| audit23_ui.test.js | 6 | AUDIT 23 Wave 4, key pins mutation-proven: a pre-rolled bonus pool means applyLevelUp draws no second one (DFU rolls BonusPool once at screen setup; the screen hands its ORIGINAL roll back); the talk activation ray reaches RayDistance 76.8 with per-mode gates and the classic too-far line (Info/Grab/Talk 6.4, Steal 3.2 alone - the silent pre-gate died); the drawn SPACE omits GlyphSpacing while the measured one keeps it (DaggerfallFont's deliberate asymmetry); backing out of Reflexes keeps the skill spinner cursor; interior destroy() frees the voxelfolk rigs; the duplicate ARROW_MOVEMENT_SPEED/SWING_WEAPON_FATIGUE_LOSS constants are gone (MISSILE_SPEED and hostCombat's constant are the single sources) |
| audit23_hosts.test.js | 13 | AUDIT 23's HOST BATCH, key pins mutation-proven: ONE clock (both exterior hosts' minuteNow reads worldMinutes % 1440 - night finally falls for music, city lights, window styles and the sun; ?tod SETS the world clock at boot, ?timescale SCALES the tick like DFU's TimeScale, the frozen demo clock deleted); the guards REQUIRE the classic-minute clock (a poisoned hit anchored at 0 caught the whole course up in one frame); the three above-ground tick calls carry IsRunning && !IsStandingStill and the motor's jump edge (the 88/min running drain and the per-jump 11 x multiplier + Jumping tally were dungeon-only, and player.running never existed); the shared ticker sink clamps fatigue 0..MaxFatigue and raises the exhaustion collapse everywhere (DaggerfallEntity.cs:360-366, rest-hour-or-death via exhaustionOutcome with per-host presenters); the 112-day NormalizeReputations boundary runs through the tick with the preventNormalizingReputations skip and clampLegalReputations on restore; Running tallies every 4th classic update; the exterior swing arms drain SWING_WEAPON_FATIGUE_LOSS on every hit frame, the bow takes the FULL tally arm, the display-name double tally is deleted, ArrowShoot at bow frame 4, whiff whoosh on the no-enemy frame; night interiors take INTERIOR_NIGHT_AMBIENT in both interior hosts; worldModes routes DEATH by live mode and hands exterior deaths back to the captured presenter; the standalone dungeon swallows F5/F6 and feeds the music director above the modal return; the sky adds the CALENDAR season on clear weather and exteriorAmbient rides the weather sunlight scale; the wandering population takes the climate's People race with the REGION's name bank; the motor's standing test ignores Jump (stealth half-speed) and a free swim FORCES the crouched capsule on the medium clock, standing back up on surfacing |
| audit23_combat.test.js | 5 | AUDIT 23 Wave 3 combat pins, mutation-proven: a wielded bow engages the BOW half of the weapon machine (isBow read per step like the unarmed gate; forced StrikeDown on the rising edge of the attack input with no swing tracking and no re-fire without release; the 0.0625 classic tick; bowSound at frame 4 before the frame-5 loose; GetBowCooldownTime armed at done and blocking the next rise; clickAttack forced too; swapping to melee disengages); the melee swing resolves EVERY foe in reach (MeleeDamage's foreach, the single-target break was invented); foes resolve before the environment (WeaponEnvDamage is the no-entity fallback); the hosts own the swing sounds - ArrowShoot(3) at bow frame 4, the whiff whoosh at the no-enemy hit frame, the rig's strike-entry whoosh gone |
| audit23_magic.test.js | 7 | AUDIT 23 Wave 3 magic pins, key ones mutation-proven: the classic Free Action patch (spell 10's (3,2) bytes read as (26,-1) per EntityEffectBroker.cs:892-900, synthetic + real-corpus, and only the exact bad bytes patch); a wall-struck AreaAtRange missile explodes at the impact point (DaggerfallMissile DoCollision); every spending cast arm tallies the effect schools through the cost table's presence gate (TallyPlayerReadySpellEffectSkills); the self-cast absorb refund cap compares the {entity,sinks} wrapper and receives the spent cost as ctx.selfCastCost; readying enforces the cost with the classic refusal line and CasterOnly casts instantly on ready (SetReadySpell :337-351); a silenced enemy cannot spend a cast while the free spider-touch rider bypasses (SilenceCheck gated on !noSpellPointCost) |
| audit23_systems.test.js | 13 | AUDIT 23 Wave 3 systems pins, key ones mutation-proven: Arrow=131/Helm=107 (ItemEnums); every item factory mints condition = template hitPoints with the weapon/plate material scale (int division) and idempotence, starting gear included; the inventory icon clamps armor variants by material family (chain cuirass draws variant 4); %wth = value x stackCount; TEXT.RSC's empty trailing variant steps back one stream (TextProvider:231); the spell-index fold keeps the FIRST duplicate (Holy Word over Holy Touch) and chargenSession rides it; the four guild subclass service switches REPLACE the base (Mages loses Repair, Thieves/Brotherhood lose Identify, the rank gates 3/5/8/6/4 and 2/4 and 1/3/5/7, KO ReceiveArmor member-only) and the Mages library opens at rank 2; TG_Spymaster is 806; FrameSpeedDivisor slows the sprite anim clock with the 4-fps floor; spawnDistanceType/playerInside ride the foe constructions and piles roll the player's gender; song channel gains resync at play and the loop seam; ACTION_FLAGS carries Unknown100; the per-minute tick never advances skills - the rest-finished close is the advancement moment (RaiseSkills at popup close, the only live site) |
| audit21_core.test.js | 8 | AUDIT 21's CORE-SYSTEMS lane. Five findings that made a ported mechanic UNREACHABLE rather than merely wrong, plus two where the shipped code was right and reverting it was invisible. F1 EVERY ENEMY WAS BUILT WITHOUT ITS CAREER - makeEnemyEntity took `career`, flattened one field out of it and dropped the rest, so `target.career ?? {}` was an empty object and all seven DFU tolerance channels read Normal for every enemy in the game; 20 of the 43 ENEMY*.CFG records carry non-zero tolerance bytes, and a Fire Daedra (immunityFlags 0x08) took full fire damage in 434 of 1000 rolls where DFU returns zero in 1000 of 1000. F2 Adrenaline Rush was listed INERT "because the consuming subsystem does not exist" - the consumer is CalculateSuccessfulHit, which is live, and a VANILLA Acrobat (CLASS09.CFG bitfield 0x1406) has the bit; pinned by walking the hit roll to its flip point at two places on the curve, since at the fixture's default chance the clamp floors both. F3 biographyAvoidHitMod was minted from the real BIOG*.TXT, persisted through save, and never read - the comment claimed the questionnaire was pending when it shipped in S3e/U13; DFU SUBTRACTS it, so the near-universal TH -5 makes a character EASIER to hit. F4 THE MAGIC-ROUND CATCH-UP: the port anchored on the clock's value at frame start, so minutes added by rest, a court sentence or a RaiseTime produced ZERO rounds - cast Levitate, rest eight hours, wake with the full duration intact. EntityEffectBroker keeps its own lastGameMinute for exactly this reason; the marker, the 2880 cap (maxCatchupDays = 2) and the load re-anchor are all pinned, the last one on the frame AFTER the rewind because it is invisible on the rewind frame itself. F5 SILENCE had a gate and no producer - `isSilenced` had no writer anywhere in src/ outside two test fixtures, so the gate was a constant false while the file header said "SILENCE IS WIRED"; the pin goes through the CLASSIC KEY (19,255) rather than hand-building the kind, because a pin that writes the kind itself only proves the fold reads what the test wrote. F8 the Argonian swimming exemption, including its SHORT-CIRCUIT - counted by draws, not outcomes, since a roll taken where DFU takes none shifts every later roll. F6 and F7 are pin-quality: the chargen reflexes pick and the Swimming tally were each one delete away from silently returning every character to Average reflexes / making Swimming unadvanceable, with the suite green. 19 mutations, 19 killed |
| doctrine.test.js | 5 | PORT-DOCTRINE, ENFORCED. AUDIT 21's doctrine lane found the project's one non-negotiable - "ARENA2 never enters the repo" - violated and PUBLISHED: fourteen before/after gallery frames sat under `public/`, twelve of them the port's framebuffer with classic WEAPON*.CIF sprites drawn in and upscaled, and `public/` is Vite's static root, so every one was copied into `dist/` and uploaded to GitHub Pages by deploy.yml - whose own header comment says the build contains no game data. The doctrine named `.gitignore` as the mechanism, and `.gitignore` blocks `ARENA2/`, `*.BSA` and `*.SND`; it cannot recognise a PNG of the same art. So the rule is a test now, and the rule itself is new doctrine text: A RENDER OF GAME DATA IS GAME DATA. Five pins, each mutation-proven by putting a removed file back: (1) an ALLOW-LIST for `public/`, since everything there is published - README and logo.png are the whole list; (2) no tracked raster anywhere in the repo; (3) no DERIVED raster either, whatever it is wearing - `src/characters/backs/*.json` held flat ART_PAL arrays whose opaque mask was IDENTICAL to the mirrored BODY00I0 sprite, 5237/5237 texels at 100.00% silhouette agreement, so the rule is on the SHAPE (a width, a height and a pixel array) rather than the extension; (4) `.gitignore` still blocks what the doctrine says it blocks; (5) THE LEDGER'S CHARTER - "if a departure or gap is not on this page, it does not exist" was false in two directions, eleven `src/` sites citing an engine-PRNG rule with no row and four files declaring a deliberate departure with no row, so every file declaring one must now be NAMED by a Ledger row. Citing the Ledger from the source side deliberately does not count: a claim of approval is not an approval, which is the whole finding. Two more unrecorded departures surfaced when that escape hatch came out (factionRep, terrainNature) |
| crashreport.test.js | 11 | THE CRASH REPORT and the never-trap under it (2026-08-21, from a real player's screenshot). The overlay printed `err.stack` ALONE, which is a browser-dependent decision it did not know it was making: V8 folds "Name: message" into `stack` and SpiderMonkey does NOT, so every crash report from a Firefox player arrived with the one line that says WHAT was null missing - and one did, two frames and no message. crashText composes the head itself and is pinned over both stack dialects, a Chrome stack not printed twice, the non-Error shapes a rejection can carry (a bare string is NOT dressed up as "Error: ..."), an error with no stack still keeping window.onerror's coordinates, and main.js registering exactly ONE listener per event (it had TWO for unhandledrejection, the second able only to overwrite the first with a worse format). Then the crash the report could not name: drawMesh dereferenced `mesh.vao` and iterated `mesh.subMeshes` with no guard, so ONE model missing from the player's own ARCH3D threw out of the frame loop and killed the whole scene. The real method is driven against a recording `this` - null, undefined, {}, a vao with no subMeshes and an EMPTY subMeshes list all return having touched no GL state, the warning fires once and not sixty times a second, and a GOOD mesh still draws (count x3, byte offset x4). Plus the producer: interiorContext pushed `await getGpuMesh(id)` into its draw list unguarded - getGpuMesh returns null for an id this data set lacks and CACHES it, and cpuModels is written only on the success path, so the same placement read `cpu.positions` off undefined one line later. It was the only builder in the port that did not skip the placement; the sibling guards are pinned too, so this fails if one of THEM loses it. THE HUNT THEN FOUND THE ACTUAL PRODUCER, and it was a null MATRIX, not a null mesh: ensureArrowModel pushed `{gpu, object:{matrix:null}}` and left the matrix for the next updateMissiles pass, but the push lands in a MICROTASK (it is async and its one caller does not await it) and both hosts draw dynamicDraws BEFORE they call drawFoes - so the next frame drew a null matrix and `uniformMatrix4fv` threw, because Float32List is a non-nullable WebIDL union. Firing a bow killed the frame loop. Worse, an arrow retired point-blank while the load was pending left an ORPHAN whose matrix nothing would ever fill, throwing on every frame after. The matrix is built at push time now, the dead check matches arrowFlight's, and the host ORDERING both depend on is pinned so a reshuffle re-reads the test rather than resurrecting the bug. The interior half was corrected too: the build died THIRTY-SEVEN LINES before that placement guard could run - collect() stores an undefined cpu model, layoutInterior dereferences it on both arms, and the climate pass reads `cpu.subMeshes` off the same undefined (`?? []` guards the value, never the receiver) - so pressing E on that door did nothing at all, for ever, with a raw TypeError instead of the doctrine's warn. Same three traps in the dungeon's action-door arm, which lacked the `if (!gpu) continue` its own placement loop fifty lines above has. Seven mutations, 7 killed |
| manifest.test.js | 1 | drift guard: this table and the total against the real suite |

Two tiers per module:
1. **Synthetic fixtures** - in-memory data built inside the test. Always run;
   CI stays green with no game data (88 pass, 49 skip).
2. **Real-data validation** - gated on `ARENA2_PATH`; skip cleanly when
   absent. Pin observed counts, names, ids, checksums, and structural
   closure invariants.

Without game data the suite runs 805 pass / 120 skip (AUDIT 18 corrected
the long-stale "88 pass, 49 skip", which described a 137-test suite that
has not existed for many milestones). Those 75 skipped pins DO NOT RUN
IN THE DEPLOY GATE - CI has no ARENA2 - so a real-data pin is a local
gate only. (Measured on main at the AUDIT 18 merge; recount by hand
when the split moves.) `manifest.test.js` guards the totals above but not this line;
recount it by hand when the split moves.

Sourcing data in a fresh session: `sh tools/fetch-data.sh`, then
`ARENA2_PATH=/home/claude/dfdata/arena2 npm test`.

Visual proof: `npm run shot [out.png]` boots vite in-process and drives the
provisioned Chromium (playwright@1.56.0 <-> /opt/pw-browsers chromium-1194;
bump both together). CONTRACT: the scene query rides the SHOT_QUERY env
var, NOT argv (`SHOT_QUERY='shot&world&tod=22:00' npm run shot out.png`) -
argv[2] is only the output path. SHOT_TIMEOUT overrides the 30s ready
wait (streaming world wants 90000); SHOT_EVAL runs a JS snippet in-page
after ready (promise-returning expressions are awaited - top-level
`await` fails). Shot-mode probe hooks in the dungeon and world scenes:
window.__move(dx, dy, dz), window.__pose(x, y, z, yaw, pitch),
window.__player {pos, warp}, and in the dungeon __activate/__activateKey/
__ray/__actions. SwiftShader renders heavy scenes at ~7 fps headless -
time-based proofs must poll state, never trust wall-clock. `?shot` in main.js fixes the vantage and raises
`window.__shotReady`. Manual proof, not a suite gate.

Pre-push gate: `npm run check` (test + build).

Drift guard: `test/manifest.test.js` pins the total line and every row of
the table above against the real suite. Recalculate this doc in the same
commit as any test change.


