import { test } from 'node:test';
import assert from 'node:assert/strict';

// Placeholder gate. Replaced by real reader tests in Readers-Arc.
test('smoke: toolchain runs', () => {
  assert.equal(1 + 1, 2);
});
